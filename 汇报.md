# 本周主要进展

这周主要继续沿着 **PC GPU + PC CPU + Phone 协同推理** 这条线往下做，主要有四部分进展：

```text
1. 把原来统一的 chunk 切分进一步改成不同 backend 独立选择 chunk，这样就新增了许多变量
2. 继续修改自动预测器，让不同拓扑和 chunk 配置真正参与全局比较
3. 尝试 跨层流水，进一步提高跨层流水程度
4. 把现有 Dense 模型上的协同方案迁移到 MoE 模型
```

整体目标还是一样：

> **尽量减少人工指定参数，让系统能够根据 PC CPU、GPU、手机、网络和模型结构，自动选择一个更合适的协同执行方案。**

---

# 一、进一步细化 Chunk：不同 Stage 独立选择粒度

## 1. 原来的 Chunk 还是比较统一

之前 Tensor Split 已经做过 chunk 化。

例如一个 256-token 的 ubatch，可以切成：

```text
256 tokens
    ↓
64 + 64 + 64 + 64
```

这样 PC 和手机就不需要等整个 FFN 都完成以后再通信，而是可以：

```text
Chunk 0: PC Compute ────────┐
                            ├→ Reduce
         Phone Compute ─────┘

Chunk 1:      PC Compute ────────┐
                                 ├→ Reduce
              Phone Compute ─────┘
```

通过 chunk 可以增加：

```text
PC Compute
Phone Compute
RPC
Reduce
```

之间的重叠。

但是之前还有一个问题：

> **不同 backend 实际适合的 chunk 大小并不一定相同。**

例如 GPU 一般更喜欢较大的 batch，而 Tensor Split 为了隐藏 RPC 延迟，可能更适合更小的 chunk。

所以如果所有阶段统一使用一个 chunk 参数，就可能出现：

```text
对 GPU 合适
但对 Tensor Split 太大

或者

对 Tensor Split 合适
但把 GPU 切得太碎
```

---

# 2. 现在改成不同 Stage 独立 Chunk

现在把原来的单一 chunk 进一步拆成：

```text
XG = GPU stage chunk
XC = CPU stage chunk
XT = Tensor Split 内部 chunk
XP = Phone Only stage chunk
```

例如一个方案可能是：

```text
XG = 256
XC = 64
XT = 48
XP = 16
```

对应：

```text
GPU
256-token macro
       ↓

CPU
64-token macro
       ↓

Tensor Split
48-token inner chunks
       ↓

Phone
16-token macro
```

这样不同设备可以使用更适合自己的计算粒度。

---

# 3. 为什么更细粒度 Chunk 可能更快

主要原因是不同 backend 的性能特征不一样。

GPU 通常更希望：

```text
较大 batch
→ GEMM 更充分
→ kernel 启动次数更少
```

所以：

```text
XG 往往偏大
```

而 Tensor Split 需要同时考虑：

```text
PC Compute
Phone Compute
PC → Phone
Phone → PC
Reduce
```

如果 XT 太大：

```text
一个 chunk 要很久才能返回
→ pipeline 重叠不足
```

但 XT 太小又会：

```text
更多 RPC command
更多 subgraph
更多 kernel
更多 reduce
更多调度开销
```

所以它本身存在一个折中点。

Phone Only 和 CPU stage 也是类似。

因此现在:

```text
GPU 最适合多大的 chunk？

CPU 最适合多大的 chunk？

Tensor Split 最适合多大的 chunk？

Phone 最适合多大的 chunk？
```

然后由预测器统一决定。

---

# 二、预测器进一步修改：从局部选参数变成全局比较

## 1. 之前虽然有预测器，但有些参数还是局部决定

原来的预测器已经能够根据 profiling 自动选择：

```text
T = Tensor Split 层数
P = Phone Only 层数
C = PC 侧层数
G = GPU 层数
R = Tensor Split PC 比例
```

但是后来发现一个问题：

有一些 chunk 参数实际上还是先在局部选择，然后再进入整体 pipeline 预测。

例如可能出现：

```text
XC=128
单独看 CPU stage 最快

XC=64
单独看 CPU stage 稍慢
```

于是局部选择会直接选择：

```text
XC=128
```

但放到整个流水中可能是：

```text
XC=128
整体 = 80 s

XC=64
整体 = 72 s
```

因为流水系统最终关心的是：

> **整个 流水线 什么时候结束，而不是某一个 backend 单独最快。**

---

# 2. 现在把不同拓扑也放到统一搜索中

现在预测器不再预先规定一定使用：

```text
GPU → TENSOR
```

而是允许不同拓扑进入竞争，例如：

```text
GPU → TENSOR
```

```text
GPU → CPU → TENSOR
```

```text
GPU → TENSOR → PHONE
```

```text
GPU → CPU → TENSOR → PHONE
```

也就是说现在真正参与搜索的是：

```text
G
C
T
P
R
XT
XG
XC
XP
```

例如：

```text
G=14
CPU=0
T=50
P=0
```

对应：

```text
GPU14 → TENSOR50
```

而：

```text
G=14
CPU=4
T=46
P=0
```

对应：

```text
GPU14 → CPU4 → TENSOR46
```

预测器会分别估计这些方案的完整执行时间，而不是人工提前指定哪一种 topology。

---

# 3. 搜索规模变大以后采用两级预测和流水仿真

现在参与规划的参数越来越多：

```text
G / C / T / P
R
XT
XG / XC / XP
```

如果把所有参数直接做完整组合，再逐个进行流水仿真，搜索量会非常大。

所以现在采用的是一种：

```text
Profiling
    ↓
Coarse Search
    ↓
Stage Simulation
```

的两级搜索方法。

首先在程序启动时，对当前设备进行实际 Profiling，得到：

```text
CPU / GPU / Phone 计算时间
不同 token 数下的计算时间
不同 Tensor Split 比例 R 的计算时间
PC ↔ Phone RPC 时间
不同 chunk 大小的运行时开销
```

这些实测数据会作为后面两级预测共同的基础。

---

## 第一阶段：Coarse Predictor 快速筛选

第一阶段主要搜索：

```text
Topology
+
R
+
XT
```

也就是：

```text
(G, C, T, P, R, XT)
```

这里不会对每一种方案都进行完整的流水调度仿真，而是利用前面 Profiling 得到的实测时间，建立一个比较便宜的近似模型。

例如粗略估算：

```text
CPU Stage Cost
Tensor Stage Cost
Phone Stage Cost
GPU Stage Cost
```

再考虑主要的计算和通信重叠关系，得到一个：

```text
Coarse Predicted Time
```

它的目的不是非常精确地预测最终运行时间，而是：

> **快速判断哪些方案明显比较差，哪些方案值得进入后面的精细搜索。**

流程可以表示成：

```text
真实设备 Profiling
│
├─ CPU / GPU / Phone 计算时间
├─ 不同 token / chunk 的耗时
├─ 不同 R 的 FFN 时间
├─ RPC 传输时间
└─ 内存开销
        │
        ├───────────────┐
        ▼               ▼
Coarse Predictor    Stage Simulator
快速近似             精细时间线仿真
        │               ▲
        ▼               │
筛掉大量候选 ─────→ 保留 Top-K
                        │
                        ▼
                  完整方案比较
```

这样可以在不进行大量完整仿真的情况下，把几十万甚至更多候选方案压缩到比较小的候选集合。

---

## 第二阶段：完整 Stage Simulator

对于第一阶段保留下来的候选，再进一步枚举：

```text
XG
XC
XP
```

这一步不再只使用简单的区域耗时相加，而是送入完整的 Stage Simulator。

Stage Simulator 会模拟实际流水的时间线，例如：

```text
GPU0 ─────────┐
              │
       CPU0 ──┼────────┐
              │        │
              └→ Tensor0
GPU1 ────────────────┐
                     └→ ...
```

对于 Tensor Split，还会进一步考虑：

```text
PC Compute
Phone Compute
H2D
D2H
Reduce
Chunk overlap
GPU / downstream overlap
```

因此它真正模拟的是：

> **在当前 chunk 配置和设备布局下，各个 stage 在时间线上什么时候开始、什么时候等待、什么时候结束。**

所以最终得到的不是简单的：

```text
GPU Time
+ CPU Time
+ Phone Time
+ RPC Time
```

而是更加接近真实流水执行的：

```text
Predicted Makespan
```

也就是整个 Prefill pipeline 最后完成的时间。

---

## 最终流程

所以现在整个自动规划过程更准确地表示为：

```text
真实设备 Profiling
      ↓
生成 G / C / T / P / R / XT
      ↓
Coarse Predictor
      ↓
快速内存过滤
      ↓
保留有希望的 Top-K 方案
      ↓
枚举 XG / XC / XP
      ↓
完整 Stage Simulator
      ↓
模拟不同 Stage / Chunk 的执行时间线
      ↓
完整内存检查
      ↓
比较 Predicted Makespan
      ↓
选择最终方案
```

这里可以把两个预测阶段理解成：

```text
Coarse Predictor
=
便宜但比较粗的近似
主要负责“筛选”

Stage Simulator
=
更贵但更精细的流水仿真
主要负责“最终排序”
```

两者使用的基础数据都来自启动时对 CPU、GPU、Phone 和 RPC 的实际 Profiling，而不是依赖固定的理论算力参数。

# 4. Dense 模型上目前预测效果已经比较稳定

例如最近在：

```text
Qwen2.5-32B-Instruct-Q4_K_M
```

预测结果：

```text
Predicted:
70.14 s

Actual:
72.59 s
```

实际 Prefill：

```text
378 tokens
5.21 tok/s
```

预测误差大约只有几个百分点。

这个结果比较有意义，因为以前可能会担心：

```text
GPU → TENSOR
```

只是因为我们手工限制了拓扑才被选中。

现在允许：

```text
GPU → CPU → TENSOR
GPU → TENSOR → PHONE
GPU → CPU → TENSOR → PHONE
```

一起竞争以后，说明至少在当前 PC、手机和网络环境下，这个区域本身确实比较有优势。

---

# 三、继续探索更充分的流水：RETURN_WAVEFRONT

## 1. 为什么还要进一步做 RETURN_WAVEFRONT

现在的 Stage Queue 已经能够实现比较粗粒度的流水：

```text
GPU0 ──────────────┐
                   ↓
               TENSOR0

      GPU1 ──────────────┐
                         ↓
                     TENSOR1
```

也就是：

> **GPU 在计算下一个 macro 时，CPU/手机可以处理上一个 macro。**

但是进入 Tensor Split 内部以后，目前大部分情况下还是存在层级 barrier。

例如一层里面：

```text
Attention
    ↓
Tensor FFN Chunk 0
Tensor FFN Chunk 1
Tensor FFN Chunk 2
Tensor FFN Chunk 3
    ↓
等待这一层全部完成
    ↓
下一层 Attention
```

这里可能存在一个空隙：

PC 本地部分已经完成了，但是手机最后几个 chunk 还没有返回。

此时 PC 实际上处于等待状态。

---

# 2. RETURN_WAVEFRONT 的思路

所以尝试做了更细的跨层 wavefront。

例如：

```text
Layer L

Chunk0 → 完成
Chunk1 → 完成
Chunk2 → PC完成，Phone还在计算
Chunk3 → Phone还没返回
```

如果下一层某一部分依赖已经满足，就希望不要等整个 Layer L 全部结束，而是提前进入：

```text
Layer L+1 Attention
```

时间线上希望变成：

```text
Layer L:
PC FFN     ███████████████
Phone FFN        ██████████████████
Return                     ███████

Layer L+1:
Attention                 ███████
```

而不是：

```text
Layer L:
PC FFN     ███████████████
Phone FFN        ██████████████████
Return                     ███████
                                   ↓
Layer L+1 Attention                  ███████
```

目标就是：

> **进一步利用 PC 等待手机返回的空隙，把下一层 Attention 提前做掉。**

---

# 3. 实际效果目前比较一般

这个思路在依赖关系上是成立的，但是实际做下来发现：

> **流水变细以后，不一定意味着整体速度一定更快。**

RETURN_WAVEFRONT 会引入很多新的运行时开销：

```text
更多小 subgraph
更多 chunk boundary
更多 graph_compute_async
更多 RPC command
更多 snapshot
更多 enqueue / wakeup
更多 scheduler 遍历
更多小 GEMM
更多 reduce ADD
```

所以虽然理论上：

```text
Overlap ↑
```

但是同时：

```text
Scheduling Overhead ↑
Kernel Overhead ↑
RPC Control Overhead ↑
```

最后可能把节省下来的等待时间抵消。

早期测试中还出现过后半段越来越慢的问题。

后来通过限制：

```text
Phone outstanding request
```

等方式以后，严重的后期恶化已经有所改善，但是从总体 Prefill 时间来看：

```text
RETURN_WAVEFRONT
```

目前仍然没有稳定超过现在的 coarse Stage Queue。

---

# 4. 目前对 RETURN_WAVEFRONT 的定位

所以现在没有把它作为主执行路径。

当前主线仍然是：

```text
Stage Queue
+
backend-specific chunks
+
自动 planner
```

而：

```text
RETURN_WAVEFRONT
```

保留成一个进一步探索的方向。

因为它本身揭示了一个比较有价值的问题：

> **更细粒度流水存在一个“隐藏等待时间”和“引入调度开销”的平衡点。**

后面如果能够降低：

```text
subgraph overhead
RPC command overhead
snapshot overhead
small GEMM overhead
```

这种跨层 wavefront 仍然可能有价值。

所以目前更准确的结论不是：

```text
RETURN_WAVEFRONT 没有意义
```

而是：

```text
当前实现的粒度已经比较细，
新增调度开销暂时大于隐藏等待带来的收益，
但这种跨层 pipeline 仍然值得继续探索。
```

---

# 四、把协同框架迁移到 MoE 模型

## 1. 为什么开始做 MoE

前面这一套工作主要是在 Dense 模型上做，例如：

```text
Qwen2.5-32B
```

Dense FFN 每个 token 都会经过：

```text
Gate
Up
Down
```

整套 FFN。

但是 MoE 模型不一样。

例如现在测试的：

```text
Qwen3-30B-A3B-Q4_K_M
```

一层里大概是：

```text
Hidden State
     ↓
Router
     ↓
Top-K Experts
     ↓
只计算被选中的 Experts
     ↓
Combine
```

因此一个 token 并不会计算所有 expert。

---

# 2. Router 不需要提前决定所有层的 Expert

MoE 模型加载时还是先把所有 expert 权重加载好。

推理过程中：

```text
Layer 0 Hidden
    ↓
Router
    ↓
选择这一层的 Experts
    ↓
Expert Compute
    ↓
Layer 1 Hidden
    ↓
Router
    ↓
重新选择 Experts
```

所以不同 layer、不同 token 实际激活的 expert 都可能不一样。

但是：

```text
每个 token 激活多少个 Expert
```

通常由模型的：

```text
n_expert_used
```

固定。

因此从协同计算角度看，它和 Dense 仍然有一些相似性。

---

# 3. MoE Tensor Split 仍然可以继续使用 R

现在没有采用：

```text
一部分 Expert 全放 PC
另一部分 Expert 全放 Phone
```

而是继续沿用原来 Tensor Split 的思路。

例如：

```text
R = 0.67
```

表示每一个 Expert 的 FFN shard：

```text
PC:
67%

Phone:
33%
```

Router 如果这一层选择了：

```text
Expert 3
Expert 17
Expert 42
...
```

那么：

```text
                   Router / Top-K
                         ↓
              selected experts
                   ┌─────┴─────┐
                   ↓           ↓
              PC Expert     Phone Expert
              67% shard      33% shard
                   ↓           ↓
                   └──── reduce ┘
```

所以现有：

```text
R
XT
```

这两个规划变量仍然可以保留。

---

# 4. MoE Profiling 也接进预测器

Dense 中原来测的是：

```text
FFN
Attention
Full Layer
```

MoE 中现在新增了：

```text
Router
Top-K
Expert MUL_MAT_ID
Expert Combine
```

也就是实际测：

```text
tokens
+
R
+
Expert branch
```

在 PC 和 Phone 上需要多长时间。

例如：

```text
XT=48
R=0.33
```

会实际 profile：

```text
PC:
Router
+ Top-K
+ 33% Expert shard

Phone:
Router
+ Top-K
+ 67% Expert shard
```

然后继续送进原来的：

```text
Tensor Split cost model
```

所以 planner 主体不需要重新写一套。

仍然是：

```text
Profiling
   ↓
Topology + R + XT
   ↓
Coarse Search
   ↓
XG / XC / XP
   ↓
Stage Simulator
   ↓
Memory Filter
   ↓
AUTO
```

---

# 5. Qwen3-MoE 已经开始能够自动选择方案

目前 Qwen3-MoE 已经可以进入完整自动规划流程。

例如最近一次自动选择：

```text
T=2
P=0
C=46
G=11

R=0.333

XG=256
XC=256
XT=48
XP=4
```

布局是：

```text
GPU 11
   ↓
CPU 35
   ↓
Tensor Split 2
```

也就是说预测器并没有因为这是 MoE 就强行让大量层参与手机协同，而是根据 profiling 以后自动认为：

```text
GPU + CPU
```

承担大部分计算，

最后只有少量层使用：

```text
PC + Phone Tensor Split
```

---

# 6. 当前 MoE Prefill 已经达到两位数

目前在 Qwen3-30B-A3B 上，Prefill 已经能够达到：

```text
10+ tok/s
```

相比之前手机单独运行：

```text
约 1.x tok/s
```

已经有比较明显的提升。

这也说明前面 Dense 模型上建立的：

```text
GPU
CPU
Tensor Split
Stage Queue
Auto Planner
```

整体框架是可以迁移到 MoE 的，并不需要完全重新设计系统。

---

# 7. MoE 目前还有一个主要问题：预测误差更大

虽然当前 MoE 实际速度已经比较好，但是：

```text
Predicted Time
vs
Actual Time
```

之间的误差目前明显比 Dense 大。

这其实也比较容易理解。

Dense 中：

```text
每个 token
→ 基本执行相同 FFN
```

而 MoE 中：

```text
每个 token
→ Router
→ 不同 expert
```

不同 prompt 可能导致：

```text
Expert routing 分布不同
Expert batching 不同
MUL_MAT_ID GEMM 大小不同
```

例如同样是：

```text
48 tokens
```

可能一种输入让 token 集中在少数 experts：

```text
Expert 3  ← 很多 token
Expert 7  ← 很多 token
```

另一种输入则分散到更多 experts。

即使总共都是：

```text
tokens × Top-K
```

实际 GPU / CPU kernel 效率也可能不同。

所以后面 MoE predictor 可能需要进一步把：

```text
Expert distribution
Expert batching efficiency
```

考虑进去。

---

# 五、下一步

现在整体路线可以概括成：

```text
原来：
统一 Chunk
+
固定 / 局部参数选择
+
Dense 模型
```

逐渐变成：

```text
Backend-specific Chunk
       ↓
全拓扑 Auto Planner
       ↓
GPU / CPU / Phone Stage Pipeline
       ↓
Dense + MoE
```

接下来主要有三个方向。

第一，继续验证 MoE 下：

```text
Predicted
vs
Actual
```

为什么偏差比较大，并针对 Router / Expert batching 改进预测模型。

第二，使用更真实的 Prompt workload 做 benchmark。

例如：

```text
WikiText
WildChat
其他 conversation / corpus 数据集
```

分别采样不同长度的 prompt，而不是一直只使用单一长上下文。

这样既可以测试：

```text
真实 Prefill tok/s
```

也可以观察：

```text
不同 Prompt
→ 不同 MoE routing
→ predictor error 是否变化
```

第三，RETURN_WAVEFRONT 暂时不作为主线，但继续保留。因为我觉得这个潜力比较大，还是有很大突破的。

如果后面能够减少：

```text
subgraph
RPC command
snapshot
scheduler
small kernel
```

带来的开销，那么更细粒度的跨层流水仍然可能带来额外收益。

---

第四，上高通手机平台进行测试，因为llama.cpp提供了gpu和npu支持，加上的话可以让推理更快。
# 六、目前整体状态


目前这套系统已经从最开始比较固定的：

```text
PC CPU + Phone
```

逐渐扩展成：

```text
                 Auto Planner
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
        GPU          CPU         Phone
          │           │           │
          └────── Tensor Split ───┘
                      │
                   Pipeline
```

下一步重点不再只是继续增加新的执行路径，而是让：

> **不同模型结构、不同 prompt 和不同设备环境下，预测器都能够比较稳定地找到接近最优的执行方案。**
