# 本周主要进展

这周主要做了两部分。

第一部分是**把电脑端 GPU 加进原来的 PC–手机协同框架里，并继续做 GPU 流水**。

第二部分是**做自动预测器**。之前很多参数，比如多少层做 Tensor Split、多少层放手机、多少层留 PC，包括切分比例和 chunk 数，基本还是需要手工测试。现在开始改成程序启动时先测一次当前设备的性能，然后自动估计不同分配方案的耗时，最后选择一个比较合适的方案。

---

# 一、把 PC GPU 加入协同计算

## 1. 原来只有 PC CPU + Phone

之前整体上主要是两端：

```text
PC CPU
   │
   │  RPC
   ▼
Phone
```

不同层可以做：

```text
Tensor Split
Phone Only
PC Only
```

但是 PC Only 实际上还是主要落在电脑 CPU 上。

这周做的一个变化就是把 CUDA GPU 加进来。

现在变成：

```text
                    PC
             ┌──────┴──────┐
             │             │
            CPU           GPU
             │             │
             └──────┬──────┘
                    │
                   RPC
                    │
                    ▼
                  Phone
```

也就是说，现在不再只是决定：

```text
这一层给 PC
还是给 Phone
```

而是进一步可以决定：

```text
Tensor Split
Phone Only
PC CPU
PC GPU
```

目前 GPU 主要是放在原来 PC Only 的区域里面。比如一个方案里 PC 一共有 20 层，可以再从里面挑一部分直接放到 CUDA 上跑，而剩下的继续在 CPU 上跑。当前实现也已经把 `gpu_pc_layers` 作为单独的规划维度，实际加载模型时会把对应 PC-only 层分配给 CUDA。

这样做的目的比较直接：

> **以前手机承担一部分计算之后，PC 剩余部分主要还是 CPU 在跑；现在希望把电脑现有的 GPU 算力也利用起来，把系统从 CPU+Phone 两端协同扩展成 CPU+GPU+Phone 三端协同。**

---

# 2. 加 GPU 之后又出现一个问题：GPU 和其他设备还是可能串行

单纯把几层搬到 GPU，并不代表 GPU 利用率就一定高。

比如两个 ubatch 如果还是这样：

```text
ubatch 0：

CPU / Phone
    ↓
GPU
    ↓
CPU / Phone
    ↓
完成


ubatch 1：

等 ubatch 0
    ↓
CPU / Phone
    ↓
GPU
    ↓
...
```

那么 GPU 算完以后可能就在等 CPU/手机，CPU 和手机又可能在等 GPU。

所以继续做了一层 **ubatch 之间的 GPU 流水**。

思路是把一张完整计算图按照 backend 区域拆成几个 stage，例如：

```text
PRE
 │
 ▼
GPU
 │
 ▼
POST
 │
 ▼
TAIL
```

这里不用把 PRE、POST 理解成某一个固定设备，本质上是：

```text
GPU 前面的计算
GPU 区域
GPU 后面的 CPU / RPC 计算
最后的 GPU 区域
```

然后用两个 pipeline slot 交替处理 ubatch。

理想情况下时间线上就不是：

```text
ubatch0: PRE → GPU → POST
                         ↓
ubatch1:                 PRE → GPU → POST
```

而是让不同 ubatch 的阶段互相穿插：

```text
时间 ───────────────────────────────────→

ubatch0: PRE0 → GPU0 ─────────→ POST0
                         ║
                         ║
ubatch1:              PRE1 → GPU1 ─────────→ POST1
                              ║
                              ╚── GPU1 和 POST0 重叠
```

当前代码就是两个 scheduler/slot 交替执行：当前 ubatch 的 GPU 工作提交后，再去运行上一 ubatch 的 POST，从而尝试把 GPU 计算和 CPU/RPC 计算叠起来。

核心思路其实就是一句话：

> **不只是“把一些层扔给 GPU”，而是尽量让 GPU 在算当前 ubatch 的时候，CPU 和手机去处理上一个 ubatch，减少不同设备之间互相等待。**

---

# 3. GPU 流水目前的状态

GPU 接入现在已经完成，流水框架也已经接上了，不过目前还在处理一个拓扑兼容问题。

最近自动规划之后实际得到了一种：

```text
CPU
 ↓
Meta(CPU, Phone)
 ↓
CUDA
 ↓
Meta(CPU, Phone)
 ↓
CPU
```

也就是只有一个连续 GPU 区域。

但目前 PIPE2 的判断条件还是之前写的，要求计算图中必须出现两个 GPU region：

```text
PRE → GPU1 → POST → GPU2
```

所以现在这种只有一个 GPU region 的图会出现：

```text
does not support PIPE2 topology
falling back to serial execution
```

代码里的 topology checker 当前确实写死要求 `gpu_region == 2`。

所以这一块目前的进度可以说是：

```text
GPU 加入计算        √
GPU 层动态分配      √
GPU 流水框架        √
不同 GPU 拓扑兼容   进行中
```

下一步主要就是让流水不仅支持：

```text
PRE → GPU1 → POST → GPU2
```

也能支持更常见的：

```text
PRE → GPU → POST
```

这样自动预测器给出的不同设备布局都能进入流水，而不是遇到某种 topology 就退回串行。

---

# 二、自动预测器

## 1. 为什么要做预测器

现在协同推理里面已经有比较多可以调的参数：

```text
Tensor Split 做多少层？
Phone Only 做多少层？
PC Only 做多少层？

Tensor Split 的 PC / Phone 比例是多少？
PC Only 里面多少层交给 GPU？
Prefill 分成几个 chunk？
```

可以把一个方案简单表示成：

```text
T / P / C / R / G / K
```

其中：

```text
T = Tensor Split 层数
P = Phone Only 层数
C = PC Only 层数

R = Tensor Split 中 PC 所占比例
G = PC Only 中放到 GPU 的层数
K = Tensor Split 的 chunk 数
```

这些参数不是相互独立的。

比如增加手机承担的计算，PC 压力会下降，但 RPC 通信可能增加；增加 GPU 层数可以提高 PC 侧计算速度，但又受到显存限制；增加 chunk 数可以增加流水重叠，但 chunk 太小以后通信和调度开销又会上升。

所以以前如果手工调，大概是：

```text
设置一组参数
     ↓
启动模型
     ↓
实际运行
     ↓
记录速度
     ↓
重新改参数
     ↓
再跑一次
```

模型、电脑、手机或者网络环境一换，之前调出来的参数很可能又不合适。

所以这部分现在想做成：

```text
设备 Profiling
      ↓
建立性能和内存画像
      ↓
枚举候选方案
      ↓
先过滤跑不下的方案
      ↓
预测剩余方案的执行时间
      ↓
自动选择一个比较优的配置
```

核心就是：

> **不再提前规定手机一定跑多少、GPU 一定跑多少，而是根据当前设备的真实性能、通信和内存条件自动决定怎么切。**

---

# 2. 第一步：启动时先做 Profiling

预测器启动以后，先用比较小的计算图测试当前设备。

主要测三类东西：

```text
               Profiling
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
      Compute      RPC       Memory
```

## 计算性能

分别测试：

```text
PC CPU
Phone
PC GPU
```

现在会测试：

```text
FFN
Attention
完整 Layer
Phone block
```

Tensor Split 还会进一步测试不同：

```text
FFN ratio
+
token / chunk size
```

例如：

```text
PC 负责 75% FFN 时需要多少 ms
Phone 负责 25% FFN 时需要多少 ms

8 token / chunk 多久
16 token / chunk 多久
```

所以后面预测一个 Tensor Split 方案时，不是简单按理论 FLOPS 算，而是尽量使用当前设备真正跑出来的数据。

---

# 3. 通信也直接实测

协同计算不能只看 CPU、GPU 或手机谁算得快，还必须把 RPC 算进去。

所以启动时还会测试：

```text
PC → Phone
Phone → PC
```

在不同 tensor 大小时需要多长时间。

因为可能出现：

```text
Phone compute = 10 ms
RPC           = 3 ms
```

这种情况手机参与比较划算。

但也可能是：

```text
Phone compute = 10 ms
RPC           = 30 ms
```

这种情况下即使手机本身算得快，总体也不一定值得把任务交过去。

另外目前 Tensor Split 已经做了 chunk pipeline，通信和后续计算能够有一部分重叠，所以预测时也不能简单认为：

```text
总时间 = Compute + RPC
```

更接近的是根据时间线判断：

```text
当前 chunk 在传输
        ║
        ║ overlap
下一 chunk 在计算
```

能被计算覆盖掉的通信不会全部暴露在最终关键路径上。

所以预测器真正关注的是：

> **最终暴露出来的通信成本，而不是简单把所有 TCP 时间直接相加。**

---

# 4. 第二步：枚举所有可能的方案

Profiling 完成以后，再开始组合：

```text
T / P / C / R / G / K
```

例如：

```text
T=24
P=8
C=32
R=0.75
G=12
K=2
```

可以理解成：

```text
24 层 Tensor Split
    PC 75%
    Phone 25%

8 层 Phone Only

32 层 PC Only
    其中 12 层交给 GPU

Tensor Split:
    每个 ubatch 分 2 个 chunk
```

预测器会自动遍历这些组合，而不是提前写死一个方案。

因此不同设备最后可能得到完全不同的结果。

比如：

```text
PC GPU 很快
→ G 增加

Phone 算力强、局域网快
→ T / P 可能增加

Phone 内存比较小
→ T / P 会受限制

RPC 比较慢
→ 更倾向 PC CPU / GPU
```

---

# 5. 第三步：先做内存过滤

这一块最近也做了一次比较重要的修改。

最开始内存估计主要考虑：

```text
模型权重
+
KV Cache
```

这种方法对纯 PC / GPU 层还比较容易估计，但是对 Tensor Split 会有问题。

因为实际运行 Tensor Split 时还有：

```text
FFN activation
临时计算 buffer
transfer buffer
reduce buffer
pipeline 中同时存活的 chunk
```

静态估算认为内存能放下，所以预测器把这个方案选出来了。

但是实际执行到一半以后，运行时峰值内存比预测值高，最后进程被系统杀掉。

这说明只计算：

```text
Weights + KV
```

还不够。

---

# 6. 现在 Tensor Split 开始按“峰值内存”估算

所以现在把内存模型改成：

```text
Required Memory

=
Static Memory
+
Runtime Peak
```

其中静态部分还是：

```text
模型权重
+
KV Cache
```

而 Tensor Split 的运行时部分开始直接从 Profiling 获取。

现在 FFN profiling 在实际建立计算图以后，会记录 backend 真正申请的 buffer 大小，然后扣掉 FFN 权重：

```text
FFN runtime memory

≈ backend allocated buffer
 - gate / up / down weights
```

也就是说现在不是人为规定：

而是实际测：

```text
这个设备
这个 FFN
这个 split ratio
这个 token 数

运行时真正需要多大的 working set
```

然后把这个值用于候选方案的内存判断。

---

# 7. K 现在也真正参与内存估计

这一点也比较重要。

以前：

```text
K=1
K=2
K=4
```

对内存估计基本没有区别。

现在会先根据实际 ubatch 算：

```text
chunk_tokens
=
ubatch_tokens / K
```

例如：

```text
ubatch = 128

K=1 → 128 token / chunk
K=2 → 64 token / chunk
K=4 → 32 token / chunk
```

然后分别估计：

```text
PC FFN runtime
Phone FFN runtime
transfer live buffer
```

所以现在 K 对内存的影响会自然体现出来。

而且因为 pipeline 中一般不是所有 chunk 同时存活，所以也不是：

```text
K=4
→ 四个 chunk 的 buffer 全部同时存在
```

而是按实际流水关系近似：

```text
一个 chunk 正在计算
+
前一个 chunk 正在传输 / reduce
```

也就是估计同时 live 的 chunk 数。

这样比简单给每个 chunk 固定增加一块 reserve 要合理得多。

---

# 8. PC Attention 的运行时峰值也需要考虑

目前 Tensor Split 的 FFN runtime 已经开始进入内存模型。

下一步还需要继续把 PC Attention 的：

```text
Q / K / V 临时 tensor
Attention mask
Flash Attention workspace
输出 tensor
```

也放进 PC runtime peak。

这里不是简单把：

```text
Attention runtime
+
FFN runtime
```

全部相加。

因为在一层里面，它们主要是前后两个阶段：

```text
Attention
    ↓
Tensor Split FFN
```

所以更合理的是估计：

```text
PC Runtime Peak
=
max(
    Attention Runtime,
    FFN Runtime + Transfer Runtime
)
```

最终 PC 内存模型会逐渐变成：

```text
PC Required Memory

=
Weights
+
KV Cache
+
max(
    Attention Working Set,
    FFN Working Set + Pipeline Transfer Buffer
)
```

这样得到的是更接近实际运行时的峰值，而不是把所有临时 tensor 都机械累加。

---

# 9. 内存过滤之后再预测性能

所有候选方案先经过：

```text
PC Memory Check
Phone Memory Check
GPU VRAM Check
```

不满足内存要求的方案直接排除。

剩下的方案再计算性能。

目前整体可以理解为：

```text
Predicted Time

=
Tensor Split Cost
+
Phone Only Cost
+
PC CPU Cost
+
PC GPU Cost
+
Handoff Cost
```

对应：

```text
predicted_tensor_ms
predicted_phone_ms
predicted_pc_cpu_ms
predicted_pc_gpu_ms
predicted_handoff_ms
```

Tensor Split 里面又不是简单算：

```text
PC 时间 + Phone 时间 + RPC 时间
```

而是根据 chunk 的执行时间线考虑：

```text
PC Compute
Phone Compute
H2D
D2H
Reduce
Overlap
```

最后估出 Tensor Split 区域什么时候真正完成。

---

# 10. 最后自动选择

最后整个流程就是：

```text
CPU / GPU / Phone Profiling
            ↓
RPC Profiling
            ↓
Memory Profiling
            ↓
枚举 T/P/C/R/G/K
            ↓
估算 Static + Runtime Peak
            ↓
淘汰 OOM 方案
            ↓
预测剩余方案执行时间
            ↓
选择 predicted time 最小的方案
            ↓
发布给实际 runtime
```

所以预测器最终解决的不只是：

```text
“哪种方案理论计算量最少？”
```

而是：

```text
“在这台电脑、
这块 GPU、
这部手机、
这个局域网、
以及当前内存条件下，
哪个方案实际最值得跑？”
```

---

# 11. 当前实际结果

目前预测器已经可以跑到真实的大模型上。

例如：

```text
Qwen2.5-32B-Instruct-Q4_K_M.gguf
```

当前测试结果：

```text
Prompt:      4.8 tok/s
Generation:  1.6 tok/s
```

这组结果现阶段更重要的意义不是说已经找到全局最优，而是说明：

```text
Profiling
   ↓
自动枚举
   ↓
内存过滤
   ↓
性能预测
   ↓
自动生成配置
   ↓
真实模型运行
```


这一部分可以概括成：

> **预测器现在已经从单纯的“速度估计”逐渐变成一个同时考虑设备算力、RPC 通信、运行时峰值内存和流水重叠的自动配置器。**


| 模型 / 运行方式        | Prompt / Prefill | Generation / Decode | 说明                      |
| ---------------- | ---------------: | ------------------: | ----------------------- |
| Llama-7B 手机单跑    |       12.8 tok/s |           3.3 tok/s | 手机本地运行                  |
| Llama-7B 协同      |       11.5 tok/s |       **5.4 tok/s** | Decode 相比手机约 **1.64×**  |
| Qwen3-30B-A3B 手机 |        1.3 tok/s |           1.5 tok/s | BigMoeOnEdge 按需加载       |
| Qwen3-30B-A3B 协同 |    **6.9 tok/s** |       **6.1 tok/s** | 非最佳参数；约 **5.3× / 4.1×** |
| Qwen2.5-32B 预测器  |    **4.8 tok/s** |       **1.6 tok/s** | 自动 Profiling + 自动规划     |
