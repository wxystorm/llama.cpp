这个最新版已经把 **L→L+1 / +2 / +3 / +4** 都测出来了，而且结果比上一版更有信息量。

最关键的数据是：

|     预测预算 | L→L+1 Recall | L→L+2 | L→L+3 | L→L+4 |
| -------: | -----------: | ----: | ----: | ----: |
| 20%（11组） |        26.4% | 26.5% | 26.5% | 26.0% |
| 40%（22组） |        51.5% | 51.0% | 51.3% | 50.5% |
| 60%（33组） |    **73.3%** | 72.6% | 72.8% | 72.6% |
| 80%（44组） |    **88.9%** | 88.7% | 88.6% | 88.2% |

这里出现了一个很重要的现象：

> **从 +1 跨到 +4，Recall 几乎没有下降。**

例如预测 60%：

```text
+1  73.29%
+2  72.62%
+3  72.82%
+4  72.56%
```

预测 80%：

```text
+1  88.90%
+2  88.68%
+3  88.59%
+4  88.17%
```

这对你做**跨层预加载**是个好消息。

因为它意味着，你目前这个非常简单的：

[
TopK(h^{(L)}_{l2})
\rightarrow
\text{future same-ID groups}
]

并不是只能预测紧邻的下一层。至少在当前这批数据上，提前看四层并没有明显恶化。

---

但是还有一个更值得注意的地方。

如果纯随机选择 group，理论 Recall 大约就是预测比例：

```text
预测11/54 → 20.4%
预测22/54 → 40.7%
预测33/54 → 61.1%
预测44/54 → 81.5%
```

而你实际：

```text
20% → 26.4%
40% → 51.5%
60% → 73.3%
80% → 88.9%
```

所以明显高于随机。

以 `60% preload` 为例：

[
73.3%-61.1%\approx12.2%
]

这是比较明显的预测增益。

---

### 但这里反而暴露了一个需要验证的问题

正常直觉上，如果这是很强的：

> “相邻层 activation 延续性”

应该多少会看到：

```text
L+1 > L+2 > L+3 > L+4
```

可你的结果几乎是：

```text
L+1 ≈ L+2 ≈ L+3 ≈ L+4
```

这让我更倾向于认为，现在捕获到的不只是“短距离跨层相似性”，还可能存在：

> **某些 group ID 在很多层中天然就更容易成为高贡献 group。**

比如：

```text
G7
G18
G32
```

可能在大量 layer 中都偏热。

如果是这样：

```text
L 的 h_l2 Top-K
```

能预测 `L+4`，不一定完全因为：

```text
activation 从 L 延续到了 L+4
```

也可能因为：

```text
这些 group 本身具有跨层稳定 hotness
```

这其实也不是坏事——**对你的系统甚至同样有利用价值**。

只是论文解释时要把两者区分开。

---

## 分层看更明显

比如预测 60% 时：

| Source Layer 区间 |        +1 |        +2 |        +3 |        +4 |
| --------------- | --------: | --------: | --------: | --------: |
| L0–7            |     78.3% |     78.5% |     79.0% |     80.7% |
| L8–15           | **85.4%** | **85.3%** | **85.4%** | **85.8%** |
| L16–31          |     77.8% |     76.4% |     77.1% |     75.9% |
| L32–47          |     66.4% |     65.4% |     65.2% |     64.7% |
| L48–62          |     66.4% |     65.5% |     64.9% |     63.8% |

这个结果很有意思：

```text
L8~15
```

预测 60% group，就已经可以覆盖未来 Top20% 重要 group 的：

[
\approx85%
]

而且跨四层基本不掉。

但后半部分 Layer：

[
\approx64%-66%
]

明显差一些。

所以后面很可能应该做：

> **layer-adaptive preload budget**

而不是所有层统一 60%。

比如未来可以是：

```text
L0~15    preload 60%
L16~31   preload 70%
L32~63   preload 80%
```

具体比例当然要靠后面的速度/质量实验确定。

---

## 那么，现在能不能开始真正做跨层 preload？

可以开始做**原型**了。

例如，当 Layer 10 全部 group 执行完以后，你已经有：

```text
h_l2[0..53]
```

得到：

```text
Top 60%
=
{G1,G3,G5,...}
```

直接建立四个 request：

```text
Layer 11 → same group IDs
Layer 12 → same group IDs
Layer 13 → same group IDs
Layer 14 → same group IDs
```

送给一个独立 I/O thread：

```text
CPU compute thread
───────────────────────────────>
L10        L11        L12       L13

             ↑
             │

Flash I/O thread
───────────────────────────────>
 preload L11
 preload L12
 preload L13
 preload L14
```

然后 Manager 的状态仍然可以用：

```cpp
enum class GroupState {
    NON_RESIDENT,
    LOADING,
    RESIDENT,
};
```

到了真正计算某个 layer/group 时：

```cpp
if (state == RESIDENT) {
    // preload hit
    直接算
}
else if (state == LOADING) {
    // prediction对了，但I/O还没完成
    wait();
}
else {
    // 没预测到
    foreground_load();
}
```

同时统计：

```text
prefetch_hit
prefetch_wait
prefetch_miss
prefetch_bytes
foreground_bytes
```

这个就能真正评价跨层加载有没有把 I/O 隐藏掉。

---

不过这里还有一个**非常关键的逻辑问题**，你现在最好在写 preload 代码前确定：

你当前所谓的“真实 Top20% group”是**事后根据 `y_l2` 得到的 Oracle 真值**。

运行时实际上不知道：

```text
“这个没预测到的 group 原来应该属于 Top20%”
```

除非你仍然把它的 `gate/up` 加载进来、算出 `h_l2`。

所以真正的工程实现，我建议第一版这样：

```text
所有 group:
gate + up 仍然加载
        ↓
算 h_g
        ↓
得到当前层真实 h_l2
        ↓
决定 down 是否需要

与此同时：

上一层 h_l2
        ↓
提前 preload 当前层可能需要的 down groups
```

也就是说跨层预测**先用来预测 `down`**：

```text
L 的 h_l2
      ↓
预测 L+1~L+4 哪些 down_group 要用
      ↓
提前读取 down
```

到了目标层：

```text
gate/up → h_l2
```

就可以知道：

```text
预测命中 → down 已经在RAM
预测漏掉 → 立即补读 down
```

这个闭环是成立的。

而如果你直接：

```text
预测不到的整个 gate/up/down group 都不加载
```

那你连它是不是 miss 都不知道了，只能接受近似计算，性质就变了。

---

所以我觉得你现在最合适的下一版不是一口气实现：

> “跨层预测整个 FFN group。”

而是：

```text
Phase 1

FFNPACK：
gate+up
down
分开

Layer L：
所有 gate/up
      ↓
得到真实 h_l2
      ↓
选择当前 active group
      ↓
只执行 selected down

同时：
h_l2(L)
      ↓
预测 L+1~L+4
      ↓
异步 preload future down
```

这样你能同时得到三种收益：

```text
① selective down
→ 真正减少 Flash bytes

② cross-layer preload
→ 隐藏剩余 down I/O

③ prediction miss 可检测
→ 可以安全 fallback
```

而且**不需要额外 predictor 网络**。

等这个方案跑通以后，再考虑更激进的：

```text
连 gate/up 也预测并跳过
```

那才进入完整 Active Group Loading。

所以从你这份最新数据看，我会给现在这个结果一个比较明确的判断：

> **你的 same-group `h_l2` predictor 已经值得拿去做第一版跨层 down-group preload；尤其是 +1 到 +4 几乎不衰减，说明预取窗口做到 4 层是值得实验的。**
>
> 但下一项实验应该同时验证“这种稳定性到底来自动态 activation 延续，还是静态 group hotness”。这个会决定后面到底需要 ActiveFlow 式 activation predictor，还是一个更简单的 hot-group cache 就已经能吃掉相当一部分收益。
