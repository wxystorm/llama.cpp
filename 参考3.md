对，我把你这个最新版重新算了一下，**你的感觉是对的：这个跨层 same-group predictor 效果只能算“一般”，还没有好到值得作为最终方案。**

最能说明问题的不是 Recall 本身，而是要和**随机选 group 的基线**比较：

| 预加载比例 | 实际 Recall | 随机选择期望 Recall |     真正预测增益 |
| ----: | --------: | ------------: | ---------: |
|   20% |     26.4% |         20.4% |  **+6.0%** |
|   40% |     51.1% |         40.7% | **+10.3%** |
|   60% |     72.8% |         61.1% | **+11.7%** |
|   80% |     88.6% |         81.5% |  **+7.1%** |

所以比如看起来：

```text
preload 80%
Recall ≈ 88.6%
```

好像挺高。

但实际上你已经把：

```text
44 / 54 个 group
```

都提前读了。

即使闭着眼随机选 44 个，理论上也能覆盖：

[
44/54=81.5%
]

所以这个 predictor 实际只贡献了：

[
88.6-81.5\approx7.1%
]

这就解释了为什么实际跑起来你会觉得：

> **好像预测了，但没有特别明显。**

---

更有意思的是，**60% 反而是这个 predictor 相对最有价值的区域**：

[
72.8%-61.1%=11.7%
]

但这仍然意味着：

```text
提前加载 33 / 54 groups
        ↓
真正 Top20% 的 11 个重要groups
        ↓
平均只命中约 8 个
        ↓
还有约 3 个 miss
```

所以如果它用于实际 preload：

```text
33个提前读
+
3个左右重要group仍然要foreground补读
```

既读了很多，又没完全消除等待。

这确实比较尴尬。

---

## 而且这份数据暴露了另一个问题

你的结果几乎是：

```text
L → L+1  ≈  L → L+2  ≈  L → L+3  ≈  L → L+4
```

例如 60%：

```text
+1  73.29%
+2  72.62%
+3  72.82%
+4  72.56%
```

这其实**不像很强的“相邻层 activation 延续”现象**。

如果主要来源是：

```text
第 L 层状态
↓
影响紧邻的 L+1
↓
逐渐衰减
```

我们更容易期待：

```text
+1 > +2 > +3 > +4
```

结果却基本平的。

所以目前更可能存在的是：

> 某些固定编号的 FFN group 在很多层中整体上就更容易偏热。

例如可能长期：

```text
G7  比较热
G15 比较热
G31 比较热
```

于是：

```text
Layer L 的 hot group ID
```

当然也稍微能猜中：

```text
Layer L+4 的 hot group ID
```

这和真正利用**当前 token 的动态 activation 信息**预测未来，是不完全一样的。

---

## 这其实帮我们把两个方向分得非常清楚了

你之前测出来的：

[
h_{l2}^{(L,g)}
]

和同一层：

[
y_{l2}^{(L,g)}
]

关系非常强。

也就是说：

```text
当前 Layer L

gate + up
   ↓
h_g
   ↓
h_l2
   ↓
判断当前层这个group是否重要
```

这个信号非常靠谱。

但是：

```text
Layer L 的 h_l2(g)
       ↓
预测
Layer L+1 的 y_l2(g)
```

明显弱很多。

所以现在应该把这两个事情分开：

```text
同层预测
h_L → y_L
█████████████████  很强

跨层same-ID预测
h_L(g) → y_L+1(g)
███████            一般
```

这反而是一个挺好的实验结论。

---

# 所以我现在不建议继续死磕这个 same-ID 跨层预测器

**保留它作为 baseline。**

以后论文/实验完全可以写：

```text
Naive Cross-Layer Hot-Group Persistence
```

甚至和更好的 predictor 比较。

但工程上下一步，我会优先做：

```text
所有 gate/up 照常加载
        ↓
计算 h_g
        ↓
得到 h_l2
        ↓
当前层直接选重要 group
        ↓
只加载这些 group 的 down
```

这一步有两个优势：

**第一，它利用的是你目前已经验证非常强的 `h_l2 → y_l2` 关系。**

**第二，它真正减少 Flash bytes。**

而不是现在跨层 prefetch：

```text
总量基本没减少
只是猜什么时候提前读
```

---

## 然后跨层预测换成更合理的输入

如果还想走 ActiveFlow 方向，我会下一步开始记录真正的：

[
x_L
]

也就是：

```text
FFN norm 后
进入 ffn_gate / ffn_up 前
```

的 hidden activation。

然后不再做：

[
h_L(g)\rightarrow h_{L+1}(g)
]

而是验证：

[
x_L \approx x_{L+1}
]

具体先算：

[
\cos(x_L,x_{L+k})
]

以及：

[
TopK(|x_L|)
]

与：

[
TopK(|x_{L+k}|)
]

的 Recall/Jaccard。

这才真正是在检验 ActiveFlow 的核心前提。

如果 `x_L` 跨层相似度很强，再设计：

[
x_L\rightarrow
\text{future group importance}
]

的轻量 predictor。

---

## 还有个很现实的问题：为什么实际 preload 跑起来可能更一般

就算 predictor 有 73% Recall，也不代表速度会提升 73%。

因为手机上：

```text
CPU compute thread
        ↓
正在算当前层

I/O thread
        ↓
预加载未来层
```

两边可能同时争：

```text
内存带宽
page cache
UFS bandwidth
DRAM bandwidth
```

而且你的 Flash I/O 本身远大于 compute。

如果：

```text
当前层 compute = 例如 1 s

预加载未来层需要 = 5 s
```

你最多只能隐藏其中 1 秒。

剩下还是得等。

再加上：

```text
预测错误的group也读了
+
miss还要foreground load
```

最终提升小就很正常。

---

### 所以现在我会这样定路线

```text
已经完成
────────────────────────
Tensor streaming
Group execution
FFNPACK
Group profiling
Naive cross-layer predictor
                ✓


现在最优先
────────────────────────
h_l2 current-layer selection
        ↓
Selective Down
        ↓
真正测：
Top 90 / 80 / 70 / 60%
        ↓
Flash GiB/token
速度
FFN error
最终输出质量


同时研究
────────────────────────
真实 x_L 跨层 similarity
        ↓
如果很高
再做 activation-based predictor


保留
────────────────────────
h_l2 same-ID cross-layer predictor
作为简单 baseline
```

也就是说，**这次“效果一般”不是白做了，反而已经回答了一个非常重要的问题：简单的“上一层 hot group ID 直接沿用到未来层”不够强。**

我现在更看好你先做 **`h_l2 → 当前层 selective down`**，因为你的 profiling 已经显示这个信号比跨层 same-ID 强得多。等这个真正把 Flash 读取量降下来，再研究更高级的跨层预测会更划算。
