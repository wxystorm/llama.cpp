对，而且这里有一个非常关键的转折：

> **不能只把“文件读取”从整个 tensor 改成 group 读取。计算本身也必须从“整个 FFN tensor 一次算完”改成“按 FFN 中间神经元 group 分块计算并累加”。**

否则现在的 `GGML_OP_MUL_MAT` 仍然认为 `ffn_up.weight` 是完整矩阵，你只加载其中一块，它还是会访问整个 tensor。

PowerInfer-2 本质上也是把 **neuron cluster 同时作为计算和存储的基本单位**，而不仅仅是把文件切小。([arXiv][1])

---

## 1. 先看 Qwen 的 FFN 到底应该沿哪个维度切

以 SwiGLU FFN 为例，逻辑上是：

[
g=\mathrm{SiLU}(W_{gate}x)
]

[
u=W_{up}x
]

[
h=g\odot u
]

[
y=W_{down}h
]

假设：

```text
hidden_size = 5120
ffn_size    = 27648
```

那么可以把 **27648 个 FFN 中间神经元**分组：

```text
FFN intermediate dimension

0                                     27648
│──────────────────────────────────────│
│ G0 │ G1 │ G2 │ G3 │ ... │ Gn       │
│512 │512 │512 │512 │     │          │
```

例如：

```text
group_size = 512 neurons
```

那么一个 group `Gk=[j0,j1)` 对应：

```text
gate 的第 j0~j1 个输出神经元
up   的第 j0~j1 个输出神经元

以及

down 的第 j0~j1 个输入神经元
```

这三个必须**绑定成同一个 group**。

---

# 2. 最关键的数学变换

原来是：

```text
完整 gate
      ↓
完整 up
      ↓
产生整个 FFN intermediate
      ↓
完整 down
```

现在可以精确拆成：

```text
Group 0
gate[0:512]
up[0:512]
        ↓
h0
        ↓
down[:, 0:512]
        ↓
partial_y0

Group 1
gate[512:1024]
up[512:1024]
        ↓
h1
        ↓
down[:, 512:1024]
        ↓
partial_y1

...

最后：
y = partial_y0
  + partial_y1
  + ...
```

数学上：

[
y=
\sum_g
W_{down}^{(g)}
\left[
\mathrm{SiLU}(W_{gate}^{(g)}x)
\odot
(W_{up}^{(g)}x)
\right]
]

这非常重要。

因为它意味着你**暂时根本不需要做稀疏预测**。

先：

```text
所有 group 都算
```

输出应当和原来基本一致，只可能因为浮点累加顺序不同产生微小数值差异。

这样我们可以先把：

> **Tensor-level FFN → Group-level FFN**

彻底打通。

然后再做：

```text
只算 active groups
```

那才真正减少 Flash read/token。

---

# 3. llama.cpp 里具体长什么样

先打印 Qwen 的：

```cpp
ffn_up->ne[0]
ffn_up->ne[1]

ffn_gate->ne[0]
ffn_gate->ne[1]

ffn_down->ne[0]
ffn_down->ne[1]
```

你大概率会看到类似：

```text
ffn_up:
ne[0] = hidden_size
ne[1] = ffn_size

ffn_gate:
ne[0] = hidden_size
ne[1] = ffn_size

ffn_down:
ne[0] = ffn_size
ne[1] = hidden_size
```

所以图形上：

```text
            hidden_size

ffn_up
        ┌──────────────────┐
group0  │██████████████████│
group1  │██████████████████│
group2  │██████████████████│
        │                  │
        └──────────────────┘
              ↑
        沿 ne[1] 切
```

`ffn_gate` 同样如此。

但是 `ffn_down`：

```text
                  ffn_size
       ┌──────────────────────────────┐
       │ G0 │ G1 │ G2 │ G3 │ ...    │
       │    │    │    │    │        │
       │    │    │    │    │        │
       └──────────────────────────────┘
              ↑
          沿 ne[0] 切
```

**这就是最大的麻烦。**

---

# 4. Up/Gate 很好切，Down 不好切

对于：

```text
ffn_up
ffn_gate
```

一个 neuron 本质上对应一整行。

假设：

```text
group = neuron 1024 ~ 1535
```

那么文件中通常就是一段连续行：

```text
GGUF

...
row 1024
row 1025
...
row 1535
...
```

所以可以近似：

```cpp
offset =
    tensor_file_offset +
    group_start * tensor->nb[1];

size =
    group_count * tensor->nb[1];
```

于是一个：

```cpp
pread(fd,
      group_buffer,
      size,
      offset);
```

就能拿出来。

这非常舒服。

---

## 但 `ffn_down` 是反的

Down：

```text
                   FFN neurons
       ┌──────────────────────────┐
row 0  │ G0 │ G1 │ G2 │ G3      │
row 1  │ G0 │ G1 │ G2 │ G3      │
row 2  │ G0 │ G1 │ G2 │ G3      │
...
```

假设只要：

```text
G2
```

你实际需要：

```text
row0 的 G2
row1 的 G2
row2 的 G2
...
```

它们在原 GGUF 中**不是连续的一大块**。

于是直接读取可能变成：

```cpp
for (int row = 0; row < ne1; ++row) {
    pread(
        fd,
        dst_for_row,
        group_bytes,
        tensor_offset
          + row * nb[1]
          + group_offset_bytes);
}
```

这会产生大量小 I/O。

而这正好解释了为什么 PowerInfer-2 要做 **neuron bundling / cluster storage layout**：他们不仅改变计算粒度，也改变存储布局，让同一个 cluster 需要的权重尽量连续。([arXiv][1])

ActiveFlow 同样非常重视通过跨层组织 active weights 来获得更大的 I/O 请求，而不是大量细碎读取。([arXiv][2])

---

# 5. Q4_K_M 还有一个坑：不能随便从第 513 个元素切

你现在模型是量化 GGUF。

Q4_K、Q6_K 等不是：

```text
一个元素 = 4bit
然后任意bit位置都能截
```

而是：

```text
若干元素组成一个 quantization block
```

所以对于 `ffn_down` 沿 `ne[0]` 切时，group 边界必须考虑：

```cpp
ggml_blck_size(tensor->type)
```

不要写死成：

```cpp
256
```

虽然很多 K-quant 的 block/superblock 确实存在 256 元素粒度，但实际代码应该：

```cpp
const int64_t block =
    ggml_blck_size(tensor->type);
```

然后要求：

```cpp
group_start % block == 0
group_size  % block == 0
```

否则可能把一个量化 block 从中间切开。

所以第一版建议：

```text
group_size = 256
```

或者：

```text
512
1024
```

但代码里仍然动态检查：

```cpp
GGML_ASSERT(group_size %
            ggml_blck_size(type) == 0);
```

llama.cpp 的量化类型本身就是 block-based representation，因此这里必须保持量化块边界。([GitHub][3])

---

# 6. 所以我建议第一版先定义一个 FFN Group

例如：

```cpp
struct flash_ffn_group {
    int layer;

    int64_t neuron_begin;
    int64_t neuron_end;

    flash_tensor_slice gate;
    flash_tensor_slice up;
    flash_tensor_slice down;

    bool resident;
};
```

其中：

```cpp
struct flash_tensor_slice {
    ggml_tensor * tensor;

    uint16_t file_idx;

    int64_t begin;
    int64_t count;
};
```

Manager 从原来的：

```text
tensor → file offset
```

升级为：

```text
Layer
 ↓
FFN Group
 ↓
├── gate slice
├── up slice
└── down slice
```

例如：

```text
Layer 10

Group 0
neurons [0,512)

Group 1
neurons [512,1024)

Group 2
neurons [1024,1536)
...
```

---

# 7. 然后第一版不要动 GGUF 文件格式

为了先验证正确性，我建议直接从原 GGUF 读。

即：

### Gate

```text
一段连续 pread
```

### Up

```text
一段连续 pread
```

### Down

```text
逐 row pread
```

很慢没关系。

你现在的目标依然是：

> **先证明 group 计算结果正确。**

所以可以完全接受：

```text
性能反而更差
```

第一版成功标准不是速度，而是：

```text
Original FFN
≈
Grouped FFN
```

---

# 8. 真正计算时不能继续直接调用原来的完整 MUL_MAT

这是代码改动最大的地方。

现在计算图是：

```text
ffn_gate.weight
      ↓
MUL_MAT

ffn_up.weight
      ↓
MUL_MAT

      ↓
SILU/MUL

ffn_down.weight
      ↓
MUL_MAT
```

如果你只加载一个 group，原来的：

```cpp
ggml_compute_forward_mul_mat()
```

仍然认为：

```text
src0->ne[1] = 整个 ffn_size
```

所以不能直接复用完整 tensor。

---

## 有两种实现路线

### 路线 A：改 graph，创建 group tensor/view

例如：

```text
Group 0

gate_group_0
      ↓
MUL_MAT
      │
up_group_0
      ↓
MUL_MAT
      │
      ↓
SwiGLU
      ↓
down_group_0
      ↓
MUL_MAT
      ↓
partial_0


Group 1
...
      ↓
partial_1


partial_0 + partial_1 + ...
```

这是“比较 ggml-native”的做法。

优点：

```text
仍然使用现有GGML算子
```

缺点：

```text
graph node 数量暴涨

64层 × 几十个groups
```

而且动态 residency / data 指针管理会比较麻烦。

---

### 路线 B：做一个专门的 Flash FFN execution path

我更推荐你**第一版采用这个**。

概念上：

```cpp
flash_compute_ffn(
    layer,
    input,
    output);
```

内部：

```cpp
zero(output);

for (group : layer.groups) {

    load_group(group);

    gate =
        matmul(group.gate, input);

    up =
        matmul(group.up, input);

    hidden =
        silu(gate) * up;

    partial =
        matmul(group.down, hidden);

    output += partial;

    release_group(group);
}
```

逻辑就是：

```text
for each FFN group:

Flash
  ↓
gate/up/down group
  ↓
RAM
  ↓
CPU
  ↓
partial result
  ↓
accumulate
  ↓
release
```

这更接近 PowerInfer-2 的 **neuron-cluster execution unit**。([arXiv][1])

等机制验证成功以后，再考虑怎么更优雅地融进 ggml graph。

---

# 9. 第一阶段仍然不会减少读取量

这个要非常明确。

假设：

```text
完整FFN每token = 15.3 GiB
```

你现在改成：

```text
Group 0
Group 1
...
Group 54
```

但：

```text
所有 group 仍然计算
```

那么：

[
\sum_g Bytes_g
\approx 15.3 GiB
]

**总读取量基本没有变。**

所以这一步不是优化。

它是在建立：

> **以后能够选择性加载的基础设施。**

---

# 10. 真正减少读量是在下一步：Active Group Selection

等 group path 正确以后：

```text
全部 groups
████████████████████

每 token 全部读
```

再统计 FFN activation。

例如每组算一个：

[
score_g =
\max |activation_g|
]

或者：

[
score_g =
|activation_g|_2
]

观察：

```text
Group 0    0.98
Group 1    0.02
Group 2    0.77
Group 3    0.01
...
```

然后：

```text
Active:
G0 G2 ...

Inactive:
G1 G3 ...
```

下一步才变成：

```text
只读取：

G0
G2
G7
G11
...
```

例如：

```text
全部FFN：
15.3 GiB/token

50% groups active：
≈7.6 GiB/token

30%：
≈4.6 GiB/token

20%：
≈3.1 GiB/token
```

当然实际不是简单线性，而且 Qwen2.5 的 SwiGLU 不具备 ReLU 那种天然严格零稀疏，所以一旦开始跳 group，**模型就变成近似计算了**。ActiveFlow 正是针对现代非 ReLU 模型引入 contextual sparsity，并使用额外方法补偿稀疏化造成的模型质量损失。([arXiv][2])

那时就必须开始同时测：

```text
tokens/s
Flash GiB/token
+
perplexity / OpenCompass
```

---

# 11. 我非常建议你最后再做一个“重新排布文件”

等 POC 成功后，不要长期从原 GGUF 做：

```text
down row0 → pread
down row1 → pread
down row2 → pread
...
```

最终可以生成一个 sidecar：

```text
Qwen32B.gguf
Qwen32B.ffnpack
```

其中：

```text
ffnpack

Layer 0 Group 0
┌──────────────────────┐
│ gate group           │
│ up group             │
│ packed down group    │
└──────────────────────┘

Layer 0 Group 1
┌──────────────────────┐
│ gate                 │
│ up                   │
│ down                 │
└──────────────────────┘

...

Layer 1 Group 0
...
```

于是运行时：

```cpp
pread(
    fd,
    group_buffer,
    group.bundle_size,
    group.bundle_offset);
```

一次大块读取就把：

```text
Gate + Up + Down
```

全部拿到。

这就开始非常接近你刚刚看的 PowerInfer-2 的 **neuron bundling / cluster-oriented storage**：计算粒度和存储粒度匹配，可以避免大量细碎随机 I/O。([arXiv][1])

---

## 所以我建议你实际按这个顺序开发

```text
现在
================================================
完整FFN tensor
load → compute → evict
✓


Step 1
================================================
确定 Qwen ffn_up/gate/down 的 ne[] / nb[]
确定 intermediate dimension
确定 quant block size


Step 2
================================================
FFN intermediate dimension
↓
切成 group_size=512（先举例）

构造：
Layer → Group metadata


Step 3
================================================
从原GGUF读取一个group：

gate → contiguous
up   → contiguous
down → strided

暂时只测试 Layer 0 / Group 0


Step 4
================================================
实现 grouped FFN：

for all groups:
    load
    gate/up compute
    SwiGLU
    down partial
    accumulate
    release

输出 ≈ 原始模型


Step 5
================================================
所有Layer改成group execution

这时读取量仍≈15.3 GiB/token


Step 6
================================================
重新组织FFN文件布局：

GGUF → .ffnpack

一个group一次连续read


Step 7
================================================
统计group activation
↓
Hot / Cold
↓
Selective active-group loading


Step 8
================================================
Cache
+
PowerInfer-2 cluster pipeline
或者
ActiveFlow cross-layer prediction
```

**我尤其建议下一步不要马上写“预测哪些 group 活跃”。**

先把最困难也最基础的一件事完成：

> **在不丢任何 group 的情况下，把一个完整 SwiGLU FFN 等价地改成 group-by-group 计算。**

只要这个跑通，你后面的缓存、热冷、ActiveFlow 跨层预测、PowerInfer-2 cluster pipeline 才真正有可以操作的“最小单位”。

而你下一刀最适合先做的其实非常具体：**打印 Qwen2.5-32B 某一层 `ffn_up / ffn_gate / ffn_down` 的 `type、ne[0..3]、nb[0..3]、ggml_blck_size(type)`。**有了这一组真实值，我们就可以直接算出 `group_size=512` 时，这三个 tensor 在 GGUF 里到底应该怎么取偏移和字节数，尤其是最麻烦的 `ffn_down`。

[1]: https://arxiv.org/abs/2406.06282?utm_source=chatgpt.com "PowerInfer-2: Fast Large Language Model Inference on a Smartphone"
[2]: https://arxiv.org/abs/2504.08378?utm_source=chatgpt.com "Scaling Up On-Device LLMs via Active-Weight Swapping Between DRAM and Flash"
[3]: https://github.com/ggml-org/llama.cpp/blob/master/tools/quantize/README.md?utm_source=chatgpt.com "llama.cpp/tools/quantize/README.md at master · ggml-org ..."
