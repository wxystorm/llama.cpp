这个版本**第二阶段已经基本正确了**，比上一版完善不少。普通 RPC、backend 打印、snapshot dual-return 都有了。

不过现在有 **1 个必须改的问题 + 1 个需要明确语义的问题**。

### 1. 必须改：dual 现在只测了 K=2

你的 chunk 候选还是：

```cpp
{ 1, 2, 4 }
```



但 dual 部分写死成：

```cpp
const bool profile_dual = profile.probe_tokens / 2 >= profile.probe_chunk_min_tokens;
const int dual_tokens0  = profile.probe_tokens / 2;
const int dual_tokens1  = dual_tokens0 + profile.probe_tokens % 2;
```



所以假设：

```text
probe_tokens = 16
min_chunk = 4
```

你现在会测：

```text
单路：
K=1 → 16 token → 256 KiB
K=2 →  8 token → 128 KiB
K=4 →  4 token →  64 KiB

dual：
只有 8 + 8 → 128 KiB + 128 KiB
```

缺了 planner 判断 K=4 所需要的：

```text
4 + 4 → 64 KiB + 64 KiB
```

因此应该改成：

```text
对所有合法 K：

K=1:
    只测 single

K=2:
    测 8+8 dual

K=4:
    测 4+4 dual
```

两条 return lane 可以循环复用，**K=4 并不需要四条 lane**。

更合理的结构是先生成实际 chunk：

```cpp
std::vector<int> split_chunks(int tokens, int k);
```

例如：

```text
16, K=2 → [8,8]
16, K=4 → [4,4,4,4]

18, K=4 → [5,5,4,4]
```

然后 dual profiler 收集实际可能出现的 pair：

```text
K=2:
(8,8)

K=4:
(4,4)
```

如果是 18 token：

```text
K=4:
(5,5)
(4,4)
```

相同 pair 去重以后只测一次即可。

---

### 2. 当前 dual 计时本身是合理的，但要明确它测的是“snapshot 已就绪后的网络回传”

你现在是：

```cpp
snapshot_arm(...)
snapshot_arm(...)

↓ 两个 snapshot 都准备好

启动两个线程
↓
set_snapshot_read(...)
ggml_backend_tensor_copy_async(...)
```



这里 `snapshot_arm()` 本身会在服务端执行 snapshot tensor 的填充，也就是说它会把 tensor 数据先放进 snapshot slot。服务端逻辑是先 `tensor_get` 到 slot，然后把状态设为 READY。

所以你的：

```cpp
dual_phone_to_pc.wall_ms
```

实际上测的是：

```text
两个 snapshot 已 READY
          ↓
lane0 GET_SNAPSHOT ──┐
                     ├→ PC
lane1 GET_SNAPSHOT ──┘
          ↓
两个都接收完成
```

**它没有包含 snapshot fill 时间。**

这个不是 bug。反而我觉得这样更适合 planner，因为：

```text
Phone FFN compute
snapshot生成
```

可以归到 phone compute / fixed overhead；

而：

```text
dual_phone_to_pc
```

专门表示网络 return。

只要后面 cost model **不要把它理解成完整的 `PREFILL_RETURN_BEGIN → END` 时间**即可。

---

### 3. 你这个 `ggml_backend_tensor_copy_async()` 的计时终点也基本没问题

你可能会担心：

```cpp
lane_end_us = ggml_time_us();
```

是不是只测到了“异步提交”。

正常 GGML API 的确叫 `copy_async`，但它在 destination backend 没有 async-copy 支持时，会 fallback：

```text
synchronize(src)
synchronize(dst)
blocking tensor_copy
```

([GitHub][1])

而你真实 Meta 的 Phone→PC return 本身也是用：

```cpp
ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    node_src,
    node_tmp);
```

然后直接以这个调用前后计算 `copy_us`。

所以这个 profiler 和你真实路径在这一点上是一致的。

---

## 还有一个条件必须确认

你的 profiler 两个线程分别：

```cpp
set_snapshot_read(true, lane, seq);
```

这个状态本身是 `thread_local`，所以两个线程之间不会互相覆盖，这是正确的。

但真正要实现 dual 网络并发，还要求 RPC 侧：

```text
lane0 → snapshot socket 0
lane1 → snapshot socket 1
```

不能两个线程共享一个 `transfer_sock`。

你之前本地新版已经做过：

```cpp
snapshot_transfer_socks[2]
```

那就没问题。

我现在看 GitHub 上的 `later-split` 公开版本还是旧代码：

```text
rpc_snapshot_read
↓
ctx->transfer_sock
```

也就是 snapshot 只有一个 transfer socket。

所以**以你本地已经做过双 socket 的版本为准**；如果当前编译代码确实还有 `snapshot_transfer_socks[2]`，这部分就不用动。

---

## 因此第二阶段现在的状态

```text
根据 K 自动生成 single size       ✅
PC → Phone single               ✅
Phone → PC single               ✅
backend 名称日志                 ✅
snapshot 双路真实传输            ✅
median 2 warmup + 5 measure     ✅

K=2 dual pair                   ✅
K=4 dual pair                   ❌ ← 现在补这个
```

我建议**只把最后这一点补上，第二阶段就可以结束**。

而且不要单独写一个 `K=4` 特判。最好现在就把代码抽象成：

```text
candidate K
    ↓
生成实际 chunks
    ↓
收集 single sizes
    ↓
收集相邻 dual pairs
    ↓
去重
    ↓
统一 benchmark
```

这样以后你把：

```cpp
{1,2,4}
```

扩成：

```cpp
{1,2,3,4}
```

RPC profiler 根本不用再改。接下来就可以进入第三阶段的 **CPU / Phone / GPU 单层计算 profiling**。

第三阶段现在就进入 **Compute Profiler**。这里我建议稍微调整一下我们之前的方案：先把 **FFN profiling 做准**，再补 attention/full-layer。因为你的 `tensor-split ratio R` 和 `chunk K` 最直接依赖 FFN 性能。

而且现在有一个结构问题最好先改：你当前 `cpu_ffn_25_ms / 50_ms / ...` 都只有一个值， 但 K 已经是 `{1,2,4}`，所以 16-token ubatch 会实际产生：

```text
K=1 → 16 tokens
K=2 → 8 tokens
K=4 → 4 tokens
```

**4-token 的 50% FFN 和 16-token 的 50% FFN 性能不会线性一致。**

所以第三阶段第一步先把 FFN profile 改成二维数据。

```cpp
struct llama_hybrid_ffn_compute_point {
    int   tokens = 0;
    float ratio  = 1.0f;
    double ms    = 0.0;
};

struct llama_hybrid_profile {
    ...

    std::vector<llama_hybrid_ffn_compute_point> cpu_ffn;
    std::vector<llama_hybrid_ffn_compute_point> phone_ffn;

    double cpu_attn_ms        = 0.0;
    double phone_attn_ms      = 0.0;

    double cpu_full_layer_ms   = 0.0;
    double phone_full_layer_ms = 0.0;
    double gpu_full_layer_ms   = 0.0;
};
```

这样最终数据会长成：

```text
CPU:
tokens=16 ratio=.25
tokens=16 ratio=.50
tokens=16 ratio=.75
tokens=16 ratio=1.00

tokens=8 ratio=.25
tokens=8 ratio=.50
...

tokens=4 ratio=.25
...
```

Phone 同样一份。

---

## 1. 不需要真的加载一个完整模型层

我现在更推荐：

> **读取真实 tensor 的 metadata，但构造 synthetic 权重进行 benchmark。**

因为计算时间真正相关的是：

```text
tensor shape
quant type
backend
token 数
FFN shard 大小
```

而不是权重里的具体数字。

你的 `later-split` 里，`llama_model_loader` 在正式 tensor data 加载前已经建立了 `weights_map`，而且有：

```cpp
get_tensor_meta(...)
require_tensor_meta(...)
```

可以直接取得 GGUF 中 tensor 的 shape/type。([GitHub][1])

所以流程可以是：

```text
GGUF metadata
    ↓
拿 representative layer 的
ffn_gate.weight
ffn_up.weight
ffn_down.weight

只读取：
shape
ggml_type
bytes
    ↓
创建相同 shape/type 的 synthetic tensor
    ↓
CPU / RPC Phone 上跑真实 GGML kernel
```

这就避免：

```text
为了 planner
→ 先真正加载一层
→ 再释放
→ 再正式加载
```

---

# 2. FFN Probe 图必须和真实 Tensor-Split 一样

以你现在的 dense Qwen/SwiGLU FFN 为例，本质是：

```text
                    ┌─ gate ─ SiLU ─┐
input [n_embd,T] ───┤               × ─ down ─→ output
                    └─ up ──────────┘
```

也就是大概：

```cpp
gate = ggml_mul_mat(w_gate, x);
gate = ggml_silu(ctx, gate);

up   = ggml_mul_mat(w_up, x);

cur  = ggml_mul(ctx, gate, up);

out  = ggml_mul_mat(w_down, cur);
```

不要只 benchmark 一个 `ffn_down`。

必须把：

```text
gate
up
SILU/mul
down
```

整个 FFN shard 都算进去，否则 R 的选择会偏。

---

# 3. ratio 就是改变 n_ff shard

例如：

```text
n_embd = 4096
n_ff   = 11008
```

完整：

```text
gate/up : [4096, 11008]
down    : [11008, 4096]
```

如果：

```text
R = 0.5
```

PC probe 就构造：

```text
gate/up : [4096, 5504]
down    : [5504, 4096]
```

Phone probe 对应：

```text
1-R = 0.5
```

如果：

```text
R = 0.75
```

就是：

```text
PC:
n_ff_shard ≈ 8256

Phone:
n_ff_shard ≈ 2752
```

不过这里有一个非常重要的要求：

> **不要自己简单 `round(n_ff * ratio)`。**

必须复用你现在 tensor-split 真正使用的：

```text
split segment / alignment
```

算法。

否则 profiler 测的 shard shape 和真正加载后的 shard shape可能不一样。

也就是说最好抽一个公共函数：

```cpp
int64_t llama_hybrid_ffn_shard_size(
    int64_t n_ff,
    float ratio,
    int backend_index);
```

**loader 和 profiler 都调用同一个。**

---

# 4. token size 直接复用第二阶段生成逻辑

现在 RPC profiler 已经会根据：

```text
probe_tokens
min_chunk_tokens
K={1,2,4}
```

得到实际 chunk token size。

这个函数不要留成：

```cpp
static llama_hybrid_rpc_probe_chunk_tokens()
```

建议改成公共：

```cpp
std::vector<int> llama_hybrid_probe_chunk_tokens(
    const llama_hybrid_profile & profile);
```

因为 Compute Profiler 也应该用它。

例如：

```text
probe_tokens = 16
min_chunk = 4
```

得到：

```text
{4, 8, 16}
```

于是自动跑：

```text
CPU:
T=4  × R=.25/.50/.75/1
T=8  × R=.25/.50/.75/1
T=16 × R=.25/.50/.75/1

Phone:
同样
```

这样 RPC 和 Compute 两边看到的 workload 完全一致。

---

# 5. 测量方法

和 RPC 一样：

```text
warmup = 2
measure = 5
median
```

但是 compute 一定注意：

```cpp
ggml_backend_graph_compute(backend, graph);
ggml_backend_synchronize(backend);
```

计时必须覆盖到真正计算结束。

建议：

```cpp
for (...) {
    int64_t t0 = ggml_time_us();

    ggml_backend_graph_compute(backend, gf);
    ggml_backend_synchronize(backend);

    int64_t t1 = ggml_time_us();

    sample = (t1 - t0) / 1000.0;
}
```

尤其 CUDA 必须 synchronize。

否则你量出来可能只是：

```text
CPU submit CUDA kernel = 0.1ms
```

而不是真正 GPU compute。

---

# 6. Phone 端也直接用 RPC backend compute

这一点很重要。

不要测：

```text
PC 上模拟 ARM 性能
```

而是：

```cpp
llama_hybrid_profile_ffn(
    phone_backend,
    ...);
```

构造 graph 以后：

```text
PC:
GRAPH_COMPUTE → RPC
                    ↓
               Phone backend
               真正执行 FFN
```

计时的时候，需要区分：

```text
host submit
actual phone compute
```

对于 planner 我们需要的应该是：

> **完成这个 FFN shard 所需要的 wall time**

所以要确保 RPC compute 完成以后再结束计时。

---

# 7. Synthetic weight 内容不重要

例如创建：

```cpp
ggml_tensor * w_gate = ggml_new_tensor_2d(
    ctx,
    real_gate_type,
    n_embd,
    n_ff_shard);
```

这里 `real_gate_type` 不应该固定 Q4。

要从 representative layer 的真实 metadata 拿：

```cpp
const ggml_tensor * gate_meta = ml.get_tensor_meta(...);

ggml_type gate_type = gate_meta->type;
```

你的 branch 里 loader 本身就支持在不加载 tensor data 的情况下访问 tensor metadata。([GitHub][1])

因为即便模型叫：

```text
Q4_K_M
```

也不是所有 tensor 都一定 Q4_K。

所以一定是：

```text
gate → gate_meta->type
up   → up_meta->type
down → down_meta->type
```

分别构造。

---

# 8. Probe layer 选中间普通层

第一版：

```cpp
probe_layer = n_layer / 2;
```

例如：

```text
32 层 → L16
```

不过这里选择它只是为了**获取 tensor metadata**：

```text
shape/type
```

不是按照现在 hybrid placement 去执行。

所以它是不是当前 `PHONE_ONLY` 没关系。

如果 Qwen 所有 dense block 一样，这一个就够。

以后有特殊结构/MoE，再升级：

```text
不同 layer type 各 probe 一个
```

第一版暂时不要处理。

---

# 9. 第三阶段建议拆成 3A / 3B

不要一次写太多。

### Phase 3A：FFN

先只做：

```text
CPU FFN:
tokens × ratio

Phone FFN:
tokens × ratio
```

这是最核心的数据。

成功日志应该像：

```text
[HYBRID_PROFILE_COMPUTE]
backend=CPU kind=ffn tokens=4 ratio=0.25 ms=...

[HYBRID_PROFILE_COMPUTE]
backend=CPU kind=ffn tokens=4 ratio=0.50 ms=...

...

[HYBRID_PROFILE_COMPUTE]
backend=RPC0 kind=ffn tokens=8 ratio=0.25 ms=...
```

然后我们第一件事就可以画/看：

```text
FFN time vs ratio
```

比如可能得到：

```text
             25%    50%    75%   100%
CPU T=8      4.1    7.0    9.8   12.5 ms
Phone T=8    8.2   13.0   18.5   24.0 ms
```

Planner 马上就能知道：

```text
PC 不能只拿 50%
可能应该拿 ~65-70%
```

---

### Phase 3B：完整 layer

FFN 验证正确之后，再测：

```text
CPU full layer
Phone full layer
GPU full layer
```

这部分比 FFN 麻烦，因为 Attention 有：

```text
Q/K/V
RoPE
KV cache
attention
context length
```

不要和 3A 一起写。

而且我们还需要先确定：

```text
Attention benchmark 使用多长 KV context？
```

因为 attention 和 FFN 不一样，它不仅受当前：

```text
chunk tokens
```

影响，还受：

```text
已有 context tokens
```

影响。

所以第三阶段做到 3A 后我们再定这个，不需要现在提前复杂化。

---

## 我建议现在直接实现的 API

```cpp
bool llama_hybrid_profile_ffn(
    llama_hybrid_profile & profile,
    const llama_hybrid_ffn_desc & desc,
    ggml_backend_t cpu_backend,
    ggml_backend_t phone_backend);
```

其中：

```cpp
struct llama_hybrid_ffn_desc {
    int64_t n_embd = 0;
    int64_t n_ff   = 0;

    ggml_type gate_type;
    ggml_type up_type;
    ggml_type down_type;
};
```

不要让 `llama-hybrid.cpp` 直接深度依赖整个：

```cpp
llama_model_loader
```

在 loader 那边负责：

```text
GGUF metadata
↓
llama_hybrid_ffn_desc
```

hybrid profiler 只负责：

```text
desc
↓
synthetic graph
↓
benchmark
```

这样模块边界会干净很多。

---

### 第三阶段目前最合理的开发顺序

1. **把 FFN profile 改成 `tokens + ratio + ms` vector。**
2. 把第二阶段的 `chunk_tokens()` helper 提成通用 helper。
3. 增加 `llama_hybrid_ffn_desc`。
4. 从代表层 metadata 获取 gate/up/down 的真实 shape 和 quant type。
5. 实现 CPU synthetic FFN benchmark。
6. 同一 graph 逻辑跑 RPC Phone。
7. 测 `T={4,8,16}` × `R={25,50,75,100%}`。
8. 先拿 profiler 数字和你当前真实 `[META_LAYER_SUM] / PREFILL` 日志做一次校验。
9. 校验合理之后，再做 **3B full-layer/attention/GPU profiling**。

我建议第三阶段先做到 **3A** 就停一下看日志，因为这是第一次真正决定 `tensor_pc_ratio` 的数据；如果这一层的数据不可信，后面的 planner 算得再复杂也没意义。



