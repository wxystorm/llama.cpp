这两个指标现在非常适合直接在你的 `FlashWeightManager` 里统计，而且建议先区分成两类：

* **逻辑读取量**：你的程序每生成一个 token，主动从 GGUF 请求读取了多少字节。
* **实际 UFS 物理读取量**：操作系统最终真的从闪存读了多少。这个会受到 page cache 影响。

第一阶段先把**逻辑读取量 + 你自己的 `read()` 耗时**统计起来，已经足够分析瓶颈。

你现在的加载函数应该大概是：

```cpp
flash_load_tensor(tensor)
{
    // 找 offset / size
    // seek
    // read
    // 标记 resident
}
```

直接加三个累计量：

```cpp
uint64_t flash_load_count = 0;
uint64_t flash_read_bytes = 0;
uint64_t flash_io_time_us = 0;
```

然后真正读权重的地方：

```cpp
#include <chrono>

void flash_weight_manager::load(ggml_tensor * tensor) {
    auto it = tensors.find(tensor);
    if (it == tensors.end()) {
        return;
    }

    auto & info = it->second;

    if (info.loaded) {
        return;
    }

    const size_t size = info.size;

    auto t0 = std::chrono::steady_clock::now();

    // 你现在已有的读取逻辑
    file->seek(info.offset, SEEK_SET);
    file->read_raw(tensor->data, size);

    auto t1 = std::chrono::steady_clock::now();

    uint64_t elapsed_us =
        std::chrono::duration_cast<std::chrono::microseconds>(
            t1 - t0).count();

    flash_load_count++;
    flash_read_bytes += size;
    flash_io_time_us += elapsed_us;

    info.loaded = true;
}
```

那么这三个量分别就是：

```text
flash_load_count
= 一共发生了多少次 FFN 权重加载

flash_read_bytes
= 一共请求读取了多少 GGUF 权重

flash_io_time_us
= 所有这些读取累计阻塞了多久
```

---

### 然后怎么变成 `/ token`

假设实际生成了：

```cpp
n_generated = 32;
```

那么：

[
\text{Flash read bytes/token}
=============================

\frac{\text{flash_read_bytes}}
{n_{\text{generated}}}
]

以及：

[
\text{Flash I/O time/token}
===========================

\frac{\text{flash_io_time}}
{n_{\text{generated}}}
]

比如最后打印：

```cpp
double gib =
    flash_read_bytes / 1024.0 / 1024.0 / 1024.0;

double mb_per_token =
    flash_read_bytes / 1024.0 / 1024.0 /
    n_generated;

double io_ms_per_token =
    flash_io_time_us / 1000.0 /
    n_generated;

double bandwidth =
    (flash_read_bytes / 1024.0 / 1024.0) /
    (flash_io_time_us / 1000000.0);

printf(
    "\n[FLASH STAT]\n"
    "load count       = %llu\n"
    "total read       = %.3f GiB\n"
    "read / token     = %.3f MiB/token\n"
    "total I/O time   = %.3f s\n"
    "I/O time / token = %.3f ms/token\n"
    "effective BW     = %.2f MiB/s\n",
    (unsigned long long) flash_load_count,
    gib,
    mb_per_token,
    flash_io_time_us / 1000000.0,
    io_ms_per_token,
    bandwidth
);
```

最后可能看到：

```text
[FLASH STAT]
load count       = 6144
total read       = 410.3 GiB
read / token     = 13129 MiB/token
total I/O time   = 278.2 s
I/O time / token = 8693 ms/token
effective BW     = 1510 MiB/s
```

这就非常直观了：

> Qwen32B 每生成一个 token，需要从 GGUF 重新读约 13 GB FFN 权重；单纯 Flash I/O 就花约 8.7 s/token。

如果你的总：

```text
eval time/token = 10 s
```

而：

```text
Flash I/O time/token = 8.7 s
```

那马上就知道：

> **87% 的 decode 时间卡在 Flash I/O。**

这种情况下下一步做 Pipeline / Cache 就非常有依据。

---

## 但有个坑：不要把 prompt 阶段也算进 generated token

比如：

```text
prompt = 30 tokens
generated = 32 tokens
```

你的 FFN 在 prompt eval 时也会被加载。

如果直接：

```text
整个程序 flash_read_bytes / 32
```

那你把 prompt 阶段的 I/O 也算给生成阶段了，数据不干净。

最好把统计分成：

```text
Prompt / Prefill

FLASH read
FLASH I/O time
```

以及：

```text
Decode

FLASH read
FLASH I/O time
```

你现在主要关心的是：

> **Decode 每生成一个 token 需要读多少 Flash。**

所以可以给 manager 加：

```cpp
void reset_stats();
```

在 prompt eval 完成、正式开始逐 token generation 的地方：

```cpp
flash_manager.reset_stats();
```

然后开始：

```text
token 1
token 2
...
token 32
```

最后再打印。

这样：

```text
read bytes/token
```

就是纯 decode 数据。

---

## 更漂亮的方法：每个 token 单独做 snapshot

其实我更推荐这个，因为后面研究 Cache 会非常好用。

Manager 提供：

```cpp
struct flash_stats {
    uint64_t loads;
    uint64_t bytes;
    uint64_t io_us;
};
```

每生成一个 token 前：

```cpp
auto before = flash_manager.get_stats();
```

生成完成后：

```cpp
auto after = flash_manager.get_stats();

uint64_t token_bytes =
    after.bytes - before.bytes;

uint64_t token_io_us =
    after.io_us - before.io_us;
```

打印：

```text
[FLASH TOKEN 1]
read = 14.82 GiB
I/O  = 9.21 s

[FLASH TOKEN 2]
read = 14.82 GiB
I/O  = 8.94 s

[FLASH TOKEN 3]
read = 14.82 GiB
I/O  = 8.80 s
```

这个信息非常有价值。

因为你以后加 Cache 以后可能变成：

```text
                    Read/token

No Cache           14.8 GB
1GB Cache          13.9 GB
2GB Cache          11.2 GB
4GB Cache           7.1 GB
```

于是 Cache 到底有没有用一眼就能看出来。

---

# 还有一个非常重要的区别

你用：

```cpp
flash_read_bytes += size;
```

测出来的是：

> **程序逻辑上请求读取了多少数据。**

我建议论文/实验里称：

**Logical Flash Read Volume**

或者：

**Weight Read Volume**

因为这不一定等于：

> 手机 UFS 芯片真的读取了这么多数据。

为什么？

假设：

```text
Token 1
read GGUF Layer 0

Token 2
又 read GGUF Layer 0
```

虽然你已经把：

```text
tensor RAM
```

释放了，但 Linux 可能仍然把 GGUF 文件页面留在：

```text
page cache
```

于是第二次：

```text
read()
```

可能实际上：

```text
page cache → tensor buffer
```

而不是：

```text
UFS → tensor buffer
```

所以：

```text
logical read = 15 GB
```

不一定意味着：

```text
physical UFS read = 15 GB
```

---

## 你还可以顺便记录真正的 `/proc/<pid>/io`

在 Termux 自己进程里，一般可以看：

```bash
cat /proc/$(pgrep -n llama-cli)/io
```

可能会看到：

```text
rchar: 123456789000
wchar: ...
syscr: ...
syscw: ...
read_bytes: 9876543210
write_bytes: ...
```

这里特别注意：

```text
rchar
```

和：

```text
read_bytes
```

不是一回事。

`rchar` 更接近：

> `read()` 系统调用一共给程序返回了多少字节。

它可能来自 page cache。

而：

```text
read_bytes
```

更接近：

> 这个进程实际导致存储层读取了多少字节。

所以以后你可以同时报告：

```text
Manager logical read:
14.8 GB/token

/proc/PID/io read_bytes:
5.2 GB/token
```

那就说明相当一部分：

```text
read()
```

其实命中了系统 page cache。

---

### 但你现在第一阶段不用搞得这么复杂

我建议你现在就记录这四个：

```text
1. load_count

2. logical_read_bytes

3. read_time

4. effective_bandwidth
```

然后专门针对 **decode** 算：

```text
MiB/token
ms I/O/token
```

最终你的实验表就能变成：

| Model       | Peak RAM | Decode tok/s | Flash Read/token | Flash I/O/token | Effective BW |
| ----------- | -------: | -----------: | ---------------: | --------------: | -----------: |
| Llama-13B   |      ... |          ... |          ... GiB |           ... s |     ... MB/s |
| Qwen2.5-32B |   3.4 GB |          ... |          ... GiB |           ... s |     ... MB/s |

这张表会马上告诉我们一个特别重要的问题：

**Qwen32B 虽然峰值 RAM 只有 3.4 GB，但为了得到这个低 RAM，每生成一个 token 到底付出了多少 GB 的 Flash 数据搬运代价。**

这个数据出来以后，再和 PC 协同那边的 **MB transferred/token** 放在一起比较，会非常有说服力。
