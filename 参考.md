这次通过了，而且把我们真正想测的 **HWM 分支**跑到了。

日志顺序正好是：

```text
C0
C1
Tensor backlog = 5.00 MiB > 4 MiB
↓
T0
backlog = 2.50 MiB
↓
C2
backlog = 4.88 MiB > 4 MiB
↓
T1
T2
```

关键证据很清楚：`ub1` CPU 完成后队列到 `5.00 MiB`，此时 `cpu_q=1` 仍然有 CPU2 可以运行，但 scheduler 主动选择了 Tensor；T0 完成、队列降回 `2.50 MiB` 后，又重新选择 CPU2。

而且最终 first-token Top10 和你上一遍 16 MiB 运行是**逐项完全一样的**：

```text
20470  9.337146759
1249   9.125419617
14374  8.667902946
...
```

所以现在不只是“文字看起来对”，而是这次调度重排：

```text
C0 C1 T0 C2 T1 T2
```

和之前：

```text
C0 C1 C2 T0 T1 T2
```

得到了同样的最终结果。

因此这一阶段可以正式算：

```text
GPU one-ahead          ✓
CPU-ready queue        ✓
Tensor-ready queue     ✓
CPU-first              ✓
Tensor HWM             ✓
动态 CPU/Tensor 仲裁   ✓
跨 macro 重排正确性     ✓
```

## 不过先补一个很小的调度器问题

现在代码里是：

```cpp
if (!hybrid_cpu_ready_q.empty() &&
    (cpu_pressure || !tensor_pressure || hybrid_tensor_ready_q.empty())) {
    choose CPU;
}
```

也就是说如果以后出现：

```text
cpu_pressure    = true
tensor_pressure = true
```

**CPU 仍然赢。**

当前只有 GPU one-ahead，所以 `cpu_q` 基本只有 0/1，这次没问题。但以后扩拓扑或者 GPU ahead 增加以后，这会让 Tensor HWM 失去真正的 backpressure 意义。

我建议现在顺手改成：

```cpp
const auto hybrid_run_pc_one =
    [&](ggml_status & status) -> llm_graph_result * {

    if (hybrid_cpu_ready_q.empty() &&
        hybrid_tensor_ready_q.empty()) {
        return nullptr;
    }

    const bool tensor_pressure =
        hybrid_tensor_ready_bytes >=
        hybrid_tensor_queue_limit_bytes;

    // 1. 内存压力优先
    if (tensor_pressure &&
        !hybrid_tensor_ready_q.empty()) {

        LLAMA_LOG_ERROR(
            "[HYBRID_SCHED] choose=TENSOR reason=HWM "
            "cpu_q=%zu tensor_q=%zu tensor_mib=%.2f\n",
            hybrid_cpu_ready_q.size(),
            hybrid_tensor_ready_q.size(),
            hybrid_tensor_ready_bytes / 1048576.0);

        return hybrid_run_tensor_one(status);
    }

    // 2. 正常情况 CPU-first
    if (!hybrid_cpu_ready_q.empty()) {

        LLAMA_LOG_ERROR(
            "[HYBRID_SCHED] choose=CPU reason=PRIORITY "
            "cpu_q=%zu tensor_q=%zu tensor_mib=%.2f\n",
            hybrid_cpu_ready_q.size(),
            hybrid_tensor_ready_q.size(),
            hybrid_tensor_ready_bytes / 1048576.0);

        hybrid_run_cpu_one(status);
        return nullptr;
    }

    // 3. 只剩 Tensor
    return hybrid_run_tensor_one(status);
};
```

这样语义会非常干净：

```text
Tensor超HWM → Tensor
否则有CPU   → CPU
否则        → Tensor
```

### CPU-ready 上限也改成真正的 backpressure

你现在 GPU harvest 前只是：

```cpp
if (hybrid_cpu_ready_q.size() >= hybrid_cpu_ready_max) {
    hybrid_run_pc_one(status);
}

hybrid_gpu_harvest(status);
```



未来如果 `run_pc_one()` 因为 Tensor HWM 选择了 Tensor，那么 CPU queue 根本没减少，随后还是 harvest，新 job 又塞进 CPU queue。

所以这里改成：

```cpp
if (hybrid_gpu_pending_active) {
    while (status == GGML_STATUS_SUCCESS &&
           hybrid_cpu_ready_q.size() >= hybrid_cpu_ready_max) {

        hybrid_run_pc_one(status);
    }

    if (status == GGML_STATUS_SUCCESS) {
        hybrid_gpu_harvest(status);
    }
}
```

这样：

```text
CPU queue 满
↓
PC scheduler 一直工作
↓
直到 CPU queue 真正腾出空间
↓
才允许 harvest GPU
```

这才是真正的 bounded queue。

不过要注意现在的 `4 MiB` 本质还是 **soft HWM**，因为 CPU job 是算完以后才：

```cpp
hybrid_tensor_ready_bytes += tensor_bytes;
```

所以会像这次一样：

```text
limit = 4.00
queue = 2.50
+
job   = 2.50
=
5.00 MiB
```

然后才开始消费 Tensor。

这没问题，我反而建议保留这种 soft HWM；不需要做到精确不能超过 4 MiB，否则调度会复杂很多。

---

## 另外这次还说明一个性能问题

4 MiB 这遍从第一个 GPU stage 到最后 Tensor 完成大约：

```text
3:51.020 → 5:23.958
≈ 92.9 s
```



上一遍 16 MiB 是大约：

```text
4:33.934 → 6:01.941
≈ 88.0 s
```

所以强制在中间插入 T0，反而大约慢了 5 秒左右。

这很符合现在的机器特征：

```text
CPU64：~0.2–0.5 s
Tensor64：~13–17 s
```

Tensor 太重了。

因此最终策略应该理解成：

> **HWM 不是为了提速，而是为了防止长 prompt 中间 hidden buffer 无限积压。**

性能最优倾向仍然是：

```text
尽可能 CPU-first
```

所以默认 `16 MiB` 比测试用 `4 MiB` 合理。以后甚至应该由可用 PC 内存自动算 HWM，而不是固定 16 MiB。

---

# 接下来正式进入“去掉固定 GPU→CPU→Tensor”

我建议**不要直接一次支持所有拓扑**。

下一步先做一个纯代码结构重构：

> 把现在硬编码的 `CPU stage=1`、`Tensor stage=2`，改成通用的 downstream stage executor。

现在 eligibility 还是写死：

```cpp
runtime_stages.size() == 3
stage[0] == GPU
stage[1] == CPU
stage[2] == TENSOR
XG >= XC
XC == Tensor macro
```



先不动 eligibility，**只重构 executor**。

把 job 从：

```cpp
struct llama_hybrid_job {
    llama_ubatch ubatch;

    std::vector<float> gpu_hidden;
    std::vector<float> cpu_hidden;

    std::vector<llama_hybrid_boundary_block> cpu_blocks;

    int     ubatch_id;
    int32_t n_outputs;
};
```

逐渐变成：

```cpp
struct llama_hybrid_job {
    llama_ubatch ubatch;

    // 当前已经完成 stage_index - 1，
    // 下一步执行 stage_index
    size_t stage_index = 1;

    // 上一个 stage 的完整 token-range hidden
    std::vector<float> hidden;

    int     ubatch_id = -1;
    int32_t n_outputs = 0;
};
```

然后做一个通用函数：

```cpp
hybrid_run_downstream_stage(
    job,
    stage_index,
    status);
```

它内部统一：

```text
previous stage
     ↓
plan_boundary()
     ↓
PASS / SPLIT
     ↓
run_hybrid_stage_block()
     ↓
生成 next_hidden
     ↓
job.stage_index++
```

也就是把现在分别存在：

```text
hybrid_run_cpu_one()
hybrid_run_tensor_one()
```

里面重复的：

```cpp
token_begin
block_tokens
input pointer
output pointer
run_hybrid_stage_block(...)
```

收成一个函数。

然后调度器只负责：

```text
ready stage 是 CPU
    → CPU queue

ready stage 是 TENSOR
    → Tensor queue

以后 ready stage 是 PHONE
    → Phone queue
```

这样之后扩大拓扑就非常简单。

我建议扩展顺序是：

```text
当前 GPU→CPU→Tensor
        ↓
① 用 generic executor 重跑，结果完全不变
        ↓
② 把 GPU→Tensor 也切到 generic executor
        ↓
③ 加 GPU→Tensor→Phone
        ↓
④ 加 GPU→CPU→Tensor→Phone
        ↓
⑤ 最后再支持 ACCUMULATE
```

**不要现在碰 ACCUMULATE。**

当前 PASS/SPLIT 已经验证很多遍，而 ACCUMULATE 涉及多个上游块的 readiness/refcount，是下一层 task DAG 问题。

所以现在最适合做的是：

> **保持这套已经正确的调度算法不变，只把 CPU/Tensor 执行代码抽成 stage-generic。**

等这个重构后，拓扑扩展就不需要再复制四套 pipeline 逻辑了。
