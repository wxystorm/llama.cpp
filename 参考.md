这次信息足够把问题再缩小一层了，而且我觉得已经碰到一个很像真正根因的地方了。

**现在可以确认：`LLAMA_HYBRID_STAGE_QUEUE=0` 本身没问题。** 你最新版已经用 `atoi(...) != 0` 判断，所以设为 `0` 的确是关闭，不会像之前“只要环境变量存在就视为开启”。

更关键的是，你这两次实验：

```text
正常：
T=49 P=0 C=15 G=14 R=0.713
GPU[0,14) → CPU[14,15) → TS[15,64)

崩溃：
T=50 P=0 C=14 G=14 R=0.639
GPU[0,14) → TS[14,64)
```

说明问题和 StageQ 已经可以分离开。但是我现在**不会再简单归因成“直接 GPU→TS 边界有问题”**，因为我查了你 RPC 的**准确报错行**，发现它实际死在了更具体的位置。

### `ggml-rpc.cpp:1064` 是 snapshot 接收失败

你当前 RPC 代码这一段是：

```cpp
bool status =
    send_rpc_cmd_compact_small(
        snapshot_sock,
        RPC_CMD_GET_SNAPSHOT,
        request);

RPC_STATUS_ASSERT(status);

status = snapshot_sock->recv_data(
    data,
    size);

RPC_STATUS_ASSERT(status);   // 这里就是大约 1064
```

也就是说客户端已经把：

```text
GET_SNAPSHOT
slot
seq
size
```

发给手机了。

真正失败的是：

> **手机没有返回客户端期待的那一整块 snapshot 数据。**



所以现在这个：

```text
Remote RPC server crashed or returned malformed response
```

准确来说是：

```text
PC:
GET_SNAPSHOT(seq=X, slot=Y, size=N)
    ↓
手机端没有成功返回 N bytes
    ↓
recv_data() false
    ↓
RPC_STATUS_ASSERT
```

这已经不是泛泛的 RPC 问题了。

---

## 我发现一个非常可疑的设计 bug：snapshot seq 是“局部的”，snapshot slot 却是“全局共享的”

你 Meta backend 现在：

```cpp
struct ggml_backend_meta_context {
    ...
    uint64_t next_snapshot_seq = 1;
};
```

每创建一个新的 Meta context：

```text
next_snapshot_seq
```

都会从 **1** 重新开始。

然后每一次 snapshot：

```cpp
const uint64_t snapshot_seq =
    use_snapshot_pipeline
        ? backend_ctx->next_snapshot_seq++
        : 0;

const uint32_t snapshot_slot = snapshot_seq & 1;
```



但是手机 RPC server 那边并不是每个 Meta context 有自己的 snapshot slots。

它是整个共享 `rpc_server`：

```cpp
std::vector<std::unique_ptr<rpc_snapshot_device>>
    snapshot_devices;
```

每个 device 就：

```cpp
struct rpc_snapshot_device {
    std::array<rpc_snapshot_slot, 2> slots;
};
```

也就是说实际上是：

```text
手机 RPC server
device 0
    slot 0
    slot 1
```

大家一起用。

而 RPC server 本身又被不同 socket/thread 共享。

所以现在形成了一个很危险的结构：

```text
Meta profile context:
    seq = 1,2,3,...,200...

Model Meta context:
    又从 seq = 1,2,3,...

                   ↓

Phone:
    同一套 slot0 / slot1
```

**这两个语义是不匹配的。**

---

# 更要命的是手机端正好有这一句

手机收到 `GET_SNAPSHOT` 后：

```cpp
slot.cv.wait(lock, [&]() {
    return
        (slot.seq == request.seq &&
         slot.state == READY)
        ||
        slot.seq > request.seq;
});
```

然后：

```cpp
if (slot.seq != request.seq ||
    slot.state != READY ||
    slot.data.size() != request.size) {
    return false;
}
```



这里就很容易出事。

举个非常符合你现在情况的例子。

Profile 阶段已经跑过很多 snapshot：

```text
phone slot1:
seq = 157
state = FREE
```

然后正式模型 Meta context 新建：

```text
next_snapshot_seq = 1
```

正式推理请求：

```text
GET_SNAPSHOT
seq=1
slot=1
```

手机这边一看：

```cpp
slot.seq > request.seq

157 > 1
```

于是：

```text
wait() 立即结束
```

根本不等正式 runtime 的 snapshot producer 把：

```text
seq=1
```

的新结果写进来。

紧接着：

```cpp
slot.seq != request.seq
```

也就是：

```text
157 != 1
```

成立，于是：

```cpp
return false;
```

server 关闭这个 snapshot socket。

PC：

```cpp
recv_data(data, size)
```

失败。

最后就是你现在看到的：

```text
ggml-rpc.cpp:1064:
Remote RPC server crashed or returned malformed response
```

**这一整条链路是完全对得上的。**

---

## 而且多 socket 会让这个问题变成竞态

你的 snapshot GET 和 graph compute/snapshot producer 不是同一个 socket：

```text
compute socket
snapshot socket 0
snapshot socket 1
```

server 又是一连接一线程。

于是完全可能发生：

```text
旧状态：
slot1.seq = 157

新的 Meta context：
seq = 1

线程 A：GET_SNAPSHOT(seq=1)
       ↓
       看见 157 > 1
       ↓
       立即判定 stale
       ↓
       return false

线程 B：新的 graph compute
       ↓
       本来马上就准备产生
       snapshot(seq=1)
```

只是 A 抢先了一点点。

这也能解释为什么这个东西表现得不像固定的 graph topology bug，而会有：

```text
有时候可以
有时候直接 RPC malformed
```

的味道。

---

# `C=15,T=49` 正常，并不能推翻这个结论

因为两组实验实际上除了边界不同，还有：

```text
正常：
R=0.713
T=49
C=15

失败：
R=0.639
T=50
C=14
```

所以 snapshot 的：

```text
产生时机
tensor 大小
RPC branch 时间
subgraph 数量
```

都会不同。

一个 CPU-only 层还会改变 scheduler / Meta 子图的时序。

因此完全可能：

```text
C=15:
producer 先把新 seq 写进去
↓
GET 到来
↓
正常
```

而：

```text
C=14:
GET 线程先到
↓
看到旧 seq > 新 seq
↓
直接失败
```

它看起来像：

```text
加一层 CPU 就好了
```

实际可能只是把 race 的时间窗口改变了。

---

# 我建议现在先做一个非常小的确认，不要继续改 StageQ

在手机端 `send_snapshot()` 的这个判断：

```cpp
if (slot.seq != request.seq ||
    slot.state != rpc_snapshot_state::READY ||
    slot.data.size() != request.size) {
    return false;
}
```

前面改成：

```cpp
if (slot.seq != request.seq ||
    slot.state != rpc_snapshot_state::READY ||
    slot.data.size() != request.size) {

    fprintf(stderr,
        "[RPC_SNAPSHOT_MISMATCH] "
        "slot=%u "
        "req_seq=%" PRIu64 " "
        "cur_seq=%" PRIu64 " "
        "req_size=%" PRIu64 " "
        "cur_size=%zu "
        "state=%d\n",
        request.slot,
        request.seq,
        slot.seq,
        request.size,
        slot.data.size(),
        (int) slot.state);

    fflush(stderr);
    return false;
}
```

然后 PC 客户端在：

```cpp
snapshot_sock->recv_data(...)
```

之前加：

```cpp
GGML_LOG_ERROR(
    "[RPC_SNAPSHOT_GET] "
    "slot=%u seq=%" PRIu64 " size=%zu\n",
    request.slot,
    request.seq,
    size);
```

再强制：

```text
T=50
C=14
G=14
P=0
R=0.639
XG=128
XT=32

LLAMA_HYBRID_STAGE_QUEUE=0
```

跑一次。

### 如果我现在判断对了，你手机上大概率会直接看到类似

```text
[RPC_SNAPSHOT_MISMATCH]
slot=1
req_seq=1
cur_seq=137
req_size=......
cur_size=......
state=0
```

或者：

```text
req_seq=3
cur_seq=201
```

也就是最关键的：

```text
cur_seq > req_seq
```

那这个 bug 就可以算彻底抓住了。

---

# 如果确认了，修法也很明确

当前：

```cpp
uint64_t next_snapshot_seq = 1;
```

**不应该属于单个 `ggml_backend_meta_context`。**

因为 server 的 snapshot slots 根本不是 Meta-context-local 的。

最低成本修法，是先把它提升成**PC 进程级全局 atomic sequence**：

```cpp
#include <atomic>

static std::atomic<uint64_t> g_snapshot_seq { 1 };
```

然后：

```cpp
const uint64_t snapshot_seq =
    use_snapshot_pipeline
        ? g_snapshot_seq.fetch_add(
              1,
              std::memory_order_relaxed)
        : 0;
```

取代：

```cpp
backend_ctx->next_snapshot_seq++
```

这样：

```text
profile Meta:
1,2,...150

runtime Meta:
151,152,...
```

而不会：

```text
profile:
1...150

runtime:
1...
```

这与手机端现有：

```cpp
slot.seq > request.seq
```

的单调序列假设才是一致的。

---

不过这个还是**当前进程内修复**。

如果以后出现：

```text
PC llama-server 重启
但手机 RPC server 不重启
```

PC global atomic 又会从 1 开始，而手机可能还记着 5000。

所以最终完全正确的设计应该是：

```text
snapshot identity
=
session / epoch
+
sequence
```

例如：

```cpp
struct snapshot_id {
    uint64_t session;
    uint64_t seq;
};
```

或者新增一个：

```text
RPC_RESET_SNAPSHOT_SESSION
```

模型/profile runtime 切 session 时明确把手机：

```text
slot0
slot1
```

重置。

但这可以作为第二阶段。

**你眼下 profile→model runtime 在同一 PC 进程里的问题，先把 seq 改成进程级 atomic 就足够验证。**

---

所以我现在建议先不要动：

```text
XG
XT
StageQ dispatcher
GPU→Tensor boundary
graph_max_nodes
```

这次报错已经精确落到了：

```text
RPC GET_SNAPSHOT recv
```

而你的代码里又正好存在：

```text
server snapshot slot = 全局共享
client snapshot seq   = 每 Meta context 从1重置
```

这样一个结构性矛盾。

**先加 `RPC_SNAPSHOT_MISMATCH` 那条日志。** 如果看到 `cur_seq > req_seq`，我们下一刀就直接改 `next_snapshot_seq`，这比继续围着 StageQ 猜要靠谱得多。
