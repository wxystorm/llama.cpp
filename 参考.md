是的，现在先不要继续调 `tensor-split` 或调度优先级了。**我把你刚上传的 `ggml-backend-meta(1).cpp` 和 `ggml-rpc.cpp` 对着看了一遍，现在至少有一个非常明确的 correctness 问题，应该先把它修掉。**

而且先纠正我前面一个判断：**你现在的 `0->1 transfer_end` 并不代表手机端已经真正收到并写完 tensor。**

### 现在最明确的问题：`SET_TENSOR` 没有 ACK

你当前 client 的 `ggml_backend_rpc_buffer_set_tensor()` 最后还是：

```cpp
const bool status = send_rpc_cmd(
    ctx->sock,
    RPC_CMD_SET_TENSOR,
    input.data(),
    input.size());
```

它调用的是**无 response 版本**。也就是说，只要 PC 把 request/data 交给 socket，这个函数就可以返回。

而 server 端：

```cpp
case RPC_CMD_SET_TENSOR: {
    std::vector<uint8_t> input;
    if (!recv_msg(sock, input)) {
        return;
    }
    if (!server.set_tensor(input)) {
        return;
    }
    break;
}
```

在 `server.set_tensor()` 真正完成之后，**没有给 client 发任何 ACK**。

这在原来的**单 socket** 版本里基本没事，因为：

```text
transfer socket:

SET
↓
server set_tensor
↓
server 才继续读下一条 RPC
```

同一 socket 自带顺序。

但现在你已经拆成：

```text
compute socket        transfer socket
      |                     |
    ADD                SET_TENSOR
```

两边完全独立。

于是现在真的可能发生：

```text
PC transfer thread                  Phone transfer thread

SET_TENSOR ---------->
send() 返回
    ↓
transfer_one() 返回
    ↓
transfer_end
    ↓
incoming_done[phone] = true
    ↓
ADD ready


                                       recv SET
                                          ↓
                                    tensor_set(tmp)
```

而与此同时 compute socket：

```text
Phone compute thread

收到 ADD 请求
    ↓
node_local += node_tmp
                  ^
                  |
            这里 tmp 可能还没写完
```

这就是一个真正的数据 race。

---

而你的 scheduler **确实把 `transfer_one()` 返回直接当成了 transfer 完成**：

```cpp
incoming_done[task.chunk][transfer.j_dst] = true;
outgoing_done[task.chunk][transfer.j_src] = true;

enqueue_add_locked(...);
```



所以 scheduler 的逻辑本身没错：

```text
transfer_done
→ incoming_done
→ ADD ready
```

错的是：

> 对 RPC `SET_TENSOR` 来说，当前的 `transfer_done` 是假的，它只是“PC send 返回”，不是“Phone tensor_set 完成”。

更关键的是，你 RPC 的：

```cpp
ggml_backend_rpc_synchronize()
```

目前还是 no-op：

```cpp
static void ggml_backend_rpc_synchronize(ggml_backend_t backend) {
    GGML_UNUSED(backend);
    // this is no-op because we don't have any async operations
}
```



所以 `transfer_one()` 前面的：

```cpp
ggml_backend_synchronize(...)
```

也救不了这个 race。

---

### 这和你刚才那份异常 trace 能很好对应

刚才有一份：

```text
chunk0 0->1 transfer_end = 226787 us

chunk0 add_ready 1       = 226787 us

chunk0 add_begin 1       = 226812 us
```

也就是说：

```text
SET send 返回
       ↓
只隔 25 us
       ↓
Phone ADD 开始
```

**25 μs 根本不能证明手机端已经完成 `tensor_set`。**

因此这种运行非常容易变成：

```text
Phone ADD:
B += tmp

同时

Phone transfer thread:
tmp <- A
```

那么最终结果当然可能随机错。

这比我们之前怀疑的 floating-point 差异严重得多，也更能解释你说的“输出变了”。

---

## 修复非常直接：给 `SET_TENSOR` 也加 completion ACK

server：

```cpp
case RPC_CMD_SET_TENSOR: {
    std::vector<uint8_t> input;

    if (!recv_msg(sock, input)) {
        return;
    }

    if (!server.set_tensor(input)) {
        return;
    }

    // tensor_set 真正完成以后再 ACK
    if (!send_msg(sock, nullptr, 0)) {
        return;
    }

    break;
}
```

client 改成：

```cpp
const bool status = send_rpc_cmd(
    ctx->sock,
    RPC_CMD_SET_TENSOR,
    input.data(),
    input.size(),
    nullptr,
    0);
```

于是语义变成：

```text
PC                                  Phone

SET_TENSOR ------------------------->
                                     recv
                                     tensor_set
                                     █████
                                     done
                 <-------------- ACK

ggml_backend_tensor_set()
返回
↓
transfer_end
↓
incoming_done = true
↓
ADD ready
```

这样：

> `transfer_end` 才真正等价于“destination tensor 已经 ready”。

这和你给 `GRAPH_COMPUTE` 加 ACK 是同一个道理。

你当前 `GRAPH_COMPUTE/RECOMPUTE` 已经是 client 等空 response，server 在图计算结束后才发送空 response，这部分 completion 语义是明确的。 

---

有一点你要预期：

加完以后，你现在这种：

```text
0->1 SET
≈ 0.5~0.9 ms
```

大概率会重新变长。

这是正常的。

因为现在测的是：

```text
旧：

PC memcpy到input
+
send给socket
----------------
返回
```

而加 ACK 后测的是：

```text
PC send
+
网络
+
Phone recv
+
Phone tensor_set
+
ACK回来
----------------
返回
```

所以**不要因为 SET timing 变长又把 ACK 撤掉**。前一个数字本来就不是 transfer completion latency。

---

### 好消息是，之前那个 `tmp` 重用问题你现在已经修了

我看了你当前 meta 文件。

`backend_config::bufs` 已经改成：

```cpp
bufs.resize(n_reduce_steps * n_pipeline_chunks);
```

也就是给 pipeline chunk 预留了独立 slot。

然后 `prepare_reduce()`：

```cpp
set_tmp_data(
    node_tmp,
    j_dst,
    buffer_slot * backend_ctx->n_reduce_steps);
```

而调用的时候：

```cpp
prepare_reduce(
    nodes,
    reduce_states[i_chunk],
    i_chunk);
```

所以三个 chunk 现在分别使用自己的 tmp slot。

也就是已经从：

```text
C0 -> tmp[0]
C1 -> tmp[0]  ❌
C2 -> tmp[0]  ❌
```

变成：

```text
C0 -> tmp[0]
C1 -> tmp[1]
C2 -> tmp[2]
```

所以**我上一轮担心的跨 chunk `node_tmp` 覆盖，在你当前上传的版本里已经不是主要问题了。**

这也是为什么现在不要继续同时改很多东西，我们已经可以把问题一个一个剥出来。

---

还有第二个风险我看到了，但我建议**先不要动它**。

你现在 server 是：

```cpp
auto server = std::make_shared<rpc_server>(...);

std::thread(...) {
    rpc_serve_client(*server, ...);
}
```

所以 compute connection 和 transfer connection 确实共享同一个 `rpc_server`。

但是 `rpc_server` 里面：

```cpp
std::unordered_set<ggml_backend_buffer_t> buffers;
std::vector<std::unordered_map<uint64_t, stored_graph>> stored_graphs;
```

本身没有 mutex。

现在两个 server thread 会同时进入：

```text
thread compute:
graph_compute / graph_recompute

thread transfer:
get_tensor / set_tensor
```

稳定推理阶段大部分时候：

```text
buffers       基本只读
stored_graphs 基本只有 compute thread 操作
```

所以它**不一定是当前输出错误的来源**。

先不要给整个 `rpc_server` 加一个大 mutex：

```cpp
lock_guard lock(server_mutex);
```

否则会直接重新变成：

```text
GET
█████████████████

COMPUTE
.................████
```

双 socket 的意义就没了。

如果之后真要处理，只锁：

```text
buffer registry
graph cache metadata
```

这种很短的 metadata critical section。

---

所以我建议现在把代码“冻结”到一个明确 baseline，只做这一件事：

1. 保留双 socket。
2. 保留 `GRAPH_COMPUTE/RECOMPUTE` ACK。
3. 保留现在每 chunk 独立 tmp。
4. 保留 ADD 的 `compute_done + incoming_done + outgoing_done` 三依赖。
5. **只给 `SET_TENSOR` 增加 completion ACK。**

然后 PC 和手机端一起重新编译。

接着不要先测速，先做 correctness test：

```powershell
--temp 0 --seed 1234
```

同一个 prompt 连续跑 5～10 次。

如果修复前：

```text
run1 -> A
run2 -> B
run3 -> A
run4 -> C
```

而修复后：

```text
run1 -> A
run2 -> A
run3 -> A
run4 -> A
run5 -> A
```

那基本就抓到主要 race 了。

**我现在最建议你先修 `SET_TENSOR ACK`。** 这是我从你这两份当前代码里看到的最明确、可以直接证明的跨连接 correctness 缺口。其他性能和调度问题先都不要动。
