对，这个我更倾向于判断为：**你回退/重组这一版时，把我们刚刚修掉的旧问题带回来了一部分**，而不是 predictor / Active Stage 这条新路线本身导致了 RPC 崩。

我看了你这次的 `llama-context.cpp`，已经能确认两个回归点。

第一处非常明确：这一版把 `LLAMA_HYBRID_STAGE_QUEUE=0` 也当成“开启”了：

```cpp
const bool stage_queue_requested =
    std::getenv("LLAMA_HYBRID_STAGE_QUEUE") != nullptr;
```

也就是说只要环境变量存在：

```bat
set LLAMA_HYBRID_STAGE_QUEUE=0
```

结果依然是：

```text
stage_queue_requested = true
```



而我们之前已经改成了正确版本：

```cpp
const char * stage_queue_env = std::getenv("LLAMA_HYBRID_STAGE_QUEUE");

const bool stage_queue_requested =
    stage_queue_env != nullptr &&
    std::atoi(stage_queue_env) != 0;
```

所以这一处应该先恢复。

而且这一版还有第二个回归：

```cpp
if (stage_queue_plan_eligible) {
    ...
    stage_queue_runtime_enabled = true;
    runtime_ubatch = xg;
}
```

现在即使：

```text
batch=2
XG=128
macros=1
```

它仍然：

```text
stage_queue_runtime_enabled=true
```



我们之前跑通的稳定版本是：

```cpp
stage_queue_runtime_enabled = n_macros >= 2;

if (stage_queue_runtime_enabled) {
    runtime_ubatch = xg;
}
```

也就是：

```text
macros=1 → 不启 StageQ
macros>=2 → 才启 StageQ
```

这两个最好一起恢复成：

```cpp
llama_hybrid_plan runtime_plan;
const bool has_runtime_plan =
    llama_hybrid_runtime_plan_get(runtime_plan);

const char * stage_queue_env =
    std::getenv("LLAMA_HYBRID_STAGE_QUEUE");

const bool stage_queue_requested =
    stage_queue_env != nullptr &&
    std::atoi(stage_queue_env) != 0;

const bool stage_queue_plan_eligible =
    stage_queue_requested &&
    has_runtime_plan &&
    runtime_plan.gpu_pc_layers > 0 &&
    runtime_plan.pc_layers == runtime_plan.gpu_pc_layers &&
    runtime_plan.tensor_layers > 0 &&
    runtime_plan.phone_layers == 0;

bool stage_queue_runtime_enabled = false;
uint32_t runtime_ubatch = cparams.n_ubatch;

if (stage_queue_plan_eligible) {
    const uint32_t xg =
        std::min<uint32_t>(
            cparams.n_ubatch,
            runtime_plan.gpu_chunk_tokens);

    const uint32_t n_macros =
        (n_tokens_all + xg - 1) / xg;

    stage_queue_runtime_enabled = n_macros >= 2;

    if (stage_queue_runtime_enabled) {
        runtime_ubatch = xg;
    }

    LLAMA_LOG_ERROR(
        "[STAGEQ] batch=%u XG=%u macros=%u XT=%d enabled=%d overlap=%d warmup=%d\n",
        n_tokens_all,
        xg,
        n_macros,
        runtime_plan.tensor_chunk_tokens,
        (int) stage_queue_runtime_enabled,
        (int) (n_macros >= 2),
        (int) cparams.warmup);
}
```

### 不过，`ggml-rpc.cpp:1064` 我首先还是怀疑之前那个 snapshot bug 回来了

因为这个报错位置和我们刚才定位的一模一样：

```text
GET_SNAPSHOT request 成功
↓
snapshot_sock->recv_data(...)
↓
失败
↓
line 1064
```

而之前已经实锤是：

```text
req_seq = 1
cur_seq = 0x80000000000000A7
```

服务端错误地使用：

```cpp
|| slot.seq > request.seq
```

把不同 snapshot session 的 seq 当成全局递增序号。

你刚才一删这句，马上就正常了。

所以如果这次“回退”包含 `ggml-rpc.cpp`，我高度怀疑这里也被回退成旧代码了。

你现在直接在手机端/服务端 `ggml-rpc.cpp` 搜：

```cpp
slot.seq > request.seq
```

如果看到：

```cpp
slot.cv.wait(lock, [&]() {
    return
        (slot.seq == request.seq &&
         slot.state == rpc_snapshot_state::READY)
        ||
        slot.seq > request.seq;
});
```

那就不用继续查 predictor 了，**就是同一个 bug 被恢复了。**

先重新变回：

```cpp
slot.cv.wait(lock, [&]() {
    return
        slot.seq == request.seq &&
        slot.state == rpc_snapshot_state::READY;
});
```

并继续保留我们之前的：

```text
[RPC_SNAPSHOT_MISMATCH]
```

诊断打印。

---

所以我建议这一次不要动新架构，按这个顺序恢复：

```text
① ggml-rpc.cpp
   确认旧的 "slot.seq > request.seq" 没回来

② llama-context.cpp
   getenv != nullptr
   ↓
   getenv + atoi != 0

③ StageQ
   runtime_enabled=true
   ↓
   runtime_enabled=(n_macros>=2)

④ 固定旧 baseline
   G=14 C=14 T=50 P=0
   XG=128 XT=32

⑤ 先 StageQ=0
   确认 RPC + 输出

⑥ 再 StageQ=1
   确认 pipeline
```

特别是第②点，你这次上传的代码里是**确定存在的 bug**：`set LLAMA_HYBRID_STAGE_QUEUE=0` 现在实际上关不掉 StageQ。

而 predictor 的那次修改只改变了 Tensor cost 中 macro 的选择规则，不会直接改变 RPC snapshot 协议；当前实现已经正确按 `C-G>0 → XC，否则有 GPU → XG` 选择 Tensor macro。

所以这次先别怀疑新路线。**最可能是 rollback 把 RPC snapshot 修复和 StageQ gating 的两个旧版本一起带回来了。**
