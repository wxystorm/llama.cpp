第一步我建议先只做一件事：**把 PC_ONLY 区域改成“GPU 前缀 + CPU 后缀”的静态放置，并确保能正确跑通。先完全不碰 Prefill token chunk。**

这样我们先把“层放置”这个变量单独验证掉，后面再做 GPU→CPU 的 Prefill pipeline。

你现在 `load_tensors()` 的标准逻辑本质上是 `-ngl` 的“尾部上 GPU”：

```cpp
const int i_gpu_start = std::max(n_layer_all + 1 - n_gpu_layers, 0);

if (il < i_gpu_start || ...) {
    return CPU;
}

return GPU;
```

也就是天然形成 CPU→GPU，而不是我们现在想要的 GPU→CPU。

所以第一刀就是：**对 PC_ONLY 层绕过这个默认 `i_gpu_start` 逻辑。**

### 先增加一个独立的 PC 放置状态

不要改现有：

```cpp
enum class llama_hybrid_layer_mode {
    PC_ONLY,
    TENSOR_SPLIT,
    PHONE_ONLY,
};
```

它仍然描述：

```text
PC / Phone 之间的 ownership
```

再增加：

```cpp
enum class llama_pc_layer_backend {
    CPU,
    CUDA,
};
```

然后 `llama_model` 里加：

```cpp
std::vector<llama_pc_layer_backend> pc_layer_backends;
```

现有 `hybrid_layer_modes` 本来就是每层一个状态。

再加环境变量：

```cpp
static int llama_hybrid_gpu_pc_layers() {
    const char * value = std::getenv("LLAMA_HYBRID_GPU_PC_LAYERS");

    if (value == nullptr) {
        return 0;
    }

    return std::max(0, std::atoi(value));
}
```

然后在 `hybrid_layer_modes` 已经生成之后，构造第二层 policy。

比如：

```cpp
pc_layer_backends.assign(
    n_layer,
    llama_pc_layer_backend::CPU);

std::vector<int> pc_layers;

for (int il = 0; il < n_layer; ++il) {
    if (hybrid_layer_modes[il] ==
            llama_hybrid_layer_mode::PC_ONLY) {
        pc_layers.push_back(il);
    }
}

const int n_gpu_pc =
    std::min<int>(
        llama_hybrid_gpu_pc_layers(),
        pc_layers.size());

// 关键：从 PC_ONLY 的前面开始放 GPU
for (int k = 0; k < n_gpu_pc; ++k) {
    pc_layer_backends[pc_layers[k]] =
        llama_pc_layer_backend::CUDA;
}
```

例如你 Qwen32B 当前布局：

```text
0  ~ 35   TENSOR_SPLIT
36 ~ 47   PHONE_ONLY
48 ~ 63   PC_ONLY
```

设置：

```powershell
$env:LLAMA_HYBRID_GPU_PC_LAYERS="12"
```

得到：

```text
0  ~ 35   CPU + Phone TS
36 ~ 47   Phone
48 ~ 59   GPU
60 ~ 63   CPU
```

正好是我们刚讨论的：

```text
Phone → GPU → CPU
```

---

### 第二处才是 `load_tensors()` 真正选择 device

现在 loader 已经有：

```cpp
pimpl->cpu_buft_list = make_cpu_buft_list(...);

for (const auto & dev : devices) {
    buft_list_t buft_list =
        make_gpu_buft_list(dev.dev, split_mode, tensor_split);

    ...
    pimpl->gpu_buft_list.emplace(dev.dev, ...);
}
```



而 `pimpl->dev_layer[il]` 本来就是逐层选择 device/buft list 的。

所以把当前：

```cpp
auto get_layer_buft_list = [&](int il) {
    ...
};
```

最前面插入一个 hybrid PC_ONLY 分支：

```cpp
auto get_layer_buft_list =
    [&](int il) -> llama_model::impl::layer_dev {

    const bool valid_layer =
        il >= 0 && il < n_layer_all;

    if (valid_layer &&
        hybrid_layer_mode(il) ==
            llama_hybrid_layer_mode::PC_ONLY) {

        if (pc_layer_backends[il] ==
                llama_pc_layer_backend::CUDA) {

            return {
                cuda_dev,
                &pimpl->gpu_buft_list.at(cuda_dev)
            };
        }

        return {
            cpu_dev,
            &pimpl->cpu_buft_list
        };
    }

    // TENSOR_SPLIT / PHONE_ONLY
    // 继续你原来的 META device 放置逻辑
    ...
};
```

这里有一个**非常关键的点**：

> `cuda_dev` 必须是 plain `CUDA0`，不能是 `Meta(CUDA0,RPC0)`。

也就是说最终我们希望模型层设备是这种：

```text
layer 0~47:
    Meta(CPU, RPC)

layer 48~59:
    CUDA0

layer 60~63:
    CPU
```

而不是：

```text
Meta(CPU, CUDA, RPC)
```

因为你现在 `ggml-backend-meta.cpp` 的优化路径大量建立在：

```text
backend0 = PC primary
backend1 = Phone
n_backends = 2
```

之上。刚修好的 `reduce_to_phone`、snapshot、Phone block 都不要动。

---

### 第一版我甚至建议先接受一次多余中转

最终我们当然想做到：

```text
Phone layer47
       ↓
    直接 CUDA
       ↓
GPU layer48~59
       ↓
     CPU
```

但第一版先别碰你已经稳定的 Phone handoff。

可以暂时：

```text
Phone
  ↓
META primary CPU
  ↓
CUDA
  ↓
CPU
```

也就是：

```text
Phone → CPU → CUDA → CPU
```

多一次 16KB/Token（Decode）或者 Prefill activation 的中转。

**第一阶段只验证：**

```text
权重是不是正确进显存
48~59 是不是真正在 CUDA 算
60~63 是不是真正在 CPU 算
结果是否正确
显存是否符合预期
```

这一步稳定以后，再把：

```text
Phone → CPU → CUDA
```

优化成：

```text
Phone → CUDA
```

这样我们不会同时动：

```text
模型加载
layer placement
Phone handoff
Prefill pipeline
```

四个地方，否则再出问题会非常难查。

---

建议第一版加非常明确的日志：

```cpp
LLAMA_LOG_INFO(
    "[PC_PLACE] layer=%d hybrid=%s backend=%s\n",
    il,
    "PC_ONLY",
    pc_layer_backends[il] ==
            llama_pc_layer_backend::CUDA ?
        "CUDA" : "CPU");
```

Qwen32B 启动时我们应该看到：

```text
[PC_PLACE] layer=48 backend=CUDA
...
[PC_PLACE] layer=59 backend=CUDA
[PC_PLACE] layer=60 backend=CPU
...
[PC_PLACE] layer=63 backend=CPU
```

然后 `nvidia-smi` 能看到对应权重占用。

**所以第一步的验收目标非常明确：先实现**

```text
PC_ONLY = [GPU prefix][CPU suffix]
```

**不做 token pipeline、不做动态 chunk、不改 META 双后端。**

这个跑通之后，第二步才是把 `Phone→GPU` 直接化；第三步才开始把 Prefill 切成：

```text
GPU chunk0 ─────► CPU chunk0
GPU chunk1 ─────► CPU chunk1
GPU chunk2 ─────► CPU chunk2
```

然后才有必要研究我们刚才说的 `B_gpu / B_cpu` 粒度。
