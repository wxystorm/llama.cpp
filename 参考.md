可以。你现在这个方案已经不缺“想法”了，接下来最重要的是**按依赖关系拆开实现**，不要一上来同时改 tensor-split、Wdown chunk、RMSNorm、QKV partial 和线程调度，不然一旦数值错了很难定位。

你的规划本身已经把主线定好了：Attention 留 PC、FFN 协同、手机 Wdown 按 hidden 输出 chunk 回传、PC 收到 chunk 后提前做下一层 QKV。

另外，你现在实际工作的那个 `7a394db90` 提交和当前 master 代码结构可能不完全一样。下面我会用**功能点/张量名**来告诉你怎么找，而不是死绑某个当前 master 的函数名。当前 master 里 LLaMA 的 `ffn_gate/up` shape 是 `{n_embd,n_ff}`，`ffn_down` 是 `{n_ff,n_embd}`；其 split 配置也正好是 gate/up 沿 axis 1、down 沿 axis 0，也就是统一沿 FFN intermediate 维切。([GitHub][1])

## 我建议你严格按这 7 步改

1. **第一步只改权重放置：Attention 不切，FFN 继续切。**

   先不要碰 decode pipeline。

   找你那个提交里处理 `tensor_split` / tensor backend placement 的地方：

   ```bash
   git grep "tensor_split"
   git grep "ffn_gate"
   git grep "ffn_down"
   git grep "attn_q"
   ```

   目标是变成：

   ```text
   attention:
       attn_norm -> PC
       Wq        -> PC
       Wk        -> PC
       Wv        -> PC
       Wo        -> PC

   FFN:
       ffn_norm  -> PC
       Wgate     -> PC + Phone
       Wup       -> PC + Phone
       Wdown     -> PC + Phone
   ```

   **这里第一版只验证权重到底在哪。**

   加打印：

   ```text
   blk.0.attn_q.weight   -> PC
   blk.0.attn_k.weight   -> PC
   blk.0.attn_v.weight   -> PC
   blk.0.attn_output     -> PC

   blk.0.ffn_gate        -> split
   blk.0.ffn_up          -> split
   blk.0.ffn_down        -> split
   ```

   先运行模型，确保能正常生成，而且 attention 确实没有 RPC 到手机。

   特别注意 GGML 维度：

   ```text
   gate/up:
       ne[0] = n_embd = 4096
       ne[1] = n_ff   = 11008

   down:
       ne[0] = n_ff   = 11008
       ne[1] = n_embd = 4096
   ```

   所以**设备之间的 FFN 切分**是：

   ```text
   gate/up -> ne[1]
   down    -> ne[0]
   ```

   这两种物理方向看起来相反，但逻辑上切的是同一个 `n_ff`。当前 llama.cpp 也是这样处理的。([GitHub][2])

2. **第二步先做“最傻”的 FFN 协同版：不 chunk，不流水。**

   这一阶段非常重要。

   当前正常计算大致是：

   ```text
   attention
       ↓
   residual
       ↓
   ffn_norm
       ↓
   gate/up
       ↓
   SwiGLU
       ↓
   down
       ↓
   residual
   ```

   当前 master 的图构造也能看到 `ffn_inp -> ffn_norm -> build_ffn -> residual add` 这个顺序。([GitHub][1])

   你先改成：

   ```text
                      PC
                attention 完成
                      │
                    h_att
                      │
                  FFN RMSNorm
                      │
                    x_ffn
                   /     \
                  /       \
                PC       Phone
                │          │
             gate/up    gate/up
                │          │
              SwiGLU     SwiGLU
                │          │
              Wdown      Wdown
                │          │
            y_pc[4096] y_ph[4096]
                  \       /
                   \     /
                     +
                     │
                 + h_att
                     │
                  h_next
   ```

   PC 把 **FFN RMSNorm 后的 4096 维 `x_ffn`** 发给手机。

   手机完整算自己的：

   ```text
   gate_phone
   up_phone
   SwiGLU
   down_phone
   ```

   然后先一次性返回完整：

   ```text
   y_phone[4096]
   ```

   PC：

   $$
   h_{next}=h_{att}+y_{pc}+y_{phone}
   $$

   此时完全不要 chunk。

   **这一阶段通过以后，说明 FFN tensor split 本身是正确的。**

   你要比较：

   ```text
   原始版本 logits
   vs
   协同 FFN 版本 logits
   ```

   允许浮点加法顺序造成极小误差，但不能有明显偏差。

3. **第三步只改 Wdown：手机由一次返回 4096 改成 4 × 1024。**

   到这里才开始实现你规划里的 chunk。

   手机的 gate/up 不变：

   ```text
   x_ffn
      ↓
   Wgate/Wup
      ↓
   SwiGLU
      ↓
   z_phone[n_ff_phone]
   ```

   然后把**手机自己的 Wdown 输出维度**切：

   ```text
   chunk 0: output [   0,1024)
   chunk 1: output [1024,2048)
   chunk 2: output [2048,3072)
   chunk 3: output [3072,4096)
   ```

   这里要特别记住：

   > 设备 split 和 Wdown chunk 是两个不同方向。

   对 `Wdown`：

   ```text
   GGML:
   ne[0] = n_ff      ← PC/Phone 权重切分在这里
   ne[1] = n_embd    ← 1024 output chunk 在这里
   ```

   也就是说是一个二维切法：

   ```text
                 n_ff
        ┌────────────────────────┐
        │        PC part         │
   1024 ├────────────────────────┤ chunk 0
        │                        │
   1024 ├────────────────────────┤ chunk 1
        │                        │
   1024 ├────────────────────────┤ chunk 2
        │                        │
   1024 ├────────────────────────┤ chunk 3
        └────────────────────────┘

   手机也有自己的 n_ff slice。
   ```

   第一版甚至可以让 **PC 的 Wdown 还是一次完整算 4096**。

   即：

   ```text
   PC:
       y_pc[4096] 已经算好

   Phone:
       y_phone_chunk0 -> send
       y_phone_chunk1 -> send
       ...
   ```

   收到 chunk 0：

   ```cpp
   h0 = residual[0:1024]
      + y_pc[0:1024]
      + y_phone_chunk0;
   ```

   收到 chunk 1 同理。

   先把最终四块重新拼起来，验证：

   ```text
   chunk版本 h_next == 第二步完整4096版本 h_next
   ```

   **这一步仍然不要做下一层 QKV。**

4. **第四步加入 next-layer RMS 的流式统计，但仍然先不改 QKV。**

   每个最终 chunk：

   ```text
   h_chunk =
       residual_chunk
     + pc_down_chunk
     + phone_down_chunk
   ```

   一旦得到，就立即做两件事：

   $$
   S += \sum h_i^2
   $$

   以及：

   $$
   u_c=\gamma_c\odot h_c
   $$

   其中 `gamma` 是**下一层 `attn_norm.weight`**。

   推荐直接融合：

   ```cpp
   for (int i = 0; i < chunk_size; ++i) {
       float h = residual[offset + i]
               + pc_down[offset + i]
               + phone_down[i];

       hidden[offset + i] = h;

       sum_sq += h * h;

       norm_pre[offset + i] =
           h * next_attn_norm_weight[offset + i];
   }
   ```

   所有 chunk 到了：

   ```cpp
   inv_rms = 1.0f / sqrtf(sum_sq / n_embd + eps);
   ```

   然后先构造：

   ```cpp
   norm_out[i] = norm_pre[i] * inv_rms;
   ```

   和原始：

   ```text
   ggml_rms_norm
   +
   attn_norm.weight
   ```

   输出逐元素比较。

   **第四步的唯一目标就是证明你把 RMSNorm 拆开以后数值是对的。**

5. **第五步才做真正的 partial Q/K/V。**

   现在你已经有：

   ```text
   chunk0 -> gamma0 * h0
   chunk1 -> gamma1 * h1
   ...
   ```

   下一层 `Wq/Wk/Wv` 全部在 PC。

   对于每个 hidden chunk，要切的是 **QKV 权重的输入/K dimension**。

   对 Llama 2 7B：

   ```text
   Wq: 4096 x 4096
   Wk: 4096 x 4096
   Wv: 4096 x 4096
   ```

   chunk0：

   $$
   q_0'=W_Q[:,0:1024]u_0
   $$

   得到的是**完整 4096 维 partial Q**，不是 1024 维 Q。

   所以：

   ```cpp
   memset(q_acc, 0, ...);
   memset(k_acc, 0, ...);
   memset(v_acc, 0, ...);
   ```

   然后：

   ```cpp
   // chunk 0
   q_acc += Wq_K_chunk0 * u0;
   k_acc += Wk_K_chunk0 * u0;
   v_acc += Wv_K_chunk0 * u0;

   // chunk 1
   q_acc += Wq_K_chunk1 * u1;
   ...
   ```

   最后：

   ```cpp
   q = q_acc * inv_rms;
   k = k_acc * inv_rms;
   v = v_acc * inv_rms;
   ```

   然后再走正常：

   ```text
   RoPE
   KV cache
   attention
   ```

   这里在 GGML shape 上，QKV 的 input hidden 通常对应 `ne[0]`，所以和 Wdown 的 output chunk 又不是同一个轴。

   **这里是你整个实现最可能遇到 backend 问题的地方。**

   因为 Q4_K/Q5_K/Q6_K 等量化权重沿 `ne[0]` 切 view 时，要满足 block 对齐，而且未必仍然走原来的高速 GEMV kernel。

   所以第一版可以先用：

   ```text
   F16/F32 模型
   ```

   或者你最熟悉、最容易验证的量化格式跑通。

   然后再处理 K-quant。

6. **第六步才加入 worker / task queue，让 overlap 真正发生。**

   你的任务队列不要设计得太碎。

   不要：

   ```text
   task: residual add
   task: sum square
   task: gamma mul
   task: q
   task: k
   task: v
   ```

   我建议一个 chunk 一个大任务：

   ```cpp
   struct decode_chunk {
       int layer;
       int chunk_id;
       int offset;
       int len;

       // recv buffer etc.
   };
   ```

   状态大致：

   ```text
   PHONE_DOWN_READY
          ↓
   REDUCE_AND_PREP
          ↓
   QKV_PARTIAL_DONE
   ```

   `REDUCE_AND_PREP` 内一次完成：

   ```text
   phone + pc + residual
   sum_sq
   gamma * hidden
   ```

   然后直接 partial QKV。

   两个线程/worker 的逻辑大概：

   ```text
   Transfer worker:
       等手机 chunk
       ↓
       放入 ready queue
       ↓
       继续收下一个 chunk

   Compute/control worker:
       取 ready chunk
       ↓
       reduce
       ↓
       sum_sq + gamma
       ↓
       partial QKV
       ↓
       取下一个
   ```

   buffer 最开始用双缓冲就够：

   ```cpp
   recv_buf[2][chunk_size];
   ```

   关键是：

   ```text
   手机正在算/传 chunk n+1
              ↕ overlap
   PC 正在处理 chunk n
   ```

   这一阶段再记录 timestamp：

   ```text
   phone down start
   phone down finish
   transfer start
   PC recv
   PC reduce start/end
   QKV partial start/end
   ```

   如果看不到这种交叠：

   ```text
   PHONE  ████ TX ██
                   PC QKV ██
   ```

   而还是：

   ```text
   PHONE █████████ TX TX TX TX
                              PC QKV QKV QKV QKV
   ```

   那说明只是“切块”，根本还没流水。

7. **最后才做泛化和性能优化。**

   第一版我甚至建议你只支持：

   ```cpp
   decode only
   n_tokens == 1
   Llama 2 7B
   n_embd == 4096
   n_chunks == 4
   chunk_size == 1024
   ```

   加一个实验开关，例如：

   ```text
   --rpc-ffn-pipeline
   ```

   不满足条件：

   ```cpp
   return original_path();
   ```

   等数值和性能全正确以后，再依次放开：

   ```text
   arbitrary n_embd
   2/4/8 chunks
   GQA
   quantized QKV
   batching
   different LLaMA variants
   ```

   最后一层也要特殊处理，因为最后一个 Transformer layer 后面没有“下一层 QKV”可以提前算，直接走原始 final norm/output 即可。

---

### 换成 Git commit 的顺序，大概应该是这样

| commit | 做什么                               | 判断成功的标准              |
| ------ | --------------------------------- | -------------------- |
| A      | Attention 强制 PC、FFN 保持 split      | 正常生成，attention 无手机计算 |
| B      | PC/Phone FFN 协同，整块 4096 reduce    | logits 与 baseline 一致 |
| C      | Phone Wdown 4×1024 流式回传           | 拼回 4096 与 B 一致       |
| D      | chunk 化 RMS 统计 + gamma            | RMSNorm 输出与原版一致      |
| E      | partial Q/K/V                     | Q/K/V 与原版一致          |
| F      | transfer + compute worker overlap | timeline 出现真实重叠      |
| G      | chunk size/profile/quant 优化       | tok/s 有提升            |

我特别建议**不要跳过 B、C、D 三个中间验证阶段**。你这个方案本身数学不复杂，真正难调的是“一旦最终 logits 不对，到底是权重切错、Wdown view 错、residual 错、RMS 错，还是 partial QKV 切错”。逐层验证后，这些问题会非常容易定位。

当前 master 也明确把 `ffn_norm → FFN → residual → 下一层输入` 放在同一 layer 构图流程里，因此你最终真正需要“撕开”的地方，本质就是 **当前层 FFN 输出与下一层 attention norm/QKV 之间这个 layer boundary**。([GitHub][1])

你现在最应该做的其实就是 **第一步**。先在 `7a394db90` 那个 worktree 里跑：

```bash
git grep -n "tensor_split"
git grep -n "ffn_gate"
git grep -n "ffn_down"
git grep -n "attn_q"
git grep -n "build_ffn"
```

把搜出来的**模型加载/权重切分那部分代码**贴给我。下一步我就可以不再讲抽象方案，直接按你这个 commit 告诉你：**第一个 `if` 加在哪里、哪些 tensor 要排除、gate/up/down 各自该怎么保留 split。**

[1]: https://github.com/ggml-org/llama.cpp/blob/master/src/models/llama.cpp "llama.cpp/src/models/llama.cpp at master · ggml-org/llama.cpp · GitHub"
[2]: https://github.com/ggml-org/llama.cpp/blob/master/src/llama-model.cpp "llama.cpp/src/llama-model.cpp at master · ggml-org/llama.cpp · GitHub"
