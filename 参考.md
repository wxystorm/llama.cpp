这下反而不奇怪了。**CPU 也复现，基本可以把 CUDA、CUDA→RPC copy 都排除掉。现在更像是 Decode 图本身在 `TENSOR_SPLIT → PHONE_ONLY` 边界有逻辑 bug。**

而且我重新看了你现在的 `llama.cpp`，有一个非常吻合现象的地方。

你的 Decode chunk FFN 只在：

```cpp
n_tokens == 1
```

时启用，也就是**恰好只影响 Decode，不影响 Prefill**。

这正好解释：

```text
Prefill -> 第一个 token 正常
Decode  -> 从第二个 token 开始彻底乱
```

### 我现在最怀疑的是 layer 17 偷偷预计算了 layer 18

在 TS layer 17 的 decode chunk 里，你现在不只是算：

```text
FFN17
+ residual
→ l_out-17
```

还提前算了下一层的一部分东西：

```cpp
if (il + 1 < n_layer) {
    ...
    norm_pre_chunks.push_back(norm_pre);

    if (use_partial_qkv) {
        ...
        q_acc = ...
        k_acc = ...
        v_acc = ...
    }
}
```

然后最后构造：

```cpp
inpL_attn_norm = ...
inpL_qkv = {
    finish_projection(...),
    ...
};
```

 

下一次进入 layer 18 时，如果这些值非空，代码**不会重新从 `l_out-17` 算 layer18 norm/QKV**，而是直接吃这些预计算结果：

```cpp
if (inpL_attn_norm != nullptr) {
    cur = inpL_attn_norm;
} else {
    cur = build_norm(inpL, ...);
}

...

if (inpL_qkv.q != nullptr) {
    Qcur = inpL_qkv.q;
    Kcur = inpL_qkv.k;
    Vcur = inpL_qkv.v;
} else {
    build_qkv(...);
}
```



问题就在这里：

```text
layer 17 = TENSOR_SPLIT
layer 18 = PHONE_ONLY
```

但是目前这个预计算逻辑只检查：

```cpp
当前 layer17 是 TENSOR_SPLIT
```

**没有检查下一层 layer18 还是不是 TENSOR_SPLIT。**

于是实际上变成：

```text
layer17 TS
   │
   ├── 正确生成 l_out-17
   │       ↓
   │   META 传给 Phone
   │
   └── 同时提前生成 layer18 的
       attn_norm / QKV prepared
               ↓
       下一轮 layer18 直接使用
```

而我们的 META handoff 设计只保证：

```text
完整 l_out-17
CUDA/CPU → Phone
```

根本没有设计去同步这些“跨层提前计算”的：

```text
inpL_attn_norm
inpL_qkv
```

所以 **Phone layer18 很可能吃的是错误 ownership 下的 prepared tensor**。

这同时解释了三个现象：

```text
Prefill 正常             ✓
第一次 Decode 后开始乱   ✓
CPU / CUDA 都会乱        ✓
```

---

## 我建议直接这样修

在 decode chunk FFN 开头，现在你有：

```cpp
const llama_layer * next_layer =
    il + 1 < n_layer ? &model.layers[il + 1] : nullptr;
```

紧接着加：

```cpp
const bool prepare_next_layer =
    il + 1 < n_layer &&
    model.hybrid_layer_mode(il + 1) ==
        llama_hybrid_layer_mode::TENSOR_SPLIT;
```

然后：

```cpp
const bool use_partial_qkv =
    prepare_next_layer &&
    next_layer != nullptr &&
    next_layer->wqkv == nullptr &&
    can_slice_input(next_layer->wq) &&
    can_slice_input(next_layer->wk) &&
    can_slice_input(next_layer->wv);
```

更重要的是，下面这段现在是：

```cpp
if (il + 1 < n_layer) {
    ggml_tensor * norm_weight = ...
    ...
    norm_pre_chunks.push_back(norm_pre);

    if (use_partial_qkv) {
        ...
    }
}
```

改成：

```cpp
if (prepare_next_layer) {
    ggml_tensor * norm_weight =
        ggml_view_1d(
            ctx0,
            model.layers[il + 1].attn_norm,
            length,
            offset * model.layers[il + 1].attn_norm->nb[0]);

    ggml_tensor * norm_pre =
        ggml_mul(ctx0, hidden, norm_weight);

    norm_pre_chunks.push_back(norm_pre);

    if (use_partial_qkv) {
        // 原来的 partial QKV 代码
        ...
    }
}
```

然后循环结束后，现在是：

```cpp
if (il + 1 < n_layer) {
    inpL_attn_norm = norm_pre_chunks[0];
    ...
}
```

也必须一起改成：

```cpp
if (prepare_next_layer) {
    inpL_attn_norm = norm_pre_chunks[0];

    for (int i = 1; i < n_chunks; ++i) {
        inpL_attn_norm =
            ggml_concat(
                ctx0,
                inpL_attn_norm,
                norm_pre_chunks[i],
                0);
    }

    ggml_tensor * rms =
        ggml_scale_bias(
            ctx0,
            sum_sq,
            1.0f / n_embd,
            hparams.f_norm_rms_eps);

    rms = ggml_sqrt(ctx0, rms);
    inpL_attn_norm =
        ggml_div(ctx0, inpL_attn_norm, rms);

    cb(inpL_attn_norm, "next_attn_norm", il);

    if (use_partial_qkv) {
        // 原来的 finish_projection
        ...
    }
} else {
    inpL_attn_norm = nullptr;
    inpL_qkv = { nullptr, nullptr, nullptr };
}
```

### 这意味着 layer17→18 会变成

```text
17 TENSOR_SPLIT
      ↓
完成 residual
      ↓
完整 l_out-17
      ↓
CPU/CUDA → Phone
      ↓
18 PHONE_ONLY
      ↓
Phone 从 l_out-17 重新算
attn_norm
Q
K
V
```

这才符合我们一直坚持的 boundary invariant：

> **跨设备边界只传最终 `l_out`，目的端从这个 `l_out` 正常开始下一层。**

而对于：

```text
TS layer 0 → TS layer 1
TS layer 1 → TS layer 2
...
```

仍然保留原来的 `next_attn_norm + partial QKV` 提前计算，不损失正常 TS 区间的优化。

---

你刚修的：

```cpp
strcmp(tensor->name, expected)
```

**先保留，不要改回去。**

因为它确实修复了：

```text
ffn_inp-17 (view) 2048
```

误当成：

```text
ffn_inp-17 4096
```

这个明确存在的问题。

现在属于：

```text
Bug 1:
residual finder 找错 view
→ assert
→ 已修

Bug 2:
Decode 跨 hybrid boundary 还保留 next-layer precompute
→ 不 assert，但输出乱码
→ 现在需要修
```

我认为 **Bug 2 比继续检查 `pc_l_out` copy 更值得先改**。因为 `n_tokens==1` 这个条件和“第一个 token 正常、之后全坏”对得太严丝合缝了，而且 CPU/CUDA 都复现进一步支持这是 graph-level 问题。
