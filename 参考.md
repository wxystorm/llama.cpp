那下一步就可以正式进入 **`GPU → CPU → TENSOR` 三阶段流水** 了。现在两级 `GPU → TENSOR` 已经把最难的 scheduler slot、handoff、RPC snapshot、final macro 都跑通，再往后扩展最合适。

这一阶段先不要碰 Phone，也不要先改 predictor 联合搜索。目标只定成：

```text
GPU [0,G)
    ↓ XG
CPU [G,C)
    ↓ XC
TENSOR [C,C+T)
    ↓
output
```

其中最重要的语义是：

```text
XG = GPU 外层 token chunk
XC = CPU-only stage token chunk
XT = Tensor stage 内部 FFN chunk
```

例如：

```text
XG = 128
XC = 32
XT = 16
```

应该执行成：

```text
GPU:
[128]

CPU:
[32][32][32][32]

TENSOR:
macro 32:
    Attention(32)
    FFN(16 + 16)

macro 32:
    Attention(32)
    FFN(16 + 16)
...
```

**Tensor Attention 此时必须按 XC=32，而不是 XG=128。** 这是之前 predictor 那个“最近有效上游 stage”语义在 runtime 里的真正落地。

现有 eligibility 里类似：

```cpp
runtime_plan.pc_layers == runtime_plan.gpu_pc_layers
```

就要开始去掉。新的条件应该至少允许：

```cpp
G > 0
C > G
T > 0
P == 0
```

也就是：

```text
GPU → CPU → TENSOR
```

然后队列从现在的两段：

```text
GPU submitted
→ Tensor ready
```

扩成逻辑上：

```text
GPU submitted
→ CPU ready
→ Tensor ready
```

但这里有一个坑：**不能直接把 128-token GPU slot 当成一个 CPU task。** GPU→CPU 边界要生成 token-span，例如：

```cpp
struct stage_span {
    uint32_t token_begin;
    uint32_t token_count;
    ...
};
```

`XG=128, XC=32` 就从一个 GPU macro 产生：

```text
span 0: [0,32)
span 1: [32,64)
span 2: [64,96)
span 3: [96,128)
```

CPU 每完成一个 32-token span，就可以往 Tensor 推一个 macro，不需要等整块 128 CPU 都结束。

理想时间线应该逐渐变成：

```text
G0(128)
    C0(32)
       T0(32)
    C1(32)
       T1(32)
    C2(32)
       T2(32)
    C3(32)
       T3(32)

同时 G1(128) 尽可能提前执行
```

不过**第一版不要一上来追求 CPU 与 Tensor 的完全并行**。因为 Tensor stage 自己也会占 PC CPU/Meta：

```text
Tensor Attention
Tensor PC-side FFN shard
reduce
```

所以 `CPU_ONLY(i+1)` 与 `TENSOR(i)` 可能发生物理 CPU/Meta 资源冲突。

我建议第一版先做成：

```text
GPU(i+1) || CPU/Tensor downstream(i)
```

而 CPU 与 Tensor 之间先保持安全串行：

```text
CPU span
→ Tensor macro
→ CPU next span
→ Tensor next macro
```

先把 correctness 跑通。之后再细化成资源级 overlap。

这样下一阶段的验收标准非常明确：

```text
Plan:
G=14
C=15 或更大
T>0
P=0

必须看到：
StageQ enabled

GPU macro = XG
CPU span  = XC
Tensor Attention = XC
Tensor FFN inner = XT

输出与 serial baseline 一致
```

最好先强制一个很简单的：

```text
G=14
C=15
T=49
P=0

XG=128
XC=32
XT=32
```

这里只有 **1 个 CPU-only layer**，最容易定位问题。跑通后再增加到：

```text
C-G = 2, 4, 8...
```

所以现在主线就变成：

**把现有两级 StageQ 泛化成 `GPU → CPU → TENSOR`，先实现 GPU→CPU 的 token-span 拆分和 XC 真正生效。**

这一块完成以后，下一步再做 Phone/XP，会顺很多。
