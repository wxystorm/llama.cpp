对，下一步应该正式做 **“通用 runtime stage 化”**，但我建议不要直接去补 `GPU→CPU`、`CPU→Tensor` 等 6 个特例。你现在的 `GPU→Tensor` Stage Queue 已经是一个很好的原型，下一步应该把它抽象掉。

当前源码已经具备四个独立粒度 `XG/XC/XT/XP`，而且层布局固定为 `GPU → CPU → Tensor → Phone`。  但 runtime 现在仍只接受 `G=C、T>0、P=0` 的 `GPU→Tensor` 特例，并直接令 `runtime_ubatch=XG`。

### 第一步：先修 predictor 的一个语义问题

这个建议在扩 runtime **之前就改**。

当前 Tensor scorer 写的是：

```cpp
const int macro_chunk_tokens =
    plan.gpu_pc_layers > 0 ?
        plan.gpu_chunk_tokens :
        work_tokens;
```

也就是说只要有 GPU，Tensor 的 macro 就继承 `XG`。

对于目前：

```text
GPU → Tensor
```

这是对的。

但是如果以后是：

```text
GPU → CPU → Tensor
XG=128
XC=64
XT=16
```

Tensor 实际收到的上游块应该是：

```text
CPU64 → Tensor macro64
             ↓
          FFN16×4
```

而不是 `Tensor macro128`。

所以先改成：

```cpp
const int cpu_layers =
    plan.pc_layers - plan.gpu_pc_layers;

int tensor_macro_tokens = work_tokens;

if (cpu_layers > 0) {
    tensor_macro_tokens = plan.cpu_chunk_tokens;
} else if (plan.gpu_pc_layers > 0) {
    tensor_macro_tokens = plan.gpu_chunk_tokens;
}
```

即：

```text
Tensor macro = 最近一个有效上游 stage 的 chunk

CPU存在 → XC
否则GPU存在 → XG
否则 → 当前外层 work_tokens
```

这一步很重要，不然后面 runtime 做对了，predictor 和 runtime 又会不一致。

---

## 第二步：把当前 Stage Queue 从 “GPU→Tensor 特例” 抽象成 Active Stage List

现在不要继续写：

```cpp
if (GPU_TO_CPU) ...
if (GPU_TO_TENSOR) ...
if (CPU_TO_TENSOR) ...
```

而是在 `llama-context.cpp` 加一个运行时描述。

例如：

```cpp
enum class llama_hybrid_stage_kind {
    GPU,
    CPU,
    TENSOR,
    PHONE,
};

struct llama_hybrid_runtime_stage {
    llama_hybrid_stage_kind kind;

    int layer_begin;
    int layer_end;

    int macro_tokens;

    // 只有 Tensor 使用
    int inner_chunk_tokens;
};
```

然后根据 plan 自动构造：

```cpp
std::vector<llama_hybrid_runtime_stage>
build_runtime_stages(const llama_hybrid_plan & plan) {
    std::vector<llama_hybrid_runtime_stage> stages;

    const int G = plan.gpu_pc_layers;
    const int C = plan.pc_layers;
    const int T = plan.tensor_layers;
    const int P = plan.phone_layers;

    if (G > 0) {
        stages.push_back({
            GPU,
            0,
            G,
            plan.gpu_chunk_tokens,
            0
        });
    }

    if (C - G > 0) {
        stages.push_back({
            CPU,
            G,
            C,
            plan.cpu_chunk_tokens,
            0
        });
    }

    if (T > 0) {
        stages.push_back({
            TENSOR,
            C,
            C + T,
            0, // 后面从 upstream 推导
            plan.tensor_chunk_tokens
        });
    }

    if (P > 0) {
        stages.push_back({
            PHONE,
            C + T,
            C + T + P,
            plan.phone_chunk_tokens,
            0
        });
    }

    return stages;
}
```

这样：

```text
G=14 CPU=0 T=50 P=0
```

自然是：

```text
GPU → Tensor
```

而：

```text
G=10 C=16 T=40 P=8
```

自动是：

```text
GPU → CPU → Tensor → Phone
```

不需要针对组合写拓扑。

---

## 第三步才是真正的难点：Stage 需要独立 graph token size

这里不能只改现在的 `runtime_ubatch`。

当前 Stage Queue 之所以 `GPU→Tensor` 能比较容易实现，是因为：

```text
GPU macro = XG=128

Tensor macro也还是128
但 Tensor FFN 内部再按 XT=32 切
```

所以整个 graph 的外层 token shape 都是：

```text
128
```

当前 queue 只需要把 GPU split 和后面的 Tensor split 分开执行即可。现在代码已经有 `handoff_q`、`tensor_ready_q` 和两个 scheduler slot。

但假设：

```text
GPU XG=128
CPU XC=64
```

真正数据流应该是：

```text
GPU:
[          128          ]
              ↓

CPU:
[    64    ][    64    ]
```

如果仍然构造一个：

```text
ubatch=128
```

的完整 graph，然后只调用：

```cpp
graph_compute_range(...)
```

那么 CPU graph 里的 tensor shape 仍然是 **128 tokens**。

它不会因为：

```text
XC=64
```

就自动变成两次 64。

因此：

> **从这里开始，不能只靠现在的 scheduler split-range。**

这是下一阶段真正需要解决的问题。

而当前 `llm_graph_params` 里只有 ubatch、scheduler、memory 等参数，没有 layer begin/end 这样的 stage graph 范围。

---

# 所以下一步核心：支持“局部层 graph”

我建议扩：

```cpp
struct llm_graph_params {
    ...

    int32_t hybrid_layer_begin = 0;
    int32_t hybrid_layer_end   = -1;

    bool hybrid_hidden_input = false;
};
```

语义：

```text
layer_begin = 0
layer_end   = G
```

构建：

```text
GPU stage graph
```

然后：

```text
layer_begin = G
layer_end   = C
```

构建：

```text
CPU stage graph
```

再：

```text
layer_begin = C
layer_end   = C+T
```

构建：

```text
Tensor stage graph
```

需要让中间 stage 不再从 token embedding 开始，而是从**前一 stage 的 hidden state**开始。

你当前 `llama-graph.h` 本身已经存在：

```cpp
llm_graph_input_embd_h
```

并且它明确带：

```cpp
ggml_tensor * h;
```

作为 hidden-state 类型输入。

所以方向并不是完全从零开始，但当前它还不是“任意 Transformer 层 stage 输入”，这一块需要接到 Qwen2/LLama/Qwen3 的 graph builder。

---

## 然后加统一 Token Block Queue

数据对象建议直接是：

```cpp
struct hybrid_token_block {
    uint32_t token_begin;
    uint32_t n_tokens;

    ggml_tensor * hidden;
};
```

Stage 完成以后输出：

```text
GPU XG128：

block [0,128)
```

下一个 CPU 要 XC=64：

```text
[0,128)
    ↓ split
[0,64)
[64,128)
```

CPU 如果反过来要 128，而上游给 32：

```text
[0,32)
[32,64)
[64,96)
[96,128)
    ↓ aggregate
[0,128)
```

所以 Boundary Adapter 只干三件事：

```text
A == B → pass

A > B  → split/view

A < B  → accumulate
```

设备传输是另一个维度：

```text
GPU→CPU      CUDA → Host
GPU→Tensor   CUDA → Host/Meta
GPU→Phone    CUDA → Host → RPC

CPU→Tensor   local
CPU→Phone    Host → RPC
Tensor→Phone Host → RPC
```

这样 chunk reshape 和 device transfer 不要混成一个巨大 if/else。

---

# 开发顺序我建议这样

先**不要碰 Phone**。

最稳的顺序是：

```text
现在已有
GPU → Tensor
XG + XT
        ↓
第一步
抽出 Active Stage 描述
现有 GPU→Tensor 行为必须完全不变
        ↓
第二步
实现 Stage-local graph
支持 layer_begin / layer_end / hidden input-output
        ↓
第三步
GPU → CPU → Tensor
开始真正消费 XC
例如：
GPU128 → CPU64×2 → Tensor macro64 + XT16
        ↓
第四步
验证缺 stage
GPU → CPU
CPU → Tensor
只有 GPU
只有 Tensor
        ↓
第五步
接 Phone
Tensor → Phone
CPU → Phone
GPU → Phone
        ↓
最后
完整
GPU → CPU → Tensor → Phone
```

为什么先做 `GPU→CPU→Tensor`？

因为它能同时验证最困难的两个机制：

```text
不同 macro chunk:
XG != XC

以及

Tensor:
macro = XC
inner = XT
```

却还没有 RPC Phone-only stage 的额外复杂性。

比如专门固定一个测试 plan：

```text
G=10
C=16
T=48
P=0

XG=128
XC=64
XT=16
```

目标日志应该能清楚看到：

```text
GPU block:
[0,128)

CPU:
[0,64)
[64,128)

Tensor block0:
macro=64
XT=16
→ 4 chunks

Tensor block1:
macro=64
XT=16
→ 4 chunks
```

这跑通以后，架构基本就成型了。

---

还有一点：**先保留你现在这个 GPU→Tensor Stage Queue，不要直接删。**

它现在正好可以当 regression baseline：

```text
T=50
P=0
C=14
G=14
XG=128
XT=32
```

现有代码会把 `runtime_ubatch` 设成 XG，并通过 handoff/tensor queue 做 GPU 与 Tensor 的交叠。

每次重构以后，都先跑这个 plan：

```text
输出必须一致
速度不能明显倒退
STAGEQ 不得 fallback
```

再测试新拓扑。

**所以现在最先动的文件我会选 `llama-hybrid.cpp` + `llama-graph.h/.cpp`：先修 Tensor macro 的上游继承规则，然后给 graph 增加 stage layer range/hidden-state 边界。等这两个基础做好，再重构 `llama-context.cpp` 的 Stage Queue。**

这比现在直接在 `llama-context.cpp` 里继续堆 `GPU→CPU` 特判稳得多。
