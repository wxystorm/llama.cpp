对，你这个观察是对的。现在的 `allreduce_fallback()` **本身已经天然分成了“传输”和“归约计算”两段**，没必要把整个 AllReduce 推倒重写。

现在 `push_data()` 实际做了两件事：

```cpp
// ① 真正的数据传输
ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    node_src,
    node_tmp);

// ② 只构造 ADD graph，还没有执行
node_red->op = GGML_OP_ADD;
node_red->src[0] = node_dst;
node_red->src[1] = node_tmp;
...
step_cgraphs[j_dst] = cgraph_aux;
```



真正执行：

```text
node_dst = node_dst + node_tmp
```

是在 `push_data()` 外面：

```cpp
ggml_backend_graph_compute_async(
    bcj.backend,
    step_cgraphs[j]);
```



所以从逻辑上已经是：

```text
push_data()
├─ copy partial
└─ prepare ADD graph

外层
└─ execute ADD graph
```

### 因此确实可以直接再拆一刀

我会把它拆成：

```text
prepare + copy
        ↓
reduce compute
```

例如两个函数：

```cpp
push_data(...)
```

保留现在的：

```text
src → dst tmp
```

并构造好 `step_cgraphs`。

再把外面的：

```cpp
for (...) {
    ggml_backend_graph_compute_async(...);
}
```

抽成：

```cpp
compute_reduce(...)
```

于是整个 AllReduce 就可以表达成：

```cpp
reduce_copy(nodes);      // 通信
reduce_compute(nodes);   // ADD
```

对于你现在只有两个 backend 的场景更简单：

```text
CPU partial ───────→ Phone tmp
Phone partial ─────→ CPU tmp
              ↑
          copy 阶段

CPU:   partial += tmp
Phone: partial += tmp
              ↑
          reduce 阶段
```

---

## 对 pipeline 来说，这正好是你需要的边界

原来是：

```text
C0
copy0
ADD0

C1
copy1
ADD1

C2
copy2
ADD2
```

我们想逐渐变成：

```text
Compute: [ C0 ][ C1 ][ C2 ]
Copy:          [ P0 ][ P1 ][ P2 ]
ADD:                [A0] [A1] [A2]
```

所以你当前真正需要独立出来的其实是：

```text
P = push_data 中的 copy
A = 外面的 graph_compute_async
```

不是重新实现 AllReduce。

---

### 不过 `push_data()` 最好还要进一步拆成两部分

因为现在 `push_data()` 同时：

```text
A. 做 copy
B. 创建 node_tmp / node_red / cgraph_aux
```

我更推荐改成：

```cpp
prepare_reduce(...)
copy_reduce(...)
compute_reduce(...)
```

其中：

```cpp
prepare_reduce()
```

只干：

```text
分配 node_tmp
设置 tmp buffer
构造 node_red
构造 cgraph_aux
```

不传数据，不计算。

```cpp
copy_reduce()
```

只干：

```cpp
ggml_backend_tensor_copy_async(...)
```

```cpp
compute_reduce()
```

只干：

```cpp
ggml_backend_graph_compute_async(...)
```

这样以后真正 pipeline 时边界最干净：

```text
C0
↓
prepare R0
↓
copy R0       ← 未来异步
   ╲
    C1
↓
wait copy R0
↓
ADD R0
```

---

## 但是这里还有一个重要限制

**代码结构虽然已经很好拆，但当前 copy 还不是真异步。**

因为你刚确认 CPU / RPC：

```cpp
cpy_tensor_async = NULL
```

所以这里虽然代码写的是：

```cpp
ggml_backend_tensor_copy_async(...)
```

实际上执行：

```cpp
ggml_backend_synchronize(src);
ggml_backend_synchronize(dst);
ggml_backend_tensor_copy(src, dst);
```

因此当前：

```text
copy_reduce()
```

仍然是**同步阻塞通信阶段**。

所以第一阶段完全可以先把：

```text
copy
ADD
```

拆出来，验证：

```text
C0 → P0 → A0 → C1 → P1 → A1...
```

结果继续正确。

但是第二阶段，如果你直接想做到：

```text
        P0
       ↗
C0 → C1
```

就还有问题，因为 P0 里面的 `synchronize(src/dst)` 会碰计算 backend。

---

## 我建议你现在就按这个结构改

先定义一个状态，比如：

```cpp
struct reduce_step {
    std::vector<ggml_cgraph *> cgraphs;
    // 后面还可以放 event / future / thread
};
```

然后原先 `push_data()` 可以先改成：

```cpp
auto prepare_and_copy = [&](const size_t j_src,
                            const size_t j_dst,
                            const size_t i_buf,
                            std::vector<ggml_cgraph *> & step_cgraphs) {
    ...
    ggml_tensor * node_tmp = get_node_aux(node_dst);
    set_tmp_data(node_tmp, j_dst, i_buf);

    // 通信
    ggml_backend_tensor_copy_async(
        bcj_src.backend,
        bcj_dst.backend,
        node_src,
        node_tmp);

    // prepare ADD
    ...
    step_cgraphs[j_dst] = cgraph_aux;
};
```

外面的 ADD 单独抽出来：

```cpp
auto compute_reduce = [&](std::vector<ggml_cgraph *> & step_cgraphs) -> ggml_status {
    for (size_t j = 0; j < n_backends; j++) {
        if (step_cgraphs[j] == nullptr) {
            continue;
        }

        auto & bcj = backend_ctx->backend_configs[j];

        const ggml_status status =
            ggml_backend_graph_compute_async(
                bcj.backend,
                step_cgraphs[j]);

        if (status != GGML_STATUS_SUCCESS) {
            return status;
        }
    }

    return GGML_STATUS_SUCCESS;
};
```

然后当前同步版本先变成：

```cpp
prepare_and_copy(...);
prepare_and_copy(...);

status = compute_reduce(step_cgraphs);
```

数值不应该有任何变化。

---

之后真正做 pipeline 时，再把 `prepare_and_copy()` 中这一句：

```cpp
ggml_backend_tensor_copy_async(...)
```

继续单独拿出去：

```cpp
prepare_reduce(...);
start_copy(...);
finish_copy(...);
compute_reduce(...);
```

最终目标就是：

```text
prepare R0
start_copy R0

C1

finish_copy R0
ADD R0

prepare R1
start_copy R1

C2

finish_copy R1
ADD R1
...
```

所以你的理解没错：**现有 `allreduce_fallback()` 已经给我们拆好了大半，真正应该利用的就是 `push_data()` 与后面 ADD graph compute 这个天然边界。**  

唯一不要混淆的是：`push_data()` 这个名字看起来像“准备发送”，但它现在实际上已经**把同步 CPU↔RPC 传输做完了**；真正做流水时，需要把其中的 copy 再拆成可启动/可等待的通信阶段。
