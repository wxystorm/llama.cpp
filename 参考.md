对，这份才是我们需要的**真正无 CUDA 基线**。而且它把前面的判断补完整了。

这里 24～31 层全部都是 `META_PRIMARY`，没有外部 CUDA/CPU backend：

```text
layer 24~31 → META_PRIMARY
```



更关键的是，正式 prompt 的 layer23→24 边界完全正确。Phone 得到完整 `l_out-23` 后复制到 PC，随后 META 的 layer24 输入仍然逐值相同：

```text
l_out-23:
sum = 1341.509254756
l2  = 2344.879313397
max = 1844.044189453
```



CUDA 那组进入 layer24 的输入也是完全一样的这组数：

```text
CUDA layer24 input:
sum = 1341.509254756
l2  = 2344.879313397
max = 1844.044189453
```



所以现在已经可以确定：

```text
                完全相同的 l_out-23
                       │
              ┌────────┴────────┐
              ▼                 ▼
       META CPU layer24    CUDA layer24
```

这就是我们之前一直想得到的干净 A/B 起点。

而最终 logits 也很有意思：

| token |    无 CUDA |      CUDA |        差值 |
| ----- | --------: | --------: | --------: |
| 13398 | 19.146011 | 19.140953 | -0.005058 |
| 7541  | 29.813015 | 29.931234 | +0.118219 |
| 27262 | 26.339056 | 26.248035 | -0.091021 |

无 CUDA 的前三个生成 token 就是之前正确基线的：

```text
Dist
ributed
 inference
```

  

CUDA 也是同样三个 token，只是 logits 有小幅漂移。  

所以**CUDA 之前那种 1～2.5 的巨大漂移已经消失了**。当前剩余的 `0.005 / 0.12 / 0.09` 已经是另一个量级。

但现在还差最后一个点，暂时不要拿当前无 CUDA 日志里的这条去和 CUDA 比：

```text
[EXT_OUTPUT] layer=24 backend=Meta(CPU,RPC0)
l2=759.332...
```

因为这条打印得**太晚了**。

日志实际顺序是：

```text
sg80  layer24 attention
sg81  layer24 FFN
sg82  first=l_out-24 ... layer25 attention
sg83
sg84
...
sg94
sg95
sg96  result_norm
META graph 全部结束

然后才：

[EXT_OUTPUT] layer=24 ...
```



所以到 `[EXT_OUTPUT]` 被读取时，**整个 META 的 24～31 层都已经执行完了**。`l_out-24` 对应的 allocator storage 很可能早已被后续 tensor 复用。

这也解释了为什么无 CUDA 这里看起来：

```text
l_out-23 L2 = 2344.88
l_out-24 L2 = 759.33
```

与 CUDA 的：

```text
l_out-24 L2 = 2194.35
```

差得离谱——目前这两个数字**不是同一执行时刻的 tensor**，不能比较。

### 现在只加最后一个调试点

在 META 的 PC-only 分支里：

```cpp
compute_workers.start(0, i);
compute_status = compute_workers.wait(0);
```

**紧跟 `wait(0)` 后面**加：

```cpp
if (pipeline_debug) {
    auto * g =
        backend_ctx->backend_configs[0]
            .cgraphs[i].cgraph_main;

    if (g != nullptr &&
        g->n_nodes > 0 &&
        std::strcmp(
            g->nodes[0]->name,
            "l_out-24") == 0) {

        meta_debug_tensor(
            backend_ctx->backend_configs[0].backend,
            g->nodes[0],
            "CPU L24 IMMEDIATE");
    }
}
```

按这份日志，它应该正好命中：

```text
sg=82 first=l_out-24 last=attn_out-25
```



于是我们就能得到真正可以比较的：

```text
无 CUDA:
[CPU L24 IMMEDIATE] l_out-24 = ???

CUDA:
[NUMDBG] layer24 external output
l_out-24
sum=891.471106987
l2=2194.354753307
max=1841.358032227
```



**只要这个 `CPU L24 IMMEDIATE` 出来，就能最终判断 CUDA layer24 的数值误差到底有多大。**

另外，文件最开始那几行 `l_out-23 = 0` 也不用管，那是在正式 META graph 计算之前调试代码提前读取了尚未产生的数据；正式执行到 layer23 handoff 后的数据是正确的。

所以现在状态可以概括为：

```text
Phone→PC handoff：已确认正确
META→CUDA input：已确认正确
生成 token：CPU/CUDA 一致
巨大 logits 漂移：已修复
剩余任务：只比较“刚算完”的 CPU/CUDA l_out-24
```

这应该就是 CUDA 正确性排查的最后一步了。
