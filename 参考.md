那实际上再ADD这里弄就行，电脑算完了，然后传出去，此时要ADD，这里去看手机算完没有，没有则可以去算下一个chunk吧。如果算完了则手机端归约并发送电脑端，之后电脑端归约？我再说的详细一些，现在有三个chunk，一层有两个计算图，一个attn，一个ffn（简称），两边开始算第一个chunk，此时电脑算的快，不用等，执行两个，发送算完的到手机，同时继续算第二个chunk。如果手机端算完，发送算完的并进行归约。不过这里有一点，手机发送过来，应该是继续第二个chunk计算，还是对第一个进行归约？应该是继续第二个chunk，然后算完再归约（归约后第一个chunk其实已经可以进入ffn计算了，但是现在由于计算弄了一个主线程，可以先等着，当然这个等不会造成一些浪费，因为实际上计算线程仍会进行计算）。不过我觉得可以搞一个任务队列？两边都有，初始有三个任务，计算chunk1,2,3。两边开始拿第一个任务，电脑发送chunk同时发送任务归约，此时对于这个任务，调度安排把他放在手机端最前面，这样的话手机算完后也发送数据和任务归约，同时取出这个任务执行。电脑执行完当前的计算就会取出最开始的执行归约。
可能有一些杂，大概是这样




你这个思路已经很接近一个真正的 **DAG / task-based pipeline scheduler** 了。核心点我赞同，但我会稍微改一下优先级逻辑：

> **传输一完成，不代表立刻打断当前计算去做归约；当前计算继续跑。等这个计算资源空下来以后，再从“已经 ready 的任务”里挑优先级最高的。**

而“旧 chunk 的归约”通常应该比“继续算更后面的 chunk”优先，因为它会解锁后续 FFN/下一 stage。

可以把每个 chunk 拆成这种依赖。

```text
以 chunk 0 的 attn 为例：

PC compute attn0
      |
      +------> PC -> Phone transfer0 ----+
                                         |
                                         v
                                    Phone ADD0
                                         |
                                         v
                                    Phone FFN0

Phone compute attn0
      |
      +------> Phone -> PC transfer0 ----+
                                         |
                                         v
                                      PC ADD0
                                         |
                                         v
                                      PC FFN0
```

也就是说，**两边的 ADD 是分别 ready 的**：

```text
PC ADD0 ready 条件：
    PC compute0 done
    &&
    Phone -> PC transfer0 done


Phone ADD0 ready 条件：
    Phone compute0 done
    &&
    PC -> Phone transfer0 done
```

不需要再搞一个“双方都必须一起到 barrier 才做归约”的大同步。

---

你举的场景可以这样跑。假设 PC 比手机快很多。

```text
时间 ---------------------------------------------------------->

PC compute engine:
ATTN C0
██████
      ATTN C1
      ██████
            ?

Phone compute engine:
ATTN C0
██████████████
              ?

PC -> Phone:
      C0 ███████

Phone -> PC:
              C0 ███████
```

PC 算完 C0：

```text
PC C0 done
```

立刻做两件事：

```text
1. enqueue/send PC->Phone(C0)
2. PC compute engine 不等，继续算 C1
```

这一点完全对。

Phone 此时还在算 C0，因此：

```text
PC -> Phone 的数据
```

甚至可以已经放进 Phone 的 tmp buffer。

然后 Phone 的 `ATTN C0` 终于完成。

这时候 Phone 可以立刻：

```text
enqueue Phone->PC(C0)
```

而此时 Phone 自己已经具备：

```text
Phone local C0 result        ✅
PC -> Phone C0 tmp           ✅
```

所以：

```text
Phone ADD C0
```

也变成 ready。

现在就到了你问的关键问题：

> 手机这时候到底应该算 C1，还是先 ADD C0？

我的建议不是“永远固定一个”，而是：

> **如果 C1 已经在执行，就不要为了 ADD0 去抢占它；如果当前 compute engine 正好空闲，那么优先 ADD0。**

也就是非抢占式：

```text
正在执行的任务
    ↓
让它跑完

跑完以后
    ↓
ready queue 重新调度
```

例如：

```text
Phone:

ATTN C0
██████████

C0结束时：

ready:
    ADD C0       ← 老 chunk，能解锁 FFN0
    ATTN C1      ← 新 chunk

优先选：
    ADD C0
```

原因很简单：

```text
ADD C0
 ↓
FFN C0
 ↓
这个 chunk 向前推进
```

而如果一直：

```text
ATTN C1
ATTN C2
ADD C0
```

就会变成：

```text
chunk0 明明数据都齐了
但是因为一直追求算新 chunk
导致 chunk0 后面的 FFN 一直无法开始
```

这实际上是在制造 pipeline bubble。

---

所以你说的“任务队列”非常适合这里，但我会设计成 **ready queue，而不是简单 FIFO queue**。

比如 Phone 侧一开始：

```text
Compute-ready queue:

ATTN C0
ATTN C1
ATTN C2
```

取：

```text
ATTN C0
```

PC C0 的数据传到以后，不代表 ADD0 ready，因为 Phone 自己 C0 还没算完。

Phone C0 算完后：

```text
C0 local compute done
+
PC->Phone C0 done
```

于是动态插入：

```text
ADD C0
```

此时队列不要变成：

```text
ATTN C1
ATTN C2
ADD C0
```

而应该按 priority：

```text
ADD C0       priority high
ATTN C1      priority normal
ATTN C2      priority normal
```

于是下一个取：

```text
ADD C0
```

ADD0 完成以后，又解锁：

```text
FFN C0
```

然后队列可能变成：

```text
FFN C0       high
ATTN C1      normal
ATTN C2      normal
```

这就开始变成真正的 dataflow scheduler 了。

---

不过这里有一个很重要的问题：**FFN C0 和 ATTN C1 谁优先？**

这个不能简单说 FFN 永远优先。

因为你实际上是在两个维度做 pipeline：

```text
chunk pipeline:
C0 -> C1 -> C2

layer/stage pipeline:
ATTN -> REDUCE -> FFN -> REDUCE -> ...
```

所以整个 DAG 大概是：

```text
             ATTN C0
                |
              RED C0
                |
              FFN C0
                |
              RED C0
                |
             next layer C0


             ATTN C1
                |
              RED C1
                |
              FFN C1
                |
               ...


             ATTN C2
                |
               ...
```

调度器要决定：

```text
纵向推进 C0
还是
横向展开 C1/C2
```

这就是典型的 pipeline scheduling 问题。

---

对于你目前只有 **3 chunks** 的原型，我建议不用上很复杂的算法，先采用一个简单规则：

```text
Priority 1:
已经 ready 的旧 chunk reduction / ADD

Priority 2:
已经 ready 的旧 chunk 后继 compute
例如 FFN C0

Priority 3:
新的 chunk compute
例如 ATTN C1 / C2
```

并且：

```text
不抢占当前正在执行的 compute
```

也就是“oldest-ready-first”。

例如 Phone：

```text
ATTN C0
████████████

结束后 ready:
ADD C0
ATTN C1

→ ADD C0

ADD C0
██

结束后 ready:
FFN C0
ATTN C1

→ FFN C0
```

这种调度比较偏 latency/critical-path。

如果后来发现 GPU/手机利用率下降，再考虑让：

```text
ATTN C1
```

插到 FFN C0 前面，增加 breadth。

---

但还有一点：**transfer 不应该放进同一个 compute task queue。**

你现在已经有双连接了，非常适合自然拆成资源队列：

```text
                    Scheduler
                        |
        +---------------+----------------+
        |               |                |
        v               v                v
   PC compute       Phone compute      Transfer
     queue              queue            queue
        |               |                |
    local CPU        RPC compute       RPC GET/SET
```

因为：

```text
Phone compute
```

和：

```text
Phone transfer
```

现在就是你专门做双 socket 后希望并行的两个资源。

千万别又做成：

```text
一个 Phone task queue:
compute
transfer
compute
transfer
```

那等于从 scheduler 层重新把它串行化。

所以任务本身可以是：

```cpp
enum class TaskType {
    COMPUTE,
    TRANSFER,
    REDUCE_ADD,
};
```

但它们要绑定不同 resource：

```text
COMPUTE PC      -> PC compute resource
COMPUTE PHONE   -> Phone compute resource

TRANSFER P->F   -> transfer resource
TRANSFER F->P   -> transfer resource

ADD PC          -> PC compute resource
ADD PHONE       -> Phone compute resource
```

注意：

> `ADD` 虽然逻辑上叫 reduction，但在你的实现里它最终也是一个 graph compute。

所以 ADD 和普通 compute 会争同一个 backend compute resource。

这就是为什么你问：

> “手机应该继续第二个 chunk，还是先归约第一个？”

这个问题确实存在。

它们不能同时在同一个 Phone compute engine 上跑的话，scheduler 必须二选一。

---

我会把每个 task 定义成这样：

```cpp
struct Task {
    TaskType type;

    int chunk;
    int stage;
    int backend;

    int priority;

    std::vector<Event *> deps;
};
```

例如：

```text
phone_attn_c0:
deps = []

pc_to_phone_c0:
deps = [pc_attn_c0_done]

phone_to_pc_c0:
deps = [phone_attn_c0_done]

phone_add_c0:
deps = [
    phone_attn_c0_done,
    pc_to_phone_c0_done
]

pc_add_c0:
deps = [
    pc_attn_c0_done,
    phone_to_pc_c0_done
]

phone_ffn_c0:
deps = [phone_add_c0_done]

pc_ffn_c0:
deps = [pc_add_c0_done]
```

某个 dependency 完成，就检查：

```text
task 所有 deps 都满足了吗？
```

满足：

```text
→ push ready queue
```

这比“代码里人工写一串 wait”会干净很多。

---

而且这样一来，你之前纠结的 ACK 问题也自然解决了。

现在：

```text
GRAPH_COMPUTE
↓
等 ACK
↓
主线程才能继续
```

未来应该变成：

```text
GRAPH_COMPUTE task submit
↓
Phone compute queue 执行
↓
完成
↓
产生 event:
phone_compute_c0_done
```

然后：

```text
Phone->PC transfer task
```

依赖：

```text
phone_compute_c0_done
```

它自己会在 ready 后执行。

主线程不需要在这里：

```cpp
wait();
```

于是：

> **同步没有消失，只是从“阻塞主线程”变成了“dependency 决定 task 什么时候 ready”。**

这是你现在这个想法里最有价值的一步。

---

所以如果把你刚才那一大段压缩成一个更准确的架构，我认为你想表达的其实是：

```text
不是：

for chunk:
    compute both
    wait both
    transfer both
    wait both
    add both


而是：

建立 task DAG

PC / Phone / Transfer
各自不停取当前 ready 的任务

谁先算完：
    谁先触发自己的 transfer

某个 ADD 的两个依赖满足：
    ADD 就进入 ready queue

旧 chunk 的 ADD / downstream compute
    通常优先于继续无限展开新 chunk
```

**这个方向我认为比“在 ADD 那里简单加一个 wait”更好。**

不过我建议你第一版先别直接做一个特别通用的线程池 scheduler。你的场景目前只有：

```text
3 chunks
2 backends
2 directions transfer
ATTN / FFN
```

完全可以先做一个非常小的：

```text
per-backend ready deque
+
event/dependency counter
```

每个 task 一个：

```cpp
std::atomic<int> pending_deps;
```

dependency 完成：

```cpp
if (--task.pending_deps == 0) {
    ready_queue.push(task);
}
```

就够验证这个 scheduling 思路了。

你现在实际上已经从“怎么让两个 socket 并发”走到了更核心的问题：**如何把整个 chunk allreduce 从 barrier-based pipeline 改成 dependency-driven pipeline。**这个方向是对的。
