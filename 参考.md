这个 trace 很有价值。**你刚才说的“两个 compute 队列 + transfer + ready task”的调度思路已经基本跑出来了**，而且从时间线上能清楚看到。不过我先指出一个比性能更重要的问题：**现在 `ADD` 的依赖可能少了一条，存在潜在 correctness race。**

先看调度成功的部分。PC 和手机现在确实已经独立计算：

| Task       |         开始 |         结束 |       时间 |
| ---------- | ---------: | ---------: | -------: |
| PC C0      |  50.905 ms |  64.142 ms | 13.24 ms |
| Phone C0   |  50.941 ms |  88.013 ms | 37.07 ms |
| PC C1      |  64.183 ms |  76.996 ms | 12.81 ms |
| PC C2      |  77.010 ms |  89.745 ms | 12.74 ms |
| Phone C1   |  88.028 ms | 145.708 ms | 57.68 ms |
| Phone ADD0 | 145.727 ms | 176.451 ms | 30.72 ms |
| Phone C2   | 176.463 ms | 231.797 ms | 55.33 ms |

这段特别漂亮：

```text
Phone:

C0 compute
50.9 ───────────── 88.0

C1 compute
                  88.0 ───────────────── 145.7

ADD0
                                             145.7 ───── 176.4

C2 compute
                                                           176.4 ────── 231.8
```

注意：

```text
C0 PC->Phone transfer end = 115.145
=> ADD0 ready              = 115.146

但是 Phone C1 正在算：
88.028 ---------------- 145.708

所以 ADD0 没有抢占 C1。

C1结束
↓
ADD0优先于C2
↓
145.727开始ADD0
↓
ADD0结束后再C2
```

**这几乎就是我们上一条讨论的调度策略：**

```text
正在执行的 compute 不抢占
        ↓
执行完
        ↓
从 ready queue 重新选
        ↓
旧 chunk ADD 优先于新 chunk compute
```

所以这一部分我认为是对的。

---

但是现在有一个很关键的 correctness 问题。

看 chunk0：

```text
Phone -> PC transfer C0:

115.180  src_tensor_get_begin
        |
        |██████████████████████████████████
        |
282.766  src_tensor_get_end
```

与此同时：

```text
Phone ADD0:

145.727 add_begin
        |██████
176.451 add_end
```

也就是：

```text
Phone -> PC GET C0
115 ───────────────────────────────────── 282
                ↑
                │
Phone ADD C0    145 ───── 176
```

**它们重叠了。**

这个地方我要修正我们前面简化过的一点：对于你现在这种 in-place ADD，实现上 Phone 的：

```text
node_phone
```

既是：

```text
Phone -> PC transfer
```

的 source，又是：

```text
Phone ADD:
node_phone += tmp_from_pc
```

的 destination。

也就是说：

```text
                    node_phone
                    /        \
                   /          \
      Phone->PC GET            ADD
           读                  写
```

所以理论上不能简单地说：

```text
Phone local compute done
+
PC->Phone transfer done

=> Phone ADD ready
```

实际上当前实现如果没有额外 snapshot buffer，应该至少是：

```text
Phone local compute done
+
PC -> Phone transfer done
+
Phone -> PC transfer done
=============================
Phone ADD ready
```

因为你必须确保 Phone 原始 partial result 已经被发给 PC，才能修改它。

否则可能发生：

```text
原来 Phone tensor = B
PC tensor = A

目标：
Phone = A+B
PC    = A+B
```

如果 Phone 先：

```text
B <- B + A
```

而 Phone→PC 的 GET 此时还没真正读取完 B，那么 PC 收到的可能已经是：

```text
A+B
```

之后 PC 再：

```text
A += (A+B)
```

就成：

```text
2A+B
```

这显然不对。

当然，你现在 RPC `GET_TENSOR` 的 server 很可能在很早的时候就已经把 Phone tensor snapshot 到一个 response buffer 里，然后后面的 100 多 ms 只是网络发送/PC recv，所以**实际运行可能恰好没错**。

之前你测到的 server backend_get 本身只有几十/几百 µs，就是这个迹象。

可能真实发生的是：

```text
115.180 GET request

Phone server:
115.xxx 把 B 拷贝进 response buffer
         ↓
       snapshot完成

145.727 ADD把原tensor改成A+B

网络：
115 --------------------------282
慢慢把之前snapshot的B发给PC
```

那这种实现下数据恰好安全。

但这属于依赖 RPC 实现细节。

从 `ggml_backend_tensor_get()` 抽象来看，在：

```text
src_tensor_get_end
```

以前就修改 source tensor，是比较危险的。

所以这个问题建议先确认，再做性能优化。

---

然后再看性能，现在最大的瓶颈已经非常明显：

> **compute queue 很好，但 transfer queue 堵得非常厉害。**

你现在 transfer 实际是：

```text
C0 0->1
64.9 ───── 115.1

C0 1->0
             115.2 ───────────────────────── 282.8

C1 0->1
                                                   282.8 ───── 323.1

C1 1->0
                                                                323.1 ───────────── 457.7

C2 0->1
                                                                                     457.8 ─── 500.1

C2 1->0
                                                                                                500.1 ───────── 641.4
```

也就是几乎严格：

```text
C0 upload
↓
C0 download
↓
C1 upload
↓
C1 download
↓
C2 upload
↓
C2 download
```

**一个 transfer worker / transfer socket 把六个通信任务完全串起来了。**

这导致非常夸张的 queue waiting。

例如：

```text
C1 PC->Phone ready:
77.000 ms

真正 transfer_begin:
282.803 ms
```

光排队就：

```text
205.8 ms
```

C2 更夸张：

```text
ready:
89.748 ms

begin:
457.763 ms
```

排队：

```text
368 ms
```

所以现在不是：

```text
计算不够异步
```

了。

现在是：

```text
Compute queue:
非常积极地产生结果
████████████████

Transfer queue:
处理不过来
████████████████████████████████████████████████████

结果：
ready tasks
|||||||||||||||||||||
大量堆积
```

---

还有一个非常有意思的变化。

你之前 `0->1 SET` 是：

```text
~1 ms
```

现在：

```text
C0: 64.927 -> 115.139 ≈ 50.2 ms
C1: 282.817 -> 323.066 ≈ 40.2 ms
C2: 457.767 -> 500.102 ≈ 42.3 ms
```

**突然变成 40~50 ms。**

这个也值得查。

因为现在真正实现并发后：

```text
Phone compute
||
PC -> Phone SET
```

很可能在手机端争某个资源：

```text
backend
memory bandwidth
server mutex
RPC handler lock
```

比如 C0：

```text
Phone compute C0:
50.9 ------------ 88.0

PC->Phone SET:
64.9 ------------------------115.1
```

两者明显 overlap。

而以前没有真正 overlap 时 SET 只需要约 1ms。

所以一个很值得做的对比实验是：

```text
SET alone                ≈ ?
SET || Phone compute     ≈ ?
```

如果：

```text
alone       ~1 ms
concurrent  ~40 ms
```

那说明 server/device 层还有非常明显的资源 contention。

---

不过从 scheduler 本身看，你这次已经取得了一个非常重要的结果：

```text
PC:
C0 -> C1 -> C2
完全不等Phone

Phone:
C0 -> C1 -> ADD0 -> C2
根据ready task动态选择

Transfer:
compute一结束就产生transfer_ready
```

这已经不是之前那个：

```cpp
for chunk:
    compute both
    wait
    transfer both
    wait
    add
```

的 barrier pipeline 了。

它已经开始变成我们说的：

```text
dependency-driven scheduler
```

了。

而且这个 log：

```text
t=115146 chunk=0 phase=add_ready 1
...
t=145708 chunk=1 phase=compute_end 1
t=145727 chunk=0 phase=add_begin 1
...
t=176451 chunk=0 phase=add_end 1
t=176463 chunk=2 phase=compute_begin 1
```

我觉得是整份 trace 里最漂亮的一段。

它明确证明了：

```text
ADD0 ready
    ↓
Phone 正忙 C1
    ↓
不抢占
    ↓
C1完成
    ↓
ADD0优先
    ↓
然后才C2
```

**你的任务队列调度思想实际已经生效了。**

---

所以我现在不会急着继续扩大 scheduler，而是先处理两个问题：

**第一优先级是 correctness：**

确认：

```text
ADD backend1
```

是否允许在：

```text
backend1 -> backend0 GET
```

尚未完成时修改同一个 source tensor。

如果 RPC GET 有明确的 server-side snapshot 语义，可以进一步利用；否则 ADD dependency 必须加上 outgoing-transfer completion，或者单独做 outbound snapshot buffer。

**第二优先级才是性能：**

现在单 transfer queue 已经成为绝对瓶颈。下一步很自然会考虑：

```text
compute channel

upload channel:   PC -> Phone
download channel: Phone -> PC
```

即从现在：

```text
compute + transfer
```

两条 connection，进一步试验：

```text
compute + upload + download
```

三条 connection，让：

```text
PC->Phone C0
        ||
Phone->PC C0
```

在两个 source 都 ready 后真正独立进行。

但我建议**先别马上做第三条 socket**。先把上面的 ADD/source race 搞清楚。因为一旦 upload/download 也完全并发，这个 race 会更容易暴露。

目前这版从调度角度看已经相当成功；真正突出出来的问题已经从“compute 被 RPC transfer 卡住”变成了 **transfer queue backlog + reduction 的精确 dependency**。
