抓到了，这个基本已经是**实锤根因**了，而且比我上一条预想的还更明显。

你的日志：

```text
[RPC_SNAPSHOT_MISMATCH]
slot=1
req_seq=1
cur_seq=9223372036854775975
req_size=40960
cur_size=655360
state=0
```

其中这个数非常关键：

```text
9223372036854775975
-9223372036854775808   // 2^63
--------------------
167
```

也就是：

```text
cur_seq = 0x80000000000000A7
        = (1ULL << 63) + 167
```

这**绝对不像随机内存垃圾**。低 63 位正好是 `167`，最高位被置 1。也就是说手机端 slot 里残留的是一个带“高位 namespace/epoch 标记”的旧 snapshot：

```text
slot 1:
seq   = HIGH_BIT | 167
size  = 655360
state = FREE
```

而新的 runtime 从：

```text
req_seq = 1
```

重新开始了。

---

现在 crash 链条已经完全闭环了。

服务端 `send_snapshot()` 当前等待条件是：

```cpp
slot.cv.wait(lock, [&]() {
    return
        (slot.seq == request.seq &&
         slot.state == rpc_snapshot_state::READY)
        ||
        slot.seq > request.seq;
});
```

随后立即检查：

```cpp
if (slot.seq != request.seq ||
    slot.state != rpc_snapshot_state::READY ||
    slot.data.size() != request.size) {
    return false;
}
```



于是你现在实际发生的是：

```text
request:
seq = 1

slot:
seq = 0x80000000000000A7
```

判断：

```cpp
slot.seq > request.seq
```

当然成立：

```text
9223372036854775975 > 1
```

所以 `wait()` **根本没有等待新的 seq=1 snapshot 被生产出来**，直接醒了。

接下来：

```cpp
slot.seq != request.seq
```

成立，于是：

```cpp
return false;
```

snapshot socket 被服务端结束。

PC 端已经发完 `GET_SNAPSHOT`，随后正好在：

```cpp
status = snapshot_sock->recv_data(data, size);
RPC_STATUS_ASSERT(status);
```

这里收不到数据，于是得到你看到的：

```text
ggml-rpc.cpp:1064:
Remote RPC server crashed or returned malformed response
```



所以这次已经可以明确说：

> **不是 StageQ 崩，不是 graph node 数量，也不是 GPU→TS 本身。是 snapshot protocol 错误地假设 `seq` 在所有 context / session 之间永远单调递增。**

---

更重要的是，服务端其实完全有能力等新的 `seq=1`。

snapshot producer 当前是：

```cpp
rpc_snapshot_slot & slot = ...;

{
    std::unique_lock<std::mutex> lock(slot.mutex);

    slot.cv.wait(lock, [&]() {
        return slot.state == rpc_snapshot_state::FREE;
    });

    slot.state = rpc_snapshot_state::FILLING;
    slot.seq   = seq;
    slot.data.resize(size);
}
```



你当前 mismatch：

```text
state=0
```

而 slot 默认状态就是：

```cpp
rpc_snapshot_state::FREE
```



所以如果 GET 线程不要因为：

```cpp
slot.seq > request.seq
```

错误提前退出，那么新的 producer 完全可以：

```text
旧：
seq = HIGH_BIT|167
state = FREE

↓ 新 snapshot producer

seq = 1
state = FILLING

↓ tensor_get

seq = 1
state = READY

↓ GET线程醒来

正确返回40960 bytes
```

---

## 我建议现在先做最小正确性修复

把：

```cpp
slot.cv.wait(lock, [&]() {
    return (slot.seq == request.seq &&
            slot.state == rpc_snapshot_state::READY) ||
           slot.seq > request.seq;
});
```

先改成：

```cpp
slot.cv.wait(lock, [&]() {
    return slot.seq == request.seq &&
           slot.state == rpc_snapshot_state::READY;
});
```

也就是**先删除这个：**

```cpp
|| slot.seq > request.seq
```

然后仍然保留：

```cpp
if (slot.seq != request.seq ||
    slot.state != rpc_snapshot_state::READY ||
    slot.data.size() != request.size) {
    ...
    return false;
}
```

这样这次：

```text
cur_seq=HIGH_BIT|167
req_seq=1
state=FREE
```

GET 线程不会错误退出，而是继续等新的 producer 将这个 FREE slot 重写成 `seq=1`。

这非常适合作为当前的验证补丁。

---

但是最终版我不建议只停在这里。

因为原来的：

```cpp
slot.seq > request.seq
```

显然是为了防这种情况：

```text
GET请求太晚
req_seq=100

slot已经变成
seq=102
```

也就是检测 stale request。

所以直接删掉以后，如果真的发生“请求 100 永远不会再生产”，GET 会一直等待。

最终正确设计应该是：

```text
session_id + seq
```

而不是只用：

```text
seq
```

例如：

```cpp
struct rpc_snapshot_id {
    uint64_t session;
    uint64_t seq;
};
```

slot：

```cpp
uint64_t session = 0;
uint64_t seq     = 0;
```

request 也带：

```cpp
request.session
request.seq
```

判断才能变成：

```cpp
if (slot.session == request.session) {
    // 同一session里，seq才允许比较大小
}
```

不同 session：

```text
旧：
session=7 seq=167

新：
session=8 seq=1
```

此时：

```text
167 > 1
```

没有任何意义，不能用于 stale 判定。

---

而你当前 Meta backend 的确是每个 context 自己：

```cpp
uint64_t next_snapshot_seq = 1;
```



使用时：

```cpp
const uint64_t snapshot_seq =
    use_snapshot_pipeline ?
        backend_ctx->next_snapshot_seq++ :
        0;

const uint32_t snapshot_slot = snapshot_seq & 1;
```



这已经证明：

```text
seq
```

根本就不是 server-global generation。

所以 server 这句：

```cpp
slot.seq > request.seq
```

从协议语义上就是不成立的。

---

还有你这个：

```text
0x80000000000000A7
```

我建议特别关注一下。

因为当前我能看到的这些版本里，Meta 的普通 `next_snapshot_seq` 就是从 1 开始递增，没有看到设置最高位的逻辑。也就是说这个高位很可能来自：

```text
另一条 profiling/session 路径
或者你当前手机端更新后的某段代码
或者上一次 PC/client 会话留下的 tagged seq
```

但**无论最高位是谁加的，都不影响我们现在的结论**：

```text
旧 namespace 的 seq
和
新 namespace 的 seq
```

不能直接用整数大小比较。

而且：

```text
cur_size=655360
req_size=40960
```

也进一步证明 slot 里面就是**另一笔旧 snapshot 数据**，不是当前请求对应的数据。

---

所以我现在建议：

先只改：

```cpp
|| slot.seq > request.seq
```

这一处，删掉它。

然后保持你刚才会稳定复现的参数：

```text
T=50
P=0
C=14
G=14
R=0.639
XG=128
XT=32

LLAMA_HYBRID_STAGE_QUEUE=0
```

重新跑。

如果这次：

```text
RPC malformed
```

消失，并且模型正常加载，那么这条 bug 就彻底确认了。

之后再做最终版的：

```text
snapshot session/epoch
```

而不是靠全局 atomic 勉强补，因为**全局 atomic 只解决同一个 PC 进程内多个 Meta context；PC 重启而手机 RPC server 不重启时，它还是会从 1 开始。**

这次你这个 `0x80000000000000A7` 基本把问题钉死了。
