可以，**现在已经可以进入“真正接入 llama-cli / 正常模型加载流程”这一阶段了**。

我看了你这次的最新代码和 Qwen2.5-32B 的结果。先说明一下：GitHub 的 branch 页面能访问，但我这里抓到的 raw 缓存大约落后两天；你这次上传的 `llama-hybrid` / `llama-model` 明显更新，所以代码判断我以你上传的最新版为准。([GitHub][1])

这次已经完成了几个很关键的闭环。Qwen2.5-32B 的 64 层模型能够正常解析 FFN/Attention、逐层权重分类以及 CPU/Phone/GPU 内存，说明 profiler 已经不只是 Llama2 smoke test。 

Phase 4A/4B 也完整跑通了：

```text
413985 candidates
→ 104644 feasible
→ score
→ rank
→ BEST PLAN

T=1
P=19
C=44
R=0.75
G=13
K=4

predicted = 6566.638 ms
```

并且 best plan 已经成功 `runtime_plan_set()`、再读回来 verify。 

更重要的是，**模型加载端已经能吃这个 plan 了**。最新版 `llama-model.cpp` 会读取 runtime plan，然后实际设置 `tensor_split` 的 R、PHONE_ONLY/PC_ONLY 数量以及前 G 个 PC_ONLY CUDA layer。 

所以现在缺的已经不是 planner，而是把：

```text
test-hybrid
    ↓
profile
    ↓
plan
    ↓
runtime_plan_set
```

搬进：

```text
llama-cli
    ↓
normal model loading
```

---

### 我建议真正接入按这个顺序做

* 在正常 `llama_model_load()` 中，`llama_model_loader ml` 创建完成、`llama_prepare_model_devices()` 完成之后，**但在 `model->load_hparams(ml)` 之前**运行自动 planner。当前正常加载顺序正是 `loader → create model → prepare devices → load_hparams → load_tensors`。
* planner 选出 best plan 后立即调用 `llama_hybrid_runtime_plan_set(best)`。这样随后进入你现在的 `load_hparams()` 时，就会走 `source=planner`，再由 `load_tensors()` 根据它真正分配权重。你的 `load_tensors()` 已经使用这些 hybrid placement 信息。
* 真正加载时必须把实际 `-c / n_ctx` 传给 planner，不能继续测试里固定 `512`。否则自动方案可能针对错误的 KV/context cost。
* 把 `K` 最后接通。现在 `runtime_plan_set()` 保存了 `K`，但它显式做的 runtime side-effect 只有开启/关闭 `GGML_META_PREFILL_DUAL_RETURN`。 而当前 `llama-model.cpp` 已经明确消费了 R/P/C/G，却没有消费 `tensor_chunks_per_ubatch`。 所以实际 Llama/Qwen2/Qwen3 的 prefill chunk getter 也必须优先读取 planner 的 K。

我建议给 K 加一个非常简单的公共接口：

```cpp
int llama_hybrid_runtime_prefill_chunks() {
    std::lock_guard<std::mutex> lock(
        g_llama_hybrid_runtime_plan_mutex);

    if (!g_llama_hybrid_runtime_plan) {
        return 0;
    }

    return g_llama_hybrid_runtime_plan
        ->tensor_chunks_per_ubatch;
}
```

然后 Llama/Qwen2/Qwen3：

```cpp
static int qwen2_prefill_ffn_chunk_count() {
    const int planned =
        llama_hybrid_runtime_prefill_chunks();

    if (planned > 0) {
        return planned;
    }

    // 原来的环境变量 fallback
    const char * value =
        std::getenv("LLAMA_PREFILL_CHUNKS");

    return value == nullptr
        ? 0
        : std::max(1, std::atoi(value));
}
```

这样才是：

```text
BEST PLAN
T P C R G K
│ │ │ │ │ │
│ │ │ │ │ └→ prefill chunk
│ │ │ │ └──→ CUDA PC layers
│ │ │ └────→ tensor ratio
│ │ └──────→ PC_ONLY
│ └────────→ PHONE_ONLY
└──────────→ TENSOR_SPLIT
```

六个变量全部真正落到 runtime。

还有一点：你刚才提到不想暴力搜索，而当前最新版 **仍然是在数学模型上暴力枚举** `T/P/R/G/K`，代码里就是五层循环。 这次 64 层产生了 413,985 个 candidate。这个**不需要阻止真正接入**——它不是实跑 41 万次，只是几十万次公式计算；相比你前面的 RPC/Phone profiler 时间几乎可以忽略。等 runtime 自动规划完全跑通之后，我们再把内部搜索器替换成前面说的 `R 求交点 + G 最大可行 + T/P/K 边界搜索`，接口完全不用改。

所以我现在会把状态定成：

```text
Profiler             ✅
Memory model         ✅
Phase 4A feasibility ✅
Phase 4B cost model  ✅
Best-plan selection  ✅
Plan publication     ✅
Loader R/P/C/G       ✅

真正接入剩余：
normal load 自动调用 planner
actual n_ctx 传入
K 接入 graph builder
runtime placement 验证
```

**可以直接开始真正接入。** 下一步最合适的就是改 `src/llama.cpp` 的 `llama_model_load()`：把现在 test-hybrid 里的整套 `profile → enumerate → score → best → publish` 封装成一个 `llama_hybrid_autoplan(...)`，插到 `llama_prepare_model_devices()` 与 `model->load_hparams()` 之间。这样下一次测试就不再运行 `test-hybrid`，而是直接启动 `llama-cli`，看它能不能自己输出 `[HYBRID_AUTO] BEST`，随后 `[HYBRID_SPLIT] source=planner` 并按该方案真正加载 32B。

[1]: https://github.com/wxystorm/llama.cpp/tree/later-split "GitHub - wxystorm/llama.cpp at later-split · GitHub"

可以。现在不是继续扩 profiler，而是把 `test-hybrid` 里的“测试主流程”抽出来，接到正常 `llama-cli` 的加载链路里。

我刷新了仓库，但 GitHub 对你这个分支的部分 raw 文件仍然有缓存滞后；你刚上传的 `test-hybrid(5).cpp` 和 `llama-model(6).cpp` 我已经按最新版看了。当前 `llama-model` 已经会消费 runtime plan：`R/P/C/G` 都能真正转成 layer placement。 

而 `test-hybrid` 现在已经完整包含：

```text
metadata
→ memory profile
→ RPC profile
→ FFN profile
→ attention profile
→ full layer profile
→ phone block profile
→ 4A
→ 4B
→ best
→ runtime_plan_set()
```

  

所以接入本质上就是把这段从 `test-hybrid::main()` 变成一个正式函数。

## 我建议这样接

最终加载流程做成：

```text
llama-cli
   ↓
解析 -m / --rpc / -c
   ↓
得到 model params + context params
   ↓
llama_model_load_from_file()
   ↓
创建 llama_model_loader
   ↓
prepare_model_devices
   ↓
[AUTO PLANNER]       ← 新插这里
   │
   ├─ profile
   ├─ 4A
   ├─ 4B
   └─ runtime_plan_set(best)
   ↓
load_hparams()
   ↓
读取 runtime_plan
   ↓
真正设置 T/P/C/R/G
   ↓
load_tensors()
   ↓
创建 context
   ↓
Prefill 使用 K
```

当前 `src/llama.cpp` 的正常顺序确实是先创建 loader、prepare devices，然后 `load_hparams()`，最后才 `load_tensors()`。

而你的 `load_hparams()` 已经在里面调用：

```cpp
llama_hybrid_runtime_plan_get(runtime_plan);
```

所以 **autoplan 必须发生在 `load_hparams()` 之前**。

---

### 但有一个实际 `n_ctx` 的问题

不能继续：

```cpp
constraints.target_ctx      = 512;
constraints.score_kv_tokens = 512;
```

因为这只是 `test-hybrid` 的测试值。

真正 `llama-cli -c 4096` 时，就应该：

```cpp
target_ctx = 4096;
```

问题是 `llama_model_load()` 本身只有 `llama_model_params`，而 `-c` 属于 context params。

正常 `common` 初始化实际上在加载模型**之前**已经同时生成：

```cpp
auto mparams = common_model_params_to_llama(params);
auto cparams = common_context_params_to_llama(params);
```

然后才：

```cpp
llama_model_load_from_file(..., mparams);
```

这正好给我们一个入口。当前 upstream 也是这个顺序。

所以我建议不要用环境变量传 `n_ctx`，而是加一个很小的字段：

```cpp
// llama_model_params
bool     hybrid_auto       = false;
uint32_t hybrid_target_ctx = 0;
```

然后在 `common`：

```cpp
auto mparams = common_model_params_to_llama(params);
auto cparams = common_context_params_to_llama(params);

if (params.hybrid_auto) {
    mparams.hybrid_auto       = true;
    mparams.hybrid_target_ctx = cparams.n_ctx;
}

llama_model_load_from_file(
    params.model.path.c_str(),
    mparams);
```

这样：

```text
-c 512  → planner 用 512
-c 4096 → planner 用 4096
-c 8192 → planner 用 8192
```

不经过环境变量。

---

## 然后在 `llama-hybrid` 做一个正式入口

建议声明：

```cpp
bool llama_hybrid_autoplan(
    llama_model_loader & ml,
    const llama_model_params & params,
    llama_hybrid_plan & best_plan);
```

这个函数基本就是把你 `test-hybrid` 的 3～8 阶段搬进去。

内部逻辑：

```cpp
bool llama_hybrid_autoplan(
        llama_model_loader & ml,
        const llama_model_params & params,
        llama_hybrid_plan & best) {

    llama_hybrid_runtime_plan_clear();

    //
    // 1. 找 CPU / RPC / CUDA
    //
    ggml_backend_t cpu   = nullptr;
    ggml_backend_t phone = nullptr;
    ggml_backend_t gpu   = nullptr;

    ...
```

这里不要再让用户重新传 RPC endpoint。

正常 `--rpc 192.168.101.253:50052` 后，RPC device 已经注册到 backend device list；直接扫描：

```cpp
for (size_t i = 0;
     i < ggml_backend_dev_count();
     ++i) {

    ggml_backend_dev_t dev =
        ggml_backend_dev_get(i);

    ggml_backend_reg_t reg =
        ggml_backend_dev_backend_reg(dev);

    const char * reg_name =
        reg
            ? ggml_backend_reg_name(reg)
            : "";

    if (ggml_backend_dev_type(dev) ==
            GGML_BACKEND_DEVICE_TYPE_CPU) {

        cpu = ggml_backend_dev_init(
            dev, nullptr);

    } else if (strcmp(reg_name, "RPC") == 0) {

        phone = ggml_backend_dev_init(
            dev, nullptr);

    } else if (
        strcmp(reg_name, "CUDA") == 0 &&
        ggml_backend_dev_type(dev) ==
            GGML_BACKEND_DEVICE_TYPE_GPU) {

        gpu = ggml_backend_dev_init(
            dev, nullptr);
    }
}
```

于是命令依旧只需要：

```powershell
llama-cli.exe `
  -m Qwen2.5-32B-Instruct-Q4_K_M.gguf `
  --rpc 192.168.101.253:50052 `
  --split-mode tensor `
  --hybrid-auto `
  -c 512 `
  --flash-attn on
```

不需要再写：

```text
LLAMA_HYBRID_PC_PCT
LLAMA_HYBRID_PHONE_LAYERS
LLAMA_HYBRID_GPU_PC_LAYERS
LLAMA_PREFILL_CHUNKS
```

---

### Autoplan 中 descriptor/profile 部分直接搬现有代码

你现在 test 已经验证过这套：

```cpp
int n_layer = 0;

for (int il = 0; il < 1024; ++il) {
    llama_hybrid_ffn_desc tmp;

    if (!ml.get_hybrid_ffn_desc(
            il, tmp)) {
        break;
    }

    ++n_layer;
}

const int probe_layer =
    n_layer / 2;

llama_hybrid_ffn_desc ffn_desc;
llama_hybrid_attn_desc attn_desc;

if (!ml.get_hybrid_ffn_desc(
        probe_layer,
        ffn_desc)) {
    return false;
}

if (!ml.get_hybrid_attn_desc(
        probe_layer,
        attn_desc)) {
    return false;
}
```

这正是当前 test 已经在用的路径。 

接着：

```cpp
llama_hybrid_profile profile;

profile.n_layer = n_layer;
profile.n_embd  = ffn_desc.n_embd;
profile.n_ff    = ffn_desc.n_ff;

profile.probe_tokens           = 16;
profile.probe_chunk_min_tokens = 4;
```

然后照搬：

```cpp
get_hybrid_weight_bytes(...);

llama_hybrid_profile_memory(...);
llama_hybrid_profile_rpc(...);
llama_hybrid_profile_ffn(...);
llama_hybrid_profile_attention(...);
llama_hybrid_profile_full_layer(...);
llama_hybrid_profile_phone_blocks(...);
```

---

## 4A/4B 最后不要 sort 十万方案

正式 runtime 里没必要像 test：

```cpp
std::sort(ranked.begin(), ranked.end(), ...);
```

因为真正只需要 best。

直接：

```cpp
const auto feasible =
    llama_hybrid_enumerate_feasible_plans(
        profile,
        constraints);

bool found = false;

for (auto plan : feasible) {

    if (!llama_hybrid_score_plan(
            profile,
            constraints,
            plan)) {
        continue;
    }

    if (!found ||
        plan.predicted_ms <
            best.predicted_ms) {

        best = plan;
        found = true;
    }
}

if (!found) {
    return false;
}
```

这样：

```text
104644 feasible
```

也只线性扫一遍，不再为打印排名做 `O(N log N)` 排序。

后面我们再把 4A 本身从 40 万公式枚举换成前面说的结构化/DP搜索；**现在不影响真正接入**。

---

# 最关键：在 `src/llama.cpp` 插入它

当前代码：

```cpp
bool ok =
    llama_prepare_model_devices(
        params,
        model_ptr.get());

if (!ok) {
    return {-1, nullptr};
}

auto * model =
    dynamic_cast<llama_model_base *>(
        model_ptr.get());

...
model->load_hparams(ml);
```



改成：

```cpp
bool ok =
    llama_prepare_model_devices(
        params,
        model_ptr.get());

if (!ok) {
    return {-1, nullptr};
}

//
// Hybrid auto planner
//
if (params.hybrid_auto &&
    params.split_mode ==
        LLAMA_SPLIT_MODE_TENSOR) {

    llama_hybrid_plan best;

    LLAMA_LOG_INFO(
        "[HYBRID_AUTO] profiling begins, "
        "target_ctx=%u\n",
        params.hybrid_target_ctx);

    if (!llama_hybrid_autoplan(
            ml,
            params,
            best)) {

        LLAMA_LOG_ERROR(
            "[HYBRID_AUTO] planner failed\n");

        return {-1, nullptr};
    }

    llama_hybrid_plan_print(best);

    if (!llama_hybrid_runtime_plan_set(
            best)) {

        LLAMA_LOG_ERROR(
            "[HYBRID_AUTO] failed to "
            "publish plan\n");

        return {-1, nullptr};
    }

    LLAMA_LOG_INFO(
        "[HYBRID_AUTO] selected "
        "T=%d P=%d C=%d "
        "R=%.3f G=%d K=%d "
        "predicted=%.3f ms\n",
        best.tensor_layers,
        best.phone_layers,
        best.pc_layers,
        best.tensor_pc_ratio,
        best.gpu_pc_layers,
        best.tensor_chunks_per_ubatch,
        best.predicted_ms);
}

auto * model =
    dynamic_cast<llama_model_base *>(
        model_ptr.get());
```

然后继续原来的：

```cpp
model->load_hparams(ml);
```

这样 `load_hparams()` 马上就会打印：

```text
[HYBRID_SPLIT] source=planner ...
```

而不是：

```text
source=environment
```

你的最新版已经具备这个判断。

---

# K 再真正接进去

现在 Qwen2 还是：

```cpp
static int
qwen2_prefill_ffn_chunk_count() {

    const char * value =
        std::getenv(
            "LLAMA_PREFILL_CHUNKS");

    return value == nullptr
        ? 0
        : std::max(
            1,
            std::atoi(value));
}
```



所以在 `llama-hybrid.h` 加：

```cpp
LLAMA_API int
llama_hybrid_runtime_prefill_chunks();
```

实现：

```cpp
int llama_hybrid_runtime_prefill_chunks() {
    std::lock_guard<std::mutex> lock(
        g_llama_hybrid_runtime_plan_mutex);

    if (!g_llama_hybrid_runtime_plan) {
        return 0;
    }

    return
        g_llama_hybrid_runtime_plan
            ->tensor_chunks_per_ubatch;
}
```

然后 Qwen2：

```cpp
static int
qwen2_prefill_ffn_chunk_count() {

    const int planned =
        llama_hybrid_runtime_prefill_chunks();

    if (planned > 0) {
        return planned;
    }

    //
    // 手动 debug fallback
    //
    const char * value =
        std::getenv(
            "LLAMA_PREFILL_CHUNKS");

    return value == nullptr
        ? 0
        : std::max(
            1,
            std::atoi(value));
}
```

Qwen3 同样改。它当前也是环境变量版本。

Llama 也一样。你之前最新版 Llama 的 prefill getter 当前也是 `LLAMA_PREFILL_CHUNKS`。

于是 K 也彻底进入：

```text
planner → runtime plan → graph builder
```

---

## 还有一个我建议现在就修的真实加载问题

**自动模式不要继续依赖 `-ngl all`。**

你的 `G` 已经自己决定：

```text
PC_ONLY 前 G 层 → CUDA
```

最新版 `llama-model.cpp` 也已经直接这么放。

但是标准 `n_gpu_layers/-ngl` 仍然会影响 **output layer**，因为 output 最后依然走：

```cpp
pimpl->dev_output =
    get_layer_buft_list(n_layer_all);
```



这可能让 planner 认为 GPU 只有：

```text
G × PC_ONLY layer
```

实际却又偷偷塞了 LM head/output 到 CUDA，导致显存估算和真实加载不一致。

所以第一版 auto planner 我建议：

> **hybrid auto 模式下明确把 output 留 CPU。**

在 `load_tensors()`：

```cpp
if (!hybrid_layer_modes.empty()) {
    pimpl->dev_output = {
        cpu_dev,
        &pimpl->cpu_buft_list
    };
} else {
    pimpl->dev_output =
        get_layer_buft_list(
            n_layer_all);
}
```

这样 planner 的：

```text
non_layer_weight_bytes → PC
```

与真实 placement 就一致了。

以后如果想把 output head 也纳入 GPU 优化，再把它变成 planner 的另一个显存项。

---

所以**真正接入的第一轮目标**不是直接看速度，而是看下面这条链有没有完整出现：

```text
[HYBRID_AUTO] profiling begins target_ctx=512

...

[HYBRID_AUTO] selected
T=1 P=19 C=44 R=.75 G=13 K=4

[HYBRID_RUNTIME] plan published ...

[HYBRID_SPLIT]
source=planner
tensor_split=1
phone_only=19
pc_only=44
gpu_pc=13
R=.750

[PC_PLACE]
... 13 个 CUDA PC_ONLY layer ...

Prefill:
实际 n_prefill_chunks = 4
```

这全部正确以后，才进行 **32B 真模型加载 + 16/64/128 token prefill 实测**。

如果你想让我下一条直接按你当前代码给出**可逐文件照抄的第一版接入 patch**，最好把这几个最新版文件手动上传：

`src/llama.cpp`、`src/llama-hybrid.cpp/.h`、`include/llama.h`、`common/common.cpp`，以及 `src/models/qwen2.cpp`。GitHub 现在 `src/llama.cpp` 我能读到，但部分 later-split raw 仍显示两天前缓存；你上传后我就可以完全按你当前 HEAD 对行修改，不会出现版本差异。
