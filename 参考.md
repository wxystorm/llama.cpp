对，这次我基本能定位了：**不是 prompt 的问题，也不是模型加载/显存/graph capacity 的问题，而是你这次加进去的 `warmup / n_macros>=2` bypass 把启动阶段重新送回了 serial full-graph 路径。**

你现在这版 `llama-context(9).cpp` 里是这样：

```cpp
bool stage_queue_runtime_enabled = false;
uint32_t runtime_ubatch = cparams.n_ubatch;

if (stage_queue_plan_eligible) {
    const uint32_t xg =
        std::min<uint32_t>(
            cparams.n_ubatch,
            runtime_plan.gpu_chunk_tokens);

    const uint32_t n_macros =
        (n_tokens_all + xg - 1) / xg;

    stage_queue_runtime_enabled =
        !cparams.warmup &&
        n_macros >= 2;

    if (stage_queue_runtime_enabled) {
        runtime_ubatch = xg;
        ...
    }
}
```

也就是说只要：

```text
warmup == true
```

或者：

```text
n_macros == 1
```

就**彻底不启用 StageQ**。

而你的最新日志恰好：

```text
GRAPH_RESERVE_END
...
Remote RPC server crashed
```

中间连：

```text
[STAGEQ] enabled ...
```

都没有。

这和上一版成功启动时非常不一样。

上一版成功时明确看到：

```text
[STAGEQ] batch=2 XG=128 macros=1 XT=32
[STAGEQ] batch=2 XG=128 macros=1 XT=32
...
model loaded
```

所以现在最大的差异其实已经非常明显：

```text
旧版：
startup batch=2
    ↓
StageQ path
    ↓
正常

新版：
startup batch=2 / warmup
    ↓
因为 n_macros < 2 或 warmup
StageQ disabled
    ↓
serial full graph
    ↓
RPC crash
```

---

## 所以我建议把我之前让你加的这个判断撤掉

不是 StageQ 只能在两个 macro 时使用。

应该把两个概念分开：

```text
能不能使用 StageQ
```

和：

```text
能不能形成跨 macro overlap
```

完全不是一回事。

一个 macro 时确实：

```text
没有流水收益
```

但它仍然可以通过：

```text
PRE
→ GPU
→ explicit handoff
→ TENSOR
```

这条安全执行路径。

而你当前新的：

```text
GPU → TENSOR
```

拓扑，看起来启动阶段恰恰需要这条路径。

---

### 建议恢复成这样

```cpp
const bool stage_queue_runtime_enabled =
    stage_queue_plan_eligible;

uint32_t runtime_ubatch = cparams.n_ubatch;

if (stage_queue_runtime_enabled) {
    const uint32_t xg =
        std::min<uint32_t>(
            cparams.n_ubatch,
            runtime_plan.gpu_chunk_tokens);

    runtime_ubatch = xg;

    const uint32_t n_macros =
        (n_tokens_all + xg - 1) / xg;

    LLAMA_LOG_ERROR(
        "[STAGEQ] batch=%u XG=%u macros=%u XT=%d overlap=%d warmup=%d\n",
        n_tokens_all,
        xg,
        n_macros,
        runtime_plan.tensor_chunk_tokens,
        (int) (n_macros >= 2),
        (int) cparams.warmup);
}
```

即：

```text
stage_queue_runtime_enabled
=
这个 topology 能不能使用 StageQ
```

而：

```text
overlap = n_macros >= 2
```

只用来表示：

> 这次有没有跨 macro 流水机会。

不要让它决定是否走 StageQ。

---

## `warmup` 也不要排除

这一点现在也已经有实际证据了。

你的 `cparams.warmup` 本身就是一个运行状态，默认初始化为 false，也可以后续由 server 切换。

但这不意味着：

```text
warmup → 必须走 serial
```

对于我们新 topology：

```text
GPU → TENSOR
```

warmup 也应该保持同样的数据边界。

也就是说：

```text
normal:
GPU → handoff → TENSOR

warmup:
GPU → handoff → TENSOR
```

区别只是：

```text
warmup 不关心性能统计
```

而不是换一套执行 topology。

---

## Decode 也不用担心

你可能会问：

> 那 batch=1 Decode 也会显示 StageQ enabled？

可以让 `stage_queue_runtime_enabled=true`，但它实际还是不会进入 partial PIPE。

因为后面真正执行 StageQ 还有：

```cpp
const bool pipe_candidate =
    pipeline_runtime_enabled &&
    cparams.causal_attn &&
    ubatch.n_tokens > 1 &&
    n_outputs == 0 &&
    ...
```

其中：

```cpp
ubatch.n_tokens > 1
```

已经自动把 Decode token 排除了。

所以：

```text
Decode batch=1
```

还是正常完整 graph。

不用在前面再加一个：

```cpp
n_macros >= 2
```

做第二层过滤。

---

# 这里其实暴露出一个很重要的设计点

我们之前把 StageQ 想得有点太像：

> “为了 overlap 才存在的优化器”。

现在看来它还有另一个职责：

> **显式维护新逻辑 stage 之间正确的 boundary/handoff。**

所以：

```text
StageQ
=
正确 stage execution
+
有条件的 pipeline overlap
```

而不是：

```text
StageQ
=
只有两个以上 macro 时才打开的性能优化
```

对于一个 macro：

```text
GPU0
  ↓
H0
  ↓
Tensor0
```

虽然没有：

```text
GPU1 || Tensor0
```

但这个执行路径仍然完全合法，而且从你前一次测试看，**甚至比 serial full graph 更稳定**。

---

## 另外旧 PIPE2 还是继续关掉

你现在代码还有：

```cpp
bool legacy_pipeline_runtime_enabled =
    std::getenv("LLAMA_PREFILL_STAGE_PIPELINE") != nullptr;

bool pipeline_runtime_enabled =
    stage_queue_runtime_enabled ||
    legacy_pipeline_runtime_enabled;
```



所以运行环境保持：

```text
LLAMA_HYBRID_STAGE_QUEUE=1
```

并确保：

```text
LLAMA_PREFILL_STAGE_PIPELINE
```

不存在。

这部分你上一次已经验证过。

---

### 我建议你现在只改这一处，再跑

从：

```cpp
stage_queue_runtime_enabled =
    !cparams.warmup &&
    n_macros >= 2;
```

恢复为：

```cpp
stage_queue_runtime_enabled = true;
```

当然前提仍然在：

```cpp
if (stage_queue_plan_eligible)
```

里面。

也就是最终：

```cpp
if (stage_queue_plan_eligible) {
    const uint32_t xg =
        std::min<uint32_t>(
            cparams.n_ubatch,
            runtime_plan.gpu_chunk_tokens);

    runtime_ubatch = xg;

    const uint32_t n_macros =
        (n_tokens_all + xg - 1) / xg;

    stage_queue_runtime_enabled = true;

    LLAMA_LOG_ERROR(
        "[STAGEQ] batch=%u XG=%u macros=%u XT=%d overlap=%d warmup=%d\n",
        n_tokens_all,
        xg,
        n_macros,
        runtime_plan.tensor_chunk_tokens,
        (int) (n_macros >= 2),
        (int) cparams.warmup);
}
```

然后启动时我预期重新看到：

```text
[STAGEQ] batch=2 XG=128 macros=1 XT=32 overlap=0 warmup=1
```

随后：

```text
model loaded
```

而真正长 Prompt 时：

```text
[STAGEQ] batch=... XG=128 macros=... XT=32 overlap=1 warmup=0
```

再进入真正的跨 macro 流水。

所以这次**不用去动 RPC，也先不用去查 phone server**。你这版和上一版成功版本之间已经有一个非常明确的行为差异，就是我之前建议加的这个 bypass；先把它撤回最合理。
