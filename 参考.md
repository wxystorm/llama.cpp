可以，而且我觉得现在已经到了**适合做动态策略**的时候了。不过我不会一上来做很复杂的在线调度器，而是先做一个很小的反馈控制：**chunk 数先固定，动态决定“要不要让 SET 和 GET 并发”以及“Phone ADD 是 snapshot 后立即放行还是等 GET 完成”。**

你刚传的这组 3-chunk 数据其实很有意思。前半部分是 strict，后面你明确标了“下面是宽松的，也是3chunk”。 我按两边共同的 `ffn_out-0~29` 算了一下：

| 30 个 FFN                | strict 3chunk | relaxed 3chunk |
| ----------------------- | ------------: | -------------: |
| wall 总和                 |       40.72 s |    **16.85 s** |
| wall 中位数                |       1212 ms |     **502 ms** |
| Phone compute 中位数       |        222 ms |     **187 ms** |
| upload busy 中位数         |        564 ms |     **251 ms** |
| download busy 中位数       |       1089 ms |     **458 ms** |
| snapshot 中位数            |        144 ms |     **102 ms** |
| download tail 中位数       |        953 ms |     **360 ms** |
| download queue wait 中位数 |       1001 ms |     **274 ms** |
| Phone ADD 中位数           |     **93 ms** |         146 ms |

而且共同 30 层里，**relaxed 有 29 层 wall 更快**。strict 中甚至出现 `ffn_out-8 wall=3.18s`、`ffn_out-6=2.56s` 这种长尾。 relaxed 大部分层则回到了 370～600ms 左右。

所以这里有个重要提醒：

> 现在不能简单制定成“GET tail 高 → strict ADD”。

因为这组数据里 strict 反而整体差很多。当然这是两次独立运行，Wi-Fi/手机状态有很大抖动，不能直接认定是 strict 导致的；但至少说明策略必须依据**运行时状态**，不能写死。

### 我建议动态控制分成两层

第一层先动态控制 **transfer overlap**，这个最值得做。

假设 download 是当前 critical path，那么可以维护几个 EWMA：

```cpp
struct pipeline_runtime_stats {
    double down_busy_ewma;
    double down_tail_ewma;
    double down_qwait_ewma;

    double up_busy_ewma;
    double up_qwait_ewma;

    double phone_compute_ewma;
};
```

每个 `ffn_out` 完成后更新：

```cpp
ewma = 0.8 * ewma + 0.2 * current;
```

然后不要再写成：

```text
upload ready -> 一定马上传
```

而是：

```cpp
bool download_congested =
    down_qwait_ewma > 0.5 * down_busy_ewma ||
    down_tail_ewma  > 0.7 * wall_ewma;

if (download_active && download_congested) {
    // GET 已经明显是关键路径
    // 暂缓新的 SET，优先把 GET 放完
    hold_upload = true;
} else {
    // 链路目前没堵
    // 允许 full-duplex overlap
    hold_upload = false;
}
```

这里的 `0.5/0.7` **只是初始实验阈值，不是最终值**。

这样实际调度会从现在的：

```text
GET0 ========================
       SET1 =======
              SET2 ========
```

在网络状态差的时候自动变成：

```text
GET0 =================
                    SET1 =====
```

而链路状态好的时候仍然允许：

```text
GET0 ========================
       SET1 =======
```

这比在启动时固定“单 transfer / 双 transfer”合理得多。

---

第二层才是动态 **relaxed / strict ADD**。

这里甚至不需要改变 correctness，只改变什么时候把 `add_ready[phone]` 置位：

```text
snapshot_ready
      │
      ├── relaxed -> ADD ready
      │
      └── strict  -> 等 GET complete
```

可以做成：

```cpp
if (!phone_memory_congested) {
    // 网络比较健康，或者 ADD 很短
    // 用 overlap 隐藏 ADD
    release_add_on_snapshot();
} else {
    // 最近发现 ADD 与网络并发时会把关键路径拖长
    release_add_on_transfer_end();
}
```

`phone_memory_congested` 不要猜 CPU 利用率，直接用你已经有的数据：

```cpp
bool phone_memory_congested =
    phone_compute_ewma > phone_compute_baseline * 1.8 ||
    down_tail_ewma     > down_tail_baseline     * 1.8;
```

也就是说：

```text
正常：
snapshot -> ADD
            || GET tail

发现 contention：
snapshot -------- GET end -> ADD
```

不过基于你这次的新数据，我会让 **relaxed 成为默认模式**。因为这份 3-chunk A/B 里 relaxed 明显健康得多，例如 relaxed 第一层直接有：

```text
snapshot_ready = 144.206 ms
Phone ADD      = 189.631 -> 221.790 ms
GET0 end       = 328.936 ms
```

也就是 ADD 被很好地藏在 GET tail 中。

只有在线监测到它确实造成 contention，才临时切 strict。

---

### 但动态策略最好不是“每个事件乱切”

我建议做成一个非常简单的三态状态机：

```text
NORMAL
  |
  | download backlog 连续 2~3 层过高
  v
DOWNLOAD_PRESSURE
  |
  | compute/tail 同时恶化
  v
SERIALIZE_MORE
```

行为可以是：

```text
NORMAL
  3 chunks
  SET/GET 并发
  relaxed ADD

DOWNLOAD_PRESSURE
  3 chunks
  GET 优先
  限制新 SET
  relaxed ADD

SERIALIZE_MORE
  2 chunks（后面再做）
  GET 优先
  strict/relaxed 根据最近 ADD 影响决定
```

恢复也要加 hysteresis，例如连续 3 层状态变好才降级回 `NORMAL`，否则很容易：

```text
normal -> congested -> normal -> congested
```

来回抖。

---

而且你现在的 summary 已经几乎够用了。

我只建议再补两个很便宜的指标：

```text
upload_overlap_with_download_us
phone_compute_overlap_with_download_us
```

这样就能真正建立相关关系：

```text
SET/GET overlap 越多
↓
GET tail 是否变长？

compute/GET overlap 越多
↓
Phone compute 是否膨胀？
```

到时候动态策略就不再是凭感觉。

---

更进一步，**chunk 数本身当然也可以动态**，但我建议放第二阶段。

以后可以做到：

```cpp
if (n_tokens == 1) {
    chunks = 1;                 // decode
} else if (network_good &&
           queue_pressure_low) {
    chunks = 3;
} else if (queue_pressure_mid) {
    chunks = 2;
} else {
    chunks = 1;
}
```

这里关键不是“37 tokens 永远 3 chunk”，而是：

```text
37 tokens + 网络好       -> 3
37 tokens + GET backlog  -> 2
37 tokens + 严重拥塞     -> 1
```

这其实非常适合作为你这个工作的一个亮点：

> **不是静态选择 tensor-split pipeline 深度，而是根据在线观测到的计算/通信 contention，自适应调节 pipeline aggressiveness。**

但实现顺序我会控制住，先别一下写成大系统：

```text
第一步：
3 chunk 固定
↓
动态 SET/GET 是否 overlap
↓
验证 wall 是否稳定下降

第二步：
动态 relaxed/strict ADD

第三步：
再让 active_chunks = 1/2/3 动态
```

**第一步最值得现在马上做。** 因为你已经有 `download_busy / tail / queue_wait`，基本不需要新增 profiling 基础设施，只要在 transfer scheduler 取任务时加一个“download pressure 下是否允许启动 upload”的判断就行。

对，**需要记录一点运行时历史**，但不需要把每一层完整日志都保存下来。你真正需要的是：

> 当前层指标 + 一个平滑后的“正常水平” + 连续异常计数器。

也就是很轻量。

比如每次 `ffn_out` 完成后，你现在已经能拿到：

```cpp
wall_us
compute1_us
download_busy_us
download_tail_us
download_queue_wait_us
```

然后维护：

```cpp
struct pipeline_adaptive_state {
    enum mode_t {
        NORMAL,
        DOWNLOAD_PRESSURE,
        SERIALIZE_MORE,
    } mode = NORMAL;

    // 正常状态下的历史基线
    double compute1_ewma = 0;
    double down_tail_ewma = 0;
    double down_qwait_ewma = 0;
    double wall_ewma = 0;

    // 连续异常计数
    int pressure_streak = 0;
    int severe_streak = 0;
    int healthy_streak = 0;
};
```

### “连续 2~3 层过高”是什么意思？

假设当前一层：

```text
download_queue_wait = 500 ms
download_busy       = 600 ms
```

那明显说明 download queue 已经堵得很厉害。

可以定义：

```cpp
double qwait_ratio =
    download_queue_wait_us /
    std::max<double>(download_busy_us, 1);
```

例如：

```cpp
bool pressure =
    qwait_ratio > 0.5;
```

然后：

```cpp
if (pressure) {
    pressure_streak++;
} else {
    pressure_streak = 0;
}
```

于是：

```text
layer 10: qwait_ratio = 0.62  pressure_streak = 1
layer 11: qwait_ratio = 0.71  pressure_streak = 2
layer 12: qwait_ratio = 0.83  pressure_streak = 3
```

到第三层：

```cpp
if (mode == NORMAL && pressure_streak >= 3) {
    mode = DOWNLOAD_PRESSURE;
}
```

这就是我说的：

```text
download backlog 连续 2~3 层过高
```

为什么不一层就切？

因为你现在日志里已经看到了很多：

```text
某一层突然 1.5s
下一层又恢复到 400ms
```

这种瞬时 jitter。

如果看到一次 spike 就切状态，scheduler 会疯狂：

```text
NORMAL
↓
PRESSURE
↓
NORMAL
↓
PRESSURE
```

反而更糟。

---

### “compute / tail 同时恶化”也是同样道理

这里重点不是看它们的绝对值，而是和**之前正常状态下的水平**比较。

比如 NORMAL 状态下过去若干层：

```text
phone compute ≈ 160 ms
download tail ≈ 320 ms
```

保存成 EWMA：

```text
compute1_ewma  = 160 ms
tail_ewma      = 320 ms
```

突然后面的层变成：

```text
phone compute = 310 ms
download tail = 650 ms
```

那么：

```text
compute:
310 / 160 ≈ 1.94x

tail:
650 / 320 ≈ 2.03x
```

两者一起恶化。

代码可以先粗暴写：

```cpp
bool compute_bad =
    compute1_us > compute1_ewma * 1.5;

bool tail_bad =
    download_tail_us > down_tail_ewma * 1.5;

bool severe =
    compute_bad && tail_bad;
```

然后也是连续计数：

```cpp
if (severe) {
    severe_streak++;
} else {
    severe_streak = 0;
}

if (mode == DOWNLOAD_PRESSURE &&
    severe_streak >= 2) {
    mode = SERIALIZE_MORE;
}
```

这里的逻辑其实是在判断：

```text
仅仅 queue 堵
    ↓
DOWNLOAD_PRESSURE

不仅 queue 堵
而且 Phone compute 也一起慢
GET tail 也一起慢
    ↓
说明并发本身开始造成资源 contention
    ↓
SERIALIZE_MORE
```

所以“同时恶化”不是必须发生在微秒级同一时刻，而是：

> **同一个或连续几个 pipeline invocation 中，compute 和 communication 两类指标相对于正常基线都明显变差。**

---

## EWMA 就是你需要记录的“历史”

没必要存：

```cpp
std::vector<所有100层数据>
```

只保存一个平滑值：

```cpp
ewma = 0.8 * ewma + 0.2 * current;
```

例如 tail：

```text
历史 EWMA = 300ms

当前层 = 400ms
new EWMA = 0.8*300 + 0.2*400
         = 320ms
```

下一层 350ms：

```text
new EWMA
= 0.8*320 + 0.2*350
= 326ms
```

所以它代表：

> 最近一段时间大概正常是什么水平。

内存基本可以忽略。

---

但是有一个非常重要的细节：

### **进入异常状态之后，不要继续用异常数据污染 baseline。**

假设正常：

```text
tail baseline = 300ms
```

后来网络堵了：

```text
600
700
800
900
```

如果你还不断：

```cpp
tail_ewma = 0.8 * tail_ewma + 0.2 * current;
```

最后 baseline 会变成：

```text
700ms
```

那系统就会认为：

```text
700ms 很正常啊
```

状态永远恢复不了。

所以我会写成：

```cpp
if (state.mode == NORMAL) {
    update_baseline(stats);
}
```

甚至更稳一点：

```cpp
if (state.mode == NORMAL && !current_is_outlier) {
    update_baseline(stats);
}
```

这样 baseline 始终表示：

> **“健康运行时”的参考值。**

---

恢复也需要记录 `healthy_streak`。

比如在 `DOWNLOAD_PRESSURE` 状态：

```cpp
bool healthy =
    qwait_ratio < 0.25 &&
    download_tail_us < down_tail_ewma * 1.2;
```

连续三层健康：

```cpp
if (healthy) {
    healthy_streak++;
} else {
    healthy_streak = 0;
}

if (healthy_streak >= 3) {
    mode = NORMAL;
}
```

这样就形成 hysteresis：

```text
进入 PRESSURE：
qwait > 50%，连续3次

退出 PRESSURE：
qwait < 25%，连续3次
```

而不是同一个阈值。

---

我建议你第一版甚至不要直接上三个状态，可以先做最简单的两个状态：

```text
NORMAL
  |
  | qwait 连续3次高
  v
DOWNLOAD_PRESSURE
  |
  | 连续3次恢复
  v
NORMAL
```

行为：

```text
NORMAL:
    upload/download 允许充分 overlap

DOWNLOAD_PRESSURE:
    GET 优先
    暂缓新的 upload
```

先证明：

```text
动态策略
<
永远并发
和
永远串行
```

或者至少让 jitter 明显下降。

之后再加：

```text
SERIALIZE_MORE
```

---

而且我会额外保留一个很小的 ring buffer，比如最近 4 层：

```cpp
struct sample {
    int64_t wall;
    int64_t compute1;
    int64_t down_tail;
    int64_t down_qwait;
};

sample recent[4];
```

它不是调度必须的，主要是**调试非常方便**。

状态切换时打印：

```text
[META_ADAPT]
NORMAL -> DOWNLOAD_PRESSURE
qwait_ratio=0.74
pressure_streak=3
tail=512ms baseline_tail=301ms
compute1=181ms baseline_compute1=165ms
```

以后你看日志就知道：

> 为什么这一层 scheduler 突然开始限制 SET。

所以可以理解成：**是的，需要记，但不是记录整个历史；核心只需要 `EWMA + streak counter`，最多再加最近 3~4 个 sample 用来调试。**

而且你当前这个场景，我会先用：

```text
download_queue_wait
download_tail
phone compute
```

三个指标，先别塞太多变量进去。这样动态控制逻辑还比较容易解释和验证。
