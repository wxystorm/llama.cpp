对，这次“差一点”已经比较清楚了：**不是 wavefront 没生效，而是它已经生效，但新增的细粒度执行开销和每段尾部 barrier 还在吃掉收益。**

当前计划预测是：

```text
predicted = 69810.9 ms
XT = 48
T = 50
G = 14
```

实际 378 token 的 prefill 是：

```text
79931.96 ms
4.73 tok/s
```

也就是比预测多了大约 **10.1 秒，约 14.5%**。 

但 wavefront 本身是正常工作的。实际推理里连续出现：

```text
overlap_boundaries=29
ahead_attn=6~8
ahead_phone_submit=6~8
dependency_wait_count=0
```

所以不是“下一层又被 dependency 卡住了”。

我现在看到两个最值得查的损耗源。

第一，**每个 Meta 段仍然保留 1 次 legacy barrier**。这次实际 prompt 里从 L58 到 L72 一共有 15 条 `RETURN_WAVEFRONT_SUM`，而每条都是：

```text
legacy_layer_barrier_count=1
```

也就是说整次 prefill 至少经历了约 **15 个段尾 barrier**。

这非常可能是现在剩余差距的一部分。注意目前 summary 只打印：

```text
legacy_layer_barrier_count
```

却没有打印已经存在的：

```cpp
layer_barrier_wait_us
layer_barrier_wait_max_us
```

所以现在我们知道“有 15 次”，但不知道这 15 次总共到底等了 0.5 秒、3 秒还是 8 秒。

第二个更可能的大头是 **XT=48 带来的细粒度 CPU compute 损耗**。378 token 被拆成：

```text
48 × 7 + 42
```

即每层 8 个 chunk。

对于 50 个 Tensor 层，就是大量更小的：

```text
Attention
FFN gate/up/down
norm
residual
```

kernel。

这种小 M GEMM 对 CPU 一般比一次较大的 378-token GEMM 效率低。我们现在把 Phone return 很多都隐藏住了，但为了隐藏它，PC 自己变成了大量 48-token 小计算，所以：

```text
隐藏掉的通信时间
        -
chunk 化导致增加的 PC compute
        -
segment tail wait
```

最后净收益目前还略负。

这也和 predictor 偏乐观一致：它预测整个 downstream 约 69.68 秒，GPU 只有约 0.27 秒，所以这 10 秒误差基本都来自 Tensor 阶段，而不是 GPU prefix。

另外，lane 数量目前我反而不急着动。上一轮 debug 里绝大多数 lane reuse wait 都只有 `0~0.001ms`，虽然偶尔有一次 `64.918ms`，但从量级上看很难单独解释这 10 秒差距。

所以我建议下一步**先不要改算法**，只增加两个几乎零开销的统计：

```text
lane_reuse_wait_ms
legacy_layer_barrier_wait_ms
```

把 `RETURN_WAVEFRONT_SUM` 改成类似：

```text
[RETURN_WAVEFRONT_SUM]
...
lane_reuse_wait_count=...
lane_reuse_wait_ms=...
lane_reuse_wait_max_ms=...
legacy_layer_barrier_count=1
legacy_layer_barrier_wait_ms=...
legacy_layer_barrier_wait_max_ms=...
```

然后仍然关闭 `GGML_META_PIPELINE_DEBUG` 正式跑一次。

这样一轮就能直接区分：

```text
如果 barrier_wait 总共几秒甚至接近 10 秒
→ 下一步继续消掉 segment tail barrier

如果 barrier_wait 很小
→ 基本坐实是 XT=48 的 chunk compute efficiency
→ 下一步应该做 XT=64 / 96 / 128 的 A/B，
   而不是继续改调度逻辑
```

我更倾向于**后者会占比较大**：现在 `dependency_wait_count=0`，ahead 已经能做到 6~8，说明调度已经不怎么“干等”；剩下很可能是为了 overlap 把计算切得太碎了。
