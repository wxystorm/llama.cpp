这份日志很有价值，已经把 L0~15 内部流水关系看清楚了：**两个 chunk 之间其实已经有重叠，但还有一个非常明确的串行点可以继续优化。**

以完整 16-token 的 layer5 为例：

```text
RETURN_BEGIN chunk0      8019834
CHUNK1_SUBMIT_BEGIN      8020516
RETURN_END chunk0        8026697
CHUNK1_SUBMIT_END        8029221
```

也就是说 chunk0 return 总共 `6.863 ms`，而 chunk1 的提交在 chunk0 return 还没结束时就已经开始了，两者实际重叠约：

```text
8026697 - 8020516 = 6.181 ms
```

也就是这一层 **chunk0 return 的约 90% 已经和 chunk1 submit 重叠**。

layer0 也是同样结构，只是 return0 比较慢：

```text
return0 begin       7768739
chunk1 submit begin 7769527
chunk1 submit end   7775914
return0 end         7791469
return1 begin       7791535
return1 end         7810164
```

所以这里 chunk1 submit 也完全塞进了 return0 的等待窗口，只不过 return0 本身有 `22.73 ms`，chunk1 submit 只有约 `6.39 ms`，只能隐藏掉一部分。 

### 真正还串行的是这里

目前实际上是：

```text
chunk0 compute
    ↓
return0 BEGIN ─────────────────────┐
                                   │
        chunk1 submit ───────       │  ← 已经重叠
                                   │
return0 END   ─────────────────────┘
    ↓
return1 BEGIN
    ↓
return1 END
    ↓
next layer
```

尤其是 **return1 必须等 return0 完成以后才开始**。

日志非常明确：

```text
RETURN_END   layer=0 chunk=0
RETURN_BEGIN layer=0 chunk=1
RETURN_END   layer=0 chunk=1
NEXT_BEGIN   layer=1 chunk=0
```

几乎一点空隙都没有。

layer5 也是一样，`chunk1 return` 一结束，下一层立即开始：

```text
RETURN_END layer=5 chunk=1  t=8043196
NEXT_BEGIN layer=6 chunk=0   t=8043271
```

只差 **0.075 ms**。这说明我们已经找到了真正的 layer boundary barrier。

我把第一轮完整 16-token ubatch 的 16 层时间戳汇总了一下，大约是：

```text
16层 chunk0 return 总时间     ≈ 189 ms
16层 chunk1 submit 总时间     ≈ 112 ms
其中已经重叠                 ≈ 109 ms

16层 chunk1 return 总时间     ≈ 248 ms
```

所以现在并不是之前猜的：

```text
return0
→ compute1
→ return1
```

完全串行。

实际上已经是：

```text
return0
|| submit/compute1
→ return1
```

这是好消息。

而 USB 下整轮 `1->0` 33 次 copy 总量仍然在约 `438~482 ms / ubatch`。

## 下一步怎么改

现在最值得做的就是把：

```text
wait return0
→ start return1
```

改成：

```text
start return0 async
       ↓
submit chunk1
       ↓
start return1 async
       ↓
wait return0 + return1
       ↓
next layer
```

也就是：

```text
                  ┌── return0 ───────────┐
chunk0 submit ────┤                      │
                  │                      ├─ JOIN → layer+1
                  └─ chunk1 submit ─ return1 ┘
```

最小改动的目标不是让下一层提前跑，而是**让两个 return 都成为 outstanding operation，最后在 layer boundary 一次性 join**。

按照现在的时间关系，单纯做到这一点，第一几个完整 ubatch 理论上大约还能隐藏 **40~70 ms/ubatch** 左右。不是数量级提升，但对现在 `PRE≈1.2 s` 来说已经是比较实在的 **3~6%**。

顺便这次新的 PIPE2 时间也不错：

```text
ub0 PRE = 1232 ms
ub1 PRE = 1194 ms
```

POST 只有 `36~40 ms`，所以方向仍然没变：**继续优化 L0~15，完全不用碰 POST/CUDA/30~31 的布局。** 

所以现在下一刀非常明确：**把两个 `PREFILL_RETURN` 从“前一个阻塞完成后再处理后一个”，改成“双 outstanding + layer-end join”。** 这比继续改 chunk 数更值得做。
