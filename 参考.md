这个 2 chunk + 手机 10 线程的 trace 比较干净，而且有两个很明显的结论。

先算这一轮主要耗时：

| 阶段            |   chunk0 |   chunk1 |
| ------------- | -------: | -------: |
| PC compute    |  3.98 ms |  0.53 ms |
| Phone compute | 11.54 ms |  8.46 ms |
| PC→Phone SET  | 12.52 ms | 12.95 ms |
| Phone→PC GET  | 23.45 ms | 16.17 ms |
| PC ADD        | 0.025 ms | 0.025 ms |
| Phone ADD     | 12.96 ms |  8.89 ms |

从：

```text
17.824 ms compute_begin
```

到：

```text
95.841 ms final ADD end
```

整个窗口约：

```text
78.0 ms
```

但这里**不能直接拿 78 ms 和你上一份 3 chunk 的 370 ms 比**，因为上一份每个 chunk 传的是 `180224/212992 bytes`，而这次只有：

```text
16384 bytes
```

明显不是同一个 workload / token shape，所以只能分析结构，不能说“2 chunk 快了 4 倍”。

---

### 这轮最明显的瓶颈还是 transfer

四个传输：

```text
SET0  12.52 ms
GET0  23.45 ms
SET1  12.95 ms
GET1  16.17 ms
----------------
合计 ≈65.1 ms
```

而整个窗口才 78 ms。

并且 transfer worker 几乎从：

```text
21.818 ms
```

一直忙到：

```text
86.938 ms
```

中间没有什么空隙：

```text
SET0  21.8 -------- 34.3
GET0               34.3 ------------ 57.8
SET1                               57.8 ------ 70.8
GET1                                           70.8 -------- 86.9
```

这再次证明我们前面那个判断：

> **单 transfer worker / 单 transfer socket 已经是流水线的主 critical path。**

你当前代码也确实只有一个 `transfer_queue` 和一个 `transfer_thread`。 

---

## 但有一个非常漂亮的新现象

看：

```text
Phone ADD0:
57.813 -> 70.770
≈ 12.96 ms

SET1:
57.809 -> 70.760
≈ 12.95 ms
```

它们：

```text
几乎同时开始
几乎同时结束
```

差到只有十几个微秒。

这个很值得注意。

它说明：

```text
Phone ADD0
      ||
PC -> Phone SET1
```

虽然使用的是 compute / transfer 两条不同 RPC 通道，但在手机端很可能在争同一类资源：

```text
CPU cores
memory bandwidth
cache
backend thread pool
```

因此所谓“并行”很可能变成：

```text
两个任务一起跑
→ 两个都变慢
→ 最后差不多一起结束
```

而不是两个完全独立的资源。

这次 Phone ADD1 没有 transfer 和它竞争：

```text
ADD1:
86.951 -> 95.841
≈ 8.89 ms
```

而 ADD0 是：

```text
≈12.96 ms
```

差了约 46%。

虽然只是一轮，但和我们之前的观察是完全一致的：

> **Phone ADD 和 SET/GET overlap 会产生明显 contention。**

---

### Phone compute 反而没有被 GET 拖得太严重

Phone C1：

```text
29.421 -> 37.877
≈8.46 ms
```

GET0：

```text
34.352 -> 57.796
```

两者有约 3.5 ms overlap。

但 C1 compute 并没有出现之前那种突然翻倍到 60~120ms 的情况。

所以**10 threads 在这次 workload 上至少看起来不糟糕**。

不过单次 trace 还不足以说 10 是最佳值。我还是会测：

```text
-t 6
-t 8
-t 10
```

重点不要只看 Phone compute，要看：

```text
first compute_begin
→
last add_end
```

这个总窗口。

---

## 2 chunk 本身我觉得值得保留继续测

3 chunk 的主要优势是：

```text
更细的 compute / transfer overlap
```

但现在 compute 太短：

```text
PC      0.5~4 ms
Phone   8~12 ms
```

transfer 却：

```text
13~23 ms
```

这时候继续把 chunk 切得更碎，得到的计算 overlap 收益越来越有限，却会增加：

```text
RPC次数
SET次数
GET次数
ACK次数
ADD次数
scheduler wakeup
```

所以从结构上看，**2 chunk 很可能比 3 chunk 更适合当前这个网络/设备组合**。

我甚至建议顺手测一下 **1 chunk**。

不是说 1 chunk 一定最好，而是你现在非常接近这种 regime：

```text
compute 很便宜
communication 很贵
```

那么可能出现：

```text
1 chunk:
少 RPC，少开销，但没有 chunk overlap

2 chunks:
保留一次有效 overlap，RPC开销尚可

3 chunks:
多出的 overlap已经抵不过额外通信/调度
```

我的预期排名很可能是：

```text
2 chunks 最好
1 chunk 第二
3 chunks 最差
```

但需要同一 workload 实测。

---

## 下一步我反而最推荐“双 transfer worker”实验

目前代码的 transfer 选择是：

```cpp
min_element(..., chunk 小的优先)
```

所以 SET/GET 全部在一条队列里。

现在可以尝试拆成：

```text
upload worker:
0 -> 1

download worker:
1 -> 0
```

对应两个 socket。

以你这份 trace举例，理论上可以从：

```text
SET0
     GET0
          SET1
               GET1
```

变成：

```text
upload:
SET0 █████████
     SET1 █████████

download:
        GET0 ███████████████
             GET1 ██████████
```

当然它们会争 Wi-Fi 和 Phone memory，所以不会获得理想 2×，但**这是目前结构上最大的优化点**。

现在 2 chunk 已经让 scheduler 简单很多，非常适合做这个实验。

---

还有一点：如果你下一步做 upload/download 双 worker，我建议**不要先改 ADD dependency**。

仍然保持：

```text
compute_done
+
incoming_done
+
outgoing_done
→ ADD ready
```

这样 correctness baseline 不动。

只把：

```text
一个 transfer worker
```

变成：

```text
一个 0->1 worker
一个 1->0 worker
```

这样变量只有一个，很容易看收益。

所以我对这轮的判断是：

> **2 chunk 是一个不错的方向；10线程目前看也正常。真正剩下的大瓶颈仍是单 transfer queue。**

下一步我会优先做 **2 chunk + upload/download 双 socket/双 worker**，而不是继续把 chunk 数切得更细。
