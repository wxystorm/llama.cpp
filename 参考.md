够，而且这次已经能比较明确地下结论了：**两条 return lane 不是主要瓶颈，真正明显的是 layer barrier。**

你这个 256-token Tensor block：

```text
total                 = 49.919 s
lane_reuse_wait       =  0.462 s
layer_barrier_wait    =  3.080 s
```

其中 lane reuse 一共发生了 196 次，但累计只等了 462 ms；反而 49 次 layer barrier 累计等了 3.08 s。

122-token 尾块更明显：

```text
total                 = 27.667 s
lane_reuse_wait       =  0.099 s
layer_barrier_wait    =  4.672 s
```

lane reuse 49 次只花了约 99 ms，而 layer barrier 49 次花了 **4.67 秒**。

所以整个 378-token prompt 两块合起来大约是：

```text
lane reuse stall  ≈ 0.561 s
layer barrier     ≈ 7.752 s
```

也就是说，即使把 `PREFILL_RETURN_LANES=2` 魔法般改成无限 lane，把所有 lane-reuse wait 全消掉，理论上也就省 **半秒左右**。不值得优先做 4 lanes。

反过来，layer barrier 已经接近 **7.8 秒**，这才值得继续研究。

而且你这里：

```text
tensor_layers = 50
layer_barrier_count = 49
```

非常吻合。当前方案是 `GPU 14层 + Tensor 50层`。 每个 macro 中基本就是：

```text
Tensor layer 14 完成
   ↓ 等本层所有 pending return
layer 15

Tensor layer 15 完成
   ↓ 等
layer 16

...
```

50 个 Tensor 层之间正好有大约 49 个层间边界。

### 这还解释了一个很重要的现象

256-token 块：

```text
barrier 3.08 s / 49
≈ 63 ms / layer
```

122-token 块：

```text
barrier 4.67 s / 49
≈ 95 ms / layer
```

尾块 token 更少，PC FFN 更快，但每层反而平均等得更久。

这意味着小 batch 下 PC 更容易“追上”手机 return path，于是到层尾时大量时间只能空等手机结果。

这和你的 `TENSOR_BREAKDOWN` 也一致：

```text
256:
PC FFN = 35.03 s
D2H    = 27.11 s
wait   =  4.82 s

122:
PC FFN = 14.84 s
D2H    = 13.86 s
wait   =  5.12 s
```



122-token 明明计算量少很多，`wait` 却没有跟着降，说明 tail pipeline utilization 很差。

---

所以我现在不会继续改 lane 数。

下一个真正有潜力的是：

```text
当前：

Layer L:
 chunk0
 chunk1
 chunk2
 ...
 wait all returns
 ↓
Layer L+1
```

变成类似：

```text
L.chunk0 完成
     ↓
L+1.chunk0 可以开始

同时：
L.chunk1 / chunk2
仍然继续在 PC/Phone 上运行
```

也就是**跨 Tensor layer 的 token-chunk pipeline**。

不过这里要谨慎，它比加 lane 大得多。下一层是 Attention，不是单纯 FFN，所以不能简单地说“chunk0 一回来就无条件执行下一层”。需要确认当前 prefill attention/KV 对 token chunk 的依赖是否允许按因果顺序流水。

在真的改 runtime 前，我建议再补**最后一个非常小的统计**：

```text
每次 layer_barrier：
当前 layer
wait_ms
正在 pending 的 lane 数
最后卡住的是 lane0 还是 lane1
```

例如：

```text
[PREFILL_LAYER_BARRIER]
layer=14
wait_ms=72.4
pending=2
last_lane=1

[PREFILL_LAYER_BARRIER]
layer=15
wait_ms=105.8
pending=1
last_lane=0
```

这不是为了再判断 lane 数，而是为了确认：

> 这 7.75 秒确实是在层尾等待 Phone→PC return，而不是 `wait_all_prefill_reduces()` 里面还混着别的 PC reduce/synchronize 成本。

如果大多数 barrier 都是 `pending=1`，那证据就非常强：**PC 端本层工作已经做完，只剩最后一个手机结果回来。**

所以答案是：**现在的输出已经足够否定“加更多 lane”这个方向；再加一个 per-layer barrier detail，就足够决定要不要开始做跨层 pipeline。**
