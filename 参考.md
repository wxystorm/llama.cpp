我看了你这次的新日志和新 `ggml-backend-meta.cpp`。这版已经把问题钉得更死了：

**你新加的 external handoff 代码确实编进去了，但运行时一次都没有触发。**

源码里已经有：

```cpp
[PHONE_EXIT_DBG]
```

以及 `external_backend_handoff` 的判断。

但这份运行日志里 **`PHONE_EXIT_DBG` 一次都没有出现**。

而且仍然只有：

```text
[META_LAYER_HANDOFF] layer=31 0->1 ...
[PHONE_BLOCK_FUSED] sg=128..159 layers=32..47
[META_PIPELINE] ... subgraphs=161
```

完全没有：

```text
[META_LAYER_HANDOFF] layer=47 1->0
```



所以这次不是“external handoff 做错了”，而是：

> **我们判断“Phone block 已经退出”的条件仍然没识别出这个边界。**

---

### 为什么 `phone_owner_exit` 没触发

你现在代码是：

```cpp
const bool phone_owner_exit =
    n_backends == 2 && active_count == 1 && active_backend == 1 &&
    (i + 1 >= backend_ctx->n_subgraphs ||
     !subgraph_will_execute_phone(i + 1));
```



而当前 Prefill：

```text
PHONE_BLOCK_FUSED sg=128..159 layers=32..47
META_PIPELINE subgraphs=161
```

也就是说：

```text
最后一个真正 PHONE_ONLY SG = 159

但 META 内部还有：
sg160
```



因此：

```cpp
159 + 1 >= 161
```

显然是 false。

你后来加的：

```cpp
const bool terminal_phone_exit =
    communication_sg + 1 >= backend_ctx->n_subgraphs &&
    subgraph_is_phone_only(communication_sg);
```

同样救不了，因为 159 后面确实还有 sg160。

而既然完全没有 `[PHONE_EXIT_DBG]`，说明这个：

```cpp
!subgraph_will_execute_phone(160)
```

也是 false。

也就是说：

```text
sg160
```

虽然已经不是 layer48 的正常 PHONE_ONLY 层了，但它内部仍然有 Phone COMPUTE 标志。

这很像 **外层 scheduler 为 META→CUDA 边界生成的桥接/尾部 SG**。

所以用：

```text
“下一个 META SG 是否还有 phone compute”
```

判断 Phone ownership 是否结束，现在不成立了。

---

## 还有一个非常强的旁证

你这次 Prefill 0~31 一共有：

```text
32 layers × 2 chunks = 64
```

个 Phone→PC FFN partial 回传。

而统计是：

```text
[META_COPY_SUM] 1->0 count=65
```



正好多了 **1 次**。

也就是说现在极大概率实际上发生的是：

```text
64 次 TS chunk Phone→PC

+

1 次 layer47 后面的 generic 1→0 copy
```

但因为没有：

```text
[META_LAYER_HANDOFF] layer=47 1->0
```

这第 65 次 copy 走的并不是：

```text
Phone FFN output
    +
residual
    ↓
完整 l_out-47
    ↓
Phone→PC
```

而只是普通：

```text
Phone terminal node
    ↓
generic copy
    ↓
PC
```

所以 **数据是传回来了，但 residual 没完成。**

这正好和现在所有现象一致。

而最终输出仍然明显没有恢复；这次首 token 已经变成：

```text
top[0]: token=315
...
of
```



---

# 现在不要再用 sg160 判断 ownership

这里应该改成一个更可靠的语义：

> **既然 `find_phone_block()` 已经明确识别出 PHONE_ONLY 连续块是 layers 32..47，那么这个 block 的末尾本身就是一个需要 materialize 完整 `l_out-47` 的边界。**

也就是：

```text
PHONE_ONLY block 完成
        ↓
强制完成最后一层 residual
        ↓
把完整结果 mirror 到 primary
        ↓
后面的 sg160 / CUDA scheduler 自己继续
```

不需要让 sg160 决定“Phone 是否结束”。

---

## 我建议这样改

### 1. 给 `specialized_communication()` 增加两个参数

现在：

```cpp
auto specialized_communication =
    [&](size_t i,
        bool & handled,
        bool & next_compute_complete) -> ggml_status {
```

改成：

```cpp
auto specialized_communication =
    [&](size_t i,
        bool & handled,
        bool & next_compute_complete,
        bool force_phone_block_exit,
        int force_phone_block_layer) -> ggml_status {
```

---

### 2. 修改 `phone_owner_exit`

把：

```cpp
const bool phone_owner_exit =
    n_backends == 2 && active_count == 1 && active_backend == 1 &&
    (i + 1 >= backend_ctx->n_subgraphs ||
     !subgraph_will_execute_phone(i + 1));
```

改成：

```cpp
const bool phone_owner_exit =
    n_backends == 2 &&
    active_count == 1 &&
    active_backend == 1 &&
    (force_phone_block_exit ||
     i + 1 >= backend_ctx->n_subgraphs ||
     !subgraph_will_execute_phone(i + 1));
```

这样：

```text
sg159
```

即使发现：

```text
sg160 phone_compute=1
```

也没关系。

我们已经由更高层语义知道：

```text
layer47 是 PHONE_ONLY block 的最后一层
```

---

## 3. 强制边界时直接使用已知 layer47

这一点很重要。

你现在：

```cpp
int layer = -1;

ggml_tensor * src_residual =
    (internal_pc_handoff || phone_owner_exit) ?
        find_recent_ffn_inp_layer(1, i, layer) :
        nullptr;
```

但 `find_recent_ffn_inp_layer()` 仍然是：

```cpp
std::sscanf(tensor->name, "ffn_inp-%d", &parsed_layer)
```

它仍然会把：

```text
ffn_inp-47 (view)
```

也识别成 layer47。

我们之前已经被这个坑过一次。

既然 `find_phone_block()` 已经知道：

```text
last_fused_layer = 47
```

这里根本没必要猜。

改成：

```cpp
int layer = force_phone_block_exit ?
    force_phone_block_layer : -1;

ggml_tensor * src_residual = nullptr;

if (internal_pc_handoff || phone_owner_exit) {
    if (force_phone_block_exit) {
        src_residual =
            find_recent_ffn_inp(1, i, layer);
    } else {
        src_residual =
            find_recent_ffn_inp_layer(1, i, layer);
    }
}
```

这样强制路径会调用你已经修好的精确版本：

```cpp
strcmp(tensor->name, expected) == 0
```

不会再捡到 `(view)`。

---

# 4. external handoff 不要 `find_layer_output(0, layer)`

这是我这次比较建议你改掉的一点。

你现在写的是：

```cpp
ggml_tensor * dst_l_out = internal_pc_handoff ?
    disable_layer_output_producers(0, i + 1, layer) :
    (phone_owner_exit ? find_layer_output(0, layer) : nullptr);
```



对于：

```text
Phone → external CUDA
```

其实我们已经知道 generic 路径原本会把：

```cpp
nodes[1]
```

copy 到：

```cpp
nodes[0]
```

而且现在那个额外的第65次 1→0 copy 就说明这条 bridge 本来就在工作。

所以最小改动应该是：

```cpp
ggml_tensor * dst_l_out = internal_pc_handoff ?
    disable_layer_output_producers(0, i + 1, layer) :
    (phone_owner_exit ? nodes[0] : nullptr);
```

也就是：

```text
原先：
raw phone node ───────→ nodes[0]

现在：
raw phone node
      +
residual
      ↓
complete phone node ─→ nodes[0]
```

这样 **完全保持 sg160 和外层 CUDA scheduler 现有的数据路径不变**。

我们只是把 generic copy 的源数据修正确。

这比：

```cpp
find_layer_output(0, 47)
```

更稳，因为不知道 sg160 到底持有哪一个 scheduler bridge tensor，而 `nodes[0]` 是 generic 路径已经证明会使用的正确目标。

---

## 5. 外层循环把 `last_fused_layer` 传进去

你现在这里：

```cpp
} else if (is_phone_only_sg) {
    size_t phone_block_end = i;
    int first_fused_layer = -1;
    int last_fused_layer = -1;

    phone_block_fused = find_phone_block(
        i,
        phone_block_end,
        first_fused_layer,
        last_fused_layer);
```



需要把 `last_fused_layer` 提到这个 `if` 外面。

例如循环开始：

```cpp
for (size_t i = 0; i < backend_ctx->n_subgraphs; i++) {
    size_t communication_sg = i;

    bool phone_block_fused = false;
    int phone_block_last_layer = -1;
```

Phone 分支：

```cpp
int first_fused_layer = -1;

phone_block_fused = find_phone_block(
    i,
    phone_block_end,
    first_fused_layer,
    phone_block_last_layer);
```

然后 communication：

```cpp
const bool force_phone_block_exit =
    phone_block_fused &&
    phone_block_last_layer >= 0;

const ggml_status specialized_status =
    specialized_communication(
        communication_sg,
        communication_complete,
        compute_complete,
        force_phone_block_exit,
        phone_block_last_layer);
```

---

# 最终关键部分应该类似这样

```cpp
const bool phone_owner_exit =
    n_backends == 2 &&
    active_count == 1 &&
    active_backend == 1 &&
    (force_phone_block_exit ||
     i + 1 >= backend_ctx->n_subgraphs ||
     !subgraph_will_execute_phone(i + 1));

if (active_count == 1) {
    handled = true;

    auto & bcj_src =
        backend_ctx->backend_configs[active_backend];

    const bool internal_pc_handoff =
        n_backends == 2 &&
        active_backend == 1 &&
        i + 1 < backend_ctx->n_subgraphs &&
        (subgraph_is_prefill_pc_only(i + 1) ||
         subgraph_is_decode_pc_only_norm(i + 1));

    int layer =
        force_phone_block_exit ?
            force_phone_block_layer : -1;

    ggml_tensor * src_residual = nullptr;

    if (internal_pc_handoff || phone_owner_exit) {
        src_residual =
            force_phone_block_exit ?
                find_recent_ffn_inp(1, i, layer) :
                find_recent_ffn_inp_layer(1, i, layer);
    }

    ggml_tensor * dst_l_out =
        internal_pc_handoff ?
            disable_layer_output_producers(
                0, i + 1, layer) :
        phone_owner_exit ?
            nodes[0] :
            nullptr;

    const bool external_backend_handoff =
        phone_owner_exit &&
        !internal_pc_handoff &&
        src_residual != nullptr &&
        dst_l_out != nullptr &&
        ggml_nbytes(nodes[1]) ==
            ggml_nbytes(src_residual) &&
        ggml_nbytes(nodes[1]) ==
            ggml_nbytes(dst_l_out);

    const bool layer_handoff_to_pc =
        internal_pc_handoff ||
        external_backend_handoff;
```

然后下面 ADD + copy 那部分基本保持你现在的代码。

---

### Debug 也建议改一下

```cpp
printf(
    "[PHONE_EXIT_DBG] sg=%zu layer=%d "
    "node=%s residual=%s "
    "next_pc=%d next_phone=%d "
    "internal=%d external=%d forced=%d\n",
    i,
    layer,
    nodes[1] ? nodes[1]->name : "(null)",
    src_residual ? src_residual->name : "(null)",
    i + 1 < backend_ctx->n_subgraphs ?
        (int) subgraph_will_execute_pc(i + 1) : -1,
    i + 1 < backend_ctx->n_subgraphs ?
        (int) subgraph_will_execute_phone(i + 1) : -1,
    (int) internal_pc_handoff,
    (int) external_backend_handoff,
    (int) force_phone_block_exit);
```

下一次 Prefill 我预计必须看到：

```text
[PHONE_BLOCK_FUSED]
sg=128..159 layers=32..47

[PHONE_EXIT_DBG]
sg=159
layer=47
next_pc=1
next_phone=1
internal=0
external=1
forced=1

[META_LAYER_HANDOFF]
layer=47 1->0 ...
```

这里即使：

```text
next_phone=1
```

也不用慌。

反而它正好能证明我们的判断：

> **sg160 是 scheduler bridge，而不是另一层真正的 PHONE_ONLY computation。**

Decode 对应应该看到：

```text
[PHONE_BLOCK_FUSED] sg=96..127 layers=32..47

[PHONE_EXIT_DBG]
sg=127 layer=47 ... forced=1 external=1

[META_LAYER_HANDOFF]
layer=47 1->0 tensor=... bytes=20480
```

Qwen hidden=5120，Decode 单 token F32 正好：

```text
5120 × 4 = 20480 bytes
```

所以这个也很好检查。

这次我建议**只改这一处强制 block-exit 语义，不再动 CUDA placement、sg160 或外层 scheduler**。现在 CUDA 48~53 / META 54~63 的放置已经正常，日志明确显示了这个布局。

下一轮只看三个标记就够：

```text
PHONE_EXIT_DBG layer=47 forced=1 external=1
META_LAYER_HANDOFF layer=47 1->0
首 token 恢复基线
```

如果前两个出来了但 token 仍错，那下一步就直接查 **sg160 对 handoff 后 `nodes[0]` 做了什么**，而不是再猜 ownership。
