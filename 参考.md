这组日志已经把范围缩得非常小了。**手机端 backend 本身几乎没有问题，慢的 150–340 ms 基本发生在“手机 RPC server 把响应交出去之后，到电脑 client 完整收到响应”这一段。**

先直接对齐两边的耗时。

| chunk |  bytes | 电脑 `src_tensor_get` | 手机 `server_get` | 手机 `response_send` |
| ----- | -----: | ------------------: | --------------: | -----------------: |
| 0     | 180224 |      **343.886 ms** |        0.203 ms |           0.636 ms |
| 1     | 180224 |      **156.962 ms** |        0.267 ms |           0.691 ms |
| 2     | 212992 |      **182.343 ms** |        0.950 ms |           3.921 ms |

手机内部更细的 backend get：

```text
chunk1:
response_alloc = 183 us
backend_get    = 33 us

chunk2:
response_alloc = 317 us
backend_get    = 506 us
```

所以非常明确：

```text
手机 backend tensor_get
       ↓
几十 ~ 几百 us

手机整个 server_get
       ↓
0.2 ~ 1 ms

手机 response_send()
       ↓
0.6 ~ 4 ms

--------------------------------
但电脑 tensor_get()
       ↓
157 ~ 344 ms
```

中间丢了两三个数量级的时间。

---

## 最重要的一点：`response_send_us` 很可能不等于“数据已经到电脑”

如果手机 server 做的是类似：

```cpp
send(socket, response.data(), response.size(), ...);
```

即使这个调用只花：

```text
636 us
```

它通常只能说明：

> 应用层成功把 180 KB 数据交给了手机内核的 socket/TCP send buffer。

并不意味着：

```text
180 KB 已经经过 Wi-Fi
        ↓
到达电脑
        ↓
电脑 TCP stack
        ↓
client recv()
        ↓
完整 response decode
```

都完成了。

可能实际是：

```text
Phone application
      |
      | response_send() 0.6 ms
      ▼
┌─────────────────────┐
│ kernel TCP send buf │
└─────────────────────┘
      |
      |        <<< 这里花 150~300 ms >>>
      |
      ▼
   Wi-Fi / TCP
      |
      ▼
PC kernel receive buf
      |
      ▼
RPC recv_exact()
      |
      ▼
src_tensor_get_end
```

所以现在服务器端这个：

```text
response_send_us=636
```

**不能证明网络下行只花了 636 μs。**

---

# 而你的数据呈现非常强的“方向不对称”

目前：

```text
电脑 → 手机
0->1 tensor_set
≈ 0.7~1 ms / 180 KB

手机 → 电脑
1->0 tensor_get
≈ 157~344 ms / 180 KB
```

这非常值得关注。

大约有效吞吐：

```text
PC -> phone:

180 KB / 0.7 ms
≈ 250 MB/s（这里也不能完全当纯网络带宽看）

phone -> PC:

180 KB / 157 ms
≈ 1.15 MB/s

最差：
180 KB / 344 ms
≈ 0.52 MB/s
```

完全不是正常的小幅 asymmetry。

所以你现在已经可以基本排除：

```text
❌ ggml ADD
❌ synchronize
❌ 手机 backend tensor_get kernel
❌ response allocation
```

真正剩下的是：

```text
RPC transport / socket / client receive
```

或者一个很重要的另一种可能：

```text
RPC client serialization / mutex contention
```

---

# 我现在会优先检查 client 的 `tensor_get()` 内部

你目前 client 只有：

```text
src_tensor_get_begin
        |
        | 150~340ms
        |
src_tensor_get_end
```

这还太粗。

下一步最好拆成：

```text
client_tensor_get_begin

rpc_mutex_wait_begin
rpc_mutex_wait_end

request_send_begin
request_send_end

response_header_recv_begin
response_header_recv_end

response_body_recv_begin
response_body_recv_end

deserialize_begin
deserialize_end

client_tensor_get_end
```

这一次 profiling 应该可以直接定案。

---

## 情况 1：卡在 `rpc_mutex_wait`

例如：

```text
src_tensor_get_begin
97 ms

mutex_wait_begin
97 ms

mutex_wait_end
400 ms

request_send
<1 ms

recv
<1 ms
```

那问题根本不是网络。

而是：

> **你现在 pipeline 开了两个 host thread，但 RPC client/connection 本身被一个全局 mutex 串行化了。**

这种情况在你的实现里特别值得怀疑，因为现在本来就在做：

```text
main thread:
compute(chunk N+1)

worker:
tensor_get(chunk N)
```

如果它们共同操作同一个 RPC client/socket：

```text
           ┌──────────── RPC connection ────────────┐
compute ---┤                                        │
transfer --┤                                        │
           └────────────────────────────────────────┘
```

RPC 层可能为了保证 request/response 不串包，本身有：

```cpp
std::mutex mutex;
```

这样你虽然创建了 pipeline：

```text
compute || transfer
```

到了 RPC 层却变成：

```text
compute
   ↓
transfer
```

或者部分序列化。

这个要重点查。

---

# 情况 2：request 很快，但 `response_header_recv` 卡很久

例如：

```text
request_send_end             100 ms

response_header_recv_begin   100 ms
response_header_recv_end     300 ms
```

而手机：

```text
request_recv      几十 us
server_get        几百 us
response_send     几百 us
```

那说明：

> server 已经处理了，但是 response 很久没真正到 client。

这时候才强烈指向：

```text
TCP / Wi-Fi / socket buffering / network
```

---

# 情况 3：header 很快，body recv 卡 150 ms

这个就最直接：

```text
response_header:
0.1 ms

response_body_recv:
180 KB
████████████████████
170 ms
```

那就是：

> **手机 → PC 的 payload 传输真的慢。**

这时再查：

```text
TCP retransmission
Wi-Fi power saving
手机 Wi-Fi uplink
socket send/receive buffer
TCP window
AP QoS
```

才是正确方向。

---

# 我现在尤其想知道 `response_send` 怎么测的

比如代码如果是：

```cpp
auto t0 = now();
send_data(sock, response.data(), response.size());
auto t1 = now();
```

要看 `send_data()` 内部是什么。

如果只是：

```cpp
send(fd, data, size, 0);
```

或者循环：

```cpp
while (sent < size) {
    sent += send(...);
}
```

即使循环完成，也只是说明：

> 所有数据都成功 copy 到 kernel TCP send buffer。

还是不代表 peer 已经收到了。

比如 180KB，而手机 socket send buffer 假设有几百 KB：

```text
App:
180 KB
  │
  │ 636 us
  ▼
TCP send buffer:
[180 KB...................]

send() 返回

然后内核自己慢慢发：
██████████████████████ 200 ms
```

完全能出现你现在这种现象。

---

# 还有一个很有意思的证据

看 chunk0：

电脑：

```text
src_tensor_get_begin = 97.884 ms
src_tensor_get_end   = 441.770 ms

≈343.9 ms
```

手机：

```text
request_recv = 12 us
server_get   = 203 us
send         = 636 us
```

手机应用层总共甚至：

```text
< 1 ms
```

这意味着剩余：

```text
~343 ms
```

不可能藏在：

```text
backend_get()
```

里面。

要么发生在：

```text
client 请求真正到 server 之前
```

要么发生在：

```text
server send() 返回之后
```

而你现在 server 只有**duration**，没有和 client 可以对应的绝对时间，因此还区分不了这两个。

---

# 我建议加一个非常小但特别有价值的信息：RPC sequence ID

例如：

```text
GET seq=137 chunk=0
```

PC：

```text
seq=137
client_get_begin
lock_acquired
request_send_begin
request_send_end
response_recv_begin
response_recv_end
```

手机：

```text
seq=137
request_handler_begin
server_get_begin
server_get_end
send_begin
send_end
```

这样即使两台机器 monotonic clock 不同步，也可以靠每边内部 duration 把整条 RPC 分解出来。

甚至不需要同步 PC/手机时钟。

---

# 还可以做一个非常简单的实验来快速判断网络 vs RPC contention

暂时完全关掉 pipeline。

不要：

```text
compute || tensor_get
```

只做一个已经准备好的 remote tensor：

```cpp
for (int i = 0; i < 20; ++i) {
    tensor_get(remote_tensor, local);
}
```

大小固定：

```text
180224 bytes
```

如果得到：

```text
standalone GET:
~1 ms
```

而 pipeline：

```text
GET:
150~340 ms
```

那我会优先怀疑：

> **RPC client contention / 同一个 connection 上 compute 和 GET 互相阻塞。**

如果 standalone 仍然：

```text
150~300 ms
```

那么基本就可以确定：

> **手机 → PC RPC/TCP 数据路径本身有问题。**

这个实验的信息量非常高，而且改代码很少。

---

## 你现在整个 pipeline 的性能模型其实已经很清楚了

当前大约：

```text
Compute:
~14-18 ms   （除了第一次 warmup 94ms）

PC -> phone SET:
~0.7 ms

phone -> PC GET:
~157-344 ms      <<< 绝对瓶颈

ADD:
~0.3-0.4 ms
```

所以 pipeline 虽然确实做到了：

```text
GET(chunk N)
      ||
compute(chunk N+1)
```

但是：

```text
Tcompute ≈ 15 ms
Tget     ≈ 150-300 ms
```

最多只能隐藏十几毫秒。

**现在不用再优化 pipeline 调度本身了。下一刀直接切进 RPC client `tensor_get()`：锁等待、request send、response header recv、response body recv 四段分别打点。** 那份 trace 一出来，我们基本就能确定究竟是 RPC 锁串行化还是手机→电脑的 TCP/Wi-Fi 下行路径。
