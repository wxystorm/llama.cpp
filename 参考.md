那这个结果反而更有价值：**单线程仍然存在同样的差异，就基本可以把“多线程任务划分/线程间归约顺序”排除了。**

现在更像是：

```text
同一个 backend1
同一个权重
同一个 token 数据
同一个线程数 = 1

N=35  → 路径 A
N=9   → 路径 A'，结果恰好 bitwise 一致
N=11  → 路径 A'，结果恰好 bitwise 一致
N=8   → 路径 B，出现 ~1e-8 差异
N=12  → 路径 B，出现 ~1e-8 差异
```

所以现在优先怀疑的应该是 **`MUL_MAT` 根据 `ne[1]` 选择了不同 kernel / GEMM 路径 / packing 方式**，而不是线程调度。

特别是你的 down 权重如果是量化类型，实际计算一般不是单纯的：

```cpp
for each token:
    dot(weight, x)
```

而可能根据 `N` 决定走类似：

```text
GEMV
vs
GEMM
vs
不同 tile 大小的 GEMM
vs
不同批量 vec_dot kernel
```

即使只有一个线程，F32 累加顺序也可能不同。

你现在最好不要继续靠尺寸猜了，直接去 **手机端实际执行的 `GGML_OP_MUL_MAT` 分支里打印它到底选了什么路径**。

例如在 `ggml_compute_forward_mul_mat` 附近，把这些东西打印出来：

```cpp
fprintf(stderr,
    "[MUL_MAT_PATH] "
    "name=%s "
    "src0_type=%s "
    "src1_type=%s "
    "src0=[%lld,%lld,%lld,%lld] "
    "src1=[%lld,%lld,%lld,%lld] "
    "dst=[%lld,%lld,%lld,%lld] "
    "nth=%d\n",
    dst->name,
    ggml_type_name(src0->type),
    ggml_type_name(src1->type),

    (long long) src0->ne[0],
    (long long) src0->ne[1],
    (long long) src0->ne[2],
    (long long) src0->ne[3],

    (long long) src1->ne[0],
    (long long) src1->ne[1],
    (long long) src1->ne[2],
    (long long) src1->ne[3],

    (long long) dst->ne[0],
    (long long) dst->ne[1],
    (long long) dst->ne[2],
    (long long) dst->ne[3],

    params->nth);
```

但更重要的不是这个 shape 日志，而是把函数里面类似下面这些**分支条件**也打印：

```cpp
if (use_gemm) {
    fprintf(stderr, "[MUL_MAT_PATH] GEMM\n");
} else {
    fprintf(stderr, "[MUL_MAT_PATH] VEC_DOT\n");
}
```

以及如果有：

```cpp
ggml_gemm(...)
ggml_gemv(...)
vec_dot(...)
```

分别打印：

```cpp
fprintf(stderr, "[MUL_MAT_PATH] ggml_gemm\n");
fprintf(stderr, "[MUL_MAT_PATH] ggml_gemv\n");
fprintf(stderr, "[MUL_MAT_PATH] vec_dot\n");
```

你真正想比较的是这几组：

```text
N=35
N=8
N=9
N=11
N=12
```

最终很可能看到类似：

```text
35 → GEMM kernel X
8  → GEMM kernel Y
9  → kernel X
11 → kernel X
12 → kernel Y
```

或者虽然都叫 GEMM，但内部 tile 不一样。

---

另外，**之前那个 `1 chunk × 35` 实验现在尤其重要**。

也就是仍然使用你自己新建的：

```text
cgraph_prefix
+
chunk.cgraph
```

但：

```cpp
chunk.n_tokens = 35;
chunk.token_start = 0;
```

完全不切：

```text
35
```

然后比较 backend1 的：

```text
原 cgraph_main 的 MUL_MAT N=35
vs
chunk.cgraph 的 MUL_MAT N=35
```

如果：

```text
EXACT
```

那几乎可以正式排除：

```text
chunk tensor descriptor
chunk graph
UID
RPC graph cache
output data pointer
```

剩下的就是：

> **改变 `ne[1]` 本身导致 backend1 的 MUL_MAT 数值路径改变。**

这个实验我建议你一定补一下。

---

还有一个值得注意的地方：你现在已经发现：

```text
8  → different
9  → exact
11 → exact
12 → different
```

所以这不像简单的：

```text
N < 某阈值 → GEMV
N >= 某阈值 → GEMM
```

更像某种 **tile/block 对齐规则**。

比如内部可能有类似：

```text
nr = 某个 SIMD/microkernel 一次处理的行数
```

然后：

```text
N % 某个块尺寸
```

会影响最后一部分怎么处理。

因此如果你想快速画出规律，可以直接扫：

```text
N = 1..16
```

但这里有个问题：你的 chunk 总数必须加起来仍然是 35，所以不方便单独测试所有 N。

最干净的办法是每次只关注第一个 chunk：

```text
N + (35-N)
```

然后只比较 `[0,N)`：

```text
1+34
2+33
3+32
...
16+19
```

最后做成：

```text
N    exact       max diff
1    ?
2    ?
3    ?
...
8    false       2.6e-8
9    true        0
10   ?
11   true        0
12   false       8.9e-8
...
```

这样再去对照 `ggml-cpu` 的 kernel 分支，会比盲查源码容易很多。

目前证据已经很强：

```text
单线程仍然不同
+
差异跟 chunk N 移动
+
backend0 exact、backend1 only
+
AllReduce 前就已经出现
+
误差始终只有 ~1e-8~1e-7
```

所以我现在会把 **backend1 的 shape-dependent MUL_MAT kernel/packing 路径** 放在最高优先级，而不再把它当成 chunk 地址或 AllReduce 正确性 bug。
