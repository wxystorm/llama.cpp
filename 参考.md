对，确认按“正确方案”做这个较大的改造。你这段判断是对的：**`GPU → CPU → TENSOR` 不能靠在现有 StageQ 后面再塞一个 `cpu_ready_q` 就实现。**

现在的 StageQ 本质还是：

```text
一张完整 ubatch graph
        ↓ scheduler split
PRE | GPU | POST
            ↑
      CPU-only + Tensor
      都还在这个 POST/Meta 里面
```

现有 Stage Queue 只是把 scheduler split 划成 `PRE/GPU/POST`，POST 仍然是 GPU 后面的整个剩余图。 所以如果 planner 给出：

```text
G=14
C=15
T=49
```

那 layer 14 的 CPU-only 和 layer 15～63 的 Tensor 仍然属于同一个 Meta graph，外面的队列根本没有一个真正的“layer14 输出边界”可以拿出来切成 `XC=32`。

因此下一步应该引入真正的 **stage-boundary graph**。

最小范围我赞成你给的：

```text
模型：
Qwen2 / Qwen2.5 dense

拓扑：
G=14
C=15
T=49
P=0

第一组：
XG=128
XC=32
XT=32
```

先不做 Phone、不做 MoE、不做任意拓扑、不追求 CPU 与 Tensor overlap。先把：

```text
GPU(128)
→ CPU(32)
→ Tensor(Attention32, FFN XT32)
```

的语义彻底做对。

---

### 具体应该怎么泛化

核心是把原来的：

```cpp
build_full_graph(ubatch)
```

逐步抽象成类似：

```cpp
build_stage_graph(stage_desc, stage_batch)
```

其中 stage 描述至少包含：

```cpp
struct llama_stage_desc {
    int layer_begin;
    int layer_end;

    enum kind {
        GPU_PREFIX,
        CPU_MIDDLE,
        TENSOR_SUFFIX,
    } kind;

    bool build_embeddings;
    bool build_output_head;
};
```

Qwen2 builder 原来大致是：

```text
token/embedding
↓
layer 0
↓
layer 1
...
↓
layer 63
↓
output_norm
↓
lm_head
```

要允许变成三张图：

```text
Graph G:
token/embedding
↓
layer 0 ... 13
↓
boundary_G
```

```text
Graph C:
boundary_G_view[span]
↓
layer 14
↓
boundary_C
```

```text
Graph T:
boundary_C
↓
layer 15 ... 63
↓
output_norm
↓
lm_head（只有需要 output 的 span）
```

这里的 `boundary_G` 和 `boundary_C` 都必须是**真正可外部注入/导出的 activation tensor**，而不是 scheduler 内部一个临时节点。

---

### 边界位置必须严格选对

以 transformer residual stream 为准：

```text
layer13 完整结束后的 l_out
          ↑
       GPU输出

layer14 norm/attention/FFN...
layer14 完整结束后的 l_out
          ↑
       CPU输出

layer15 norm...
          ↑
       Tensor输入
```

不能在：

```text
attn_out
ffn_norm
ffn_down
```

这些层内部位置随便切。

所以 stage ABI 可以简单理解成：

```text
输入/输出都是 [n_embd, n_tokens] residual stream
```

这样以后扩层数最干净。

---

## GPU → CPU 的 XC 是怎么实现的

假设：

```text
XG=128
XC=32
```

GPU graph 一次仍然运行：

```text
tokens [0,128)
```

得到：

```text
boundary_G:
[n_embd,128]
```

然后不重新执行 GPU，而是对这个 boundary 建 view：

```text
C0 = boundary_G[:,  0:32 ]
C1 = boundary_G[:, 32:64 ]
C2 = boundary_G[:, 64:96 ]
C3 = boundary_G[:, 96:128]
```

分别作为四次 CPU-stage graph 的外部输入：

```text
CPU(0..31)
CPU(32..63)
CPU(64..95)
CPU(96..127)
```

所以：

```text
XG=128
```

没有被破坏。

这就是为什么不能简单：

```cpp
runtime_ubatch = XC;
```

因为那样会直接变成：

```text
GPU32 → CPU32 → Tensor32
```

XG 就失去意义了。

---

## 但真正麻烦的是 position / KV

activation 切成 32 只是最表面的部分。

CPU span0：

```text
global tokens [0,32)
```

CPU span1：

```text
global tokens [32,64)
```

它的 Attention **不能认为自己又从 position 0 开始**。

所以 stage task 至少要携带：

```cpp
struct stage_span {
    uint32_t macro_id;

    uint32_t token_begin;   // ubatch/global macro 内偏移
    uint32_t token_count;

    // 对应原 ubatch 的 metadata view
    positions;
    seq_ids;
    output mapping;
};
```

比如 CPU 第二个 span：

```text
token_begin = 32
token_count = 32
```

它的 layer14 Attention 必须看到正确的：

```text
pos[32..63]
```

同时 layer14 KV cache 应当已经包含：

```text
CPU span0 写入的 K/V
```

于是执行顺序必须是：

```text
CPU0 → 更新 layer14 KV
CPU1 → 使用 CPU0 的 KV
CPU2
CPU3
```

Tensor 也是一样。

Tensor0 运行 layer15～63：

```text
pos 0..31
```

Tensor1 再运行：

```text
pos 32..63
```

它自己的 layer15～63 KV 必须能够看到 Tensor0 已经写入的对应 KV。

这也是为什么第一版我建议：

```text
CPU0 → T0
CPU1 → T1
CPU2 → T2
CPU3 → T3
```

先保持 CPU/Tensor downstream 串行。

不要第一版就做：

```text
CPU1 || T0
```

因为两者都在使用 Meta/CPU，而且 KV/memory context 生命周期也更复杂。

---

## `llm_build_context` 最好怎么改

我不建议复制三份 Qwen2 builder。

更好的方式是在 Qwen2 graph builder 中增加类似：

```cpp
int il_begin;
int il_end;

ggml_tensor * stage_input;
bool build_input_embedding;
bool build_output;
```

原来：

```cpp
ggml_tensor * cur = inp_embd;

for (int il = 0; il < n_layer; ++il) {
    ...
}

cur = build_output_norm(cur);
cur = build_output(cur);
```

逐步变成：

```cpp
ggml_tensor * cur;

if (il_begin == 0) {
    cur = build_model_input(...);
} else {
    cur = stage_input;
}

for (int il = il_begin; il < il_end; ++il) {
    cur = build_layer(il, cur, ...);
}

if (il_end == n_layer && build_output) {
    cur = build_output_norm(cur);
    cur = build_output_head(cur);
}
```

那么：

```text
GPU:
il_begin=0
il_end=14
stage_input=null

CPU:
il_begin=14
il_end=15
stage_input=boundary_G_span

Tensor:
il_begin=15
il_end=64
stage_input=boundary_C
```

同一套 layer builder 就可以复用。

---

## output/logits 也必须重新映射

比如你的 prompt 是 378 token：

```text
128 + 128 + 122
```

最后一个 GPU macro：

```text
[256,378)
```

再按 `XC=32`：

```text
32 + 32 + 32 + 26
```

真正需要 first-token logits 的通常只有最后那个：

```text
26-token span
```

而原来 output index 是整个 ubatch/global batch 下的索引。

所以进入 Tensor suffix graph 时必须把：

```text
global output token index
```

映射成：

```text
stage-local index
```

例如：

```text
global macro offset = 96
final token         = 121

local index = 121 - 96 = 25
```

前面三个 CPU/Tensor span：

```text
build_output_head = false
```

最后一个：

```text
build_output_head = true
output_local_index = 25
```

否则非常容易再次出现以前那种：

```text
计算都正常
但最终 logits 不对应正确 token
```

的问题。

---

### 第一版 runtime 队列反而可以保持很简单

等 graph boundary 真正建立以后，队列才有意义：

```text
GPU macro queue
      ↓
GPU boundary ready
      ↓ split XC spans
CPU span queue
      ↓
CPU boundary ready
      ↓
Tensor queue
```

第一版甚至可以只保持：

```text
G1 || (C0→T0→C1→T1→C2→T2→C3→T3)
```

也就是只保留你现在已经验证过的：

```text
GPU 与 downstream overlap
```

不新增 CPU/Tensor 并发。

等 correctness 过了再讨论：

```text
C(i+1) || T(i)
```

是否值得以及资源是否允许。

---

## 我建议具体分 5 个提交做

1. **Qwen2 builder 支持 layer range + external hidden input**，先不接 StageQ。单独构造 `14→15`、`15→64` graph，确认 tensor shape 和节点正确。

2. **增加 GPU/CPU boundary buffer 和 token view**，让 `GPU128 → CPU32×4` 能运行，但 Tensor 暂时可以直接接着 serial。

3. **让 Tensor suffix 接受 CPU boundary**，验证：

   ```text
   Attention=32
   FFN XT=32
   ```

   并验证 KV/position 连续。

4. **处理 output-index remap + final span**，用相同 prompt 对比完整 serial logits，最好直接比较 first-token top logits。

5. **最后把现有 StageQ dispatcher 接进来**，形成：

   ```text
   G0
      G1 || C0→T0→C1→T1→...
   ```

这样任何一步错了，都能知道是 builder、boundary、KV、logits mapping 还是调度器，而不会一次把五种变化混在一起。

所以可以确认范围：

> **先只支持 Qwen2/Qwen2.5 dense，`G=14,C=15,T=49,P=0`，实现真正的 stage-boundary graph；不采用“全局 ubatch=XC”的过渡方案。**

这条路线虽然改动明显大一些，但做完之后 `GPU→CPU→TENSOR→PHONE` 的架构基础才是真正成立的。Phone 后面只是在这个 stage-boundary/token-span 框架上再增加一个 stage，而不是再重写一次整个流水。
