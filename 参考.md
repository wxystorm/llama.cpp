这个 breakdown 很有价值，**现在瓶颈基本抓到了，而且和之前猜的“单纯 XT 越大越好”不一样。**

这次实际 Prefill 从 `4:59.140` 到 `6:56.071`，378 token 大约 **116.9 s，也就是约 3.23 tok/s**。但 predictor 对 **512 token** 都只预测 `91.9 s`。所以进入 `XG=256 / XT=64` 这片新搜索空间后，预测模型已经出现了明显低估，不是几%的误差。

更关键的是两次 Tensor：

```text
ub0 256 tokens:
total   66.60 s
attn     8.66 s
pc_ffn  40.24 s
h2d      1.18 s
phone    0.03 s
d2h     55.71 s
wait    14.78 s

ub1 122 tokens:
total   50.09 s
attn     4.16 s
pc_ffn  17.58 s
h2d      0.14 s
phone    0.006 s
d2h     45.60 s
wait    23.22 s
```

这里有两个非常重要的结论。

### 1. `d2h=55s` 不能理解成“网络传 55 秒”

这些字段显然互相重叠，因为：

```text
55.7 + 40.2 + 8.7 + ...
```

已经远大于 `total=66.6s`。

更重要的是，你现在 predictor 里的 `snapshot_phone_to_pc` profile 测的是：

```cpp
tensor_set(phone)
rpc_fence(phone);       // 数据已经 ready

snapshot_arm(...);

begin
snapshot_read/copy
end
```

也就是说，**profile 测的是“snapshot 已经 ready 以后取回来要多久”**。

而 runtime 里 return worker 请求 snapshot 时，结果可能还没算好，需要等：

```cpp
slot.cv.wait(... READY ...)
```

所以你现在 runtime 的 `d2h` 很可能实际上包含：

```text
等待手机结果 READY
+
真正 Phone→PC 传输
```

而 predictor 用的只是后半段。

这正好解释了：

```text
预测 512 tokens = 91.9 s
实际 378 tokens = 116.9 s
```

你的 Tensor predictor 虽然把 `phone_ms` 和 snapshot transfer 分开模拟了，snapshot transfer 本身用的确实就是 `snapshot_phone_to_pc` profile。

### 2. `phone=33 ms` 基本不能当作“手机实际只算了 33 ms”

这个数太小了。

50 个 Tensor 层，256 token，XT=64：

```text
4 个 XT chunk / layer
× 50 layer
= 200 个 Phone FFN shard
```

不太可能整个手机计算只需要 33 ms。

更可能是你当前这个 `phone` timer 主要量到了：

```text
RPC submit / graph submit
```

真正远端完成的时间最终被：

```text
snapshot READY wait
```

吸收到 `d2h` / `wait` 里面了。

---

## 现在第一件事：把 D2H 拆成两个时间

不要继续先优化 scheduler。

直接去 RPC server 的 snapshot read 处理位置，在：

```cpp
slot.cv.wait(lock, [&]() {
    return slot.seq == request.seq &&
           slot.state == READY;
});
```

前后分别打时间。

要得到：

```text
snapshot_ready_wait_ms
snapshot_send_ms
```

逻辑类似：

```cpp
const int64_t t0 = ggml_time_us();

slot.cv.wait(lock, [&]() {
    return slot.seq == request.seq &&
           slot.state == READY;
});

const int64_t t1 = ggml_time_us();

// 真正发送 snapshot payload
...
const int64_t t2 = ggml_time_us();

ready_wait_ms += (t1 - t0) / 1000.0;
send_ms       += (t2 - t1) / 1000.0;
```

不用每个 chunk 打日志，累计后打印：

```text
[SNAPSHOT_BREAKDOWN]
requests=200
ready_wait=...
send=...
bytes=...
```

最好 lane0/lane1 分别统计。

这样下一步就彻底明确了：

```text
如果 ready_wait 很大
→ 手机结果生产不够快 / pipeline 没盖住计算

如果 send 很大
→ 真的是 Phone→PC 返回链路慢

如果两者都不大
→ host return-worker / 同步逻辑还有额外等待
```

---

## 第二个我非常建议马上做的实验：固定 topology 做 2×2 A/B

先不要让 autoplan 改 T/P/C/G/R，否则变量太多。

固定：

```text
T=50
P=0
C=14
G=14
R=0.759
```

只测试：

```text
A: XG=128 XT=32
B: XG=128 XT=64
C: XG=256 XT=32
D: XG=256 XT=64   ← 你现在这组
```

XC/XP 这组 topology 没有 CPU-only 和 Phone-only，实际上无所谓。

这样一次就能判断到底是：

```text
XT 32 → 64
```

出了问题，还是：

```text
Tensor outer macro 128 → 256
```

出了问题。

我现在**比较怀疑 XT=64**。

原因很明显。

当前：

```text
ub0 = 256 tokens, XT=64
→ 4 chunks/layer

ub1 = 122 tokens, XT=64
→ 2 chunks/layer
```

第二个 macro 的 token 数只有第一个的 48%，但：

```text
66.6s → 50.1s
```

只快了 25%。

而 `d2h`：

```text
55.7s → 45.6s
```

甚至只下降约 18%。

这明显不是一个纯带宽型传输的行为，更像：

> chunk 数减少以后流水线深度下降，return worker 经常提前到达，然后在那里等手机结果 READY。

如果确实如此，**XT=32 反而可能比 XT=64 好**，因为更细粒度可以把：

```text
PC FFN
||
Phone FFN
||
return
```

流水铺得更满。

所以我现在反而不建议直接继续上 `XT=128`。

先把 32/64 对照跑出来。

---

还有一个架构上的信息现在也越来越明显。

Tensor-Split 每一层 Phone 分支最后都必须返回一个：

```text
[n_embd × tokens]
```

partial Down output。

这个返回大小基本**不随 R 减少**。

也就是说，即便：

```text
R = 0.9
```

手机只算很少一部分 FFN，最后还是得返回同样大小的 hidden partial。

所以如果最终发现真正的 `snapshot_send` 本身就很贵，那么：

```text
Tensor 50 层
```

这个 topology 天然会很吃亏，因为它每一层都必须：

```text
PC → Phone
Phone compute
Phone → PC
```

那时候解决方案就不是继续调 R，而是 planner 应该倾向：

```text
更多 CPU-only 层
或
连续 Phone-only 层
更少 Tensor-Split 层
```

因为 Phone-only 连续区域不用**每层**把 partial hidden 拉回来。

但现在还不能下这个结论，因为你当前的 `d2h` 很可能主要是 **READY wait，而不是实际网络传输**。

---

所以现在我会把路线改成：

```text
当前：
GPU→Tensor runtime                ✓
XG256 / XT64                     ✓ 能跑
breakdown                         ✓

下一步：
① snapshot ready_wait / send 分离   ← 最重要
② 固定 topology 跑 128/256 × 32/64
③ 判断 XT64 为什么反而出现巨大等待

然后：
ready_wait 大 → 优化 Tensor 内部流水/Phone producer
send 大       → 优化 return transport / 减少 Tensor layers
```

**暂时先别做 CPU||Phone 那种更大的并发改造。** 现在这份日志已经指出了更大的收益点：Tensor-Split 的 **Phone→PC 返回链路/结果就绪链路**。把这 45–55 秒到底是什么拆清楚，下一步很可能能直接找到大块性能。
