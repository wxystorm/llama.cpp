这组日志一出来，问题就明显多了。**我现在会把之前“model-loader 的 `max_n_tensors` 不够”降到第二嫌疑，第一嫌疑改成：`ubatch=512 + X=24` 产生的 chunk 数超过了 Meta backend 现在固定的 `compute_headroom=16`。**

你的日志关键是：

```text
ubatch=512
...
selected ... X=24
```

因此真正 runtime 的 Tensor-Split Prefill 会把一个 512-token ubatch 切成：

$$
\left\lceil\frac{512}{24}\right\rceil = 22
$$

也就是：

```text
24 × 21 + 8

总共 22 chunks
```

而你当前 `ggml-backend-meta.cpp` 里面明确还是：

```cpp
constexpr size_t compute_headroom = 16;
// Maximum number of views per statically allocated tensor
// that can be created between evals.
```

然后 compute context 的 metadata pool 只按：

```cpp
compute_headroom * ggml_get_mem_size(ctx)
```

来预留。

这和你的现象非常吻合。

---

## 为什么会报 `GGML_ASSERT(obj_new)`

Meta backend 对一个 Meta tensor 会维护：

```text
static tensor
    ↓
compute 时产生各种 view / split tensor
```

而你的 Prefill token chunk 又会为每一个 chunk 建：

```text
prefill_ffn_norm_chunk_0
prefill_ffn_down_chunk_0
...

prefill_ffn_norm_chunk_1
prefill_ffn_down_chunk_1
...
```

当前 Meta backend 本来就专门准备了两个 rotating compute containers 来容纳这些外部 view。源码注释也明确说这些 compute containers 是用来装 evaluation 期间产生的 external views。

可是容量假设是：

```text
最多约 16 个 view / static tensor
```

你现在一口气变成：

```text
22 chunks
```

非常容易把这个 ggml context 的 object pool 填满，随后：

```text
ggml_new_object(...)
    ↓
obj_new == nullptr
    ↓
GGML_ASSERT(obj_new)
```

所以这里的 `obj_new` 更像是：

> **ggml metadata/object context 空间耗尽**

而不是：

> PC RAM 真的剩 0 了。

---

# 而且你当前 predictor 还有一个明显的不一致

Planner 显示：

```text
reference_tokens=128
ubatch=512
X=24
```

枚举 X 时，你现在实际上主要用 `128` 来检查 chunk：

```text
128 / 24
→ 24,24,24,24,24,8
→ 6 chunks
```

这当然完全没问题。

但真实 runtime：

```text
512 / 24
→ 22 chunks
```

所以 predictor 认为：

```text
6 chunks
```

而 Meta runtime 最坏实际上要承受：

```text
22 chunks
```

这就是一个我们必须补上的约束。

---

## 最简单的验证方法

我建议你**先不要动 model-loader**。

直接把 `ggml-backend-meta.cpp`：

```cpp
constexpr size_t compute_headroom = 16;
```

临时改成：

```cpp
constexpr size_t compute_headroom = 64;
```

重新编译 PC 端再跑一次。

如果这样模型直接能继续加载/初始化，基本就可以确定是这里。

这个修改只是扩大：

```text
Meta compute tensor metadata context
```

并不是给每个 tensor 多分 4 倍实际权重内存。

当然会多一些 host metadata 内存，但和 32B 权重相比很小。

---

# 不过最终我不建议硬编码成 64

真正应该让 predictor 和 runtime 对齐。

现在对每一个 X 都应该计算：

```cpp
const int runtime_tokens =
    constraints.target_ubatch_tokens > 0
        ? constraints.target_ubatch_tokens
        : profile.probe_tokens;

const int max_chunks =
    (runtime_tokens + chunk_tokens - 1) / chunk_tokens;
```

当前就是：

```text
runtime_tokens = 512
X = 24

max_chunks = 22
```

如果 Meta backend 暂时仍限制 16：

```cpp
if (max_chunks > 16) {
    continue;
}
```

那么 `X=24` 就不能被选。

对于：

```text
ubatch = 512
```

要求：

$$
\left\lceil\frac{512}{X}\right\rceil\le16
$$

所以必须：

$$
X\ge32
$$

例如：

```text
X=32

512 / 32 = 16 chunks
```

刚好符合当前 headroom。

---

## 但长期最好不是限制 X≥32

因为这违背我们前面正在做的设计：

```text
GPU 大块
CPU/TS 小块
Phone 小块
```

如果最佳 TS chunk 明明是：

```text
X_ts=8
```

你不能因为外层 llama `ubatch=512`，就被迫：

```text
X_ts >= 32
```

这不合理。

最终结构应该是：

```text
llama.cpp upper ubatch = 512

        ↓

Hybrid macro block
例如 CPU stage 一次接 64

        ↓

Tensor Split chunk
8 + 8 + 8 + 8 + 8 + 8 + 8 + 8
```

而不是：

```text
512
↓
直接切成 64 个 X=8
```

这样 Meta graph 也不会一下产生 64 个 chunk views。

这其实正好和我们上一轮讨论的：

```text
X_gpu
X_cpu
X_ts
X_phone
```

多粒度设计连上了。

---

# 另外还有一个必须处理的风险

你这次选出来：

```text
gpu_budget   = 4,794,089,472
gpu_required = 4,785,430,528
```

只剩：

```text
8,658,944 bytes
≈ 8.26 MiB
```

这个太极限了。

PC：

```text
pc_budget   = 10,401,711,308
pc_required = 10,327,086,814
```

还有约：

```text
71 MiB
```

也已经相当贴边。

Phone 倒是很宽松。

所以虽然**这次 `obj_new` 不像 GPU OOM**，但是就算把 `obj_new` 修好，我也不太信这个 GPU plan 能长期稳定。

你现在 GPU：

```text
0.90 free
+
512 MiB runtime reserve
```

实际最后只有 8 MiB planner margin。

建议至少把：

```cpp
LLAMA_HYBRID_GPU_MEMORY_FRACTION = 0.90;
```

降到：

```cpp
0.85
```

或者保留 0.90，但：

```text
gpu_runtime_reserve
512 MiB → 768 MiB / 1 GiB
```

我倾向后者，因为 reserve 的语义更明确。

---

## 所以现在先做两件事

第一步，**验证这个 assert**：

```cpp
// ggml-backend-meta.cpp

constexpr size_t compute_headroom = 64;
```

再跑一次。

第二步，如果确认解决，就不要永久停在 64，而是正式改成：

```text
planner:
    actual_chunks = ceil(actual_ubatch / X)

Meta:
    compute headroom >= runtime 最大 chunk 数 + margin
```

或者更进一步直接接我们准备做的：

```text
X_cpu / X_ts
```

两级 chunk，避免拿整个 512 直接切几十份。

我现在认为这条比我上一条说的 `llama-model-loader max_n_tensors` **更符合你这次新日志**，尤其是 `ubatch=512、X=24 → 22 chunks` 和源码硬编码 `compute_headroom=16` 这两个数字刚好撞上。
