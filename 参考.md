这次已经越过了前面的 QKV reshape，新的问题出在 **Meta backend 的 split-state 扫描**。

`ggml-backend-meta.cpp:1253` 对应的就是：

```cpp
static ggml_backend_meta_split_state ggml_backend_meta_get_split_state(
        const ggml_tensor * tensor, bool assume_sync) {
    GGML_ASSERT(ggml_backend_buffer_is_meta(tensor->buffer));
    ...
}
```

也就是说：**Meta 正在查询某个 tensor 的 split state，但这个 tensor 实际上不是 Meta buffer。**

### 为什么以前没问题，DAG 一开就触发

DAG 后，我们把一整个 Tensor layer 拆成很多 `(layer, chunk)`，并且引入了：

```cpp
hidden = ggml_view_2d(... inpL ...);
pos_chunk = ggml_view_1d(... inp_pos ...);
```

以及每个 chunk 独立的 KV/mask input。 

这些 graph input / view 里面会有 **host/simple backend tensor**。

而你现有 Meta 后端的“延迟归约/子图合并”扫描代码里有这种逻辑：

```cpp
if (ggml_backend_meta_get_split_state(
        next->src[s], false).axis !=
        GGML_BACKEND_SPLIT_AXIS_MIRRORED) {
    return i;
}
```

它在调用前**没有检查 `next->src[s]->buffer` 到底是不是 Meta buffer**。

后面 `skip_unrelated()` 里也一样：

```cpp
if (ggml_backend_meta_get_split_state(
        next->src[s], false).axis !=
        GGML_BACKEND_SPLIT_AXIS_MIRRORED) {
    safe = false;
    break;
}
```

旧 graph 的节点排列刚好满足这些代码的假设；现在 DAG 把不同 layer/chunk 交错排列以后，这个扫描会碰到 host-backed input/view，于是：

```text
host/simple tensor
      ↓
ggml_backend_meta_get_split_state()
      ↓
要求 buffer 必须是 Meta
      ↓
ASSERT
```

所以这次**不是 DAG 依赖本身错了，也不是手机计算错了，而是 Meta 原来的 graph-scanning heuristic 对新 DAG topology 不够健壮。**

---

### 修法我建议用保守方式

不要让非 Meta tensor 假装成 `MIRRORED`。

而是加一个安全判断：

```cpp
static bool ggml_backend_meta_tensor_is_mirrored(
        const ggml_tensor * tensor,
        bool assume_sync) {
    if (tensor == nullptr ||
        tensor->buffer == nullptr ||
        !ggml_backend_buffer_is_meta(tensor->buffer)) {
        return false;
    }

    return ggml_backend_meta_get_split_state(
        tensor, assume_sync).axis ==
        GGML_BACKEND_SPLIT_AXIS_MIRRORED;
}
```

然后原来的：

```cpp
if (ggml_backend_meta_get_split_state(
        next->src[s], false).axis !=
        GGML_BACKEND_SPLIT_AXIS_MIRRORED) {
    return i;
}
```

改成：

```cpp
if (!ggml_backend_meta_tensor_is_mirrored(
        next->src[s], false)) {
    return i;
}
```

`skip_unrelated()` 同理：

```cpp
if (!ggml_backend_meta_tensor_is_mirrored(
        next->src[s], false)) {
    safe = false;
    break;
}
```

对于：

```cpp
ggml_backend_meta_get_split_state(next, false)
```

也先判断：

```cpp
if (next->buffer == nullptr ||
    !ggml_backend_buffer_is_meta(next->buffer)) {
    break; // 或 return i，取决于所在扫描
}
```

这样处理的语义是：

> 遇到不属于 Meta 的 tensor，就停止“延迟归约/跨节点合并”优化，不去猜它的 split state。

这只是让 Meta **少做一点 graph fusion/delay optimization**，不会破坏计算正确性，比较适合作为 DAG 第一版。

还有几处类似的未保护调用：

```cpp
next->src[1]
next->src[2]
```

用于 `ADD_ID` / `MUL` 的 delayed-reduce 链，也应该一起加 guard，否则修完第一处很可能下一处继续 assert。现有代码确实有多处直接调用。

所以这次不要只在 `1253` 把 assert 去掉——**那会把真正的类型错误吞掉**。应该修调用方，让 delayed-subgraph scanner 只对 Meta tensor 查询 split state。

你这两次报错其实是在逐步进入 DAG：

```text
第一次：
DAG graph build
→ QKV token shape 错
→ 已修

这次：
DAG graph build 成功更多
→ 进入 Meta graph partition
→ 遇到 host/simple tensor
→ split-state scanner assert
```

下一步就应该修这个 Meta scanner，而不是动 DAG 调度算法本身。
