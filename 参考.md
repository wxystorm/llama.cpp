对，现在这几份文件一补齐，**RPC 本地加载的完整调用链已经能串起来了**。而且结合你刚才那个“只有 3:1 明显异常”的现象，我现在会把排查重点进一步收窄到：**Meta 端生成的 2D 分片参数是否在 3:1 时指向了正确的 GGUF 字节区域**，而不是先怀疑 `rpc-server.cpp` 里的文件读取本身。

完整路径实际上是：

```text
llama-model-loader.cpp
load_all_data()
        ↓
ggml_backend_tensor_set(meta tensor)
        ↓
ggml-backend-meta.cpp
ggml_backend_meta_buffer_set_tensor()
        ↓
计算每个 simple backend 的：
src_offset
dst_offset
copy_size
n_copies
src_stride
dst_stride
        ↓
ggml_backend_meta_try_set_local_file_2d()
        ↓
ggml-rpc.cpp
ggml_backend_rpc_set_tensor_from_local_file_2d()
        ↓
RPC_CMD_SET_TENSOR_FROM_LOCAL_FILE
        ↓
rpc_server::set_tensor_from_local_file()
        ↓
local_tensor_source.read()
        ↓
rpc-server.cpp
llama_rpc_read_local_tensor()
        ↓
llama-model-loader.cpp
read_local_tensor()
        ↓
GGUF file
```

`rpc-server.cpp` 这里确实只是负责把 `llama_model_loader` 包成回调：创建 `local_loader`，设置 `get_info/read`，然后传给 `ggml_backend_rpc_start_server_ex()`。

实际读文件的回调也很直接，而且你加了 mutex：

```cpp
std::lock_guard<std::mutex> lock(ctx->mutex);

return ctx->loader->read_local_tensor(
    name,
    dest,
    static_cast<size_t>(tensor_offset),
    size);
```

这意味着 `seek + read` 的共享文件位置已经被保护起来了；至少从这里看，我不认为“本地 GGUF 多线程 seek 互相覆盖”是当前 3:1 问题的首要嫌疑。

而 `llama_model_loader::read_local_tensor()` 最终做的是：

```cpp
file_read_offset =
    info.file_offset + tensor_offset;

file->seek(file_read_offset, SEEK_SET);
file->read_raw(dest, dest_size);
```

并且前面检查了：

```text
tensor_offset + dest_size <= tensor byte_size
```

因此只要传进来的 `tensor_offset` 正确，它就是在 GGUF 中对应 tensor 的正确相对位置读取。

---

真正**随 3:1 / 1:1 / 1:3 改变**的地方，恰恰是在 Meta。

你这里对于 split tensor 计算的是：

```cpp
src_offset = offset + offset_j;
dst_offset = simple_offset;

copy_size  = chunk_size_j;
n_copies   = i_stop - i_start;

src_stride = chunk_size_full;
dst_stride = chunk_size_j;
```

然后把这六个量传给 RPC 本地加载。

其中最关键的是：

```text
offset_j
chunk_size_j
```

因为 tensor split 一变：

```text
3:1
1:1
1:3
```

它们就变。

例如假设某个 axis-0 权重的一整行是：

```text
|---------------- full row ----------------|
```

`3:1` 理论上是：

```text
|--------- PC 75% ---------|-- RPC 25% --|
                            ^
                            offset_j
```

那么手机本地 GGUF 应该读取：

```text
每一行：
full_row_start + offset_j
长度 chunk_size_rpc
```

下一行：

```text
full_row_start + full_row_stride + offset_j
```

所以 Meta 给出的：

```text
src_offset = offset + offset_j
src_stride = chunk_size_full
copy_size  = chunk_size_j
```

**从设计上是对的**。

Server 接到以后也基本没有再解释这个布局，只是机械地：

```cpp
src_i = request.src_offset
      + i * request.src_stride;

dst_i = request.dst_offset
      + i * request.dst_stride;
```

然后：

```cpp
local_tensor_source.read(... src_i ... staging ...);

ggml_backend_tensor_set(
    tensor,
    staging.data(),
    dst_i,
    staging.size());
```



所以整个链路可以理解成：

> **Meta 决定“读 GGUF 哪些字节”，ggml-rpc/server/loader 基本只是照单执行。**

这也是为什么，我现在最想查的是 Meta 产生的那组六元组，而不是继续查锁。

---

### 我建议不要先“禁掉 local loading”，直接做字节级 A/B

这个测试会比看 logits 强很多。

因为 PC 这边在：

```cpp
ggml_backend_meta_buffer_set_tensor(...)
```

里其实同时拥有完整原始 tensor 的：

```cpp
data
```

所以对于准备发送给 RPC 的某个 local-file request：

```cpp
src_offset
copy_size
n_copies
src_stride
```

PC 可以直接从 `data` 计算出**理论上手机应该从本地 GGUF 读出的字节 hash**。

比如：

```cpp
uint64_t h = HASH_INIT;

for (uint64_t i = 0; i < n_copies; ++i) {
    const uint8_t * p =
        (const uint8_t *) data
        + src_offset
        + i * src_stride;

    hash_update(h, p, copy_size);
}
```

注意这里 `src_offset` 在 Meta 里的语义本来就是“相对于完整 tensor 起点”，这正好和你 RPC 接口定义一致。你的 `ggml-rpc.h` 里也明确把 `src_offset/dst_offset/copy_size/n_copies/src_stride/dst_stride` 定义成了这套 2D copy 参数。

然后手机 `rpc_server::set_tensor_from_local_file()` 每次：

```cpp
local_tensor_source.read(... staging ...)
```

之后，对 `staging` 做完全相同的 hash。

最终日志打：

```text
[LOCAL_CHECK]
name=blk.0.ffn_down.weight
src_offset=...
copy_size=...
n_copies=...
src_stride=...
client_hash=...
server_hash=...
```

当然 client/server 在不同进程，最简单是两边分别打印相同的：

```text
name
src_offset
copy index
hash
```

再人工对。

---

这个测试一旦跑 `3:1`，结果会非常有决定性。

如果：

```text
PC expected hash == Phone local GGUF hash
```

所有权重全部一致，那么：

> **本地加载链路基本可以洗清。**

这时候再回头查 pipeline / reduction / backend 并发。

但如果出现：

```text
例如：

blk.0.ffn_down.weight
copy 0

PC expected = 0x12345678
Phone local = 0xA8B9CDEF
```

那就直接实锤：

> **手机从 GGUF 读的不是 PC 原始 tensor 中同一段数据。**

之后我们只需要围绕：

```text
src_offset
src_stride
copy_size
```

查，不需要再碰 scheduler。

---

而且我建议**先只检查几个 FFN 权重**，尤其就是你 pipeline 实际命中的：

```text
ffn_down / ffn_out
```

不需要把所有 7B 权重 hash 一遍。

因为你的 pipeline 匹配的本身就是最后一个 `ffn_out-* MUL_MAT` partial reduction。Meta 代码只对符合 `PARTIAL + ffn_out + MUL_MAT + shape` 的图启用这个 chunk pipeline。

可以先打印：

```text
tensor name
type
full ne[0]
simple PC ne[0]
simple RPC ne[0]

chunk_size_full
PC chunk_size_j
RPC chunk_size_j

RPC offset_j
src_offset
src_stride
copy_size
```

分别跑：

```text
3:1
1:1
1:3
```

我尤其想看 `3:1` 的：

```text
RPC offset_j
```

是不是恰好：

```text
PC simple tensor 每行的真实字节数
```

---

另外，我看完你新上传的 `llama-model-loader.cpp` 后，有一点现在可以明确：

`get_local_tensor_info()` 保存的是完整 GGUF tensor 的：

```cpp
info.file_offset = weight.offs;
info.byte_size   = ggml_nbytes(weight.tensor);
```



所以 server 使用的 `src_offset` **必须是相对于“完整 tensor”的字节偏移**，不能是相对于 RPC simple tensor 的偏移。

而你现在 Meta 里确实想做的就是：

```cpp
src_offset = offset + offset_j;
```

其中 `offset_j` 是前面 backend 分片在**完整 tensor 一行里**占掉的字节数。

因此这个地方的语义链是对齐的。

换句话说，目前我还没有从源码里看到一个“一眼确定就是错”的 local-loading bug。

但 **3:1 唯一异常** 又非常像 boundary-specific 问题，所以现在最有效的方法不是继续推测，而是：

> **直接证明 RPC 本地加载的每一段 bytes 和 PC 原始 GGUF 中理论上的同一段 bytes 是否完全一致。**

这个 hash test 做完，本地加载这一整条链基本就能一次性定罪或者排除。
