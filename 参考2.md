可以。既然你已经验证 `LLAMA_PREFILL_CHUNKS>1` 后结果正确，**`llama(6).cpp` 这一步先不要再动了**。现在它已经提供了我们需要的两个明确边界：

```text
prefill_ffn_norm_chunk_N-L
prefill_ffn_down_chunk_N-L
```

而且每个 chunk 独立 `build_ffn()`，最后沿 token 维 `axis=1` concat，这正是后面 Meta 流水需要的图结构。

下一步就进入真正的 **Meta Prefill pipeline**。

## 先修一个现在代码里的隐患

当前 Meta 判断 Decode chunk 用的是：

```cpp
std::strstr(node->name, "ffn_down_chunk")
```

包括 `get_ffn_down_boundary_node()` 也是这么判断的。

但现在你的名字：

```text
prefill_ffn_down_chunk_0-0
```

**同样包含 `"ffn_down_chunk"`**。

所以它可能被误认为 Decode 的：

```text
ffn_down_chunk_0-0
```

这一步必须先分开。

在 `ggml-backend-meta.cpp` 前面加：

```cpp
static bool ggml_backend_meta_parse_decode_ffn_chunk(
        const char * name,
        int & chunk,
        int & layer) {
    return std::sscanf(
        name,
        "ffn_down_chunk_%d-%d",
        &chunk,
        &layer) == 2;
}

static bool ggml_backend_meta_parse_prefill_norm_chunk(
        const char * name,
        int & chunk,
        int & layer) {
    return std::sscanf(
        name,
        "prefill_ffn_norm_chunk_%d-%d",
        &chunk,
        &layer) == 2;
}

static bool ggml_backend_meta_parse_prefill_down_chunk(
        const char * name,
        int & chunk,
        int & layer) {
    return std::sscanf(
        name,
        "prefill_ffn_down_chunk_%d-%d",
        &chunk,
        &layer) == 2;
}
```

然后这个：

```cpp
if (std::strstr(node->name, "ffn_down_chunk") == nullptr ||
        !(node->flags & GGML_TENSOR_FLAG_COMPUTE)) {
    return nullptr;
}
```

改成：

```cpp
int chunk = -1;
int layer = -1;

if (!ggml_backend_meta_parse_decode_ffn_chunk(
        node->name, chunk, layer) ||
        !(node->flags & GGML_TENSOR_FLAG_COMPUTE)) {
    return nullptr;
}
```

这样 Decode snapshot pipeline 就只认真正的 Decode chunk。

---

# 然后建立我们想要的 SG

现在 Meta 只会因为：

```cpp
split_state.axis == GGML_BACKEND_SPLIT_AXIS_PARTIAL
```

或者 `result_norm` 建立 SG。

所以虽然 `llama.cpp` 已经有：

```text
prefill_ffn_norm_chunk_0
```

Meta 并不会自动在那里断开。

我们要强制得到：

```text
SG0:
PC Attention
PC residual
PC ffn_norm
PC prefill_ffn_norm_chunk_0
        ↓
     PC -> Phone C0

SG1:
PC FFN(C0)
Phone FFN(C0)
        ↓
prefill_ffn_down_chunk_0

SG2:
PC prefill_ffn_norm_chunk_1
        ↓
     PC -> Phone C1

SG3:
PC FFN(C1)
Phone FFN(C1)
        ↓
prefill_ffn_down_chunk_1

SG4:
PC prefill_ffn_norm_chunk_2
        ↓
     PC -> Phone C2

SG5:
PC FFN(C2)
Phone FFN(C2)
        ↓
prefill_ffn_down_chunk_2
```

这正是我们要的。

在：

```cpp
const bool new_subgraph =
```

这里增加：

```cpp
int prefill_chunk = -1;
int prefill_layer = -1;

const bool is_prefill_norm_chunk =
    ggml_backend_meta_parse_prefill_norm_chunk(
        node->name,
        prefill_chunk,
        prefill_layer);

const bool new_subgraph =
    i + 1 == cgraph->n_nodes ||
    split_state.axis == GGML_BACKEND_SPLIT_AXIS_PARTIAL ||
    is_decode_result_norm ||
    is_prefill_norm_chunk;
```

---

## 同时必须阻止 `get_i_delayed()` 吞掉下一个 chunk

现在这里：

```cpp
auto skip_unrelated = [&]() {
    while (id + 1 < cgraph->n_nodes) {
        ggml_tensor * next = cgraph->nodes[id+1];

        if (ggml_backend_meta_get_split_state(
                next, false).axis !=
                GGML_BACKEND_SPLIT_AXIS_MIRRORED) {
            break;
        }

        ...
        id++;
    }
};
```



`prefill_ffn_norm_chunk_1` 是一个 VIEW/MIRRORED tensor，所以它很可能被这里当成“无关 mirrored node”跳过去。

在最前面加：

```cpp
int next_prefill_chunk = -1;
int next_prefill_layer = -1;

if (ggml_backend_meta_parse_prefill_norm_chunk(
        next->name,
        next_prefill_chunk,
        next_prefill_layer)) {
    break;
}
```

这样：

```text
down_chunk_0
```

不会把：

```text
norm_chunk_1
```

吞进同一个 SG。

---

# Attention 的通信边界也要推迟到 chunk0

这是另一个关键点。

当前 Prefill 原本是：

```text
attn_out [4096,35]
        ↓
      Phone
```

如果不改这里，我们虽然切了 FFN chunk，前面还是会先发送整个 `[4096,35]`，那就失去意义了。

现在 `get_i_delayed()` 已经有 Decode 专用：

```cpp
const bool decode_pc_only_attn =
    std::strncmp(node->name, "attn_out-", 9) == 0 &&
    node->ne[1] == 1 &&
    attn_active_count == 1 &&
    attn_active_backend == 0;
```

然后一路追依赖到 `ffn_norm`。

我们扩成：

```cpp
const bool pc_only_attn =
    std::strncmp(node->name, "attn_out-", 9) == 0 &&
    attn_active_count == 1 &&
    attn_active_backend == 0;

const bool decode_pc_only_attn =
    pc_only_attn &&
    node->ne[1] == 1;

const bool prefill_pc_only_attn =
    pc_only_attn &&
    node->ne[1] > 1;
```

然后搜索终点时：

```cpp
if (decode_pc_only_attn &&
        std::strncmp(next->name, "ffn_norm-", 9) == 0) {
    return depends_on_delayed ? ii : i;
}

int chunk = -1;
int layer = -1;

if (prefill_pc_only_attn &&
        ggml_backend_meta_parse_prefill_norm_chunk(
            next->name, chunk, layer) &&
        chunk == 0) {
    return depends_on_delayed ? ii : i;
}
```

这样 Prefill 第一段就从：

```text
Attention
↓
attn_out
====通信====
```

变成：

```text
Attention
residual
ffn_norm
view chunk0
↓
prefill_ffn_norm_chunk_0
====通信====
```

也就是我们之前说的：

```text
PC 不再发整个 attn_out
PC 只开始发 C0
```

---

# 然后把所有 norm chunk 设置成 PC-only

chunk0 可以通过上面的 `attn_out -> chunk0` propagation 自动把 Phone COMPUTE 清掉。

但：

```text
chunk1
chunk2
...
```

是独立 view，所以要主动关掉 Phone COMPUTE。

在目前处理 `result_norm` compute flag 的附近加：

```cpp
for (int i = 0; i < cgraph->n_nodes; ++i) {
    ggml_tensor * node = cgraph->nodes[i];

    int chunk = -1;
    int layer = -1;

    if (!ggml_backend_meta_parse_prefill_norm_chunk(
            node->name,
            chunk,
            layer)) {
        continue;
    }

    for (size_t j = 1; j < n_backends; ++j) {
        backend_ctx->backend_configs[j]
            .nodes[i]->flags &=
            ~GGML_TENSOR_FLAG_COMPUTE;
    }
}
```

那么 norm SG 的最后节点就会变成：

```text
PC:    COMPUTE=1
Phone: COMPUTE=0
```

于是它天然就是：

```text
PC -> Phone
```

输入边界。

---

# 下一步才是 scheduler 核心

现在老 `feat/rpc-local-model` 中其实已经有我们需要的骨架：

```cpp
if (pending_input_task != 0) {
    const size_t independent_backend =
        1 - pending_input_backend;

    compute_workers.start(independent_backend, i);

    transfer_worker->wait(pending_input_task);

    compute_workers.start(pending_input_backend, i);

    ...
}
```

也就是**先让 PC 开算，Phone 输入到齐以后再启动 Phone**。

我们重新把这个概念放回当前 Meta，但只给 Prefill 用。

我建议状态写成：

```cpp
uint64_t pending_prefill_input_task = 0;
int pending_prefill_input_layer = -1;
int pending_prefill_input_chunk = -1;

uint64_t pending_prefill_reduce_task = 0;
int pending_prefill_reduce_layer = -1;
```

### 遇到 `prefill_ffn_norm_chunk`

不要像现在 `active_count == 1` 那样同步 copy。

当前 active_count==1 是直接：

```cpp
ggml_backend_tensor_copy_async(...)
```

然后函数返回。

Prefill 改成：

```cpp
pending_prefill_input_task =
    backend_ctx->transfer_worker->enqueue(
        [&, src, dst, src_backend, dst_backend, i]
        (uint64_t) -> ggml_status {

            const int64_t copy_start_us =
                ggml_time_us();

            ggml_backend_tensor_copy_async(
                src_backend,
                dst_backend,
                src,
                dst);

            const int64_t copy_us =
                ggml_time_us() - copy_start_us;

            reduce_copy_wait_us += copy_us;
            record_meta_copy(
                i, 0, 1, src, copy_us);

            return GGML_STATUS_SUCCESS;
        });
```

**不等它。**

---

# 下一个 FFN chunk：PC 立刻开算

比如刚刚发送：

```text
C0 [4096,11]
```

进入下一个 `prefill_ffn_down_chunk_0` SG：

```cpp
compute_workers.start(0, i);    // PC 马上算 C0

const ggml_status input_status =
    backend_ctx->transfer_worker->wait(
        pending_prefill_input_task);

pending_prefill_input_task = 0;

if (input_status != GGML_STATUS_SUCCESS) {
    compute_workers.wait(0);
    return input_status;
}

compute_workers.start(1, i);    // C0 到 Phone，Phone 开算

const ggml_status pc_status =
    compute_workers.wait(0);

const ggml_status phone_status =
    compute_workers.wait(1);
```

于是：

```text
network:
C0 >>>>>>>>>>>>>>>

PC:
   █████████ FFN(C0)

Phone:
                 █████ FFN(C0)
```

这才开始实现你最开始想要的那个 overlap。

---

# Phone 的 down 结果也不能同步等

`prefill_ffn_down_chunk_0` 算完以后，不 allreduce 回两边。

只：

```text
Phone partial C0 -> PC
PC partial C0 + Phone partial C0
```

然后将这个操作再次扔进 `transfer_worker`：

```cpp
pending_prefill_reduce_task =
    backend_ctx->transfer_worker->enqueue(...);
```

内部第一版可以先用：

```cpp
rpc_fence(phone);
Phone -> PC GET;
PC ADD;
```

而且 GET 时设置当前已有的 `set_stage_ready`，让它走独立 TRANSFER socket。

当前 RPC/Meta 已经具备这一套机制，不需要现在继续改 RPC。当前 Decode 的 Phone→PC 路径也是 worker 中做 fence/snapshot、copy 和 PC ADD。

然后**主线程不要等 C0 返回**。

直接进入：

```text
norm_chunk_1
```

再准备 C1。

最终就形成：

```text
时间 ──────────────────────────────────────────────>

Input worker:
       send C0            send C1            send C2

PC:
          FFN C0 ─────→ FFN C1 ─────→ FFN C2
             │             │             │

Phone:
                FFN C0        FFN C1        FFN C2
                   │             │             │

Return:
                   D0 >>>        D1 >>>        D2 >>>

PC ADD:
                         R0            R1           R2
```

不过第一版我们**只用现在这个单 `transfer_worker`**。

所以真实情况更可能：

```text
worker:
send C0
      ↓
return D0
      ↓
send C1
      ↓
return D1
      ↓
send C2
      ↓
return D2
```

但重要的是：

```text
PC FFN C1
```

可以在：

```text
return D0 + send C1
```

期间执行。

这已经实现了你说的：

> 手机 C0 还没有回来，电脑开始处理下一个 chunk。

而且这样有个很大的优点：**现在先不搞两个 transfer worker，不会引入 COMPUTE socket 并发写入的竞争。**

---

## 层尾才真正等待

假设 3 chunks。

C0/C1 的 Phone 返回都可以挂着。

但是准备进入下一层 Attention 之前：

```text
concat(C0,C1,C2)
residual
Layer N+1 Attention
```

必须保证 C0/C1/C2 全部已经 reduce 到 PC。

所以在离开当前 layer chunk 序列时：

```cpp
if (pending_prefill_reduce_task != 0) {
    const ggml_status status =
        backend_ctx->transfer_worker->wait(
            pending_prefill_reduce_task);

    pending_prefill_reduce_task = 0;

    if (status != GGML_STATUS_SUCCESS) {
        return status;
    }
}
```

因为 worker 是 FIFO：

```text
D0
D1
D2
```

都在同一队列。

所以等最后一个 task，就代表之前的全完成了。

然后：

```text
PC concat
PC residual
PC Attention
PC ffn_norm
PC norm_chunk0(next layer)
```

再开始下一层。

也就是目前先做：

```text
层内流水
```

不做：

```text
跨层流水
```

这个边界非常合适。

---

### 改完以后，35 tokens / 3 chunks 我预期日志形态

应该变成类似：

```text
[PREFILL_INPUT] layer=0 chunk=0
tensor=prefill_ffn_norm_chunk_0-0
ne=[4096,11]

[PREFILL_REDUCE] layer=0 chunk=0
tensor=prefill_ffn_down_chunk_0-0
ne=[4096,11]

[PREFILL_INPUT] layer=0 chunk=1
ne=[4096,12]

[PREFILL_REDUCE] layer=0 chunk=1
ne=[4096,12]

[PREFILL_INPUT] layer=0 chunk=2
ne=[4096,12]

[PREFILL_REDUCE] layer=0 chunk=2
ne=[4096,12]
```

通信统计大概从以前每层：

```text
PC -> Phone:
attn_out       [4096,35]
Wdown partial  [4096,35]

Phone -> PC:
Wdown partial  [4096,35]
```

变成：

```text
PC -> Phone:
norm C0 + C1 + C2
= 总计 [4096,35]

Phone -> PC:
down C0 + C1 + C2
= 总计 [4096,35]
```

所以 **0→1 总字节大约直接减半**，同时又把 `[4096,35]` 大传输拆开，允许 PC FFN 插进去。

这里还有一个必须一起改的地方：当前 Decode snapshot 的 `get_ffn_down_boundary_node()` 和 `is_ffn_down_chunk` 都是 substring 判断，必须先改成精确 Decode parser，否则 Prefill chunk 会误闯 Decode pipeline。当前源码确实存在这个问题。

所以现在的修改范围可以控制在 **`ggml-backend-meta.cpp` 一个文件**。第一版也**不用动 `ggml-rpc.cpp`**。等这一版结果正确以后，再把单 worker 升级成 input/output 双 worker，让 `send C1` 和 `return D0` 真正全双工重叠。
