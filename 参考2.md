这次双 worker 已经**真正生效了**，而且效果比前一版明显。

最直接的证据就在 layer0：

```text
C0:
[META_COPY] C0 0->1  0.676 ms
[PREFILL_SNAPSHOT_ARM] C0

C1:
[PREFILL_INPUT] C1
[META_COPY] C1 0->1  0.650 ms
[PREFILL_SNAPSHOT_ARM] C1

然后才：
[META_COPY] C0 1->0 172.930 ms
[META_COPY] C1 1->0  57.649 ms
```

也就是说 **C0 还在 Phone→PC 回传的时候，C1 已经完成 PC→Phone 上传并启动了后续流程**。之前单 worker 时，C1 的实际 `0->1` 要等 C0 回传结束才发生；现在这个串行点已经没了。

而且 C1 的 snapshot GET 只有 `57.649 ms`，这也很有意义：C1 的 Phone 计算/等待 READY 很大一部分已经藏在 C0 的 `172.930 ms` 回传期间了。换句话说，现在已经出现我们想要的：

```text
PC                Phone                  network

C0 FFN            C0 FFN
                     │
                     └──── C0 return ──────────────┐
                                                  │
C1 FFN ─send C1→   C1 FFN                         │
                     │                            │
                     └──── C1 snapshot ready      │
                                                  ↓
                                             C0完成
                                             C1直接回
```

统计上也对应：

```text
compute = 1711.188 ms
PC backend = 1698.878 ms
Phone backend = 145.640 ms
```

前一版 `compute` 还是 `4520 ms` 左右；现在 `1711 ms` 几乎已经贴着 PC 自己的 `1699 ms`，说明**主调度计算路径基本不再因为 input/output 共用 worker 被卡住**。不过这不等于整次 Prefill 只有 1.7 秒，因为异步 snapshot 等待主要记在 `copy_wait`，层间 barrier 仍然存在。

## 下一步现在非常清楚：消掉 C1 → C2 之间这个“假依赖”

你注意 layer0：

```text
chunk1:
[PREFILL_SNAPSHOT_ARM] sg=3 ...

chunk2 norm SG:
[PREFILL_PHONE_NODE]
    node_37 op=CONCAT ne=[4096,23]

然后：
C0 return 172.930 ms
C1 return 57.649 ms

最后才：
[PREFILL_INPUT] chunk=2
```



因此现在流水是：

```text
C0 compute ───────┐
                  ├── return C0 ──────┐
C1 compute ───────┘                   │
                  └── return C1 ──────┤
                                      ↓
                               CONCAT(C0,C1)
                                      ↓
                                  C2 compute
```

但是 **C2 本身其实根本不依赖 `CONCAT(C0,C1)`**。

C2 的输入是：

```text
prefill_ffn_norm_chunk_2
```

它只是当前完整 `ffn_norm` 的 token view。

真正依赖 C0/C1 的是最终 FFN 输出重新拼起来的时候：

```text
down C0
down C1
down C2
   ↓
concat
   ↓
residual
```

现在之所以 `CONCAT(C0,C1)` 被拓扑排序放到了 C2 前面，是 graph builder 当前用了**左折叠 concat**：

```cpp
cur = down_chunks[0];

for (int i = 1; i < n_prefill_chunks; ++i) {
    cur = ggml_concat(ctx0, cur, down_chunks[i], 1);
}
```

3 chunks 等价于：

```text
tmp = concat(C0, C1)
out = concat(tmp, C2)
```

所以 DFS/topological order 很自然变成：

```text
C0
C1
concat(C0,C1)   ← 就卡在这里
C2
concat(...,C2)
```

### 下一刀建议只改 graph builder：把 concat 改成右折叠

改成：

```cpp
GGML_ASSERT(!down_chunks.empty());

cur = down_chunks.back();

for (int i = (int) down_chunks.size() - 2; i >= 0; --i) {
    cur = ggml_concat(
        ctx0,
        down_chunks[i],
        cur,
        1);
}
```

3 chunks 就变成：

```text
tmp = concat(C1, C2)
out = concat(C0, tmp)
```

最终 token 顺序仍然完全是：

```text
[C0, C1, C2]
```

但是拓扑顺序会倾向变成：

```text
C0
C1
C2
concat(C1,C2)
concat(C0,...)
```

这正是我们需要的。

---

### 为什么这一步比继续改 Meta scheduler 更干净

现在 Meta 有：

```cpp
prefill_norm_sg_has_prework(i)
```

只要 chunk2 SG 前面出现：

```text
CONCAT
```

它就正确地认为：

```text
不能越过前一个 pending reduce
```

因此当前 scheduler 没错。

真正的问题是 graph builder 把一个**和 C2 无依赖关系的 concat**排到了 C2 前面。

改右折叠以后，理想日志应该直接从：

```text
[PREFILL_NORM_SG]
chunk=2
first=node_37
last=prefill_ffn_norm_chunk_2
nodes=2
```

变成：

```text
[PREFILL_NORM_SG]
chunk=2
first=prefill_ffn_norm_chunk_2
last=prefill_ffn_norm_chunk_2
nodes=1
```

于是你现有的：

```cpp
!prefill_norm_sg_has_prework(i)
```

会自动得到：

```text
true
```

C2 就可以像 C1 一样继续流水，Meta 这里暂时不用再改。

---

## 改完我们想看到的 layer0

现在：

```text
send C1
↓
C0 return
↓
C1 return
↓
send C2
```

下一版应该接近：

```text
send C1
send/compute C2
        │
        ├──────── C0 return
        │
        └──────── C1 return
                         ↓
                     C2 return
                         ↓
                concat C1+C2
                concat C0+...
                         ↓
                     next layer
```

有一个细节不用担心：snapshot 目前只有两个 slot，而 C0/C1/C2 会是：

```text
C0 -> slot 1
C1 -> slot 0
C2 -> slot 1
```

server 在填一个 snapshot slot 前本来就会等：

```cpp
slot.state == FREE
```

然后才进入 `FILLING`。

而 GET 发送结束后才把 slot 恢复：

```cpp
slot.state = rpc_snapshot_state::FREE;
slot.cv.notify_all();
```



所以 C2 复用 slot1 时，如果 C0 还没发送完，它会自然等待 C0 释放 slot；**不会覆盖 C0 数据**。第一版先不用为了这个把 snapshot 扩成 3 slots。

因此我建议下一步就只改这一处：

```text
Prefill down concat:
左折叠
    ↓
右折叠
```

`ggml-backend-meta.cpp` 暂时不碰。

如果成功，最关键的验证不是先看速度，而是看：

```text
chunk2 norm SG: nodes=1
```

以及 **C2 的 `0->1` 出现在 C0/C1 `1->0` 完成之前**。这一步一旦成立，3 个 token chunk 的层内 pipeline 就基本完整了。
