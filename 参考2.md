对，那就把 **`LLAMA_PREFILL_CHUNKS=2` 定死**，不再碰 1 chunk。这个变量已经验证完了：**14/21 的两段流水确实是当前最快方案。**

接下来我建议不再改层布局，也先不做 Phone-only。下一刀直接针对 **Prefill 的 `GRAPH_COMPUTE` 元数据膨胀**。

你现在 RPC 的 `add_tensor()` 会对 graph node 的 `src[]`、`view_src` 一直递归：

```cpp
for (int i = 0; i < GGML_MAX_SRC; i++) {
    add_tensor(tensor->src[i], tensors, visited);
}
add_tensor(tensor->view_src, tensors, visited);
tensors.push_back(serialize_tensor(tensor));
```



而 `rpc_tensor` 本身又保存所有 `src[]` 和 `view_src`。 服务端收到后又递归 `create_node()`，因此这些祖先 tensor 全得跟着发送。

这正好解释之前手机上很奇怪的：

```text
Phone FFN graph:
n_nodes = 4

但：
n_tensors = 1011
           1080
           1118
...
GRAPH_COMPUTE payload ≈ 300 KB
```

实际上 Phone 只需要算几颗 FFN 节点，却因为：

```text
prefill_ffn_norm_chunk
        ↑
ffn_norm
        ↑
attention
        ↑
上一层 residual
        ↑
...
```

被 `add_tensor()` 顺着依赖一直追到了前面几十层。

### 下一步：在 `prefill_ffn_norm_chunk_*` 这里“截断依赖”

这是非常适合现在做的一刀。

因为：

```text
prefill_ffn_norm_chunk_0-X
prefill_ffn_norm_chunk_1-X
```

本来就是一个明确的 **PC → Phone 输入边界**。

Phone 已经通过：

```text
[PREFILL_INPUT]
[META_COPY]
RPC_SET_DIRECT
```

拿到了它的实际数据。

所以 Phone 构建 FFN graph 时根本不需要知道：

> 这个 tensor 在 PC 上是经过哪些 attention / norm / concat 算出来的。

对 Phone 来说它应该就是：

```text
一个已经存在于远端 buffer 中的输入 tensor
```

即叶子节点。

---

我建议先只改 `ggml-rpc.cpp` 的 graph serialization，不碰 Meta。

先加：

```cpp
static bool rpc_is_prefill_external_input(
        const ggml_tensor * tensor) {
    return tensor != nullptr &&
        std::strncmp(
            tensor->name,
            "prefill_ffn_norm_chunk_",
            23) == 0;
}
```

名字长度你编译前再确认一下，或者直接用：

```cpp
static constexpr const char * prefix =
    "prefill_ffn_norm_chunk_";

return std::strncmp(
    tensor->name,
    prefix,
    std::strlen(prefix)) == 0;
```

然后改 `add_tensor()`。

当前：

```cpp
static void add_tensor(
        ggml_tensor * tensor,
        std::vector<rpc_tensor> & tensors,
        std::unordered_set<ggml_tensor *> & visited) {

    if (tensor == nullptr) {
        return;
    }

    if (visited.find(tensor) != visited.end()) {
        return;
    }

    visited.insert(tensor);

    for (int i = 0; i < GGML_MAX_SRC; i++) {
        add_tensor(tensor->src[i], tensors, visited);
    }

    add_tensor(tensor->view_src, tensors, visited);

    tensors.push_back(serialize_tensor(tensor));
}
```

先实验性改成：

```cpp
static void add_tensor(
        ggml_tensor * tensor,
        std::vector<rpc_tensor> & tensors,
        std::unordered_set<ggml_tensor *> & visited) {

    if (tensor == nullptr) {
        return;
    }

    if (!visited.insert(tensor).second) {
        return;
    }

    if (rpc_is_prefill_external_input(tensor)) {
        GGML_ASSERT(
            tensor->buffer != nullptr &&
            ggml_backend_buffer_is_rpc(tensor->buffer));

        rpc_tensor rt = serialize_tensor(tensor);

        // 对 RPC server 来说，这就是已经写入 remote buffer 的叶子输入。
        rt.op = GGML_OP_NONE;

        for (int i = 0; i < GGML_MAX_SRC; ++i) {
            rt.src[i] = 0;
        }

        rt.view_src = 0;
        rt.view_offs = 0;

        rt.flags &= ~GGML_TENSOR_FLAG_COMPUTE;

        tensors.push_back(rt);

        if (RPC_DEBUG) {
            printf(
                "[RPC_GRAPH_CUT_INPUT] "
                "name=%s ne=[%u,%u,%u,%u]\n",
                rt.name,
                rt.ne[0], rt.ne[1],
                rt.ne[2], rt.ne[3]);
        }

        return;
    }

    for (int i = 0; i < GGML_MAX_SRC; ++i) {
        add_tensor(tensor->src[i], tensors, visited);
    }

    add_tensor(tensor->view_src, tensors, visited);

    tensors.push_back(serialize_tensor(tensor));
}
```

这里为什么可行：RPC 反序列化 tensor 时，`buffer` 和 `data` 是直接恢复出来的，并不依赖 `view_src` 才能找到实际数据。

所以我们不是“删掉数据”，而只是告诉服务端：

```text
别追问这个数据是怎么来的。

它已经在这个 remote buffer 地址上了，
直接拿来做 FFN。
```

### 我还建议顺手给 `serialize_graph()` 加一条统计

例如：

```cpp
if (RPC_DEBUG) {
    printf(
        "[RPC_GRAPH_SERIALIZE] "
        "uid=%" PRIu64
        " nodes=%u tensors=%zu bytes=%zu\n",
        graph_uid,
        n_nodes,
        tensors.size(),
        output.size());
}
```

这样改前可能看到：

```text
nodes=4
tensors=1011
bytes≈300KB
```

改完我们的目标不是某个绝对数字，但应该出现非常大的下降，比如：

```text
nodes=4
tensors=十几个/几十个
bytes=几KB ~ 十几KB
```

如果仍然：

```text
tensors=900+
```

那说明还有另一个依赖入口没有截断，我们再根据名字继续找。

---

这一步比现在立刻做 `GRAPH_RECOMPUTE` 更值得优先。

你其实已经有完整的 graph cache / recompute 机制：只有 `cgraph->uid` 可缓存且 UID 已存在时才走 reuse。 甚至 reuse 后已经支持把：

```text
SET input
+
GRAPH_RECOMPUTE
+
SNAPSHOT
```

合成一个 `SET_TENSOR_RECOMPUTE_SNAPSHOT` 请求。

这个以后非常有价值。

但 cache 有一个天然问题：

> 第一次看到这个 graph 还是得发送完整 graph。

而我们现在这个“切断祖先依赖”的优化是：

> **第一次 Prefill 就立刻受益。**

而且完全保留目前已验证最快的：

```text
24 层连续 Tensor Split
    ×
14/21 两 chunk
+
8 层 tail PC-only
```

不改变计算策略，只缩 RPC metadata。

如果成功，我甚至比较期待它不只是减少 PC→Phone 流量，还会让 **Phone→PC snapshot 的等待下降**：因为 COMPUTE socket 不再每层两个 chunk 各塞约 300KB graph metadata，无线链路和 socket 排队都会轻很多。

**所以现在先做这一刀：只把 `prefill_ffn_norm_chunk_*` 在 RPC graph serialization 中当成叶子输入。其他东西全部不改。** 跑完我主要看 `n_tensors` 和 GRAPH payload 能不能从上千直接掉下来。
