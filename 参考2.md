对，就按这个顺序最合理，而且我建议**现在就把内部抽象按最终三态设计好**，第一阶段只启用其中两态。这样第二阶段加入 Phone-only 时不用重新推翻第一阶段。

第一阶段：

```text
PC_ONLY
+
PC_PHONE_TENSOR_SPLIT
```

第二阶段再扩展为：

```text
PC_ONLY
+
PHONE_ONLY
+
PC_PHONE_TENSOR_SPLIT
```

### 第一阶段的实际执行形态

目前 Attention 本来就是 PC 主导，所以：

```text
PC_ONLY layer
────────────────────────────
PC:
Attention
FFN full
Residual
↓
next layer

Phone:
不参与

RPC:
0 次/layer
```

而现有 Tensor-Split 层继续：

```text
PC_PHONE_SPLIT layer
────────────────────────────
PC:
Attention
FFN shard
      ↘
       reduce
      ↗
Phone:
FFN shard

RPC:
ffn_norm PC→Phone
ffn_down Phone→PC
```

因此本质就是：

```text
Layer 0   PC
Layer 1   PC+Phone Split
Layer 2   PC+Phone Split
Layer 3   PC+Phone Split
Layer 4   PC
...
```

目的非常直接：**用 PC 多算一些 FFN，换掉一部分每层约 15~20 ms 的固定 RPC 等待。**

现有日志里一个稳定 Decode 仍然是每 token 32 次 Phone→PC 返回，而较好的一次平均也约 `16.9ms`/次。 所以减少交互次数比继续削 16KB payload 更有希望。

---

## 代码结构建议现在直接做三态

不要先写：

```cpp
bool pc_only_layer(int il);
```

以后再改。

直接写：

```cpp
enum class llama_hybrid_layer_mode {
    PC_ONLY,
    TENSOR_SPLIT,
    PHONE_ONLY,
};
```

第一阶段 policy 暂时永远不会返回 `PHONE_ONLY`：

```cpp
static llama_hybrid_layer_mode llama_hybrid_layer_mode_for(int il) {
    // 第一版测试：
    // 0,4,8,... 为 PC_ONLY
    if (il % 4 == 0) {
        return llama_hybrid_layer_mode::PC_ONLY;
    }

    return llama_hybrid_layer_mode::TENSOR_SPLIT;
}
```

后面扩 Phone-only 时，只需要让这个函数开始返回第三种状态。

### `llama.cpp` 这里也按这个 policy 判断

现在 Decode 特殊 Tensor-Split FFN 是：

```cpp
const bool use_decode_chunked_ffn =
    !embed &&
    n_tokens == 1 &&
    model.split_mode() == LLAMA_SPLIT_MODE_TENSOR &&
    ...
```



改成逻辑上：

```cpp
const auto hybrid_mode =
    llama_hybrid_layer_mode_for(il);

const bool use_decode_chunked_ffn =
    !embed &&
    n_tokens == 1 &&
    model.split_mode() == LLAMA_SPLIT_MODE_TENSOR &&
    hybrid_mode == llama_hybrid_layer_mode::TENSOR_SPLIT &&
    n_chunks >= 1 &&
    loras->empty() &&
    cvec->tensor_for(il) == nullptr;
```

于是：

```text
TENSOR_SPLIT
→ 走现在 ffn_down_chunk + snapshot

PC_ONLY
→ 不进入特殊路径
→ 普通 build_ffn()
```

当然，仅这里改还不够，**真正决定 PC_ONLY 的是静态 weight placement**。

---

## Weight split callback 是第一阶段的核心

Meta 对静态 tensor 本来就通过 `get_split_state()` callback 决定分片。

所以以后可以按照：

```text
tensor name
    ↓
解析 layer id
    ↓
hybrid policy(layer)
```

返回不同 split。

例如原来：

```text
layer 1 ffn_up
PC     75%
Phone  25%

layer 1 ffn_gate
PC     75%
Phone  25%

layer 1 ffn_down
PC     75%
Phone  25%
```

保持不变。

而：

```text
layer 0 = PC_ONLY
```

则变成：

```text
ffn_up:
PC     100%
Phone    0%

ffn_gate:
PC     100%
Phone    0%

ffn_down:
PC     100%
Phone    0%
```

Meta 已经有 zero-sized source slice 时清掉对应 backend `COMPUTE` flag 的机制，所以这种表示法和当前结构是兼容的。

---

## 第一阶段我建议先只测三个比例

不要一开始弄复杂自适应。

```text
A. 当前 baseline
PC_ONLY       0
TENSOR_SPLIT 32

B. Hybrid 25%
PC_ONLY       8
TENSOR_SPLIT 24

C. Hybrid 50%
PC_ONLY      16
TENSOR_SPLIT 16
```

分布分别可以：

```cpp
// 8 PC-only
il % 4 == 0
```

以及：

```cpp
// 16 PC-only
il % 2 == 0
```

我更喜欢**交错布局**而不是：

```text
0~7 PC
8~31 Split
```

因为第一阶段我们想测的是纯粹的：

> “少一次 RPC 是否值得 PC 多算这一层 FFN？”

交错可以把层位置因素压低。

预期日志应该非常好辨认：

```text
32 Split:
1->0 count ≈ 32

8 PC + 24 Split:
1->0 count ≈ 24

16 PC + 16 Split:
1->0 count ≈ 16
```

而 PC-only layer 应该完全没有：

```text
ffn_norm -> Phone
RPC_SNAPSHOT
ffn_down -> PC
```

---

### Prefill 先接受它一起改变

第一版建议不要为了保持现在 14/21 Prefill 再复制一整套 weight。

由于权重布局是静态的：

```text
PC_ONLY layer
```

在 Prefill 中也会变成 PC-only FFN。

所以例如 8+24：

```text
Prefill:
8 层 PC full
24 层继续现在的 14/21 Tensor Split

Decode:
8 层 PC full
24 层 Tensor Split
```

这样实现最干净。

测试时同时记录：

```text
prompt eval tok/s
eval tok/s
```

如果结果是：

```text
Prefill 轻微下降
Decode 明显上升
```

那就说明 Hybrid 成立。

之后才值得考虑“Prefill 用 split 权重、Decode 用 PC full shadow weight”这种双布局。

---

## 第二阶段再加入真正 PHONE_ONLY

到时候三种状态就变成：

```text
PC_ONLY
────────────────
Attention PC
FFN PC


TENSOR_SPLIT
────────────────
Attention PC
FFN PC + Phone


PHONE_ONLY
────────────────
Attention Phone
FFN Phone
Residual Phone
KV cache Phone
```

这里 `PHONE_ONLY` 和第一阶段的 PC-only 完全不是对称的小改动，因为要把 **Attention + KV cache ownership** 也迁到手机。

但一旦做到连续 Phone block，例如：

```text
0~7    PC_ONLY
8~23   TENSOR_SPLIT
24~31  PHONE_ONLY
```

就有机会做到：

```text
PC/Split 区
      ↓
一次 hidden 传到 Phone
      ↓
layer24
layer25
...
layer31
      ↓
一次返回
```

而不是 8 个 Phone layer 每层各付一次 RTT。

所以最终三态 architecture 确实很有潜力。

**现在第一刀就做 `PC_ONLY + TENSOR_SPLIT`，而且代码的 policy enum 从一开始保留 `PHONE_ONLY`。** 下一步最该找的是当前实际生成 `tensor_split` 的 callback——在那里做 per-layer `100/0` 和现有比例的选择，而不是先碰 Meta scheduler。

对，**第一版我更建议传“PC-only 比例”一个参数**，不要同时传“PC层数 + Split层数”两个值。

因为两者本来就是互补的：

```text
PC_ONLY + PC_PHONE_SPLIT = n_layer
```

传两个反而容易出现：

```text
8 + 20 != 32
```

这种无效配置。

我建议第一版直接做：

```bash
LLAMA_HYBRID_PC_PCT=25
```

含义：

```text
25% layer = PC_ONLY
75% layer = PC + Phone Tensor Split
```

对于现在 32 层：

```text
0%      0 PC   + 32 Split
12.5%   4 PC   + 28 Split
25%     8 PC   + 24 Split
37.5%  12 PC   + 20 Split
50%    16 PC   + 16 Split
62.5%  20 PC   + 12 Split
75%    24 PC   +  8 Split
100%   32 PC   +  0 Split
```

这样以后换 40 层、80 层模型也不用重新解释参数。

代码可以这么解析：

```cpp
static double llama_hybrid_pc_pct() {
    const char * value = std::getenv("LLAMA_HYBRID_PC_PCT");
    if (value == nullptr) {
        return 0.0;
    }

    return std::clamp(std::strtod(value, nullptr), 0.0, 100.0);
}
```

然后模型有 `n_layer` 后：

```cpp
const int pc_layers = std::clamp(
    (int) std::llround(n_layer * llama_hybrid_pc_pct() / 100.0),
    0,
    n_layer);

const int split_layers = n_layer - pc_layers;
```

### 层怎么选也很重要

我仍然不建议第一版写成：

```text
PC_ONLY = 前 8 层
Split   = 后 24 层
```

先用**均匀交错**。

例如 25%：

```text
PC:     0,4,8,12,16,20,24,28
Split:  其余
```

50%：

```text
PC:     0,2,4,6,...,30
Split:  1,3,5,7,...,31
```

而且不要直接用 `il % 4` 写死，因为以后 37.5%、40% 这种比例就麻烦。

可以先生成一张 policy 表：

```cpp
static std::vector<uint8_t> build_hybrid_policy(
        int n_layer,
        int pc_layers) {

    std::vector<uint8_t> pc_only(n_layer, 0);

    if (pc_layers <= 0) {
        return pc_only;
    }

    if (pc_layers >= n_layer) {
        std::fill(pc_only.begin(), pc_only.end(), 1);
        return pc_only;
    }

    // 均匀地在所有 layer 中挑出 pc_layers 个
    for (int k = 0; k < pc_layers; ++k) {
        const int il =
            (int) ((int64_t) k * n_layer / pc_layers);

        pc_only[il] = 1;
    }

    return pc_only;
}
```

例如：

```cpp
n_layer   = 32;
pc_layers = 8;
```

自然得到：

```text
0 4 8 12 16 20 24 28
```

如果：

```cpp
pc_layers = 12;
```

也会自动近似均匀分布，不需要特殊处理。

---

我还建议启动时一定打印实际解析结果：

```text
[HYBRID_SPLIT]
pc_pct=37.5
n_layer=32
pc_only=12
tensor_split=20
pc_layers=[0,2,5,8,10,13,16,18,21,24,26,29]
```

这样每次测速一眼就知道自己跑的是什么配置。

### 第一轮比例怎么扫

不用一开始扫特别细，先：

```text
0%
25%
50%
75%
100%
```

其中：

```text
0%   = 当前 Tensor Split baseline
100% = 纯 PC baseline
```

这两个其实非常有价值，它们把整个性能曲线的两端钉住。

假设结果例如：

```text
0%    1.10 tok/s
25%   1.30
50%   1.48
75%   1.39
100%  1.20
```

那我们就知道峰值在 `25~75%` 之间，再补：

```text
37.5%
62.5%
```

32 层模型里每 `12.5%` 正好就是 **4 层**，非常适合这样扫。

还有一点要记住：第一阶段静态权重跟着这个比例走，所以 `25%` 现在实际意味着：

```text
Decode:
8 PC-only
24 Split

Prefill:
8 PC-only
24 继续 14/21 Split
```

因此每次同时记 `prompt eval tok/s` 和 `eval tok/s`。

我会把接口定成 **`LLAMA_HYBRID_PC_PCT` 单参数 + 均匀分层 + 启动时打印实际 layer list**。以后加入 Phone-only 时，可以自然扩成：

```text
LLAMA_HYBRID_PC_PCT=25
LLAMA_HYBRID_PHONE_PCT=25

剩余 50% 自动 = PC_PHONE_SPLIT
```

这样第一阶段的接口到第二阶段也不用推翻。
