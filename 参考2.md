找到了一个比上一刀更像根因的点：**你刚才改的是 RPC 层，但真正把 `(reshaped)` 错当成 chunk 边界的是 Meta 层。**

`ggml-backend-meta.cpp` 现在这个 parser：

```cpp
static bool ggml_backend_meta_parse_prefill_norm_chunk(
        const char * name,
        int & chunk,
        int & layer) {
    return std::sscanf(
        name,
        "prefill_ffn_norm_chunk_%d-%d",
        &chunk,
        &layer) == 2;
}
```

这里有坑。

对于：

```text
prefill_ffn_norm_chunk_0-46
```

返回 2。

但对于：

```text
prefill_ffn_norm_chunk_0-46 (reshaped)
```

**一样返回 2。**

`sscanf()` 读完两个整数以后就已经算匹配成功，它根本不要求字符串到此结束。

所以现在发生的其实是：

```text
原始 boundary:
prefill_ffn_norm_chunk_0-46
        ↓
Meta 识别为 boundary       正常

build_moe_ffn()
        ↓
RESHAPE
        ↓
名字变成
prefill_ffn_norm_chunk_0-46 (reshaped)
        ↓
Meta 又识别为 boundary     ← BUG
        ↓
额外切一次 subgraph
        ↓
[META_COPY]
tensor=prefill_ffn_norm_chunk_0-46 (reshaped)
        ↓
Phone MoE graph
        ↓
segfault
```

这也解释了为什么你改：

```cpp
rpc_is_prefill_external_input()
```

完全没效果。

因为等 RPC serializer 看到图的时候，**Meta 早就已经错误地在 `(reshaped)` 这里切图了**。

---

### 直接改这个 parser

我建议先只改 `prefill_norm_chunk` 做验证：

```cpp
static bool ggml_backend_meta_parse_prefill_norm_chunk(
        const char * name,
        int & chunk,
        int & layer) {
    if (name == nullptr) {
        return false;
    }

    int n = 0;
    if (std::sscanf(
            name,
            "prefill_ffn_norm_chunk_%d-%d%n",
            &chunk,
            &layer,
            &n) != 2) {
        return false;
    }

    return name[n] == '\0';
}
```

注意 `%n` **不计入 `sscanf` 的返回值**，所以这里仍然是：

```cpp
!= 2
```

而不是 3。

这样：

```text
prefill_ffn_norm_chunk_0-46
                              → true

prefill_ffn_norm_chunk_0-46 (reshaped)
                              → false
```

---

然后你之前 RPC 那个修改也先保留：

```cpp
if (std::strstr(tensor->name, " (reshaped)") != nullptr) {
    return false;
}
```

先两边都收紧，避免干扰。

### 修完以后我最想看到的日志变化

现在是：

```text
[META_COPY]
sg=2
0->1
tensor=prefill_ffn_norm_chunk_0-46 (reshaped)
```

修完 Meta parser 后，理论上应该变成类似：

```text
[META_COPY]
...
tensor=prefill_ffn_norm_chunk_0-46
```

也就是传输的是 **真正的 2D chunk boundary**，而不是它后面的 reshape 派生 tensor。

然后 Phone 自己执行：

```text
prefill_ffn_norm_chunk_0-46
        ↓
RESHAPE [2048,2] → [2048,1,2]
        ↓
Router / Top-K
        ↓
MUL_MAT_ID
```

这才是正常图结构。

---

而且代码里不止这一处有同样的问题：

```cpp
ggml_backend_meta_parse_decode_ffn_chunk()
ggml_backend_meta_parse_prefill_norm_chunk()
ggml_backend_meta_parse_prefill_down_chunk()
ggml_backend_meta_parse_prefill_wave_ffn_inp_chunk()
ggml_backend_meta_parse_prefill_wave_attn_out_chunk()
ggml_backend_meta_parse_prefill_wave_l_out_chunk()
```

现在全都是：

```cpp
sscanf(...) == N
```

所以都有可能把：

```text
xxx-46 (reshaped)
xxx-46 (view)
...
```

误认成正式 marker。

**但先别一口气全改。** 这次只改 `ggml_backend_meta_parse_prefill_norm_chunk()`，然后继续：

```text
T=2
chunked MoE ON
Stage ON
```

跑一次。

如果最后一条从：

```text
[META_COPY] ... (reshaped)
```

变成真正的：

```text
[META_COPY] ... prefill_ffn_norm_chunk_0-46
```

并且跑通，那这个 bug 就基本抓住了。这个方向现在比 `MUL_MAT_ID` kernel 本身可疑得多。
