对，那我刚才把你那组 `ne=[4096,2,1,1]` 的数据当成 decode 来解释了，这里需要修正：**那组巨大的 `1->0` 时间主要是在 prefill 的 generic fallback 路径里，不应该直接拿来判断 decode 的 Phone→PC 网络延迟。**

你这次上传的完整日志把两阶段分得很清楚。Prefill 的 35 token 那次是：

```text
ne=[4096,35,1,1]

reduce_to_primary=0
fallback=32

0->1: 34.484 MiB / 100.417 ms
1->0: 16.969 MiB / 7860.410 ms
         avg 245.638 ms
```

也就是说 prefill 仍然是 32 层 `allreduce_fallback()`；这里 `1->0` 的 245ms 不只是传输，还吞进了 Phone 端尚未完成的 FFN 计算、RPC 排队和同步等待。

所以 prefill 的方向差：

```text
0->1 ≈ 1~2 ms
1->0 ≈ 200~300 ms
```

**很大程度上是计时语义不对称，不代表 Wi-Fi 上行真的比下行慢上百倍。**

不过有意思的是，你现在上传的 **decode 日志反而证明：decode 的 Phone→PC 确实还有很大的等待可以挖。**

例如真正 decode 已经进入：

```text
reduce_to_primary=32
fallback=0
```

稳定一些的 token：

```text
1->0 avg = 21.281 ms
1->0 avg = 21.105 ms
1->0 avg = 18.476 ms
1->0 avg = 21.593 ms
```

而 PC 端基本所有时间都落在：

```text
[RPC_SNAPSHOT_COMPACT_CLIENT]
request ≈ 0.05~0.1 ms
recv    ≈ 15~25 ms
```

上。 

更关键的是，我把你 PC 和手机 **seq=171~192** 对上了。

这 22 次平均大概是：

```text
Phone GRAPH compute       ≈ 3.30 ms
Phone wait_ready          ≈ 4.09 ms
Phone RAW_SEND            ≈ 1.14 ms

PC recv                   ≈ 25.82 ms
```

也就是说：

```text
25.82 - 4.09 - 1.14
≈ 20.59 ms
```

还有平均 **20ms/层根本不在 Phone 的 FFN 计算，也不在 Phone 的 `send_data()` 调用时间里面。**

比如特别明显的几层：

```text
seq=187
Phone:
compute    2.617 ms
wait_ready 3.234 ms
raw_send   1.617 ms

PC:
recv      46.999 ms
```

```text
seq=188
Phone:
compute    4.719 ms
wait_ready 4.533 ms
raw_send   1.233 ms

PC:
recv      63.794 ms
```

```text
seq=191
Phone:
compute    4.791 ms
wait_ready 5.456 ms
raw_send   1.549 ms

PC:
recv      63.050 ms
```

手机日志里的 FFN compute、snapshot fill、wait_ready、raw send 都只有几毫秒量级。 PC 同序号却会出现十几到六十多毫秒的 `recv`。

这就很重要了。

### 所以你的直觉实际上是对的，但应该针对 decode 来处理

现在稳定 decode 大致可以理解成：

```text
每层：

PC算 attention/norm
      |
      | ffn_norm -> Phone
      v
Phone FFN
约 3~5 ms
      |
      | snapshot ready
      v
Phone send_data
约 1 ms
      |
      | 这里出现额外 10~60 ms
      v
PC recv 完成
      |
      v
PC继续下一层
```

32 层是严格串行依赖。

如果平均额外等待真有约：

```text
20 ms × 32
≈ 640 ms/token
```

那就正好和你的数据对应上了：

```text
compute ≈ 110~120 ms
reduce  ≈ 590~700 ms
```

例如这次就是：

```text
compute=116.873 ms
reduce_total=593.994 ms
1->0=591.239 ms
```



所以现在 **decode 的主瓶颈已经非常像是 RPC/TCP/系统调度这条返回路径，而不是 Phone FFN 算力。**

---

但这个剩下的 20ms 我暂时还不会全部叫“网络时间”。

因为 Phone 的：

```cpp
sock->send_data(data, size)
```

计时结束，只代表 `send_data()` 返回。它很可能只是把 16KB 数据交给了内核 socket buffer，并不意味着 PC 已经收到。

而 PC 的：

```text
recv=25 ms
```

包含：

```text
GET_SNAPSHOT 请求到达 Phone
+ Phone transfer 线程被调度
+ wait_ready
+ TCP 实际发送
+ Wi-Fi
+ PC TCP 栈
+ PC recv 线程被唤醒
```

所以现在应该再切最后一刀。

我建议**暂时先不动 FFN ownership**，花一次实验把这个 20ms 定位清楚。最有价值的三个计时是：

1. 在 `socket_t::recv_data()` 里面记录 **等待第一个字节用了多久**，以及第一个字节之后收完剩余 16383 bytes 用了多久。
2. Phone transfer socket 在收到 `RPC_CMD_GET_SNAPSHOT` 后，记录从命令完整收到到进入 `send_snapshot()` 的 dispatch 时间。
3. Phone 的底层 TCP `send_data()` 如果内部有多次 `send()`，记录每次真正 `send()` 的字节数和耗时。

这样只需要看：

```text
first_byte_wait = 20~50 ms
remaining       < 1 ms
```

那几乎就确定是 **请求/线程调度/TCP/Wi-Fi latency**，而不是带宽。

如果：

```text
first_byte_wait ≈ 5 ms
remaining       ≈ 20~50 ms
```

才说明是实际 payload 传输、TCP 拥塞或者重传。

而你的 payload 才 **16KB**，我目前更怀疑第一种。

这条非常值得继续追，因为现在已经不是“小优化”了。**如果能把每层 20ms 的额外等待压到 5ms，32 层理论上就是几百毫秒/token 的空间。** 即使最终只能省掉一半，对现在约 1.4 tok/s 都会很明显。

并且这和我们前面讨论的 FFN ownership 并不冲突：先把单次同步的 20ms 尽量降下来，然后 ownership 再把 **32 次同步降成约 8 次**，两者是乘法关系。
