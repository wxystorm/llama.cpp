这个日志已经把问题暴露出来了。**现在不是普通的浮点误差，而是 `TENSOR_SPLIT → PHONE_ONLY` 的入口交接没做完整。**

最关键的证据是通信计数。

你的 35-token Prefill 现在是 22 层 Tensor Split，每层 2 个 chunk，所以正常就有：

```text
22 × 2 = 44 次 PC -> Phone
22 × 2 = 44 次 Phone -> PC
```

而日志实际是：

```text
0->1 count=45
1->0 count=48
```



这里 `0->1=45` 基本正好就是：

```text
44 次 Prefill chunk 输入
+ 1 次最后 result_norm
= 45
```

**也就是说 layer21 Tensor Split 算完以后，进入 layer22 PHONE_ONLY 时，没有额外的一次 PC→Phone 完整状态交接。**

而 `1->0=48` 则非常漂亮地反过来证明了另一件事：

```text
44 次 Tensor Split partial 返回
+ 4 次 PHONE_ONLY subgraph 返回
= 48
```

所以 layer22、23 确实已经在 Phone 上执行了，但每个 SG 算完又被拷回 PC；`META_KEEP_PHONE` 目前实际上还没生效。

---

更明显的是 Decode。

layer21 结束在：

```text
sg=42 ... ffn_norm-21
```

然后马上出现了四个 RPC graph：

```text
nodes=34
nodes=7
nodes=24
nodes=7
```

紧接着：

```text
sg=48 first=l_out-23 last=ffn_norm-24
```

这四个正好就是 layer22/23 的 Phone-only 两层。

但是 Decode 汇总却是：

```text
0->1 count=23
1->0 count=26
```



22 个 Tensor Split 层本来就需要：

```text
22 次 ffn_norm PC -> Phone
22 次 ffn_down Phone -> PC
```

再加：

```text
result_norm PC -> Phone = 1
```

刚好：

```text
0->1 = 23
```

所以这里也同样：

> **layer21 → layer22 根本没有新的 PC→Phone handoff。**

而：

```text
1->0 = 22 + 4 = 26
```

说明 Phone-only 的四个 SG 全部又 mirror 回 PC 了。

---

这也解释了为什么结果虽然“还有点像”，但 logits 已经明显错了。

你这次第一个 token：

```text
token 13398 = 17.1803
```



之前正确路径相同位置大约是：

```text
token 13398 = 19.2366
```



第二个 `7541` 之前大约 `29.89`，现在只有 `22.12`。 

这个幅度已经不是“换 ARM kernel 所以略有误差”能解释的了。

### 实际发生的事情大概率是这个

Tensor Split layer21：

```text
PC:
  自己的 FFN partial
       +
  Phone 返回的 FFN partial
       ↓
  完整 ffn_down-21       <-- 正确结果只在 PC

Phone:
  仍然只有自己的 partial
```

然后当前代码直接启动 Phone-only layer22：

```text
                错误
Phone partial ───────→ layer22 Attention
                       layer22 FFN
                       layer23 ...
```

但应该是：

```text
PC 完整 reduce
      ↓
PC -> Phone handoff
      ↓
Phone 完整输入
      ↓
layer22
layer23
```

Prefill 更典型。layer21 最后两个 chunk 已经分别 reduce：

```text
chunk0 = [4096,14]
chunk1 = [4096,21]
```



Phone 端留下的是自己的 shard partial，而真正 **PC+Phone 相加后的完整 chunk** 在 PC。

所以 Phone layer22 如果直接拿自己的那两个旧 tensor 去 CONCAT，再做 residual/Attention，数值当然会偏。

---

## 现在先只修这一处

**暂时别继续优化 `META_KEEP_PHONE`。先把 correctness 修回来。**

在 `Phone -> PC reduce` 完成、PC 已经做完 ADD 以后，如果这是：

```text
最后一个 TENSOR_SPLIT layer
          ↓
下一层 PHONE_ONLY
```

就把 **reduce 完成后的 PC tensor 再覆盖回 Phone 对应 tensor**。

Decode 类似：

```cpp
Phone partial
    ↓ snapshot
PC node_dst += phone partial
    ↓
node_dst 现在是完整结果

// 新增
PC node_dst
    ↓
copy
Phone node_src / corresponding tensor

// copy 完成之后
start PHONE_ONLY sg
```

建议加日志：

```text
[META_PHONE_HANDOFF]
layer=21
chunk=0
tensor=ffn_down_chunk_0-21
bytes=...
```

Decode 应该只有一次：

```text
[META_PHONE_HANDOFF] layer=21 chunk=0 bytes=16384
```

Prefill 35 tokens 应该两次：

```text
[META_PHONE_HANDOFF] layer=21 chunk=0 bytes=229376
[META_PHONE_HANDOFF] layer=21 chunk=1 bytes=344064
```

总量刚好还是：

```text
573440 bytes
```

也就是把完整 hidden state 交给 Phone 一次，只不过沿用你现在的两个 token chunk。

### 特别注意

这次 handoff **必须等 PC reduce 真正完成以后**。

不要沿用之前 Tensor-Split pipeline 那种：

```cpp
Phone next graph early start
```

的逻辑。

普通 TS→TS 可以这么做，是因为 Phone 下一个工作不依赖 PC 刚 reduce 出来的完整结果。

但是：

```text
TS layer21
   ↓
PHONE_ONLY layer22
```

这里 layer22 **明确依赖完整 reduce 输出**。

所以边界必须：

```text
wait Phone partial
       ↓
PC reduce/add
       ↓
PC -> Phone handoff
       ↓
handoff 完成
       ↓
启动 sg44
```

不能提前启动 sg44。

---

把这个修完以后，我预计通信统计首先会变成：

```text
Decode:
0->1: 23 -> 24

Prefill:
0->1: 45 -> 47
```

因为增加了正确的入口 handoff。

这时候先看 logits。理想情况应该重新接近之前：

```text
13398 ≈ 19.x
7541  ≈ 29.x
27262 ≈ 26.x
```

而不是现在：

```text
17.18
22.12
21.46
```

当前第三个 token 也只有 `21.455`。

等输出恢复正确，第二步再修 `META_KEEP_PHONE`。那时 layer22/23 四个 Phone SG 应该：

```text
sg44 -> KEEP_PHONE
sg45 -> KEEP_PHONE
sg46 -> KEEP_PHONE
sg47 -> Phone -> PC   // 因为 layer24 是 PC_ONLY
```

于是最终期望大概是：

```text
Decode:
0->1 = 24
1->0 = 23

Prefill:
0->1 = 47
1->0 = 45
```

也就是 **进入 Phone block 一次，内部不往返，离开 Phone block 一次**。

所以现在方向是对的：**22/23 的权重和计算确实已经搬到 Phone 了；错的是跨域边界的数据 ownership。先补 `TS -> PHONE_ONLY` 的 full-reduce handoff，输出很可能就会恢复。**
