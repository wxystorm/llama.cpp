对，这次日志已经把范围缩得很小了。**模型加载本身没有问题，plan、层布局、显存分配和 graph reserve 都已经通过；真正出错是在加载完成后的第一次 `decode()`，而且新 Stage Queue 被错误地用在了一个只有 2 token 的 batch 上。**

最关键的一行就是：

```text
[STAGEQ] batch=2 XG=128 macros=1 XT=32
```

你当前代码只要满足 `GPU→TENSOR` plan 就立即打开 Stage Queue，然后把 `runtime_ubatch` 设为 `min(n_ubatch, XG)`；它目前**没有判断这个 batch 到底有没有至少两个 macro**。

而 2 token 时：

```text
XG = 128
batch = 2

n_macros = ceil(2 / 128) = 1
```

这种情况根本不存在流水机会。

更糟的是，它仍然会走：

```text
prepare_pipe_slot
→ GPU partial compute
→ handoff_q
→ drain
→ prepare_post
→ Tensor partial compute
```

而不是原来的完整 graph 串行执行。Stage Queue 的执行代码确实会在单个 macro 时这么走。

所以我认为**第一处必须先修这个**。

---

### 这大概率就是 server 加载后的 warmup/probe

从时序看：

```text
模型分配完成
GRAPH_RESERVE 完成
↓
[STAGEQ] batch=2 ...
↓
RPC malformed response
```

这个 `batch=2` 几乎肯定不是你真正的 512-token Prefill，而是加载后的很小一次 warmup / probe。

你的 context 本身也有专门的 `cparams.warmup` 状态和 `llama_set_warmup()` 接口。

无论它这次是否真的把 `warmup=true` 传到了这里，**只有 1 个 macro 时 Stage Queue 都不应该启用**。

---

## 我建议把当前逻辑改成两层判断

现在是：

```cpp
bool stage_queue_runtime_enabled =
    stage_queue_requested &&
    has_runtime_plan &&
    runtime_plan.gpu_pc_layers > 0 &&
    runtime_plan.pc_layers == runtime_plan.gpu_pc_layers &&
    runtime_plan.tensor_layers > 0 &&
    runtime_plan.phone_layers == 0;

uint32_t runtime_ubatch = cparams.n_ubatch;

if (stage_queue_runtime_enabled) {
    runtime_ubatch =
        std::min<uint32_t>(
            runtime_ubatch,
            runtime_plan.gpu_chunk_tokens);

    const uint32_t n_macros =
        (n_tokens_all + runtime_ubatch - 1) /
        runtime_ubatch;
}
```

改成：

```cpp
const bool stage_queue_plan_eligible =
    stage_queue_requested &&
    has_runtime_plan &&
    runtime_plan.gpu_pc_layers > 0 &&
    runtime_plan.pc_layers == runtime_plan.gpu_pc_layers &&
    runtime_plan.tensor_layers > 0 &&
    runtime_plan.phone_layers == 0;

bool stage_queue_runtime_enabled = false;
uint32_t runtime_ubatch = cparams.n_ubatch;

if (stage_queue_plan_eligible) {
    const uint32_t xg = std::min<uint32_t>(
        cparams.n_ubatch,
        runtime_plan.gpu_chunk_tokens);

    const uint32_t n_macros =
        (n_tokens_all + xg - 1) / xg;

    // 一个 macro 没有任何 stage overlap 的意义。
    // warmup/probe 也优先走原有完整 graph 路径。
    stage_queue_runtime_enabled =
        !cparams.warmup &&
        n_macros >= 2;

    if (stage_queue_runtime_enabled) {
        runtime_ubatch = xg;

        LLAMA_LOG_ERROR(
            "[STAGEQ] enabled batch=%u XG=%u macros=%u XT=%d\n",
            n_tokens_all,
            xg,
            n_macros,
            runtime_plan.tensor_chunk_tokens);
    } else {
        LLAMA_LOG_DEBUG(
            "[STAGEQ] bypass batch=%u XG=%u macros=%u warmup=%d\n",
            n_tokens_all,
            xg,
            n_macros,
            (int) cparams.warmup);
    }
}
```

然后原来的：

```cpp
if (stage_queue_requested && !stage_queue_runtime_enabled) {
    LLAMA_LOG_WARN(
        "[STAGEQ] disabled: requires ...");
}
```

也要改。

因为现在：

```text
plan 不支持 StageQ
```

和：

```text
plan 支持，但是这个 batch 只有一个 macro
```

是两回事。

应该写成：

```cpp
if (stage_queue_requested && !stage_queue_plan_eligible) {
    LLAMA_LOG_WARN(
        "[STAGEQ] disabled: requires a runtime GPU->TENSOR "
        "plan with G=C, T>0, and P=0\n");
}
```

这样 `batch=2` 不会打印误导性的 topology warning。

---

## 修完后，这次启动应该变成

加载阶段的小 batch：

```text
[STAGEQ] bypass batch=2 XG=128 macros=1 warmup=...
```

然后它走原来的：

```text
full serial graph
```

不会进入：

```text
handoff_q
tensor_ready_q
prepare_split
```

等真正来了 512 token prompt：

```text
[STAGEQ] enabled batch=512 XG=128 macros=4 XT=32
```

才开始：

```text
G0
H0
G1 || T0
H1
G2 || T1
...
```

这才是我们真正想测的东西。

---

### 为什么我现在优先怀疑这个，而不是模型/内存？

前面的证据很完整：

```text
T=50 P=0 C=14 G=14
GPU=[0,14)
TENSOR=[14,64)
```

placement 也确实是 0~13 CUDA，说明新 topology 正常。

模型 buffer 也全部分配完成：

```text
MODEL_ALLOC_CTX_END CUDA0
MODEL_ALLOC_CTX_END Meta()
```

graph reserve 也正常：

```text
tokens=512 nodes=6641
max_nodes=78918
```

说明之前那个 graph capacity 问题已经没有了。

直到第一次：

```text
[STAGEQ] batch=2 ...
```

才炸。

所以现在最明显的新变量就是：

> **我们让一个完全没必要流水的 2-token graph，进入了刚写的 partial-stage execution path。**

另外你当前 `prepare_pipe_slot()` 的 Stage Queue topology 确实允许单 GPU island，然后把 GPU 后所有 splits 当作 POST。

所以这次不是“Stage Queue 没启用”，反而是**启用得太积极了**。

---

不过我不会仅凭 PC 端这一行就断言手机进程一定是哪个函数崩了：

```text
Remote RPC server crashed or returned malformed response
```

这个错误本身只是客户端发现 RPC 对端断开或返回长度不符合协议。你的 RPC 请求/响应路径在返回尺寸不匹配时也会直接判失败。

所以**如果修掉单-macro StageQ 后仍然崩**，下一步不要继续猜，我需要手机 RPC server 崩溃前最后大约 30~50 行日志。那样可以直接定位到底是：

```text
GRAPH_COMPUTE
GRAPH_RECOMPUTE
snapshot
SET_TENSOR
```

哪一步把 server 打掉。

还有一个小提醒：第一次测试时最好确保旧的

```text
LLAMA_PREFILL_STAGE_PIPELINE
```

没有同时开启，只开：

```text
LLAMA_HYBRID_STAGE_QUEUE=1
```

因为你现在 `pipeline_runtime_enabled` 是两者 OR 在一起的。

**先加“warmup/单 macro bypass”这一刀再跑。** 我认为这是当前最该改的，而且即使它不是最终 crash 根因，这个判断本身也是 runtime 必须有的。
