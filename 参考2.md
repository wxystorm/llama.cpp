对，这次信息已经把方向改得很明确了。你在 `prepare_graph_snapshot` 里加的这个：

```cpp
if (std::getenv("GGML_META_DISABLE_SNAPSHOT") != nullptr) {
    return;
}
```

**位置是对的**，至少对这次 `prefill_ffn_norm_chunk_*` 的 prefill snapshot 路径来说足以让 `snapshot_prepares[i].prepared == false`，从而让 prefill 的 `use_snapshot_pipeline` 变成 false。

而你现在又确认了：

```text
Stage 开/关       都崩
Return-wavefront  根本没开
Snapshot 开/关    都崩
```

所以我会把之前“snapshot 是主因”的判断撤掉。现在手机日志已经直接说明真正的崩溃位置了。

### 这次手机端日志非常关键

成功的前一个 graph：

```text
[graph_compute]
device: 0
graph_uid: 5305936658968066687
n_nodes: 27
n_tensors: 41

[RPC_GRAPH_SERVER]
uid=5305936658968066687
nodes=27
compute=2.013 ms
```

然后：

```text
[RPC_SET_DIRECT]
offset=0
size=16384
name=prefill_ffn_norm_chunk_0-43 (reshaped)
```

这个也成功。

紧接着：

```text
[graph_compute]
device: 0
graph_uid: 13139289399382781500
n_nodes: 20
n_tensors: 28

Segmentation fault
```

注意这里**没有出现**：

```text
[RPC_GRAPH_SERVER] uid=131392...
```

你的 server 代码是：

```cpp
const int64_t t0 = ggml_time_us();

ggml_status status =
    ggml_backend_graph_compute(
        backends[device],
        graph);

const int64_t t1 = ggml_time_us();

printf("[RPC_GRAPH_SERVER] ...");
```

因此现在已经能相当确定：

```text
RPC packet 接收完整
        ↓
graph header 解析成功
n_nodes=20 / n_tensors=28 都正常读出来
        ↓
create_node() 完成
        ↓
进入 ggml_backend_graph_compute()
        ↓
手机 SEGFAULT
```

也就是说 PC 的：

```text
ggml-rpc.cpp:1110
Remote RPC server crashed...
```

100% 是后果。

---

## 而且我现在发现了一个比 snapshot 更值得查的并发点

你的 RPC server 现在是这样的：

```cpp
auto shared_server =
    std::make_shared<rpc_server>(backends, cache_dir, tensor_source);

while (true) {
    auto client_socket = server_socket->accept();

    std::thread([shared_server, client_socket, n_devices]() {
        rpc_serve_client(shared_server, client_socket, n_devices);
    }).detach();
}
```

也就是说：

```text
COMPUTE socket thread
TRANSFER socket thread
SNAPSHOT socket 0 thread
SNAPSHOT socket 1 thread
```

全部共享：

```cpp
shared_server
    ↓
backends[0]
```

但是 `backends[0]` 周围没有任何 backend-level mutex。

所以完全可能发生：

```text
线程 A：TRANSFER
ggml_backend_tensor_get(...)
        │
        │ 还没结束
        │
线程 B：COMPUTE
ggml_backend_graph_compute(...)
        ↓
CPU backend / threadpool 被两个线程同时进入
        ↓
SEGFAULT
```

而且这正好能解释你目前所有现象。

### 为什么手机日志里没看到 GET？

因为现在 `get_tensor()` 的日志是在 **GET 完成以后**才打印：

```cpp
ggml_backend_tensor_get(...);

if (RPC_DEBUG) {
    GGML_LOG_INFO("[RPC_SERVER_GET] ...");
}
```

假设真实情况是：

```text
transfer thread:
    ggml_backend_tensor_get()
    ↓
    正在执行，还没返回

compute thread:
    ggml_backend_graph_compute()
    ↓
    SEGFAULT
```

那么：

```text
[RPC_SERVER_GET]
```

当然永远不会打印出来。

所以你现在看到：

```text
GRAPH_COMPUTE
→ Segmentation fault
```

并不能证明当时没有另外一个 `GET_TENSOR` 正在手机 backend 内执行。

---

# 我建议下一步先不要继续改 planner / snapshot

做一个**诊断性全局锁实验**。

这个实验非常值钱，而且十几分钟就能知道方向。

在 `rpc_server` 里先加：

```cpp
std::recursive_mutex backend_op_mutex;
```

为什么先用 `recursive_mutex` 而不是普通 mutex？

因为你有一些 RPC 操作内部会继续调用：

```text
set_tensor_recompute_snapshot
    → set_tensor_direct
    → graph_recompute_snapshot

graph_recompute_snapshot
    → snapshot_tensor_direct
```

普通 mutex 很容易自己把自己锁死。我们这里只是诊断，先用 recursive mutex。

然后首先只锁这几个真正会碰 backend 的函数：

```cpp
bool rpc_server::graph_compute(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::graph_recompute(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::graph_recompute_snapshot(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::get_tensor(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::set_tensor_direct(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::copy_tensor(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

```cpp
bool rpc_server::synchronize(...) {
    std::lock_guard<std::recursive_mutex> lock(backend_op_mutex);

    ...
}
```

如果想做得彻底一点，再把：

```cpp
snapshot_tensor_direct()
init_tensor()
buffer_clear()
```

也锁上。

### 然后跑你现在稳定复现的：

```text
T=5
P=0
C=43
G=11
XT=64
```

先不要管性能。

结果会非常有区分度：

```text
加 backend 全局锁之后
        │
        ├── 不再 segfault
        │      ↓
        │  基本实锤 RPC server 的 shared backend 并发
        │
        └── 仍然 segfault
               ↓
           graph 本身有坏 node / shape / pointer
```

我现在非常建议优先做这个实验。

---

## 同时我还注意到一个非常可疑的名字

你崩之前传的是：

```text
prefill_ffn_norm_chunk_0-43 (reshaped)
```

而不是：

```text
prefill_ffn_norm_chunk_0-43
```

你现在 RPC 里面判断 external input 的代码是：

```cpp
static bool rpc_is_prefill_external_input(
        const ggml_tensor * tensor) {

    static constexpr const char * prefix =
        "prefill_ffn_norm_chunk_";

    return tensor != nullptr &&
        std::strncmp(
            tensor->name,
            prefix,
            std::strlen(prefix)) == 0;
}
```

所以这些名字全部都会命中：

```text
prefill_ffn_norm_chunk_0-43

prefill_ffn_norm_chunk_0-43 (reshaped)

prefill_ffn_norm_chunk_0-43 (view)

甚至任何后面继承这个名字的 tensor
```

命中以后 RPC serializer 会干一件很激进的事：

```cpp
result.op = GGML_OP_NONE;

for (...) {
    result.src[i] = 0;
}

result.view_src  = 0;
result.view_offs = 0;

result.flags &= ~GGML_TENSOR_FLAG_COMPUTE;
```

也就是直接把它：

```text
原本：
RESHAPE
  ↓
真正 src

变成：
独立 leaf tensor
```

这本来就是你之前为了 RPC external input 做的设计。

但是现在手机真正收到的是：

```text
prefill_ffn_norm_chunk_0-43 (reshaped)
```

这意味着**被切断的其实是 reshape 后的 tensor**。

这个点我会列为第二嫌疑。

---

# 还有一个实验比 T=5/T=10 更有价值

现在你已经知道：

```text
T=1  → layer 47 Tensor → 正常
T=5  → layer 43~47     → layer43 崩
T=10 → layer 38~47     → 崩
```

建议直接跑：

```text
T=2 → C=46
T=3 → C=45
T=4 → C=44
T=5 → C=43
```

其它参数固定，尤其：

```text
G=11
P=0
R=0.667
XG=256
XC=256
```

这个测试非常有价值。

如果结果是：

```text
T=1  PASS
T=2  CRASH at layer46
T=3  CRASH at layer45
T=4  CRASH at layer44
T=5  CRASH at layer43
```

那么几乎可以直接得出：

> 不是 layer43 特殊，而是 **“非最后一层的 Tensor-Split layer”** 有 bug。

这时我们就去查：

```text
Tensor layer L
      ↓
l_out-L
      ↓
下一 Transformer layer L+1
```

这个跨层依赖。

而不是查某个 layer 权重。

反过来，如果：

```text
T=2 PASS
T=3 PASS
T=4 PASS
T=5 CRASH
```

那就完全不同了，更像：

```text
layer43 特定
或
达到某个 Tensor layer 数量 / 内存阈值
```

---

## 我尤其想看 T=2

因为它能回答最关键的问题：

```text
layer47 是模型最后一层
```

T=1 时 Tensor 只有：

```text
layer47
```

这是一个特殊情况。

它算完以后直接：

```text
output norm
→ lm_head
```

没有下一层 Transformer。

而 T=2：

```text
layer46 Tensor
layer47 Tensor
```

layer46 是第一个真正需要：

```text
Tensor FFN
→ l_out-46
→ 下一层 attention 47
```

的 Tensor 层。

如果 **T=2 立刻复现**，我会非常倾向于：

> 当前实现只偶然跑通了“最后一层 Tensor”，而普通的中间 Tensor layer 的 graph/subgraph 生命周期实际上有问题。

这个可能性现在很高。

---

# 另外，你的 `PIPE_SNAPSHOT` 开关我建议检查一下名字

我查了现在这个分支，代码里实际上没有读取：

```text
PIPE_SNAPSHOT
```

当前直接相关的是：

```cpp
GGML_META_PREFILL_DUAL_RETURN
```

以及：

```cpp
snapshot_prepares[i].prepared
```

决定：

```cpp
use_snapshot_pipeline
```

所以如果你所谓的“开关 PIPE_SNAPSHOT”只是：

```bash
PIPE_SNAPSHOT=0
```

而没有在你本地另外接代码，那它其实不会改变任何东西。

你截图加的：

```cpp
GGML_META_DISABLE_SNAPSHOT
```

才是真正在 prefill 路径把 snapshot prepare 短掉的。

最好临时再打印一句，避免环境变量没设到 PC 进程：

```cpp
if (std::getenv("GGML_META_DISABLE_SNAPSHOT") != nullptr) {
    fprintf(stderr,
            "[SNAPSHOT_DISABLED] sg=%zu tensor=%s\n",
            sg,
            tensor != nullptr ? tensor->name : "(null)");
    return;
}
```

如果日志出现：

```text
[SNAPSHOT_DISABLED]
```

才说明这个实验确实关掉了。

---

# 如果加 backend 全局锁以后仍然崩

那下一步我不会再猜了，直接抓**崩在哪一个 CPU op**。

因为你手机已经：

```text
Segmentation fault (core dumped)
```

最好直接利用 core。

手机上先：

```bash
ulimit -c unlimited
```

重新跑一次。

然后如果当前目录产生：

```text
core
core.xxx
```

执行：

```bash
gdb ./build-rpc/bin/ggml-rpc-server core
```

进去以后：

```gdb
bt
```

然后：

```gdb
bt full
```

我最想看到的是类似：

```text
#0 ggml_compute_forward_mul_mat_id ...
#1 ggml_compute_forward ...
#2 ggml_graph_compute_thread ...
#3 ggml_graph_compute ...
#4 ggml_backend_cpu_graph_compute ...
#5 ggml_backend_graph_compute ...
#6 rpc_server::graph_compute ...
```

如果真是这种，我们马上就知道是不是：

```text
MUL_MAT
MUL_MAT_ID
RESHAPE
GET_ROWS
MOE routing
```

哪一个。

---

### 所以现在我的嫌疑排序已经变了

1. **RPC server 的 shared `backends[0]` 被多个 socket thread 并发进入**
2. **非最后一层 Tensor-Split 的 graph / Meta subgraph 构造有问题**
3. **`prefill_ffn_norm_chunk_* (reshaped)` 被过宽地当成 external leaf**
4. 某个具体 layer 的 tensor/weight shape 或地址异常
5. snapshot
6. stage queue

其中 **5、6 现在已经明显降级**。

你下一步我建议只做两个事情：**先加 `backend_op_mutex` 跑一次固定 T=5；然后固定跑一个 T=2。** 这两次的结果基本就能决定我们接下来应该钻 `ggml-rpc.cpp` 的并发，还是钻 `ggml-backend-meta.cpp` 的跨层 graph。
