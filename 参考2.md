现在手机还是串行的，下一步该做的就是手机分块执行，也就是ffn_down那个矩阵乘法，弄成两个chunk。也就是说，手机要加上transfer worker，现在应该先实现这一点，手机算一个chunk，然后传这个chunk，传的同时算另一个chunk，电脑端这一阶段可以先不用动，之后再考虑收到chunk。还有你说的两个流水，就是电脑发送到手机的同时开始ffn的计算？第二个流水就是我前面说的，对不对





对，你现在这个理解是对的，而且我建议**下一步就只做你说的这一件事**，先别把 PC 端收到 chunk 后的 RMS/QKV 流水也一起掺进来。

你现在 `llama.cpp` 里已经把 `ffn_down` 沿输出 hidden 维切成了 chunk，并且 `LLAMA_CHUNKS` 默认就是 2。每个 chunk 分别构造 `ffn_down_chunk`，随后才有 residual、平方和、下一层 QKV partial 等逻辑。 

真正的问题是**执行层还是串行 barrier**：当前 `compute_workers.compute(i)` 会同时启动 PC/Phone 当前 subgraph，然后等待所有 backend 完成；之后主线程才进入 `specialized_communication()` 做 Phone→PC copy/reduce。 

所以现在实际更像：

```text
Phone:
    down chunk0
        ↓
      等待
        ↓
    transfer chunk0
        ↓
      等待
        ↓
    down chunk1
        ↓
    transfer chunk1
```

你下一阶段要做成：

```text
Phone compute:   down chunk0 █████ | down chunk1 █████
                                \
Transfer:                       transfer chunk0 █████
                                              \
                                               transfer chunk1 █████
```

也就是：

> **chunk0 一算完就把它交给 transfer worker，然后 Phone compute worker 马上继续 chunk1，不等 chunk0 传完。**

这就是你现在应该先实现的东西。

---

### 你说的“两条流水”，也确实就是这两条

**第一条流水是 FFN 入口：**

```text
                   ┌──── PC local FFN
Attention / norm ──┤
                   └──── PC→Phone transfer ─── Phone FFN
```

意思是 PC 做完 attention、得到 FFN 要使用的输入以后：

```text
PC -> Phone 发 x_ffn
```

这次发送不应该阻塞 PC 自己的 FFN。

所以理想时间线：

```text
PC:
Attention █████
              ├── PC FFN █████████
              │
Transfer:     └── send x_ffn ███
                               │
Phone:                         FFN █████████
```

注意不是：

> “PC 往手机传的同时，手机开始 FFN”。

手机当然必须先收到输入才能算。

真正 overlap 的是：

> **PC→Phone 传 FFN 输入的同时，PC 自己开始算 FFN。**

这就是第一条。

---

**第二条就是你现在正在说的 Wdown 输出流水：**

```text
Phone:
gate/up → SwiGLU
             ↓
       down chunk0
             │
             ├── transfer chunk0 → PC
             │
             └── down chunk1
```

时间线：

```text
Phone compute:
        down0 ███████
                    down1 ███████

Phone→PC:
                  TX0 █████
                               TX1 █████
```

以后再继续演化成：

```text
Phone:
down0 ███
        \
         TX0 ███
             \
              down1 ███
                     \
                      TX1 ███

PC:
             recv0
               ↓
            reduce0
               ↓
             QKV0 ███
```

所以你可以把整个 decode 的两处 overlap 很清楚地描述成：

```text
Pipeline A：FFN input overlap
PC→Phone activation transfer
        ||
PC local FFN compute


Pipeline B：FFN output chunk overlap
Phone→PC Wdown chunk transfer
        ||
Phone next Wdown chunk compute
```

然后未来才有第三种 overlap：

```text
Pipeline C：
PC 对 chunk i 做 reduce / next-layer QKV_i
        ||
Phone 算 Wdown chunk i+1
```

但 **C 现在不用做**。

---

## 所以你现在下一步的目标可以非常明确

只实现：

```text
FFN down = 2 chunks

Phone:
    compute down0
    ↓
    enqueue TX0
    ↓
    compute down1

Transfer worker:
    TX0
    ↓
    TX1
```

PC 收到之后目前依然可以：

```text
等完
↓
按现在逻辑继续
```

也就是说这一版**即使 PC 还没吃到真正的跨层流水收益也没关系**。

这一版唯一验证的东西就是：

> **Phone 的 chunk1 compute 是否和 chunk0 transfer 真正重叠。**

---

### 但这里我想修正一个叫法

你说“**手机要加 transfer worker**”，从逻辑架构上这么说没问题，但按照你现在的代码，worker **不一定应该真的创建在手机 rpc-server 代码里**。

目前 Phone→PC 这次传输是 Meta backend 在 PC 侧通过：

```cpp
ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    nodes[j_src],
    node_tmp);
```

发起的。

因此第一版最自然的是：

```text
PC / Meta backend
    ├── Phone compute control worker
    └── Phone→PC transfer worker
```

transfer worker 自己去调用 RPC copy。

从功能上看仍然是：

```text
Phone computation
||
Phone→PC transfer
```

只是控制线程实际存在 PC 端。

这和你 prefill 做双向 transfer worker 的结构应该也比较容易复用。

---

## 真正需要先拆掉的是这个 barrier

现在：

```cpp
compute_workers.compute(i);
```

里面：

```cpp
task_index = i;
++generation;
task_cv.notify_all();

done_cv.wait(...所有 backend 完成...);
```



然后外面：

```cpp
compute(i);
transfer/reduce(i);
compute(i + 1);
```



这结构天然不可能实现：

```text
TX(i) || Phone compute(i+1)
```

因为 `compute(i+1)` 必须等 `transfer(i)` 返回。

所以你第一版甚至都不用把整个调度器彻底重构。

可以专门针对：

```text
ffn_down_chunk
```

搞一条 experimental fast path。

例如逻辑上：

```cpp
phone_compute(chunk0);
wait_phone_compute(chunk0);

// 不再同步执行 transfer
transfer_worker.enqueue(chunk0);

// 马上
phone_compute(chunk1);

// 最后需要结果时
transfer_worker.wait(chunk0);
```

当然真实代码不能简单这样调用，因为 PC/Phone 当前都封在 subgraph 里，但**调度目标就是这个**。

---

### 我会把这一步拆成 4 个很小的 commit

**A. 先确认 Phone 的两个 Wdown chunk 确实是两个独立 execution boundary。**

日志只需要：

```text
[PHONE_DOWN_BEGIN] layer=0 chunk=0
[PHONE_DOWN_END]   layer=0 chunk=0

[PHONE_DOWN_BEGIN] layer=0 chunk=1
[PHONE_DOWN_END]   layer=0 chunk=1
```

如果现在 subgraph 划分不是这样，先把它整理出来。

---

**B. 加一个持久化 transfer worker。**

类似：

```cpp
struct meta_transfer_worker {
    std::thread worker;

    std::mutex mutex;
    std::condition_variable cv;

    std::deque<transfer_task> queue;

    bool stop = false;
};
```

task 最开始只需要：

```cpp
struct transfer_task {
    ggml_backend_t src_backend;
    ggml_backend_t dst_backend;

    ggml_tensor * src;
    ggml_tensor * dst;

    int layer;
    int chunk;
};
```

worker：

```text
wait task
   ↓
Phone→PC copy
   ↓
mark transfer_done
```

---

**C. chunk0 算完以后只 enqueue，不 wait。**

即：

```text
Phone down0 done
       ↓
enqueue TX0
       ↓
立即允许 Phone down1
```

这是这个 commit 最关键的变化。

---

**D. 在真正需要 chunk0 数据以前才 wait。**

因为你暂时不改 PC 收到后的处理，可以很保守：

```text
Phone down0
   ↓
TX0 async
   ||
Phone down1
   ↓
TX1

然后在原本整体 reduce / 后续 graph 需要之前：
wait TX0
wait TX1
```

这样数学行为完全不变，但通信已经能被 Phone compute 藏掉一部分。

---

## 第一版理想日志

比如你最后打出来：

```text
t=0.000  PHONE down0 start
t=3.500  PHONE down0 end

t=3.550  TX0 start
t=3.600  PHONE down1 start

t=6.200  TX0 end
t=7.100  PHONE down1 end

t=7.150  TX1 start
t=9.800  TX1 end
```

那就已经成功了。

最重要的是这两个时间区间有交集：

```text
TX0:
3.55 ---------------- 6.20

Phone down1:
3.60 ---------------------- 7.10
```

这就证明第二条流水真的跑起来了。

而如果日志还是：

```text
down0 end
TX0 start
TX0 end
down1 start
```

那 transfer worker 虽然创建了，实际依然是串行。

---

还有一个很实际的限制：**必须确认 RPC 的 compute 和 tensor copy 能并行。**

如果 Phone RPC backend 内部：

```text
GRAPH_COMPUTE
TENSOR_GET / COPY
```

最终共用一个全局 mutex / 同一条阻塞连接，并且 server 一次只能处理一个 request，那么你 PC 端即便开了 transfer worker，也会出现：

```text
线程层面并行
实际上 RPC server 串行
```

所以完成 worker 后第一件事不是看 tok/s，而是看时间戳。

如果：

```text
TX0 正在进行期间
PHONE GRAPH_COMPUTE(chunk1)
确实开始了
```

就成功。

如果开始不了，再去改 RPC 层的独立连接/worker，而不是先怀疑 FFN graph。

---

所以现在的开发路线我认为已经可以收得非常窄：

```text
现在：
Phone down0 → TX0 → Phone down1 → TX1

这一版只改成：

Phone down0 ──┬── TX0
              │
              └── Phone down1 ── TX1
```

**PC 端收到 chunk 以后怎么做，暂时保持原样。**

等这个验证成功，再做：

```text
PC recv chunk0
    ↓
reduce
    ↓
sum_sq + gamma
    ↓
QKV partial0

       ||

Phone down chunk1
```

这就是下一阶段。

而你问的两条大流水，答案就是：**对，第一条是 PC→Phone 的 FFN 输入传输与 PC 本地 FFN 重叠；第二条就是 Phone 的 Wdown chunk i 传输与 Wdown chunk i+1 计算重叠。**
