对，**应该推广**，而且从现在的数据看，方向已经比较明确了。但要区分两件事：

1. **减少一次逻辑上的跨设备依赖**——收益最大，但很难，因为 tensor-split 下每层 Wdown 后 PC 都必须拿到 Phone partial 才能继续下一层。
2. **减少同一次逻辑通信内部的 RPC 包/`send()`/server dispatch 次数**——我们刚才做的就是这个，改动小，而且已经验证有效。

当前 `GET_SNAPSHOT` 已经从：

```text
request:
send cmd
send size
send request

response:
send size
send payload
```

压成：

```text
request:
send cmd+size+request

response:
send payload
```

而稳定阶段已经出现：

```text
1->0 avg = 18.784 ms
```

相比此前常见约 `20 ms+` 又下降了一截。

所以我建议**继续沿这个方向，但按收益/风险分三步推进。**

## 第一优先级：推广到 `GRAPH_RECOMPUTE` + `SNAPSHOT_TENSOR`

这两个最适合。

现在通用 `send_rpc_cmd()` 仍然是三次：

```cpp
uint8_t cmd_byte = cmd;

sock->send_data(&cmd_byte, sizeof(cmd_byte));
sock->send_data(&input_size, sizeof(input_size));
sock->send_data(input, input_size);
```

也就是每条 RPC 至少 3 次 `send_data()`。

decode 每一层 Phone 至少有：

```text
GRAPH_RECOMPUTE
SNAPSHOT_TENSOR
```

而 `GRAPH_RECOMPUTE` 现在就是直接走普通 `send_rpc_cmd()`。

`snapshot_arm()` 也是一样：

32 层意味着：

```text
GRAPH_RECOMPUTE   32 次/token
SNAPSHOT_TENSOR  32 次/token
```

每条从 `3 send → 1 send`：

```text
64 RPC × 少 2 次 send
= 每 token 少 128 次 send()
```

这比刚才 `GET_SNAPSHOT` request 的推广空间还大。

### 我建议做一个通用 small-RPC helper

而不是再写两个专用函数：

```cpp
template <typename T>
static bool send_rpc_cmd_compact_small(
        socket_ptr sock,
        enum rpc_cmd cmd,
        const T & request) {

    constexpr size_t payload_size = sizeof(T);

    std::array<
        uint8_t,
        1 + sizeof(uint64_t) + payload_size
    > packet {};

    size_t pos = 0;

    packet[pos++] =
        static_cast<uint8_t>(cmd);

    const uint64_t size64 =
        static_cast<uint64_t>(payload_size);

    memcpy(
        packet.data() + pos,
        &size64,
        sizeof(size64));

    pos += sizeof(size64);

    memcpy(
        packet.data() + pos,
        &request,
        payload_size);

    return sock->send_data(
        packet.data(),
        packet.size());
}
```

然后 `GRAPH_RECOMPUTE`：

```cpp
bool status =
    send_rpc_cmd_compact_small(
        sock,
        RPC_CMD_GRAPH_RECOMPUTE,
        request);
```

`snapshot_arm()`：

```cpp
return send_rpc_cmd_compact_small(
    sock,
    RPC_CMD_SNAPSHOT_TENSOR,
    request);
```

服务端**完全不用改**。

因为仍然是：

```text
| cmd 1B | size 8B | payload |
```

只是从 3 次 TCP write 变成一次。

我会先只推广这两个。

---

## 第二优先级：PC→Phone 的 `ffn_norm` SET_TENSOR

这个也是每层一次：

```text
PC -> Phone
ffn_norm-X
16384 bytes
```

当前 `ggml_backend_rpc_buffer_set_tensor()` 最后仍然：

```cpp
const bool status = send_rpc_cmd(
    ctx->sock,
    RPC_CMD_SET_TENSOR,
    input.data(),
    input.size());
```



因此它也是：

```text
send cmd
send size
send ~16.7 KB payload
```

每 token 32 个 `ffn_norm`，再加一个 `result_norm`，大约：

```text
33 × 3 sends
```

不过这里我**不建议简单套刚才的 small helper**。

原因是它的 payload 已经约 16 KB。如果为了变成一次 send：

```text
原来的 input vector 16KB
↓
再 memcpy 到 packet 16KB
↓
send packet
```

会额外制造一次 16 KB 内存复制。

虽然 16KB 很小，但没必要。

### 更好的办法

先把通用 `send_rpc_cmd()` 从：

```text
3 sends
```

改成：

```text
2 sends
```

把前两个头合起来：

```cpp
static bool send_rpc_cmd(
        socket_ptr sock,
        enum rpc_cmd cmd,
        const void * input,
        size_t input_size) {

    std::array<uint8_t, 1 + sizeof(uint64_t)> header {};

    header[0] =
        static_cast<uint8_t>(cmd);

    const uint64_t size64 =
        static_cast<uint64_t>(input_size);

    memcpy(
        header.data() + 1,
        &size64,
        sizeof(size64));

    if (!sock->send_data(
            header.data(),
            header.size())) {
        return false;
    }

    if (input_size == 0) {
        return true;
    }

    return sock->send_data(
        input,
        input_size);
}
```

这样所有普通 RPC 自动：

```text
旧：
cmd       send #1
size      send #2
payload   send #3

新：
cmd+size  send #1
payload   send #2
```

服务器还是一行不用改。

然后：

```text
SET_TENSOR 33 次/token
```

又能少 33 次 `send()`。

对于大 RPC 也不会增加 payload memcpy。

---

## 但 small RPC 应继续做到“一次 send”

所以最后形成两级：

```text
小 request：

GRAPH_RECOMPUTE
SNAPSHOT_TENSOR
GET_SNAPSHOT
...

cmd+size+payload
     ↓
一次 send


大 request：

SET_TENSOR
GRAPH_COMPUTE 首次建图
...

cmd+size
   ↓
send #1

payload
   ↓
send #2
```

这是比较合理的通用 RPC transport 设计。

例如可以：

```cpp
static constexpr size_t RPC_COMPACT_THRESHOLD = 512;
```

然后通用实现：

```cpp
if (input_size <= RPC_COMPACT_THRESHOLD) {
    // 一次 send
} else {
    // 9-byte header + payload
    // 两次 send
}
```

这样以后就不用针对每个 command 手写 helper 了。

---

## 再下一步才是真正值得做的大改：融合 GRAPH_RECOMPUTE + SNAPSHOT

上面的推广我预计还能吃一点性能，但大概仍然属于几个百分点。

真正更有意思的是现在 Phone compute socket 每层的顺序：

```text
PC -> Phone ffn_norm
        ↓
GRAPH_RECOMPUTE
        ↓
Phone Wdown
        ↓
SNAPSHOT_TENSOR
        ↓
snapshot READY
```

我们实际上没有任何理由一定要把：

```text
GRAPH_RECOMPUTE
SNAPSHOT_TENSOR
```

做成两个独立 RPC。

因为 `SNAPSHOT_TENSOR` 必须严格发生在这个 graph compute 后面。

所以可以新建：

```cpp
RPC_CMD_GRAPH_RECOMPUTE_SNAPSHOT
```

request 类似：

```cpp
struct rpc_msg_graph_recompute_snapshot_req {
    uint32_t device;

    uint32_t slot;
    uint64_t graph_uid;
    uint64_t seq;

    uint32_t output_node;
    uint64_t offset;
    uint64_t size;
};
```

Phone：

```cpp
graph_recompute_snapshot(request)
{
    ggml_cgraph * graph =
        stored_graphs[device][graph_uid].graph;

    // 1. Phone Wdown
    ggml_backend_graph_compute(
        backends[device],
        graph);

    // 2. 找 Wdown 输出
    ggml_tensor * tensor =
        graph->nodes[request.output_node];

    // 3. 直接 fill snapshot slot
    snapshot_tensor_direct(
        tensor,
        request.slot,
        request.seq,
        request.offset,
        request.size);
}
```

这样每层：

```text
旧：
GRAPH_RECOMPUTE RPC
SNAPSHOT_TENSOR RPC

新：
GRAPH_RECOMPUTE_SNAPSHOT RPC
```

而且还有一个额外优势：

现在 `SNAPSHOT_TENSOR` request 带了一整个：

```cpp
rpc_tensor tensor;
```

它其实挺大的。

但 graph 已经缓存以后，Phone server 本来就拥有：

```cpp
stored_graphs[device][graph_uid].graph
```

根本没必要每层重新序列化 `ffn_down_chunk` 的完整 `rpc_tensor`。

只需要告诉它：

```text
graph_uid
output_node = 3
slot
seq
size
```

就够了。

你现在日志里 Phone 的复用 graph 固定：

```text
nodes=4
```

所以很可能：

```cpp
graph->nodes[graph->n_nodes - 1]
```

就是 `ffn_down_chunk_0-X`。

不过我不会直接硬编码 `last node`，最好 client 第一次确认并传一个 `output_node` index，风险更低。

---

## 最终还能进一步融合成“一层一次 compute RPC”

更进一步：

```text
现在每层 compute socket：

SET_TENSOR(ffn_norm)
GRAPH_RECOMPUTE
SNAPSHOT_TENSOR
```

最终可以变成：

```text
DECODE_REMOTE_FFN
```

一次 RPC：

```text
PC
 │
 │ ffn_norm 16KB
 ▼
Phone server
 │
 ├─ set ffn_norm
 ├─ graph_recompute
 ├─ snapshot output
 └─ READY
```

与此同时 transfer socket 早就：

```text
GET_SNAPSHOT(seq=N)
↓
等待 READY
```

于是整层变成：

```text
compute socket:
┌──────────────────────────────────┐
│ DECODE_REMOTE_FFN                │
│                                  │
│ ffn_norm upload                  │
│       ↓                          │
│ Wdown compute                    │
│       ↓                          │
│ snapshot fill                    │
└──────────────────────────────────┘

transfer socket:
GET_SNAPSHOT
      ↓
 wait READY
      ↓
16KB raw response
```

这就从现在每层大约：

```text
3 个 compute-side RPC
+
1 个 transfer-side RPC
```

变成：

```text
1 个 compute-side RPC
+
1 个 transfer-side RPC
```

这才是我认为协议优化的最终形态。

---

但有一条边界要明确：

**我们现在能减少的是 RPC 命令/小包/syscall 数，不是 32 次 Phone→PC 的数学同步依赖。**

因为 tensor-split FFN 是：

```text
PC partial Wdown ─┐
                  ├─ reduce → residual/norm → 下一层
Phone partial ────┘
```

下一层必须依赖这一层 reduce 后的 hidden state。

所以不能简单：

```text
32 层算完
↓
一次传回来
```

那在当前 tensor parallel 结构下数学上走不通。

要把真正的：

```text
32 次 Phone→PC
```

变成例如：

```text
4 次
```

就必须改变模型划分方式，比如让 Phone 连续完整拥有若干层，而不是每一层都 tensor-split。这已经是另一个大方向了。

### 我建议现在的执行顺序

先做最稳的一刀：

```text
1. GRAPH_RECOMPUTE：3 send → 1
2. SNAPSHOT_TENSOR：3 send → 1
```

不动服务端。

如果这还能让：

```text
18.8~20 ms/layer
↓
17~18 ms/layer
```

那就继续：

```text
3. 通用 send_rpc_cmd：
   大 payload 3 send → 2

4. 再做
   GRAPH_RECOMPUTE + SNAPSHOT_TENSOR
   → 单个 RPC
```

**我会优先做 1+2。** 因为这一步理论上每 token 能直接再干掉 **128 次 `send()`**，改动非常小，而且不会触碰我们已经验证正确的 `ffn_norm / result_norm / PC-only SG` 数学边界。
