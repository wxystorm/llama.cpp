这个文件把问题基本定位死了：**现在中间层流程已经对，真正漏的是最后一层 FFN 之后的输出阶段。**

你现在的新路径和 baseline 在第一个 decode 后已经不是完全一致：很多低 token ID 的 logit 一模一样，但 baseline 里的 `24455`、`28179` 在新路径里明显异常；到第二个 decode 更明显，baseline 的 token `27262` 是 `26.296035767`，新路径却只有 `12.785885811`。与此同时 `10115`、`512`、`20602`、`3041` 等又完全一致。   

这非常像：

```text
PC负责的 vocab shard     logits 正确
Phone负责的 vocab shard  logits 错
```

而不是 Transformer hidden 整体算错。

## 根因：最后一层没有下一层 `ffn_norm`

中间层现在是：

```text
chunk1 Phone -> PC
        ↓
PC full chunk1
        ↓
PC-only SG
        ↓
Q'K'V'
Attention
residual
FFN norm
        ↓
ffn_norm -> Phone
```

所以中间层已经闭环。

但第 31 层是最后一层，`llama.cpp` 这里没有：

```text
next layer ffn_norm
```

而是：

```text
last layer chunk0/chunk1
        ↓
完整 final hidden
        ↓
output RMSNorm
        ↓
result_norm
        ↓
lm_head
        ↓
result_output/logits
```

代码就是：

```cpp
cur = inpL;

cur = build_norm(
    cur,
    model.output_norm,
    NULL,
    LLM_NORM_RMS,
    -1);

cb(cur, "result_norm", -1);

cur = build_lora_mm(model.output, cur, model.output_s);

cb(cur, "result_output", -1);
```



而 `model.output` 是：

```cpp
output = create_tensor(
    ...,
    {n_embd, n_vocab},
    ...);
```

也就是最终 lm_head 本身也是 tensor-split 的。

所以 **Phone 最后必须参与 `result_output` 的计算**，因为 Phone 持有自己那部分 output weight / vocab shard。

问题就在这里。

---

# 现在最后一层实际上发生的是

最后一个：

```text
ffn_down_chunk_1-31
```

完成 Phone→PC reduce 后：

### PC

有：

```text
full chunk0
full chunk1
        ↓
完整最后一层 hidden
```

### Phone

只有：

```text
自己的 partial chunk0
自己的 partial chunk1
```

而且现在我们已经不传完整 `ffn_inp` / layer output 给 Phone，这本来就是故意的。

接下来最后那个 SG 又让两端执行：

```text
Phone:
partial/stale final hidden
        ↓
result_norm       ❌
        ↓
Phone自己的 lm_head shard
        ↓
部分 vocab logits ❌
```

PC：

```text
full final hidden
        ↓
result_norm       ✅
        ↓
PC自己的 lm_head shard
        ↓
部分 vocab logits ✅
```

这就完美解释了为什么：

> **一部分 token logit 精确一致，另一部分 token logit 大幅错误。**

如果 Q'K'V' 或 attention 本身错了，不可能出现这么多 logits 精确到小数点后很多位还完全相同。

---

# 正确修改：最后增加一个 `result_norm` 同步点

最终结构应该是：

```text
                    最后一层

PC chunk1 partial       Phone chunk1 partial
        │                       │
        │<──────────────────────┘
        ↓
PC full chunk1

        ↓

PC only:
last FFN residual
concat final hidden
output RMSNorm
        ↓
   result_norm
        │
        └──────────────────────> Phone
                                  │
              ┌───────────────────┴──────────────────┐
              ▼                                      ▼
       PC lm_head shard                     Phone lm_head shard
              │                                      │
              ▼                                      ▼
         vocab部分                               vocab部分

                  最终组成完整 logits
```

注意：

**我们不是把最终 layer output 发给 Phone。**

发送的是：

```text
result_norm
```

即整个模型最后只额外发一次 4096 维向量，16 KB。

所以你的原始设计完全没被破坏：

```text
每层：
PC -> Phone    ffn_norm      16 KB

FFN：
Phone -> PC    chunk          8 KB × 2

最后额外一次：
PC -> Phone    result_norm   16 KB
```

这个最后的 16 KB 是为了让两端分别完成自己的 lm_head vocab shard。

---

# 第一处修改：把 `result_norm` 也切成一个 SG boundary

你现在：

```cpp
const bool new_subgraph =
    i + 1 == cgraph->n_nodes ||
    split_state.axis == GGML_BACKEND_SPLIT_AXIS_PARTIAL;
```

改成：

```cpp
const bool is_decode_result_norm =
    node->ne[1] == 1 &&
    std::strncmp(node->name, "result_norm", 11) == 0;

const bool new_subgraph =
    i + 1 == cgraph->n_nodes ||
    split_state.axis == GGML_BACKEND_SPLIT_AXIS_PARTIAL ||
    is_decode_result_norm;
```

这样原来最后：

```text
SG96:
last chunk downstream
result_norm
result_output
```

会拆成：

```text
SG96:
last chunk downstream
result_norm
---------------- boundary

SG97:
result_output
```

因此：

```text
97 SG
```

应该变成：

```text
98 SG
```

---

# 第二处：`result_norm` 所在 SG 也必须 PC-only

你刚加的：

```cpp
is_decode_pc_only_ffn_norm_sg()
```

不要只识别：

```text
ffn_norm
```

再加：

```text
result_norm
```

例如：

```cpp
const bool is_ffn_norm =
    std::strncmp(
        pc_last->name,
        "ffn_norm-",
        9) == 0;

const bool is_result_norm =
    std::strncmp(
        pc_last->name,
        "result_norm",
        11) == 0;

if (!is_ffn_norm && !is_result_norm) {
    return false;
}
```

但这里还有一点：

### `result_norm` 当前 Phone 的 COMPUTE flag 很可能还是 1

所以你原来：

```cpp
return pc_compute && !phone_compute;
```

可能过不了。

最好在构图时直接明确：

```text
decode result_norm：
PC    COMPUTE=1
Phone COMPUTE=0
```

例如在 simple node 都建立好后：

```cpp
if (node->ne[1] == 1 &&
        std::strncmp(node->name, "result_norm", 11) == 0) {

    // output norm belongs to PC.
    for (size_t j = 1; j < n_backends; ++j) {
        backend_ctx->backend_configs[j]
            .nodes[i]
            ->flags &= ~GGML_TENSOR_FLAG_COMPUTE;
    }
}
```

这样你的现有：

```cpp
is_decode_pc_only_ffn_norm_sg()
```

稍微扩展名称就能继续用。

---

# 第三处：这样现有 `active_count == 1` 就会自动帮你 copy

这是最漂亮的地方。

`SG96` PC-only 算完：

```text
result_norm
```

以后：

```cpp
specialized_communication()
```

看到：

```text
PC result_norm    COMPUTE=1
Phone result_norm COMPUTE=0

active_count = 1
```

你已有代码自然走：

```cpp
ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    nodes[active_backend],
    nodes[j_dst]);
```

所以直接：

```text
PC result_norm -> Phone
```

不需要新写 transfer path。

然后 SG97：

```text
result_output
```

仍然：

```cpp
compute_workers.compute(97);
```

两边一起算。

这时候两边输入已经都是同一个完整：

```text
result_norm
```

所以：

```text
PC:
自己那部分 output weight
× full result_norm
= 正确 vocab shard

Phone:
自己那部分 output weight
× full result_norm
= 正确 vocab shard
```

最终 logits 就应该恢复。

---

## 修改后的日志应该非常明显

现在你是：

```text
subgraphs=97
direct=32
reduce_to_primary=64
```



改完应该变成：

```text
subgraphs=98

direct=33
          ↑
32次 ffn_norm
+
1次 result_norm

reduce_to_primary=64
          ↑
32层 × 2 chunks
```

PC→Phone：

```text
32 × 16 KB ffn_norm
+
1 × 16 KB result_norm
=
528 KB
≈ 0.516 MiB
```

所以你应该看到类似：

```text
[DECODE_PC_ONLY_SG]
sg=96
...
last=result_norm

[META_COPY]
sg=96
0->1
tensor=result_norm
bytes=16384

[META_PIPELINE]
subgraphs=98
direct=33
reduce_to_primary=64

[META_COPY_SUM]
0->1 count=33
bytes≈0.516 MiB
```

然后最后：

```text
SG97:
result_output
```

两端正常执行，不需要通信。

---

### 为什么我现在对这个判断把握很高

因为你这个 logit 差异太有特征了。

第一步 baseline：

```text
7541   29.877309799
1091   17.832878113
3224   17.561458588
6202   14.297574997
2666   14.163731575
```

新版本这些值**一模一样**。 

第二步也是：

```text
10115  16.964710236
512    15.842266083
20602  12.228991508
3041   12.227256775
6694   12.193239212
```

一模一样。

但：

```text
token 27262

baseline:
26.296035767

新版本:
12.785885811
```

 

这几乎就是在告诉我们：

```text
hidden 给 PC vocab shard 的路径正确
hidden 给 Phone vocab shard 的路径错误
```

而最后 `result_output` 恰恰就是**第一个必须让 Phone 再次参与、但 Phone 又需要完整输入**的地方。

所以现在先不要再动 Q'K'V'、early-Wdown 或中间层 `ffn_norm`。**只补“final PC-only tail → result_norm → PC→Phone → 两端 lm_head”这一处。**

这应该就是当前 correctness 的最后一个明显缺口。
