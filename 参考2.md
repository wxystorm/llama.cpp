可以，而且我刚看了你这版最新源码。现在已经是**逻辑融合完成了，但 hot path 还没有完全“做干净”**。最明显的是：你现在的 fused RPC 依然是 **2 次 `send_data()` + 2 次 16KB 数据 memcpy + 服务端每层一次 deserialize/临时 context + 一条无条件日志**。

最先建议把 **Phone 端这个无条件日志关掉**。现在 `set_tensor_recompute_snapshot()` 每层都会调用 `set_tensor_direct()`，而 `set_tensor_direct()` 里面还有：

```cpp
GGML_LOG_INFO(
    "[%s] buffer: %p, data: %p, offset: %" PRIu64
    ", size: %zu, name: '%s'\n",
    ...);
```

也就是说 decode 每 token 大约会打印 **32 次**。

直接改成：

```cpp
if (RPC_DEBUG) {
    printf(
        "[RPC_SET_DIRECT] "
        "offset=%" PRIu64
        " size=%zu "
        "name=%s\n",
        offset,
        size,
        in_tensor.name);
}
```

最终性能测试则把 `GGML_RPC_DEBUG` 也关掉。这一项最安全，先做。

更重要的是，你现在所谓的“一次 fused RPC”，**实际上仍然不是一次 socket send**。当前代码先建立：

```cpp
std::vector<uint8_t> payload(
    sizeof(request) + fused_input.data.size());

memcpy(payload.data(), &request, sizeof(request));
memcpy(
    payload.data() + sizeof(request),
    fused_input.data.data(),
    fused_input.data.size());
```

然后调用：

```cpp
send_rpc_cmd(
    sock,
    RPC_CMD_SET_TENSOR_RECOMPUTE_SNAPSHOT,
    payload.data(),
    payload.size());
```



而你的 `send_rpc_cmd()` 现在是：

```text
send_data(cmd + size)   #1
send_data(payload)      #2
```

也就是说现在：

```text
逻辑 RPC 数：1       ✓
send_data 数：2       还没压完
```

而且数据目前走了：

```text
PC ffn_norm 原始数据
      │
      │ memcpy 16KB
      ▼
fused.data
      │
      │ memcpy 16KB
      ▼
payload
      │
      ▼
socket
```

第一次复制就在 `ggml_backend_rpc_buffer_set_tensor()`：

第二次复制就在 fused graph 分支。

### 所以下一刀我建议一次把这两个问题一起解决

不要简单再加一个 `send_rpc_cmd_compact_dynamic()`，因为那样很容易变成：

```text
fused.data
→ payload
→ compact packet
```

反而多一层 copy。

正确做法是：**capture `ffn_norm` 时就直接构造最终 wire packet**。

把现在：

```cpp
struct rpc_pending_fused_ffn_input {
    ...
    rpc_tensor serialized_tensor {};
    uint64_t offset = 0;
    std::vector<uint8_t> data;
};
```

改成大概：

```cpp
struct rpc_pending_fused_ffn_input {
    std::mutex mutex;

    bool capture_next_set = false;
    const ggml_tensor * expected_tensor = nullptr;

    bool pending = false;
    const ggml_tensor * tensor = nullptr;

    rpc_tensor serialized_tensor {};
    uint64_t offset = 0;
    size_t data_size = 0;

    // 最终 wire packet
    std::vector<uint8_t> packet;
};
```

定义：

```cpp
static constexpr size_t FUSED_RPC_PREFIX =
    1 + sizeof(uint64_t);

static constexpr size_t FUSED_REQ_OFFSET =
    FUSED_RPC_PREFIX;

static constexpr size_t FUSED_DATA_OFFSET =
    FUSED_REQ_OFFSET +
    sizeof(rpc_msg_set_tensor_recompute_snapshot_req);
```

然后在 `ggml_backend_rpc_buffer_set_tensor()` capture 的地方，不再：

```cpp
fused.data.resize(size);
memcpy(fused.data.data(), data, size);
```

而是：

```cpp
rpc_msg_set_tensor_recompute_snapshot_req request {};

request.input_tensor = serialized_tensor;
request.input_offset = offset;
request.input_size   = size;

const uint64_t payload_size =
    sizeof(request) + size;

fused.packet.resize(
    FUSED_RPC_PREFIX + payload_size);

// cmd
fused.packet[0] =
    static_cast<uint8_t>(
        RPC_CMD_SET_TENSOR_RECOMPUTE_SNAPSHOT);

// request size
memcpy(
    fused.packet.data() + 1,
    &payload_size,
    sizeof(payload_size));

// request 现在 graph 部分先留 0
memcpy(
    fused.packet.data() + FUSED_REQ_OFFSET,
    &request,
    sizeof(request));

// ffn_norm 只复制这一次
memcpy(
    fused.packet.data() + FUSED_DATA_OFFSET,
    data,
    size);

fused.data_size = size;
fused.pending = true;

return;
```

此时：

```text
ffn_norm
   │
   │ memcpy 一次
   ▼
最终网络 packet
```

后面到 `ggml_backend_rpc_graph_compute()` 时，只需要补几个几十字节的 graph 字段：

```cpp
rpc_msg_set_tensor_recompute_snapshot_req request {};

memcpy(
    &request,
    fused_packet.data() + FUSED_REQ_OFFSET,
    sizeof(request));

request.graph.device      = rpc_ctx->device;
request.graph.slot        = graph_snapshot.slot;
request.graph.graph_uid   = graph_uid;
request.graph.seq         = graph_snapshot.seq;
request.graph.output_node = output_node;
request.graph.offset      = graph_snapshot.offset;
request.graph.size        = graph_snapshot.size;

memcpy(
    fused_packet.data() + FUSED_REQ_OFFSET,
    &request,
    sizeof(request));
```

最后**不再调用 `send_rpc_cmd()`**，直接：

```cpp
const bool status =
    sock->send_data(
        fused_packet.data(),
        fused_packet.size());

RPC_STATUS_ASSERT(status);
```

这样变成真正的：

```text
capture:
ffn_norm
  ↓ 1 次 memcpy
最终 packet

graph_compute:
只 patch header

network:
1 次 send_data()
```

也就是：

```text
当前：
2 × 16KB memcpy
2 × send_data

下一版：
1 × 16KB memcpy
1 × send_data
```

这个我认为是你现在 **1.2 后最应该先测的一刀**。你之前的实验已经反复证明 `send_data()` 次数对这套链路很敏感。

---

再往后还有一个更“干净”的地方，而且我觉得也值得做。

你现在 fused request 每层还携带完整：

```cpp
rpc_tensor input_tensor;
```

服务端收到后调用：

```cpp
set_tensor_direct(...)
```

而 `set_tensor_direct()` 每层又：

```text
ggml_init()
deserialize_tensor()
查 buffer
bounds check
ggml_backend_tensor_set()
```



但其实完全没必要。

因为第一次 `GRAPH_COMPUTE` 时，服务端已经创建了：

```cpp
std::unordered_map<uint64_t, ggml_tensor *> tensor_map;
```

而且 `create_node()` 会把包括 src 在内的所有 tensor 都递归放进去。 服务端现在只是构图结束以后把这个 `tensor_map` 丢掉了；目前 `stored_graph` 只保存 `buffer + graph`。

可以改成：

```cpp
struct stored_graph {
    std::vector<uint8_t> buffer;
    ggml_cgraph * graph = nullptr;

    std::unordered_map<
        uint64_t,
        ggml_tensor *> tensors;
};
```

第一次 graph 构建结束：

```cpp
graph_entry.graph   = graph;
graph_entry.tensors = std::move(tensor_map);
```

你当前 `tensor_map` 本来就在第一次 graph deserialize 时完整创建。

然后 fused server 直接：

```cpp
auto graph_it =
    stored_graphs[request.graph.device].find(
        request.graph.graph_uid);

if (graph_it == stored_graphs[request.graph.device].end()) {
    return false;
}

auto tensor_it =
    graph_it->second.tensors.find(
        request.input_tensor.id);

if (tensor_it == graph_it->second.tensors.end()) {
    return false;
}

ggml_tensor * input_tensor =
    tensor_it->second;

ggml_backend_tensor_set(
    input_tensor,
    input.data() + sizeof(request),
    request.input_offset,
    data_size);
```

这样每层直接删除：

```text
ggml_init
deserialize_tensor
临时 ggml_context
```

等这个验证正确以后，wire protocol 甚至可以进一步从：

```cpp
rpc_tensor input_tensor;
```

缩成：

```cpp
uint64_t input_tensor_id;
```

因为服务端只需要 ID 找 cached tensor。

---

还有两个小地方可以顺手整理。

你 server 当前 fused case 每次都是：

```cpp
case RPC_CMD_SET_TENSOR_RECOMPUTE_SNAPSHOT: {
    std::vector<uint8_t> input;
    recv_msg(sock, input);
    ...
}
```

所以每层还会新建一个 vector。 可以在 `rpc_serve_client()` 的 `while (true)` 外面放：

```cpp
std::vector<uint8_t> input_scratch;
input_scratch.reserve(64 * 1024);
```

每次：

```cpp
input_scratch.clear();
recv_msg(sock, input_scratch);
```

`resize()` 只改变 size，不重新分配，只要 capacity 足够。

另外你最新 `graph_compute()` 这里：

```cpp
if (prepared.active) {
    output_node = rpc_find_graph_node(...);

    if (output_node >= 0) {
        ...
    }

    prepared.active = false;
    prepared.tensor = nullptr;
}
```

现在即使 `output_node < 0` 也会把 prepare 状态消费掉。

最好改成：

```cpp
if (prepared.active) {
    output_node =
        rpc_find_graph_node(
            cgraph,
            prepared.tensor);

    if (output_node >= 0) {
        ...
        use_fused = true;

        prepared.active = false;
        prepared.tensor = nullptr;
    }
}
```

这不是提速，主要是以后流水再复杂一点时避免某个无关 graph 把 snapshot prepare 吃掉。

所以我现在会按这个顺序继续：

```text
当前 baseline
2.6 prefill / 1.2 decode

↓ 先做

关闭 set_tensor_direct 热路径 INFO
+
fused packet 直接构建
2 send → 1 send
2×16KB memcpy → 1×16KB memcpy

↓ 再测

服务端缓存 tensor_map
取消每层 ggml_init + deserialize_tensor

↓ 最后

server recv scratch 复用
prepared 状态健壮性整理
```

前两个属于真正还可能继续推性能的，后两个更多是把实现整理到稳定、低开销的状态。

而且我会**先只做第一阶段再跑一次**。如果 `1.2` 还能继续明显涨，就说明 TCP/write/syscall 这条线还没榨干；如果变化很小，再做 cached `tensor_map`，之后就应该把注意力正式转到 Phone→PC 的 32 次逐层返回上。
