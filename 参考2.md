好，那就把现在这版当成新的稳定 baseline：

```text
0 ~ 15   Tensor Split
16 ~ 23  Phone Only
24 ~ 31  PC Only

Prefill chunks = 2
RPC ancestry cut       ✅
PC-only Decode FFN     ✅
single l_out handoff   ✅
输出正确               ✅
Decode 明显加速        ✅
```

接下来我建议**先不碰 Prefill 的 2-chunk 流水线**。下一刀做一个风险较低、对 Decode 尤其有价值的优化：

## 合并 PHONE_ONLY 内部的 RPC graph

现在 8 个 Phone-only layer 实际是 **16 个 SG**：

```text
layer16:
sg64 Attention
sg65 FFN

layer17:
sg66 Attention
sg67 FFN

...

layer23:
sg78 Attention
sg79 FFN
```

日志就是这个结构。比如 layer16 后连续进入 layer17，而且中间只有 `META_KEEP_PHONE`，没有任何实际设备切换。

但目前主循环对**每一个** Phone SG 都：

```cpp
compute_workers.start(1, i);
compute_status = compute_workers.wait(1);
```

也就是说：

```text
RPC Attention16 -> 等
RPC FFN16       -> 等
RPC Attention17 -> 等
RPC FFN17       -> 等
...
```



而 `start_graph()` 本身同一 backend 一次只允许一个 outstanding graph，因此这 16 个 RPC graph 是严格串行的。

这已经没有必要了，因为 Phone block 内：

```text
Attention16
   ↓
FFN16
   ↓
Attention17
   ↓
FFN17
```

数据始终在 Phone，`META_KEEP_PHONE` 也已经证明不需要 PC 参与。

### 先做保守版本：每层两个 SG 合成一个

不要一上来把 8 层全部合并。第一版：

```text
当前：

16 个 RPC compute

A16
F16
A17
F17
...
A23
F23
```

改成：

```text
8 个 RPC compute

[A16 + F16]
[A17 + F17]
...
[A23 + F23]
```

通信量应该**完全不变**：

```text
Prefill META_COPY_SUM  不变
Decode  META_COPY_SUM  不变
```

因为我们只是减少 RPC graph dispatch，不改 activation handoff。

### Pair 判断最好明确按 layer

不要简单写“连续两个 Phone SG 就合并”。

用：

```cpp
auto phone_sg_pair_same_layer =
    [&](size_t i, int & layer) -> bool {

    if (i + 1 >= backend_ctx->n_subgraphs ||
        !subgraph_is_phone_only(i) ||
        !subgraph_is_phone_only(i + 1)) {
        return false;
    }

    auto * g0 =
        backend_ctx->backend_configs[1]
            .cgraphs[i].cgraph_main;

    auto * g1 =
        backend_ctx->backend_configs[1]
            .cgraphs[i + 1].cgraph_main;

    if (g0 == nullptr || g1 == nullptr ||
        g0->n_nodes == 0 || g1->n_nodes == 0) {
        return false;
    }

    int attn_layer = -1;
    int ffn_layer  = -1;

    const bool a =
        std::sscanf(
            g0->nodes[g0->n_nodes - 1]->name,
            "attn_out-%d",
            &attn_layer) == 1;

    const bool b =
        std::sscanf(
            g1->nodes[0]->name,
            "ffn_inp-%d",
            &ffn_layer) == 1;

    if (!a || !b || attn_layer != ffn_layer) {
        return false;
    }

    layer = attn_layer;
    return true;
};
```

这样只会匹配：

```text
attn_out-16
     ↓
ffn_inp-16
```

不会错误地把：

```text
FFN16 + Attention17
```

拼起来。

---

## 需要一个专门的 fused graph 容器

你现在的 `cgraphs_aux` **不能直接拿来干这个**，因为它初始化时容量只有 **1 node**：

```cpp
backend_ctx->cgraphs_aux[k] =
    ggml_new_graph_custom(..., 1, ...);
```



所以增加：

```cpp
std::vector<ggml_cgraph *> cgraphs_phone_fused;
```

初始化的时候给它正常 graph 容量，例如：

```cpp
backend_ctx->cgraphs_phone_fused.resize(
    backend_ctx->max_subgraphs);

for (size_t k = 0;
     k < backend_ctx->cgraphs_phone_fused.size();
     ++k) {

    backend_ctx->cgraphs_phone_fused[k] =
        ggml_new_graph_custom(
            backend_ctx->ctx.get(),
            cgraph->n_nodes,
            false);
}
```

这里直接给 `cgraph->n_nodes` 容量最省心。当前普通 backend subgraph 本来也是按这个容量创建的。

然后将：

```text
Phone cgraph[i].nodes
+
Phone cgraph[i+1].nodes
```

按原顺序放进 fused graph。

例如：

```text
layer16 fused graph

node_607
...
attn_out-16
ffn_inp-16
...
node_638
```

**顺序绝对不要重新拓扑排序。**

你现在 single-`l_out` 在第一个 Phone SG 前已经会禁掉入口处旧 producer，所以这些 flag 也直接沿用即可。当前这个 helper 确实已经存在，并会把 `l_out` 及之前的 producer COMPUTE 清掉。

### 主循环变成

大致：

```cpp
} else if (is_phone_only_sg) {

    int fused_layer = -1;

    if (phone_sg_pair_same_layer(i, fused_layer)) {
        ggml_cgraph * fused =
            build_phone_pair_graph(i, i + 1);

        compute_workers.start_graph(1, fused);
        compute_status =
            compute_workers.wait(1);

        if (pipeline_debug) {
            printf(
                "[PHONE_ONLY_FUSED] "
                "sg=%zu..%zu layer=%d nodes=%d\n",
                i, i + 1,
                fused_layer,
                fused->n_nodes);
        }

        //
        // 后续 communication 要按 i+1 这个 pair 尾部处理
        //
    } else {
        compute_workers.start(1, i);
        compute_status =
            compute_workers.wait(1);
    }
}
```

这里有一个非常重要的点：

> **不能融合完以后还分别对 `i` 和 `i+1` 跑两次 `specialized_communication()`。**

因为原来：

```text
Attention16 后：
META_KEEP_PHONE

FFN16 后：
META_KEEP_PHONE
```

融合以后应该只处理：

```text
[A16 + F16]
       ↓
检查 pair 最后一个 SG（sg65）的 ownership
```

也就是说逻辑上的：

```cpp
communication_sg = i + 1;
```

后面：

```cpp
specialized_communication(
    communication_sg,
    ...);
```

然后 loop 跳过 `i+1`。

这样 layer23 的最后一对：

```text
sg78 + sg79
```

仍会正确在 **sg79 末尾**触发：

```text
Phone l_out-23 -> PC
```

这个不能丢。

---

### UID 也别忘了

如果 fused graph 每轮随便生成新 UID，会导致 Decode 每 token 都无法利用 RPC graph recompute cache。

建议 fused UID 从两个原 SG UID确定性生成：

```cpp
uint64_t uid = g0->uid;

uid ^= g1->uid +
       0x9e3779b97f4a7c15ULL +
       (uid << 6) +
       (uid >> 2);

if (uid == 0) {
    uid = 1;
}

fused->uid = uid;
```

这样：

```text
layer16 pair
```

每次 Decode 都保持同一个 fused UID，首次发送完整 graph，以后继续走 recompute。

## 验收很简单

原来：

```text
[PHONE_ONLY_SG] sg=64
[PHONE_ONLY_SG] sg=65
[PHONE_ONLY_SG] sg=66
[PHONE_ONLY_SG] sg=67
...
```

改完应该是：

```text
[PHONE_ONLY_FUSED] sg=64..65 layer=16
[PHONE_ONLY_FUSED] sg=66..67 layer=17
[PHONE_ONLY_FUSED] sg=68..69 layer=18
...
[PHONE_ONLY_FUSED] sg=78..79 layer=23
```

同时：

```text
META_COPY_SUM
```

应该基本一模一样。

输出 logits 也必须保持一致。

如果这一版正确且 Decode 又有提升，**下一步就非常自然了**：

```text
8 次 pair RPC
     ↓
整个 layer16~23
一次 PHONE_BLOCK RPC
```

也就是最终把 8 个 Phone layer 作为真正的一整个 remote compute island 执行。

我建议现在先做 **2-SG → 1-SG 的逐层融合**。这是当前结构下最干净的下一刀，比继续碰已经跑得很好的 Tensor-Split Prefill pipeline 风险小很多。
