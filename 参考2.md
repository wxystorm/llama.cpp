可以。按你最初的设计，**下一步最合适的改法不是继续改 `llama.cpp` 的 Q'K'V' 数学，而是改 `ggml-backend-meta.cpp` 的执行粒度**。

现在 `llama.cpp` 已经把每个 chunk 后面的 `residual → norm_pre → Q'K'V'` 建进图里了，而且 `ffn_down_chunk_i` 就是明确的切分边界。 真正的问题是 Meta 目前提前执行 Phone 时直接：

```cpp
compute_workers->start(j_src, i + 1);
```

这会把 **Phone 的整个 SG(i+1)** 都执行掉。

你要把它改成：

```text
Phone：只提前执行 SG(i+1) 最后的 ffn_down_chunk_(k+1)

PC：等 chunk k reduce 完成后，
    正常执行整个 SG(i+1)
    = chunk k downstream Q'K'V'
      +
      PC 自己的 ffn_down_chunk_(k+1)
```

这样正好变成：

```text
                         chunk k

Phone chunk k ──────> PC
                       │
                       ├── reduce/add -> full chunk k
                       │
                       │        PC:
                       │        residual
                       │        norm_pre
                       │        Q'k K'k V'k
                       │        PC Wdown(k+1)
                       │
                       │             ||
                       │             ||
                       │        Phone:
                       │        Wdown(k+1)
                       │
                       └─────────────┬────────────
                                     ▼
                             chunk k+1 reduce
```

这就是你最初想要的 decode pipeline。

---

## 第一处：`llama.cpp` 暂时不要改

目前这里实际上已经对了：

```cpp
ggml_tensor * out = build_lora_mm(down, ffn_hidden);

const std::string chunk_name =
    "ffn_down_chunk_" + std::to_string(i);

cb(out, chunk_name.c_str(), il);
```

然后 boundary 后：

```cpp
hidden = out + residual;

chunk_sum_sq = sum(hidden^2);

norm_pre = hidden * next_attn_norm_weight;

q_part = Wq_chunk * norm_pre;
k_part = Wk_chunk * norm_pre;
v_part = Wv_chunk * norm_pre;
```

最后累计：

```cpp
q_acc += q_part;
k_acc += k_part;
v_acc += v_part;
```

这些都已经存在。

所以先别继续折腾 `src/models/llama.cpp`。

---

# 第二处：不要再让 Phone 提前跑完整 SG

当前需要重点改这里：

```cpp
backend_ctx->compute_workers->start(j_src, i + 1);
```

现在这个 `start()` 最终调用的是：

```cpp
ggml_backend_graph_compute_async(
    bcj.backend,
    bcj.cgraphs[i].cgraph_main);
```

也就是完整 SG。

这就是之前结果错误的根本原因。

你要新增一个：

```text
early_wdown_graph
```

这个 graph **只有一个 compute node**：

```text
ffn_down_chunk_(k+1)
```

因为这个 node 本身的输入是：

```text
ffn_hidden
+
Wdown weight slice
```

而 `ffn_hidden` 在 chunk 循环之前已经建立并计算好了。

所以对 `chunk1/2/...` 来说，完全没必要把前面的：

```text
residual(chunk0)
sum_sq(chunk0)
Q'0K'0V'0
```

也拉进去。

---

# 第三处：先做一个 helper，取下一 SG 的 Wdown node

在 `specialized_communication()` 附近加：

```cpp
auto get_ffn_down_boundary_node =
    [&](size_t j, size_t sg) -> ggml_tensor * {
        if (sg >= backend_ctx->n_subgraphs) {
            return nullptr;
        }

        ggml_cgraph * g =
            backend_ctx->backend_configs[j]
                .cgraphs[sg]
                .cgraph_main;

        if (g == nullptr || g->n_nodes == 0) {
            return nullptr;
        }

        ggml_tensor * node = g->nodes[g->n_nodes - 1];

        if (std::strstr(node->name, "ffn_down_chunk") == nullptr) {
            return nullptr;
        }

        if (!(node->flags & GGML_TENSOR_FLAG_COMPUTE)) {
            return nullptr;
        }

        return node;
    };
```

其实你当前已经在用完全相同的逻辑判断：

```cpp
next_graph->nodes[next_graph->n_nodes - 1]
```

是不是 `ffn_down_chunk`。

所以这个改动非常自然。

---

# 第四处：建立 Phone 专用的 one-node early graph

关键点来了。

不要：

```cpp
compute_workers->start(j_src, i + 1);
```

而是生成：

```cpp
ggml_cgraph * phone_early_graph = ...;

phone_early_graph->nodes[0] =
    get_ffn_down_boundary_node(j_src, i + 1);

phone_early_graph->n_nodes = 1;
```

概念上：

```text
Phone canonical SG(i+1):

[residual_k]
[sum_sq_k]
[norm_pre_k]
[Q'_k]
[K'_k]
[V'_k]
...
[ffn_down_chunk_(k+1)]


Phone early graph:

[ffn_down_chunk_(k+1)]
```

### 这里我不建议直接复用现在的 `cgraph_aux`

因为你目前 transfer worker 同时也在用一个 aux graph：

```cpp
cgraph_aux->nodes[0] = node_red;
cgraph_aux->n_nodes  = 1;
```

用于：

```text
PC local + Phone temp
```



而 early Wdown 会和它并行。

所以不要让两个线程同时碰同一个 aux graph storage。

最好给 `backend_config` 加专门的：

```cpp
std::vector<ggml_cgraph *> cgraphs_early;
```

或者更简单，Meta context 里：

```cpp
std::vector<ggml_cgraph *> cgraphs_early;
```

每个：

```text
backend × subgraph
```

一个即可。

容量只需要：

```cpp
ggml_graph_overhead_custom(1, false)
```

因为 early graph 只有一个 node。

---

# 第五处：compute worker 要支持“传 graph”，不能只传 SG index

你现在 worker 保存的是：

```cpp
std::vector<size_t> task_index;
```

线程里面：

```cpp
const size_t i = task_index[j];

ggml_backend_graph_compute_async(
    bcj.backend,
    bcj.cgraphs[i].cgraph_main);
```



建议改成：

```cpp
std::vector<ggml_cgraph *> task_graph;
```

然后 worker：

```cpp
ggml_cgraph * graph = task_graph[j];

const ggml_status status =
    ggml_backend_graph_compute_async(
        bcj.backend,
        graph);
```

再提供两个入口：

```cpp
void start(size_t j, size_t i) {
    start_graph(
        j,
        backend_ctx->backend_configs[j]
            .cgraphs[i]
            .cgraph_main);
}

void start_graph(size_t j, ggml_cgraph * graph) {
    std::lock_guard<std::mutex> lock(mutex);

    task_graph[j] = graph;
    ++generation[j];

    task_cv.notify_all();
}
```

这样原来的代码完全不用大改：

```cpp
compute_workers->start(j_dst, i + 1);
```

照旧。

新增：

```cpp
compute_workers->start_graph(
    j_src,
    phone_early_graph);
```

---

# 第六处：真正改 overlap 那几行

现在是：

```cpp
if (use_snapshot_pipeline) {
    ...
    snapshot_arm(...);

    if (!next_is_ffn_down_chunk) {
        return transfer_worker->wait(task_id);
    }

    compute_workers->start(j_src, i + 1);
}
```



改成逻辑：

```cpp
ggml_tensor * phone_next_wdown =
    get_ffn_down_boundary_node(
        j_src,
        i + 1);

ggml_cgraph * phone_early_graph = nullptr;

if (phone_next_wdown != nullptr) {
    phone_early_graph =
        get_early_graph(j_src, i + 1);

    phone_early_graph->nodes[0] =
        phone_next_wdown;

    phone_early_graph->n_nodes = 1;
}
```

然后：

```cpp
if (use_snapshot_pipeline) {
    const bool armed = snapshot_arm(
        bcj_src.backend,
        node_src,
        0,
        ggml_nbytes(node_src),
        snapshot_slot,
        snapshot_seq);

    GGML_ASSERT(armed);

    if (phone_early_graph == nullptr) {
        return backend_ctx->transfer_worker->wait(task_id);
    }

    // 这里只提前算 Phone 的下一 Wdown。
    backend_ctx->compute_workers->start_graph(
        j_src,
        phone_early_graph);
}
```

也就是把：

```cpp
start(j_src, i + 1)
```

换成：

```cpp
start_graph(j_src, phone_early_graph)
```

---

# 第七处：PC 保持完整 SG(i+1)

这一段你反而不用改：

```cpp
const ggml_status transfer_status =
    backend_ctx->transfer_worker->wait(task_id);

if (transfer_status == GGML_STATUS_SUCCESS) {
    backend_ctx->compute_workers->start(
        j_dst,
        i + 1);
}
```



它现在的意义就会变成：

```text
Phone 已经在算：
    Wdown(k+1)

Phone -> PC chunk k 完成
PC ADD chunk k 完成
             ↓
PC 开始完整 SG(k+1)
             ↓
    bias/scale chunk k
    residual chunk k
    sum_sq chunk k
    Q'kK'kV'k
    ...
    Wdown(k+1) PC partial
```

所以自然形成：

```text
时间 ───────────────────────────────>

Phone:
Wdown k
      | snapshot/copy |
                        Wdown k+1
                        ───────────────

PC:
Wdown k
      | add |
             residual k
             norm k
             Q'k K'k V'k
             Wdown k+1
             ────────────────────────
```

这就是你要的。

---

# 第八处：这里的 `next_compute_complete = true` 可以继续保留

当前最后：

```cpp
next_compute_complete = true;
```



外层因此在 `i+1`：

```cpp
if (!compute_complete) {
    compute_workers.compute(i);
} else {
    compute_complete = false;
}
```

会跳过再次 compute。

这个行为正合适。

因为到这里：

### PC

完整 `SG(i+1)` 已经执行：

```text
downstream(k)
+
Wdown(k+1)
```

### Phone

虽然没执行完整 `SG(i+1)`，但是我们**故意只需要它执行**：

```text
Wdown(k+1)
```

而这个 SG 的 boundary 正好就是：

```text
ffn_down_chunk_(k+1)
```

所以到了下一次：

```cpp
specialized_communication(i + 1, ...)
```

两边需要 reduce 的 boundary tensor 都已经 ready：

```text
PC    ffn_down_chunk_(k+1) partial ✅
Phone ffn_down_chunk_(k+1) partial ✅
```

于是直接：

```text
Phone -> PC
PC ADD
```

即可。

---

# 第九处：关掉 `PRIMARY_MIRROR_BACK`

你最终这个模式下：

```text
PC 拥有 full chunk
Phone 不需要 full chunk
```

所以这段：

```cpp
ggml_backend_tensor_get(node_dst, ...);
ggml_backend_tensor_set(node_src, ...);
```

必须不走。它现在正是 `GGML_META_PRIMARY_MIRROR_BACK` 的逻辑。

也就是最终运行时：

```powershell
Remove-Item Env:GGML_META_PRIMARY_MIRROR_BACK -ErrorAction SilentlyContinue
```

或者不要设置它。

**不再需要 PC→Phone mirror-back。**

这是这个方案最大的收益之一。

---

# 但还有一个非常重要的问题：Phone 后续 SG 不能依赖那些没执行的 downstream

这就是为什么我说这个方案与你原始设计特别匹配。

你的设计本来就是：

```text
Phone:
    只算 FFN 分片

PC:
    reduce FFN chunk
    residual
    next norm
    Q'K'V'
    attention

Attention结果再 PC -> Phone
```

所以 Phone 根本不需要：

```text
hidden full
sum_sq
Q'K'V'
```

只需要在下一 FFN 开始前收到 PC attention 结果即可。

而你的 Meta 已经有：

```cpp
if (active_count == 1) {
    active backend -> other backend
}
```

这个单 active backend copy。

所以 attention PC-only 后：

```text
PC attn_out -> Phone
```

正好重新同步两端。

---

# 我建议你不要一次把所有优化都打开

按三步做，会非常容易定位 correctness。

### 第一步：只做 Phone pruning，不 overlap

先实现：

```text
PC SG(k+1):
    完整执行

Phone SG(k+1):
    只执行 ffn_down_chunk_(k+1)
```

但是先这样：

```text
chunk k reduce 完成
        ↓
Phone Wdown(k+1)
        ↓ 等完
PC SG(k+1)
```

完全串行。

先验证输出必须和 baseline **逐 token 一致**。

如果这一步正确，就证明：

> Phone 的 `residual/QKV/...` 确实可以全部删掉。

---

### 第二步：PC 与 Phone 同时启动

正确后改成：

```cpp
start_graph(phone, early_wdown);

start(pc, full_next_sg);
```

两边一起跑。

这样先验证：

```text
PC Q'K'V' + PC Wdown next
||
Phone Wdown next
```

是不是正确。

---

### 第三步：再提前到 Phone→PC 通信阶段

最后才恢复你现在 snapshot pipeline 的优势：

```text
Phone chunk k snapshot 已安全
          │
          ├──── Phone Wdown(k+1)
          │
          └──── Phone chunk k -> PC
```

然后：

```text
PC reduce完成
      ↓
PC Q'K'V' + Wdown(k+1)
```

最终：

```text
                  ┌── transfer chunk k ─────┐
Phone Wdown k ────┤                          ├── Phone Wdown k+1
                  └──────────────────────────┘
                                  ||
                                  ||
PC Wdown k ── reduce ── Q'K'V' k ── PC Wdown k+1
```

这是最后形态。

---

## 所以你现在最先应该改的，其实只有一个核心原则

把目前：

```cpp
backend_ctx->compute_workers->start(
    j_src,
    i + 1);
```

从：

> **Phone 提前运行整个下一 SG**

改成：

> **Phone 提前运行下一 SG 最后的 `ffn_down_chunk` 单节点 graph。**

PC 仍然：

```cpp
backend_ctx->compute_workers->start(
    j_dst,
    i + 1);
```

完整执行 SG。

这样你的现有：

* FFN chunk builder
* partial QKV
* transfer worker
* snapshot
* 双 RPC socket
* PC primary reduce
* `compute_complete`
* attention PC→Phone

基本都可以保留。

**不用推翻 8ea704f，实际上是在它上面把错误的“SG 粒度 overlap”收窄成正确的“Wdown 粒度 overlap”。**

我建议你第一版先只改 `ggml-backend-meta.cpp`，不要碰 RPC 和 `llama.cpp`。等第一版 pruning 输出正确，再打开真正 overlap；这样一旦输出变了，我们能非常准确地判断是“pruning 错”还是“并发时序错”。
