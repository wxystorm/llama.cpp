那就很明确了：**`PRIMARY_DOWNSTREAM` 这个 pruning 方案本身也是错的，先整个撤掉。**

你现在的 pruning 不是单纯“少执行几个节点”，而是在 Phone 的 simple tensor 上直接清：

```cpp
graph->nodes[i]->flags &= ~GGML_TENSOR_FLAG_COMPUTE;
```



问题是这个 flag 在 Meta 里还是**数据是否有效/是否参与通信的语义状态**。后面 `specialized_communication()` 会根据它统计 `active_count`，决定走双端 reduce 还是单端 direct-copy。 `allreduce_fallback()` 同样把这个 flag 当 `has_data`，disabled 时甚至会构造 zero 节点。

所以：

> 用 `COMPUTE` flag 裁掉 Phone downstream，会改变 Meta 后续的通信/有效数据语义，不是安全的 graph pruning。

这也解释了为什么：

```text
CHUNKS=2 + full allreduce                  正确
CHUNKS=2 + full allreduce + pruning       错误
```

因此先删掉/禁用整段 `GGML_META_PRIMARY_DOWNSTREAM`。你上传的当前版本里这块就在 subgraph 构造完成后直接修改 Phone graph。 

---

下一步不要再尝试裁图。我们做一个更干净的实验，验证我最初那个核心判断：

> **primary reduce 只把完整 Wdown 留在 PC，导致 Meta 要求的 MIRRORED 状态被破坏。**

### 实验 3：primary reduce 后，把完整结果再同步回 Phone

先不追求性能，只追求 correctness。

正常特殊路径现在是：

```text
Phone partial
      ↓
Phone→PC
      ↓
PC partial + Phone partial
      ↓
PC full
```



实验改成：

```text
Phone partial
      ↓
Phone→PC
      ↓
PC ADD
      ↓
PC full
      ↓
PC→Phone
      ↓
PC full == Phone full
      ↓
再执行下一 subgraph
```

这样完全恢复 Meta 原来的：

```text
PARTIAL
→ allreduce
→ MIRRORED
```

语义。

### 最简单、最保险的测试写法

增加：

```cpp
const bool primary_mirror_back =
    std::getenv("GGML_META_PRIMARY_MIRROR_BACK") != nullptr;
```

在当前 `task_id` 创建完成后，**不要走原来的提前 `start(j_src, i + 1)`**。

snapshot 模式下先正常 ARM：

```cpp
if (primary_mirror_back) {
    if (use_snapshot_pipeline) {
        const bool armed = snapshot_arm(
            bcj_src.backend,
            node_src,
            0,
            ggml_nbytes(node_src),
            snapshot_slot,
            snapshot_seq);

        GGML_ASSERT(armed);
    }

    const ggml_status status =
        backend_ctx->transfer_worker->wait(task_id);

    if (status != GGML_STATUS_SUCCESS) {
        return status;
    }

    // task 内 PC ADD 是 async，实验阶段明确等它真正完成
    ggml_backend_synchronize(bcj_dst.backend);

    const size_t nbytes = ggml_nbytes(node_dst);
    std::vector<uint8_t> full_data(nbytes);

    // 从 PC 取已经 reduce 完整的 Wdown
    ggml_backend_tensor_get(
        node_dst,
        full_data.data(),
        0,
        nbytes);

    // 写回 Phone 对应 tensor
    ggml_backend_tensor_set(
        node_src,
        full_data.data(),
        0,
        nbytes);

    // 不提前执行 i+1
    // 外层下一轮正常 compute 两端
    return GGML_STATUS_SUCCESS;
}
```

一定要把这段放在现有：

```cpp
if (use_snapshot_pipeline) {
    ...
    compute_workers->start(j_src, i + 1);
```

**之前**，否则 Phone 又会提前用旧 partial 数据执行 sg2。你当前提前启动位置就在这里。

这版故意：

```text
不 overlap
不 pruning
不提前 Phone chunk1
```

只是验证数学和 Meta 数据语义。

运行前把之前两个实验变量清掉：

```powershell
Remove-Item Env:GGML_META_PRIMARY_DOWNSTREAM -ErrorAction SilentlyContinue
Remove-Item Env:GGML_META_FORCE_FULL_REDUCE -ErrorAction SilentlyContinue

$env:GGML_META_PRIMARY_MIRROR_BACK="1"
$env:LLAMA_CHUNKS="2"
```

性能肯定会下降，因为每个 chunk 多了一次 8KB 的 PC→Phone，并且暂时取消了 Phone chunk1 提前计算。**现在完全不看性能。**

如果这次输出正确，我们就得到非常强的结论：

```text
full allreduce                         ✓
primary reduce                         ✗
primary reduce + PC→Phone mirror-back  ✓
```

那就正式证明：

> primary ADD 算法和 chunk 图都没错；真正的问题就是 **primary-only reduce 之后 Phone 没有得到完整 Wdown，却继续按照 Meta 的 MIRRORED 语义执行。**

然后我们再设计正确的高速版本——不是改 COMPUTE flag，而是做**独立的 early-Wdown graph view**，同时保留 canonical graph 的 Meta 语义。

如果 `primary + mirror-back` 仍然错，那方向马上变成：检查 **snapshot / PC ADD 本身**，下一步直接关 snapshot 做同样测试。

现在先做这个实验最值。你当前文件本身的 Meta split-state 仍然明确把 `PARTIAL` 在 `assume_sync` 下视作 `MIRRORED`，所以这个测试正好验证这条契约。
