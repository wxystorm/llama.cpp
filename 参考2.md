我看了你现在 `prefill-baseline` 的最新提交。这里**统计确实还没有变成“纯传输时间”**，而且我现在能明确指出为什么你这次 `0->1` 和 `1->0` 都一起变慢了。

最关键的问题不是网络本身，而是你把 `RPC synchronize` 接到了通用 backend `.synchronize` 上以后，`ggml_backend_tensor_copy_async()` 会自动调用它。

你现在 RPC backend 是：

```cpp
/* .cpy_tensor_async = */ NULL,
/* .synchronize      = */ ggml_backend_rpc_synchronize,
```

也就是说 RPC 没有自己的 async copy 快路，而 `synchronize()` 已经变成真正发 `RPC_CMD_SYNCHRONIZE` 并等待 ACK。

而 ggml 通用的：

```cpp
ggml_backend_tensor_copy_async(...)
```

在没有成功走 `cpy_tensor_async` 时，会做：

```cpp
ggml_backend_synchronize(backend_src);
ggml_backend_synchronize(backend_dst);
ggml_backend_tensor_copy(src, dst);
```



这就把事情解释通了。

### 你现在 `0 -> 1` 为什么也突然从 0.x ms 变成 8~15 ms

比如：

```text
[META_COPY] 0->1 attn_out ... 14.557 ms
```

这条路径是：

```text
PC -> RPC Phone
```

而 destination 就是 RPC backend。

所以 `ggml_backend_tensor_copy_async()` 会变成：

```text
sync PC
↓
sync RPC Phone   ← 这里现在是真正一个网络 fence + ACK
↓
SET_TENSOR
```

也就是说：

> 你打印的 `0->1 time` 现在已经不是单纯 PC→Phone 发送时间了，它前面被强制插入了一次 Phone RPC synchronize。

这就是为什么原本：

```text
0.1 ~ 0.3 ms
```

突然变成：

```text
6 ~ 15 ms
```

甚至你的统计里平均几十 ms。

---

### `1 -> 0` 更严重：很可能同步了两次

你现在专用 Wdown transfer worker 里面已经手动：

```cpp
const int64_t copy_start_us = ggml_time_us();

ggml_backend_synchronize(bcj_src.backend);

...

ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    node_src,
    node_tmp);
```



注意两个问题。

第一，`copy_start_us` 放在 synchronize **之前**。

所以：

```text
[META_COPY] 1->0 50 ms
```

本来就包含：

```text
等待 Phone chunk 算完
+
RPC fence RTT
+
GET
+
tensor_get
+
网络 payload
+
写入 PC node_tmp
```

根本不是传输时间。

第二，更隐蔽：

你手动：

```cpp
ggml_backend_synchronize(phone);
```

之后又调用：

```cpp
ggml_backend_tensor_copy_async(phone, pc, ...)
```

如果这个 copy 没有命中 destination backend 的 async-copy 快路，那么通用实现内部又会：

```cpp
ggml_backend_synchronize(phone); // 第二次
ggml_backend_synchronize(pc);
```

所以当前很可能是：

```text
Phone graph command
    ↓

RPC fence #1
    ↓ ACK

RPC fence #2
    ↓ ACK

PC synchronize
    ↓

GET tensor
```

这非常符合你现在 `1->0` 从之前大约：

```text
~27-30 ms
```

变成：

```text
~50-60 ms
```

的现象。

---

## 你的日志本身已经证明“时间被挪桶了”

看这一条特别明显：

以前你是：

```text
compute ≈ 1134 ms
backend 1 ≈ 1129 ms
reduce ≈ 1914 ms
```

现在变成：

```text
compute = 108 ms
backend 1 = 11 ms
reduce = 3696 ms
```

Phone 不可能突然从：

```text
~1.1 秒计算
```

变成：

```text
11 ms
```

但整体还更慢。

发生的实际上是：

```text
原来记在 compute 里的 Phone 计算等待
             ↓
现在因为 graph fire-and-forget
             ↓
被后面的 synchronize 等掉
             ↓
于是记进 META_COPY / reduce 里
```

这和你当前代码完全吻合：GRAPH 又已经恢复成 fire-and-forget。

所以现在我不会得出“网络两边都变差了”的结论。

更准确的是：

> **现在 `[META_COPY]` 两个方向都被 synchronize 污染了。**

---

## 下一步我建议立刻改这个，而不是继续 pipeline

不要把“RPC fence”实现成 RPC backend 的普通 `.synchronize`。

因为 ggml 很多地方默认都会调用 backend synchronize，像这里的 tensor copy 就会自动调用。你原本只是想：

> 在 Phone→PC GET 前 fence 一次。

结果现在变成：

> 所有涉及 RPC 的通用 copy 都可能顺手 fence。

### 把 RPC synchronize 恢复成 no-op

恢复原来：

```cpp
static void ggml_backend_rpc_synchronize(
        ggml_backend_t backend) {
    GGML_UNUSED(backend);
}
```

但是：

```cpp
RPC_CMD_SYNCHRONIZE
```

这个命令不要删。

它很好用。

只是不要把它挂到：

```cpp
backend->iface.synchronize
```

的真实语义上。

---

### 单独做一个 `RPC fence`

就像你现在的：

```text
GGML_BACKEND_RPC_SET_STAGE_READY_PROC
```

一样，再暴露：

```cpp
#define GGML_BACKEND_RPC_FENCE_PROC \
    "ggml_backend_rpc_fence"
```

例如：

```cpp
typedef void (*ggml_backend_rpc_fence_t)(
        ggml_backend_t backend);
```

RPC：

```cpp
static void ggml_backend_rpc_fence(
        ggml_backend_t backend) {

    auto * rpc_ctx =
        static_cast<ggml_backend_rpc_context *>(
            backend->context);

    rpc_msg_synchronize_req request {};
    request.device = rpc_ctx->device;

    // 必须走 compute socket
    auto sock = get_socket(rpc_ctx->endpoint);

    RPC_STATUS_ASSERT(sock != nullptr);

    const bool status =
        send_rpc_cmd(
            sock,
            RPC_CMD_SYNCHRONIZE,
            &request,
            sizeof(request),
            nullptr,
            0);

    RPC_STATUS_ASSERT(status);
}
```

然后 proc-address：

```cpp
if (std::strcmp(
        name,
        GGML_BACKEND_RPC_FENCE_PROC) == 0) {

    return reinterpret_cast<void *>(
        ggml_backend_rpc_fence);
}
```

你现在已经用完全相同的方法暴露 stage-ready setter 了，所以很自然。

---

## Meta 里只在这一处主动 fence

现在：

```cpp
const int64_t copy_start_us = ggml_time_us();

ggml_backend_synchronize(bcj_src.backend);

set_stage_ready(...);

ggml_backend_tensor_copy_async(...);
```

改成：

```cpp
const int64_t fence_start_us = ggml_time_us();

rpc_fence(bcj_src.backend);

const int64_t fence_us =
    ggml_time_us() - fence_start_us;


// 到这里 Phone chunk 已经完成。
// 后面才开始统计真正的数据搬运。

const int64_t copy_start_us = ggml_time_us();

set_stage_ready(...);

ggml_backend_tensor_copy_async(
        bcj_src.backend,
        bcj_dst.backend,
        node_src,
        node_tmp);

const int64_t copy_us =
    ggml_time_us() - copy_start_us;
```

于是统计终于分开：

```text
[FENCE]
等待 Phone graph 真正完成

[META_COPY]
GET + tensor_get + payload + PC set
```

而且因为 RPC 普通 `.synchronize` 又恢复成 no-op：

```cpp
ggml_backend_tensor_copy_async()
```

内部即使自动：

```cpp
ggml_backend_synchronize(phone);
```

也不会再产生第二个网络 RTT。

---

还有一点可以进一步简化。

你 server 的 `graph_compute()` 已经调用：

```cpp
ggml_backend_graph_compute(...)
```

本身就是同步执行的。

因此 compute socket 顺序实际上是：

```text
GRAPH_COMPUTE
↓
真正计算结束
↓
handler 返回
↓
才读取 RPC_CMD_SYNCHRONIZE
```

所以这个：

```cpp
bool rpc_server::synchronize(...) {
    ggml_backend_synchronize(backends[device]);
    return true;
}
```

里面再次调用 backend synchronize 理论上已经多余。

可以先保守保留；等上面的重复 synchronize 问题解决以后，再考虑把它变成纯：

```cpp
bool rpc_server::synchronize(uint32_t device) {
    return device < backends.size();
}
```

此时这个 RPC command 就只是：

```text
compute socket ordering fence
```

而不是再次做 device fence。

---

### 改完后再看日志

我最想看到的是：

```text
[FENCE] chunk0  xxx ms
[META_COPY] 1->0 8192 bytes xxx ms
[META_COPY] 0->1 16384 bytes xxx ms
```

我预计会发生两件很明显的事：

```text
0->1
8~15 ms
→ 大幅回落
```

因为 PC→Phone 不再莫名先做一个远程 synchronize。

以及：

```text
1->0
50~60 ms
→ fence_ms + 一个明显小得多的 copy_ms
```

如果到了那时候：

```text
fence = 20~30 ms
copy  = 2~5 ms
```

那么网络没有问题。

如果到了那时候仍然：

```text
fence = 20 ms
copy  = 30~50 ms
```

那才说明 Phone→PC 的 GET/TCP 路径确实还有真实问题。

而如果要把它再拆准一点，我们下一步可以直接在 `send_rpc_cmd_staged()` 里分：

```text
GET request → response_size   = header/staging 时间
response_size → payload done  = 真正 payload receive 时间
```

你现在的 staged 函数刚好已经把这两个阶段天然分开了。

所以我建议**先修这个 synchronize 被通用 copy 自动调用的问题**。这比继续怀疑 Wi-Fi 带宽重要得多，因为从你最新代码来看，这就是当前性能回退里一个非常具体的额外开销来源。
