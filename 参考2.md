这次结果说明**第一阶段流水已经恢复正确，而且 snapshot 确实有效**。更关键的是，日志现在直接暴露了下一处可以优化的串行点。

layer0 最典型：

```text
[PREFILL_INPUT] chunk=1             ← C1 已经准备好
[PREFILL_SNAPSHOT_PREPARE] chunk=1

[META_COPY] C0 1->0 time=166.546ms ← 还在等/回传 C0

[META_COPY] C1 0->1 time=0.573ms   ← C0 回完后，才真正发送 C1
```

也就是 `PREFILL_INPUT` 只是把任务排进队列，**C1 并没有立刻发给手机**；因为 C0 output 和 C1 input 仍共用同一个 `transfer_worker`。

现在总体也明显改善了：

```text
compute = 4520.153 ms
PC backend = 1612.197 ms
Phone submit = 140.828 ms

0->1 = 64.648 ms
1->0 = 9368.816 ms
       avg 99.668 ms
```



上一轮临时 A/B 调度还是：

```text
compute = 7267.899 ms
1->0 avg = 185.372 ms
```



所以恢复 PC-only norm SG + dependency barrier 后，确实把流水救回来了。现在可以进下一步：**Prefill input/output 双 worker。**

## 现在的瓶颈长这样

目前一个 worker：

```text
transfer_worker:

send C0
    ↓
return C0 ───────────────────── 100ms
    ↓
send C1  0.6ms
    ↓
return C1 ─────────────────────
    ↓
send C2
```

PC 虽然已经能在 `return C0` 时计算 C1，但 Phone 不能提前开始 C1，因为：

```text
C1 input
```

也堵在同一个 worker 后面。

我们真正想要：

```text
                     时间 →

PC:
FFN C0 ── FFN C1 ─────────── FFN C2

input worker:
send C0    send C1 ──────── send C2
              │
              └──── 与 C0 return 重叠

Phone:
     FFN C0     FFN C1        FFN C2

output worker:
          return C0 ─────
                    return C1 ─────
```

这里意义很大：不是为了隐藏那 `0.6ms` 的 C1 上传本身，而是为了让 **Phone C1 FFN 可以在 C0 网络回传期间启动**。

---

# 改法：只新增一个 `prefill_input_worker`

不要动现有 `transfer_worker`。

它继续负责：

```text
Phone -> PC snapshot
PC ADD reduce
Decode 原有 transfer
```

新增一个仅用于：

```text
PC -> Phone Prefill norm chunk
```

的 worker。

### 1. `ggml_backend_meta_context` 增加

现在是：

```cpp
ggml_backend_meta_compute_workers * compute_workers = nullptr;
ggml_backend_meta_transfer_worker * transfer_worker = nullptr;
```



改成：

```cpp
ggml_backend_meta_compute_workers * compute_workers = nullptr;

ggml_backend_meta_transfer_worker * transfer_worker = nullptr;

// Prefill PC -> Phone input only
ggml_backend_meta_transfer_worker * prefill_input_worker = nullptr;
```

析构：

```cpp
ggml_backend_meta_context::~ggml_backend_meta_context() {
    delete compute_workers;
    delete transfer_worker;
    delete prefill_input_worker;

    ...
}
```

当前析构只有前两个。

---

## 2. `is_prefill_norm_input` 改用新 worker

现在这一段：

```cpp
if (backend_ctx->transfer_worker == nullptr) {
    backend_ctx->transfer_worker =
        new ggml_backend_meta_transfer_worker();
}

...

pending_prefill_input_task =
    backend_ctx->transfer_worker->enqueue(
        [&, src, dst, i](uint64_t) -> ggml_status {
            ...
        });
```



改成：

```cpp
if (backend_ctx->prefill_input_worker == nullptr) {
    backend_ctx->prefill_input_worker =
        new ggml_backend_meta_transfer_worker();
}

handled = true;

auto & bcj_src = backend_ctx->backend_configs[0];
auto & bcj_dst = backend_ctx->backend_configs[1];

ggml_tensor * src = nodes[0];
ggml_tensor * dst = nodes[1];

GGML_ASSERT(ggml_is_contiguous(src));
GGML_ASSERT(ggml_is_contiguous(dst));

pending_prefill_input_task =
    backend_ctx->prefill_input_worker->enqueue(
        [&, src, dst, i](uint64_t) -> ggml_status {
            const int64_t copy_start_us = ggml_time_us();

            ggml_backend_tensor_copy_async(
                bcj_src.backend,
                bcj_dst.backend,
                src,
                dst);

            const int64_t copy_us =
                ggml_time_us() - copy_start_us;

            // 统计这里下面再处理线程安全
            reduce_copy_wait_us += copy_us;
            record_meta_copy(i, 0, 1, src, copy_us);

            return GGML_STATUS_SUCCESS;
        });

pending_prefill_input_layer = prefill_norm_layer;
pending_prefill_input_chunk = prefill_norm_chunk;
```

---

## 3. down SG 等 input 时，也换 worker

你现在：

```cpp
compute_workers.start(0, i);

const ggml_status input_status =
    backend_ctx->transfer_worker->wait(
        pending_prefill_input_task);
```

改成：

```cpp
compute_workers.start(0, i);

GGML_ASSERT(
    backend_ctx->prefill_input_worker != nullptr);

const ggml_status input_status =
    backend_ctx->prefill_input_worker->wait(
        pending_prefill_input_task);
```

然后保持：

```cpp
pending_prefill_input_task = 0;
pending_prefill_input_layer = -1;
pending_prefill_input_chunk = -1;

...

compute_workers.start(1, i);
```

不变。

这条约束非常重要：

```text
SET C1 完成
    ↓
才能 submit Phone FFN C1
```

因此不会出现 Phone graph 读到尚未上传完的输入。

---

## 为什么这次不会重现之前 `recv size=8` 崩溃

之前崩溃是：

```text
transfer worker:
rpc_fence()          ─┐
                      ├─ 同一个 COMPUTE socket
Phone compute worker ─┘
GRAPH_COMPUTE
```

两个线程同时操作同一 COMPUTE socket。

现在不同。

Prefill output 已经是：

```text
GET_SNAPSHOT
    ↓
TRANSFER socket
```

RPC 本身确实使用独立的 `transfer_sock` 来执行 `RPC_CMD_GET_SNAPSHOT`。

而新的 input worker 是：

```text
SET C1
    ↓
COMPUTE socket
```

并且我们的调度明确保证：

```text
input worker SET C1 完成
        ↓
compute worker 才提交 GRAPH C1
```

所以同一个 COMPUTE socket 上不会让 `SET C1` 和 `GRAPH C1` 并发写。

而前一个 chunk 的：

```text
snapshot_arm C0
```

也是在进入下一块、enqueue C1 input **之前**同步调用的。当前 Meta 的顺序就是 enqueue snapshot reader → `snapshot_arm()` → return。

于是 COMPUTE socket 顺序仍然是：

```text
GRAPH C0
SNAPSHOT_ARM C0
SET C1
GRAPH C1
SNAPSHOT_ARM C1
SET C2
GRAPH C2
```

不会乱。

同时：

```text
TRANSFER socket:
GET snapshot C0
GET snapshot C1
...
```

独立运行。

---

## 但这里有一个必须一起处理的小问题：统计数据竞争

你现在 `record_meta_copy()` 会修改：

```cpp
stats.count
stats.bytes
stats.total_us
stats.max_us
reduce_copy_detail_count
```



之前只有一个 transfer worker，所以没事。

一旦我们两个 worker 同时：

```text
input worker  -> record_meta_copy(0->1)
output worker -> record_meta_copy(1->0)
```

就是 C++ data race。

所以顺手在这些统计附近加：

```cpp
std::mutex meta_copy_stats_mutex;
```

然后：

```cpp
auto record_meta_copy =
    [&](size_t i,
        size_t j_src,
        size_t j_dst,
        const ggml_tensor * node_src,
        int64_t copy_us) {

    std::lock_guard<std::mutex> lock(
        meta_copy_stats_mutex);

    const size_t copy_bytes =
        ggml_nbytes(node_src);

    auto & stats =
        reduce_copy_by_direction[
            j_src*n_backends + j_dst];

    ++stats.count;
    stats.bytes += copy_bytes;
    stats.total_us += copy_us;
    stats.max_us =
        std::max(stats.max_us, copy_us);

    ...
};
```

另外：

```cpp
reduce_copy_wait_us += copy_us;
```

也会被两个 worker 同时写。

最简单是单独再做：

```cpp
auto record_copy_wait = [&](int64_t us) {
    std::lock_guard<std::mutex> lock(
        meta_copy_stats_mutex);
    reduce_copy_wait_us += us;
};
```

然后两个 Prefill worker 都改成：

```cpp
record_copy_wait(copy_us);
record_meta_copy(...);
```

虽然锁两次，但相对于几十到上百毫秒 RPC 完全可以忽略。

---

### 这次改完最值得观察的不是 `0->1 avg`

而是 layer0 的顺序。

现在是：

```text
PREFILL_INPUT C1
META_COPY C0 1->0 166ms
META_COPY C1 0->1
```



双 worker 生效后应该变成近似：

```text
PREFILL_INPUT C1
META_COPY C1 0->1 ~0.6ms
        │
        ├── Phone C1 开始
        │
META_COPY C0 1->0 ...      ← 同时还没回完
```

日志多线程打印顺序可能稍有交错，但核心应看到：

> **C1 的 0→1 实际 copy 不再被 C0 的 1→0 completion 挡住。**

这就是下一步最有价值的一刀。

而且目前两路流量都不大：

```text
PC->Phone 17.53 MiB
Phone->PC 16.97 MiB
```



现在优化的核心已经不是减少字节，而是把这两条方向真正并起来。
