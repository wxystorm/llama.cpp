这次 debug 很有价值，已经能排掉两个方向，而且问题范围明显缩小了。

首先，**不是我刚才怀疑的 backend-specific allreduce**。真正处理 43-token prompt 的这次 Prefill 日志里明确是：

```text
subgraphs=66
comm=0.000 ms(0)
fallback=32
reduce_to_primary=0
```

也就是说 32 个 Transformer 层的 FFN 都已经走你自己的 **generic fallback reduction**，CUDA 专用 `comm_allreduce` 根本没被调用。

而且第一个 token 又精确得到：

```text
token=13398 logit=12.699981689
```

和你上一次 CUDA 的 `12.699981689` 一模一样。

所以这基本也说明：

> **不是随机 race，也不是刚才 worker async 没同步造成的偶发错误。**

这是一个**稳定、确定性的路径/数值差异**。

### 现在最该查的是第一层到底从哪里开始分叉

你的 Prefill 结构现在非常清楚：

```text
SG0:
CUDA primary
norm-0 ... attn_out-0
        ↓
CUDA -> Phone
        ↓
SG1:
CUDA FFN partial
Phone FFN partial
        ↓
generic fallback reduce
        ↓
node_30
```

日志也吻合：`sg=0` 是 `PREFILL_PC_ONLY_SG`，随后 `sg=1` 两边 `compute=1`。

而 generic fallback 的实现就是：

```cpp
ggml_backend_tensor_copy_async(
    bcj_src.backend,
    bcj_dst.backend,
    node_src,
    node_tmp);

node_red->op = GGML_OP_ADD;
node_red->src[0] = node_dst;
node_red->src[1] = node_tmp;
```

也就是把另一端 partial copy 到 `node_tmp`，再在目标 backend 上 ADD。

所以现在继续猜没意义了，直接在 **SG0 和 SG1 插数值检查点**，一跑就知道。

---

我建议临时加这个：

```cpp
static void meta_debug_tensor(
        ggml_backend_t backend,
        const ggml_tensor * t,
        const char * tag) {

    if (t == nullptr || t->type != GGML_TYPE_F32) {
        return;
    }

    ggml_backend_synchronize(backend);

    const size_t n = ggml_nelements(t);

    std::vector<float> data(n);

    ggml_backend_tensor_get(
        t,
        data.data(),
        0,
        n * sizeof(float));

    double sum = 0.0;
    double l2  = 0.0;
    double max_abs = 0.0;

    for (size_t i = 0; i < n; ++i) {
        const double x = data[i];
        sum += x;
        l2 += x * x;
        max_abs = std::max(max_abs, std::abs(x));
    }

    printf(
        "[NUMDBG] %s tensor=%s n=%zu "
        "sum=%.9f l2=%.9f max=%.9f "
        "v0=%.9f v1=%.9f v2=%.9f v3=%.9f\n",
        tag,
        t->name,
        n,
        sum,
        std::sqrt(l2),
        max_abs,
        n > 0 ? data[0] : 0.0f,
        n > 1 ? data[1] : 0.0f,
        n > 2 ? data[2] : 0.0f,
        n > 3 ? data[3] : 0.0f);
}
```

只在 debug 模式下用，性能完全不用管。

然后我们只需要取 **4 个检查点**：

```text
A. sg0 CUDA/CPU 的 attn_out-0

B. sg1 reduce 前 backend0 的 node_30
   = PC/CUDA 本地 FFN partial

C. sg1 reduce 前 backend1 的 node_30
   = Phone FFN partial

D. sg1 reduce 后 backend0 的 node_30
   = 两边合并结果
```

### 这四个值能直接把 bug 分类

假设 CPU+Phone 是正确基准。

如果：

```text
A 就明显不同
```

那就不用看 RPC reduction 了。

问题在：

```text
CPU Attention
vs
CUDA Attention
```

具体就是 CUDA Attention / Flash Attention / Attention 权重 placement 方向。

如果：

```text
A CPU ≈ CUDA

B CPU版 backend0
和
B CUDA版 backend0

开始明显不同
```

那问题在：

```text
CUDA 本地 FFN partial
```

要查 CUDA 上的 FFN weight slice 或 MUL_MAT 路径。

如果：

```text
A 接近
B 接近

但是 C 不一样
```

那很关键：

> 同样的 `attn_out-0` 到 Phone 后，Phone 算出来的 partial 不一样。

那就查：

```text
CUDA -> RPC
attn_out-0 copy
```

你的代码目前确实大量直接调用 `ggml_backend_tensor_copy_async()` 来完成 primary→Phone 复制。

如果：

```text
A 接近
B 接近
C 接近

D 突然不一样
```

那基本就抓到了：

> **RPC → CUDA partial copy 或 CUDA 上的 reduction ADD 有问题。**

这时我们再做 host staging 就非常有针对性，不需要把全代码的 copy 全换掉。

---

还有一个重要的信息：这次不是“Phone 根本没参与”。

Prompt Prefill 里：

```text
0 -> 1:
65 次
42.359 MiB

1 -> 0:
32 次
20.844 MiB
```

32 次 `1->0` 正好对应 32 层 FFN reduce。

所以现在不能再简单理解成：

```text
Phone 1/3 完全没回来
```

至少通信路径确实执行了。问题可能是**传回的数据错误、CUDA 端使用的数据错误，或者在更前面的 Attention 就已经发生偏差**。

另外后面那些：

```text
DECODE_PC_ONLY_SG
reduce_to_primary=64
fallback=0
```

暂时都不用看，因为第一个 token 在进入这些 Decode 图之前就已经错了。

**现在只盯 Prefill 第一层。**

最好先拿 CPU+Phone 跑一次这 4 个 `[NUMDBG]`，再拿 CUDA+Phone 跑一次。甚至都不用生成 32 token，`-n 1` 就够。

把：

```text
A attn_out-0
B node_30 pre backend0
C node_30 pre backend1
D node_30 post backend0
```

两组结果给我，我们基本就能精确定位到某一段路径，而不是继续在整个 CUDA/RPC 框架里猜。
