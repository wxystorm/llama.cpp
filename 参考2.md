对，**你最初设想的那个传输点应该是 `ffn_norm`，不是 `attn_out`，也不是我上一条说的 `ffn_inp`。**

而且这正好能解决现在 `MIRROR_BACK=0` 时 Phone 缺少完整 layer 输出的问题。

当前代码实际顺序是：

```text
                PC

inpSA = 本层输入 hidden
          │
          │
          ├──────────────┐
          │              │ residual
          ▼              │
Attention                │
          │              │
          ▼              │
      attn_out            │
          │              │
          └──── ADD ◄────┘
                 │
                 ▼
              ffn_inp
                 │
          RMSNorm(ffn_norm weight)
                 │
                 ▼
              ffn_norm
                 │
                 ▼
         gate/up / SiLU
                 │
                 ▼
             ffn_hidden
                 │
                 ▼
              Wdown
```

代码也是明确这样：

```cpp
cb(cur, "attn_out", il);

ggml_tensor * ffn_inp = ggml_add(ctx0, cur, inpSA);
cb(ffn_inp, "ffn_inp", il);

cur = build_norm(
    ffn_inp,
    model.layers[il].ffn_norm,
    NULL,
    LLM_NORM_RMS,
    il);

cb(cur, "ffn_norm", il);

ggml_tensor * ffn_hidden = build_ffn(
    cur,
    ...
);
```

也就是说，**`ffn_norm` 才是“已经做好 norm、可以直接喂给 FFN gate/up 的输入”**。

---

## 所以你的原始设计应该准确画成这样

```text
                         PC

Layer i input
     │
     ├──────────────────── residual
     │
     ▼
Attention
     │
     ▼
attn_out
     │
     + residual
     ▼
ffn_inp
     │
   RMSNorm
     │
     ▼
ffn_norm   ← ★ 就传这个
     │
     ├──────────────────────────> Phone
     │
     │                              │
     ▼                              ▼
 PC gate/up                    Phone gate/up
     │                              │
     ▼                              ▼
 PC ffn_hidden                 Phone ffn_hidden
     │                              │
     ▼                              ▼
PC Wdown chunk0            Phone Wdown chunk0
     │                              │
     │◄─────────────────────────────┘
     ▼
PC reduce -> full chunk0
     │
     ├── + ffn_inp residual slice
     │
     ├── next layer norm partial
     │
     └── Q'0 K'0 V'0

同时：
                         Phone Wdown chunk1
                              │
                              ▼
                         Phone -> PC
```

这才完全是你之前描述的东西。

---

## 为什么传 `ffn_norm` 比传 `ffn_inp` 更符合你的设计

如果传 `ffn_inp`：

```text
PC -> Phone:
ffn_inp

Phone:
ffn_inp
  ↓
RMSNorm
  ↓
gate/up
```

Phone 还得自己做一遍 FFN 前 RMSNorm。

而你想要的是：

```text
PC:
ffn_inp
  ↓
RMSNorm
  ↓
ffn_norm
  │
  └──────> Phone

Phone:
ffn_norm
  ↓
gate/up
```

这样 **Attention + residual add + FFN 前 Norm 都由 PC 全权负责。**

Phone 从真正的 FFN 矩阵计算开始参与。

这也更干净。

---

# 最重要的是：这样 Phone 再也不需要完整 layer output

这是核心。

现在 `mirror-back=1` 为什么需要？

因为 Phone 后面还走：

```text
Phone:
上一层 output
   ↓
下一层 Attention 后 residual ADD
   ↓
ffn_inp
```

所以 Phone 必须保存上一层完整 output。

而新设计根本不让 Phone 做这一段：

```text
                            Phone

上一层 full output             不需要
Attention                     不做
attention residual add         不做
FFN RMSNorm                   不做

                               ↓

PC直接发 ffn_norm ────────────> ffn_norm
                               ↓
                            gate/up
                               ↓
                            Wdown
```

那么 Phone 手里的上一层 output 是：

```text
正确？
错误？
partial？
没有？
```

**统统无所谓。**

因为后面的 FFN 根本不会再读它。

因此：

```text
PC full chunk -> Phone mirror-back
```

自然就可以删掉。

---

## PC 为什么仍然要留 `ffn_inp`

这里也很重要。

虽然发送给 Phone 的是：

```text
ffn_norm
```

但 PC 本地仍然必须保留：

```text
ffn_inp
```

因为 FFN 的最终 residual 是：

```cpp
ggml_tensor * residual =
    ggml_view_2d(ctx0, ffn_inp, ...);

ggml_tensor * hidden =
    ggml_add(ctx0, out, residual);
```

也就是：

```text
FFN Wdown full chunk
        +
ffn_inp 对应 chunk
        =
Layer output chunk
```

当前代码正是这么干的。

所以所有 residual 都放 PC 上：

```text
Phone:
只负责
ffn_norm
  ↓
gate/up
  ↓
Wdown partial

            Phone partial
                  │
                  ▼
PC:
PC partial + Phone partial
          ↓
full Wdown chunk
          +
ffn_inp residual slice
          ↓
full layer-output chunk
          ↓
Q'K'V'
```

非常合理。

---

# 于是我们现在应该改的“第一步”其实可以重新定义

不是：

> 把 `attn_out` 的通信边界改成 `ffn_inp`

而是直接：

> **把目前 PC → Phone 的通信边界从 `attn_out` 移到 `ffn_norm`。**

即：

### 当前

```text
PC Attention
    ↓
attn_out
    ├────────> Phone
    │
    ↓
PC residual + norm


Phone:
attn_out
    ↓
+ Phone自己的 inpSA   ← 这里现在必须依赖 mirror-back
    ↓
ffn_inp
    ↓
norm
```

### 改完

```text
PC Attention
    ↓
attn_out
    ↓
+ PC完整 inpSA
    ↓
ffn_inp
    ↓
RMSNorm
    ↓
ffn_norm
    ├──────────────> Phone
    │
    ▼
PC FFN              Phone FFN
```

这样立刻切断：

```text
Phone FFN
    X
    │
Phone上一层full output
```

这条依赖。

---

## 而且通信量还是一样

decode 下：

```text
attn_out : [n_embd, 1]
ffn_inp  : [n_embd, 1]
ffn_norm : [n_embd, 1]
```

所以把：

```text
PC -> Phone attn_out
```

换成：

```text
PC -> Phone ffn_norm
```

**字节数基本不变。**

只是 PC 多承担：

```text
residual ADD
+
RMSNorm
```

再发。

这两个相对于 FFN 矩阵乘来说很轻，而且正好让 Phone 的职责特别清晰：

```text
Phone = FFN计算设备
PC    = Attention + residual/norm + 汇聚/Q'K'V'
```

---

## 最终你想要的整个 decode layer 就非常清楚了

```text
                         Layer i

PC:
Attention
   ↓
attn_out
   ↓
+ residual
   ↓
ffn_inp
   ↓
FFN RMSNorm
   ↓
ffn_norm ───────────────────────────> Phone
   │                                   │
   ▼                                   ▼
FFN gate/up                        FFN gate/up
   │                                   │
   ▼                                   ▼
Wdown chunk0                      Wdown chunk0
   │                                   │
   │◄──────── Phone -> PC ─────────────┘
   ▼
full Wdown chunk0
   +
ffn_inp chunk0
   ↓
full output chunk0
   ↓
next attn norm partial
   ↓
Q'0 K'0 V'0

                    || Phone已经继续
                    || Wdown chunk1

PC 收 chunk1
   ↓
full output chunk1
   ↓
Q'1 K'1 V'1

   ↓
Q'0+Q'1 / K'0+K'1 / V'0+V'1
   ↓
PC Attention(i+1)
   ↓
...
   ↓
PC生成下一层 ffn_norm
   └────────────────────────────────> Phone
```

**对，这才是我们应该做的。**

所以目前 `bec3be6` 下一步我建议先别急着调 overlap：**先把 PARTIAL/通信边界从 `attn_out` 后移到 `ffn_norm`，让 Phone 从 `ffn_norm` 开始工作，然后关闭 `GGML_META_PRIMARY_MIRROR_BACK` 验证输出。**

如果这一步输出正确，就证明 Phone 已经彻底摆脱对完整 layer output 的依赖；之后再把现在已经做好的 `early Wdown` 真正并行起来。
