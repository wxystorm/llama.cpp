对，这个统计已经把情况看得非常清楚了。**前面那次 `CUT_INPUT` 是完全成功的。**

现在 24 个 Tensor-Split 层的两个 Prefill FFN graph，全都已经变成：

```text
nodes=4
tensors=8
bytes=2420
```

例如 layer 0 的两个 chunk 都只有 2420 B。 后面的 layer 1、2……23 也保持完全一样，没有随着层数继续膨胀。

也就是说以前大约：

```text
~300 KB / graph
上千个 tensors
```

现在变成：

```text
2.42 KB / graph
8 tensors
```

单个 graph metadata 大约砍掉了 **99%**。这一部分可以正式保留了。

不过这份日志还暴露出了**最后两个漏网的大 graph**，非常值得继续处理。

第一个出现在 tail 最后一层：

```text
[PREFILL_PC_ONLY_SG] sg=110 ... last=attn_out-31

... 一大串 RPC_GRAPH_CUT_INPUT ...

[RPC_GRAPH_SERIALIZE]
nodes=9
tensors=1093
bytes=323620

[META_KEEP_PRIMARY] sg=111 tensor=node_1161 ...
```



这个非常关键。

按我们的布局：

```text
24~31 = PC-only
```

那么：

```text
sg110 = layer31 Attention
sg111 = layer31 FFN + 后续选最后 token 一类操作
```

**sg111 本来也应该完全在 PC 上。**

但它最后的 `node_1161` 已经从 `[4096,35]` 收缩成了 `[4096,1]`，也就是 16384 B。你的 Prefill PC-only FFN 判断大概率仍有类似：

```cpp
pc_last->ne[1] > 1
```

所以 layer31 的这个特殊尾 SG 被漏掉了。

这也解释了为什么：

```text
sg96 ~ sg110
```

都有：

```text
[PREFILL_PC_ONLY_SG]
```

唯独 **sg111 没有 `[PREFILL_PC_ONLY_SG]`**，却突然给 Phone 序列化了一张 323 KB 的巨大 graph。

### 下一刀只改这个

如果你现在 `subgraph_is_prefill_pc_only_ffn()` 类似我们之前写的：

```cpp
return
    pc_last->ne[1] > 1 &&
    (pc_last->flags & GGML_TENSOR_FLAG_COMPUTE) != 0 &&
    (phone_last->flags & GGML_TENSOR_FLAG_COMPUTE) == 0;
```

把：

```cpp
pc_last->ne[1] > 1 &&
```

**删掉。**

因为前面已经有：

```cpp
if (!subgraph_is_prefill_pc_only_attn(sg - 1)) {
    return false;
}
```

这个条件本身已经足以证明：

> 当前 SG 是紧跟在一个 Prefill PC-only Attention SG 后面的 FFN SG。

Decode 不会误判，因为 Decode 的前一个 SG 不会满足 `subgraph_is_prefill_pc_only_attn()`。

于是 layer31 应该从现在的：

```text
sg110:
[PREFILL_PC_ONLY_SG]

↓
Phone 发送 323620B graph      ×

sg111:
[META_KEEP_PRIMARY]
```

变成：

```text
sg110:
[PREFILL_PC_ONLY_SG]

sg111:
[PREFILL_PC_ONLY_SG]          ✓
[META_KEEP_PRIMARY]

完全没有 RPC_GRAPH_SERIALIZE
```

预期日志就是：

```text
[PREFILL_PC_ONLY_SG] sg=110 ... attn_out-31
[META_KEEP_PRIMARY] sg=110 ...

[PREFILL_PC_ONLY_SG] sg=111 ... node_1161
[META_KEEP_PRIMARY] sg=111 ...
```

**中间那串 1093 tensors / 323620 bytes 应该直接消失。**

---

然后还有第二张巨大图：

```text
[DECODE_PC_ONLY_SG] sg=112 ... result_norm
[META_COPY] sg=112 0->1 tensor=result_norm bytes=16384

...

[RPC_GRAPH_SERIALIZE]
nodes=1
tensors=1099
bytes=325332
```



这个和第一个性质不一样。

这里 `result_norm` 明确已经：

```text
PC → Phone
16384 B
```

然后下一张 graph 很可能是最终 output projection / logits。因为输出权重仍然 Tensor Split，所以 **Phone 真正需要执行这个 SG**。

所以这一个不能像 sg111 那样直接禁止 Phone 执行。

但它的问题和刚刚 Prefill input 一模一样：

```text
result_norm 已经作为实际数据发送给 Phone
             ↓
Phone graph 却还沿 result_norm.src
一路追溯整个 32 层网络
             ↓
1099 tensors / 325 KB
```

因此**第二步**就可以把：

```text
result_norm
```

也当成 RPC graph 的 external leaf。

不过我们先别同时改两个地方。

### 现在只做 sg111 PC-only 修正

因为这是逻辑错误，不只是 metadata 优化：

> tail 的第 31 层本来就应该 PC-only，却因为输出 token 数变成 1，漏过了 Prefill PC-only 判断。

改完以后检查三个地方：

```text
1. 出现：
   [PREFILL_PC_ONLY_SG] sg=111 ...

2. sg110 和 sg111 之间：
   不再出现
   [RPC_GRAPH_SERIALIZE] ... tensors=1093 bytes=323620

3. 最后仍然保留：
   [RPC_GRAPH_SERIALIZE]
   nodes=1 tensors≈1099 bytes≈325KB
```

第三个**先保留是正常的**，下一轮我们再专门用 `result_norm` leaf-cut 把它从 325 KB 压到几 KB。

顺便看一下这轮整体：

```text
1->0 count = 48
avg        = 221.8 ms
total      = 10.65 s
```



比上轮的 210 ms 稍有波动，但 graph 结构已经稳定地压到了 `8 tensors / 2420 B`，所以这点速度差更像链路/运行波动，不影响我们对 CUT_INPUT 成功的判断。

**下一步就只修 sg111 的 PC-only 识别。**这应该还能白拿掉一次约 **316 KB** 的无意义 PC→Phone graph 传输，而且不改变任何计算结果。
