对，我对比了你现在这份 `ggml-backend-meta(10).cpp` 和之前那份原版，**调度逻辑本身基本就是原来的**。原版 snapshot 路径是：

```cpp
snapshot_arm(...);
start(j_src, i + 1);

wait(task_id);
start(j_dst, i + 1);

wait(j_src);
wait(j_dst);

next_compute_complete = true;
```

 

你现在只是围绕它加了 `pipeline_gap`、`completed_time_us` 和输出，没有重新引入刚才那个 prefetch 方案。

但我找到一个很明确的原因：**你这次加的输出本身就在关键路径上，而且 `gap` 统计恰好没有把输出耗时算进去。**

### 这里的位置很关键

现在代码是：

```cpp
const int64_t snapshot_arm_us = ggml_time_us();

if (pipeline_gap.valid && pipeline_gap.subgraph == i) {
    if (pipeline_debug) {
        printf("[META_PIPELINE_GAP] ...");
    }

    pipeline_gap.valid = false;
}

// 真正 ARM 在 printf 后面
const bool armed = snapshot_arm(...);
```



也就是说实际时间线是：

```text
PC sg submit 完成
      ↓
t2 = ggml_time_us()
      ↓
printf("[META_PIPELINE_GAP] ...")   ← 这段不在 gap 统计里
      ↓
snapshot_arm(chunk1)
```

所以日志告诉你：

```text
gap=0.020 ms
gap=0.024 ms
gap=0.018 ms
...
```

并不代表：

```text
PC submit → snapshot_arm
```

只有 0.02ms。

实际是：

```text
0.02ms
+
printf 到终端的时间
```

因为你的 `t2` 是 **printf 前**取的。

---

而且不是只 print 一次。

每个 token 你现在会输出：

```text
sg=2
sg=5
sg=8
...
sg=95
```

**整整 32 条 `META_PIPELINE_GAP`。**

日志里能很清楚看到一整个 token 从 sg=2 一直输出到 sg=95。

Windows 控制台上的：

```cpp
printf(... "\n");
```

并不便宜，尤其你这里还是：

```text
主调度线程
→ printf
→ snapshot_arm
```

所以它会直接把 Phone snapshot 的 ARM 往后推。

假如平均一条 console output 只花：

```text
2 ms
```

32 层就是：

```text
64 ms/token
```

如果某些时候终端输出 5~10ms：

```text
160~320 ms/token
```

完全足够让你明显感觉 decode 掉速。

而且 stdout 自己还有 CRT 锁、终端渲染和线程调度干扰。

---

### 你的日志实际上证明了我们的测量目的已经完成

真正没有输出干扰的：

```text
PC async submit 返回
→ 主线程进入下一 sg
```

间隙非常小：

```text
0.015 ~ 0.05 ms
```

基本都是几十微秒。

所以之前我们想确认的东西已经可以确认：

> **原来的第二阶段确实没有“等 PC sg2 实际计算完才 ARM chunk1”的大气泡。**

PC graph 是 async submit，下一轮 ARM 基本马上就到了。

也就是说恢复原来的逻辑是正确的。

---

不过你这次“慢很多”还有第二个因素：**这轮网络本身也明显抖了。**

第一段甚至出现：

```text
1->0:
count=64
total=8004.796 ms
avg=125.075 ms
max=679.891 ms
```



这个绝对不是正常情况，8KB 一次平均 125ms，最大接近 680ms。

后面恢复到：

```text
18.077 ms avg
18.115 ms avg
15.120 ms avg
```

  

你之前比较好的 run 是大约：

```text
10~15 ms
```

所以这轮 Wi-Fi/RPC jitter 本身也偏差一些。

因此现在看到的掉速实际上是两个因素叠加：

```text
网络这一轮偏慢
+
每 token 新增 32 条关键路径 printf
```

---

## 我建议现在立刻把这个测量改成汇总输出

不要：

```cpp
printf("[META_PIPELINE_GAP] ...");
```

每层输出。

改成只累计：

```cpp
int64_t pipeline_gap_sum_us = 0;
int64_t pipeline_submit_sum_us = 0;
int64_t pipeline_gap_max_us = 0;
size_t  pipeline_gap_count = 0;
```

这里：

```cpp
const int64_t submit_us =
    pipeline_gap.pc_submit_us -
    pipeline_gap.pc_start_us;

const int64_t gap_us =
    snapshot_arm_us -
    pipeline_gap.pc_submit_us;

pipeline_submit_sum_us += submit_us;
pipeline_gap_sum_us += gap_us;
pipeline_gap_max_us =
    std::max(pipeline_gap_max_us, gap_us);

++pipeline_gap_count;
```

**一条都不 print。**

最后和 `[META_PIPELINE]` 一起只输出一行：

```cpp
printf(
    "[META_PIPELINE_GAP_SUM] "
    "count=%zu submit_avg=%.3f ms "
    "gap_avg=%.3f ms max=%.3f ms\n",
    pipeline_gap_count,
    pipeline_submit_sum_us /
        1000.0 / pipeline_gap_count,
    pipeline_gap_sum_us /
        1000.0 / pipeline_gap_count,
    pipeline_gap_max_us / 1000.0);
```

这样一个 token 从：

```text
32 条 GAP
+ 4 条 COPY
+ summary
```

变成：

```text
4 条 COPY
+ 1 条 GAP_SUM
+ summary
```

然后重新测 decode。

至于 `completed_time_us` + `completed_at()`，它确实也是新加的：worker 多一次 `ggml_time_us()`，主线程多一次 mutex lock。 

但这个量级很小，**不是现在主要掉速来源**。如果要做最终纯性能 A/B，再一起删掉就行。

所以结论很明确：**是的，你这版除测量代码外已经恢复原来的调度；先别怀疑 pipeline。现在这个逐层 `META_PIPELINE_GAP printf` 本身就会污染性能测试，而且它恰好发生在 `snapshot_arm` 前面。把它改成每 token 一条汇总再测。**
