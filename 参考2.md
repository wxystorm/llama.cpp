对，那我们就按这个方向继续，而且我建议**分两步做，第一步先只改计算图，不碰 RPC/Meta 调度**，这样最容易确认数学结果没问题。

第一步目标是让 Prefill：

```text
ffn_norm [4096,35]
        ↓ token 维切分
C0 [4096,12]
C1 [4096,12]
C2 [4096,11]
        ↓
每个 Ck 都分别走现有 Tensor Split FFN
        ↓
down0 [4096,12]
down1 [4096,12]
down2 [4096,11]
        ↓
concat
        ↓
[4096,35]
        ↓
residual / 下一层
```

而 Decode 现在的路径一行都先不动：

```cpp
n_tokens == 1
```

继续走现有 `ffn_down_chunk_*`。当前那套 chunk 是切 `n_embd`，也就是 hidden 维；我们新加的 Prefill chunk 则专门切 `n_tokens`。

我建议新增一个独立参数，例如：

```cpp
static int llama_prefill_ffn_chunk_count() {
    const char * env = std::getenv("LLAMA_PREFILL_CHUNKS");
    if (env == nullptr) {
        return 1;
    }

    return std::max(1, std::atoi(env));
}
```

然后条件明确分开：

```cpp
const bool use_decode_chunked_ffn =
    !embed &&
    n_tokens == 1 &&
    model.split_mode() == LLAMA_SPLIT_MODE_TENSOR &&
    llama_ffn_chunk_count() >= 1 &&
    loras->empty() &&
    cvec->tensor_for(il) == nullptr;

const bool use_prefill_chunked_ffn =
    !embed &&
    n_tokens > 1 &&
    model.split_mode() == LLAMA_SPLIT_MODE_TENSOR &&
    llama_prefill_ffn_chunk_count() > 1 &&
    loras->empty() &&
    cvec->tensor_for(il) == nullptr;
```

Prefill 不要用固定 `chunk_tokens=16` 的算法，直接按照 **chunk 数均匀切**：

```cpp
const int n_chunks =
    std::min<int64_t>(
        llama_prefill_ffn_chunk_count(),
        n_tokens);

for (int i = 0; i < n_chunks; ++i) {
    const int64_t token_begin =
        n_tokens * i / n_chunks;

    const int64_t token_end =
        n_tokens * (i + 1) / n_chunks;

    const int64_t token_count =
        token_end - token_begin;
}
```

这样 35 tokens、3 chunks 自动变成：

```text
11 + 12 + 12
```

或者根据整数除法次序：

```text
11 / 12 / 12
```

不会出现：

```text
16 + 16 + 3
```

这种很差的尾块。

然后 `ffn_norm` 做 token view：

```cpp
ggml_tensor * ffn_norm_chunk =
    ggml_view_2d(
        ctx0,
        cur,
        cur->ne[0],              // 4096
        token_count,
        cur->nb[1],
        token_begin * cur->nb[1]);
```

命名建议直接区分 Decode：

```cpp
const std::string name =
    "prefill_ffn_norm_chunk_" + std::to_string(i);

cb(ffn_norm_chunk, name.c_str(), il);
```

每个 chunk 独立：

```text
prefill_ffn_norm_chunk_0-0
prefill_ffn_norm_chunk_1-0
prefill_ffn_norm_chunk_2-0
```

然后分别：

```cpp
ggml_tensor * down_chunk =
    build_ffn(
        ffn_norm_chunk,
        ffn_up, ...,
        ffn_gate, ...,
        ffn_down, ...);
```

注意这里**不是切 Wdown 输出维**。

权重仍然是当前 Meta Tensor Split：

```text
PC   75% FFN shard
Phone 25% FFN shard
```

只是输入矩阵从：

```text
[4096,35]
```

变成：

```text
[4096,11]
[4096,12]
[4096,12]
```

因此第一版数学上非常简单。

每个结果命名：

```cpp
const std::string name =
    "prefill_ffn_down_chunk_" + std::to_string(i);

cb(down_chunk, name.c_str(), il);
```

最后：

```cpp
ggml_tensor * ffn_down = down_chunks[0];

for (int i = 1; i < n_chunks; ++i) {
    ffn_down = ggml_concat(
        ctx0,
        ffn_down,
        down_chunks[i],
        1);   // 注意这里是 token 维
}
```

这里跟 Decode 现有代码最明显的区别就是：

```text
Decode concat axis = 0
Prefill concat axis = 1
```

因为现在 Decode 的 chunk 是 `[1024,1] + ... -> [4096,1]`，源码也确实是在 hidden 维重组。

Prefill 则：

```text
[4096,11]
[4096,12]
[4096,12]

沿 axis 1 concat

→ [4096,35]
```

### 第一阶段只验证一个东西

暂时**不要做异步流水**。

也就是仍然正常让 scheduler 把：

```text
C0
C1
C2
```

全部按图计算。

先检查：

```text
LLAMA_PREFILL_CHUNKS=1
```

与：

```text
LLAMA_PREFILL_CHUNKS=3
```

输出文本/logits 是否完全正常。

理论上速度甚至可能先变慢，这没关系。

因为第一阶段只是验证：

> **按 token 维切 Prefill FFN 本身正确。**

确认正确以后，第二阶段才是真正关键的：

```text
C0 send Phone
 │
 ├─ PC(C0)
 │
C1 send Phone
 ├─ PC(C1)
 │
C2 send Phone
 ├─ PC(C2)

Phone:
recv C0 -> FFN C0 -> return C0
recv C1 -> FFN C1 -> return C1
recv C2 -> FFN C2 -> return C2
```

这时候再把 `feat/rpc-local-model` 的：

```cpp
pending_input_task
pending_chunk_task
dependent_backend
```

重新接进当前 Meta。旧版已经有“输入还在传，就先启动不依赖传输的 backend”这个调度框架。

所以现在我建议**第一刀只改 `llama.cpp` 的 Prefill FFN graph**。这一步确认以后，再动 `ggml-backend-meta.cpp`，会安全很多。
